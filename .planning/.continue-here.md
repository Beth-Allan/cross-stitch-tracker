---
context: default
phase: impeccable-audit-adapt-pass
task: 2
total_tasks: 5
status: in_progress
last_updated: 2026-04-18T00:12:00Z
---

<current_state>
Working through the `/adapt` pass of the impeccable audit (`.planning/AUDIT-2026-04-16.md`).
This is the 4th of 6 audit passes. Previous passes (harden, polish/dark theme, optimize) are all complete.
All audit changes across all passes are uncommitted on `main`. Build clean, 1031/1031 tests passing.

Task 1 of 5 (session table mobile cards) is complete with updated tests.
Paused before starting task 2 (supply table mobile cards).
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Session table mobile card fallback - Done
  - Added `hidden md:block` on desktop table, `md:hidden` mobile card list to `session-table.tsx`
  - Mobile cards show: date + photo icon, project name (if showProjectName), stitches + time, edit button (44px touch target)
  - Updated `session-table.test.tsx`: changed `getByText` → `getAllByText` for duplicated content, added mobile card render test
  - 11/11 tests passing
</completed_work>

<remaining_work>

- Task 2: Supply table mobile card fallback (`supply-table-view.tsx`) — same hidden md:block / md:hidden pattern
- Task 3: Whats-next kitting progress on mobile (`whats-next-tab.tsx:158`) — currently `hidden md:block`, needs compact mobile version
- Task 4: Pattern-dive tabs aria-label (`pattern-dive-tabs.tsx:48`) — tab labels are `hidden sm:inline`, icon-only on mobile needs `aria-label`
- Task 5: Small touch targets — `size="sm"` buttons (h-7 = 28px) need `min-h-11` on mobile in:
  - `project-sessions-tab.tsx:159` (Log Session button)
  - `fabric-detail.tsx:71,77` (Edit/Delete buttons)
  - `shopping-list.tsx:62` (Mark Acquired button)
</remaining_work>

<decisions_made>

- Mobile cards use `rounded-xl` bordered cards matching existing app patterns (whats-next cards, gallery cards)
- Both table and cards render simultaneously (CSS visibility toggle), not conditional JS rendering — simpler since both share sortedSessions, sort state, and edit handlers
- Mobile card edit button uses `min-h-11 min-w-11` for 44px touch target
</decisions_made>

<blockers>
None
</blockers>

## Required Reading (in order)
1. `.planning/AUDIT-2026-04-16.md` — full audit with all 6 passes and their items
2. `src/components/features/sessions/session-table.tsx` — reference implementation for task 1 (mobile card pattern)

## Infrastructure State
- Dev server not running
- All changes uncommitted on `main`
- Build clean, 1031/1031 tests passing

<context>
The adapt pass is about making desktop-first UI work on mobile. The two big items are table→card
fallbacks for sessions and supplies. The remaining 3 items are small fixes (show hidden info,
add aria-labels, bump touch targets). The session table implementation is the template for the
supply table — same dual-render pattern with CSS visibility classes.

After the adapt pass, two more audit passes remain: `/harden` (P2 ARIA fixes) and `/distill`
(P2 shared component extraction). Then commit all audit work and move to v1.2 milestone planning.
</context>

<next_action>
Start with: Task 2 — add mobile card fallback to `supply-table-view.tsx` using the same `hidden md:block` / `md:hidden` pattern from `session-table.tsx`. The supply table has dynamic columns via `ColumnConfig`, so mobile cards need to render column data generically.
</next_action>
