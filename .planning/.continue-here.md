---
context: default
phase: backlog
task: null
total_tasks: null
status: paused
last_updated: 2026-04-11T20:38:24.587Z
---

<current_state>
All 5 planned backlog fixes are complete and pushed to main (CI + Vercel deploy triggered).

During manual testing, discovered that chart cover thumbnails on /charts still show placeholder icons — `coverThumbnailUrl` is `null` for all charts in the database. The `generateThumbnail()` function exists in `src/lib/actions/upload-actions.ts` but is never called after a cover image upload. The `coverImageUrl` field has valid R2 keys.

This is the one remaining fix before `/gsd:complete-milestone`.
</current_state>

<completed_work>

Quick tasks completed this session (all pushed to main):
- 260411-iwm: Thread sort numeric ordering — `naturalSortByCode` utility in `src/lib/utils/natural-sort.ts`
- 260411-j3i: Form submit idempotency — `isSuccess` state in `useChartForm`, button disabled after save
- 260411-j8v: "Already added" indicator — greyed-out items in `search-to-add.tsx` instead of hiding
- 260411-jhw: Chart list edit/delete — new `chart-list.tsx` client component with action buttons + modals
- 260411-js1: Chart images presigned URLs — `getPresignedImageUrls()` batch function, `CoverThumbnail` component with onError fallback
- Designer-list test fix: WEB → WEBSITE column header
- All 389 tests passing, build clean
</completed_work>

<remaining_work>

**999.0.6 followup: Auto-generate thumbnails on cover upload**

Root cause: `generateThumbnail()` in `upload-actions.ts:218` exists but is never called. When a user uploads a cover image, `confirmUpload()` saves the R2 key to `coverImageUrl` but doesn't trigger thumbnail generation. So `coverThumbnailUrl` stays `null`.

Fix needed:
1. After `confirmUpload` succeeds for `coverImageUrl` field, call `generateThumbnail(chartId, key)` to create a 400x400 WebP thumbnail and save its key to `coverThumbnailUrl`
2. Add a fallback in `charts/page.tsx` to use `coverImageUrl` when `coverThumbnailUrl` is null (so images show even for charts without thumbnails)
3. Backfill: run `generateThumbnail` for the 2 existing charts that have `coverImageUrl` but no thumbnail

After this fix:
- `/gsd:complete-milestone` to archive M1 and prep Milestone 2
</remaining_work>

<decisions_made>

- Presigned URLs resolved server-side in page.tsx, passed as `Record<string, string>` to client components — keeps R2 credentials server-only
- `CoverThumbnail` extracted as separate client component (needs useState for onError) — reused in list and detail pages
- Cover image upload flow needs `generateThumbnail` call wired in after `confirmUpload` for cover images
</decisions_made>

<blockers>
None
</blockers>

## Required Reading (in order)
1. `src/lib/actions/upload-actions.ts` — contains `confirmUpload()` (line 94), `generateThumbnail()` (line 218), and `getPresignedImageUrls()` (line 157)
2. `src/components/features/charts/form-primitives/cover-image-upload.tsx` — the upload component that calls `confirmUpload` (this is where `generateThumbnail` should be triggered)
3. `src/app/(dashboard)/charts/page.tsx` — server page that resolves thumbnail URLs (needs coverImageUrl fallback)

## Infrastructure State
- Vercel deploy triggered from push (main auto-deploys)
- Neon prod DB has 2 charts with `coverImageUrl` set, `coverThumbnailUrl` null
- R2 has the cover images stored, thumbnails not yet generated

<context>
Session focused on triaging and fixing MVP backlog items before milestone completion. 5 of 5 planned fixes done. The thumbnail issue surfaced during testing — it's a wiring gap, not a code bug. The generation function works fine, it just needs to be called.
</context>

<next_action>
Start with: `/gsd:quick 999.0.6 followup: Wire generateThumbnail into cover image upload flow and add coverImageUrl fallback for charts without thumbnails`
</next_action>
