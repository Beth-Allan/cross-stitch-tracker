---
context: default
phase: post-phase-4-polish
status: in_progress
last_updated: 2026-04-11T15:28:17.464Z
---

<current_state>
Milestone 1 MVP is complete and ready to deploy. Before deploying, we ran an `/impeccable:audit` which scored 13/20 (Acceptable). We completed the `/impeccable:harden` pass fixing all accessibility P1/P2 issues. Three more audit recommendations remain: theming polish, performance optimization, and responsive tweaks.

The 7 hardening fixes are uncommitted (modified files listed below). These should be committed before starting the next pass.
</current_state>

<completed_work>

## Audit Results (13/20 Acceptable)

| Dimension | Score | Key Finding |
|-----------|-------|-------------|
| Accessibility | 2/4 | Progress bar lacks ARIA; icon buttons missing labels |
| Performance | 2/4 | Raw img tags, width animations |
| Theming | 2/4 | 51+ hard-coded Tailwind scales; CSS vars defined but unused |
| Responsive | 3/4 | Strong mobile-first; minor min-w edge case |
| Anti-Patterns | 4/4 | Zero AI slop tells |

## Harden Pass (complete)

P1 fixes applied:
1. `progress-bar.tsx` -- added role="progressbar", aria-valuenow/min/max, aria-label
2. `user-menu.tsx` -- added aria-label="User menu" to DropdownMenuTrigger
3. `supply-grid-view.tsx` -- added descriptive aria-label to each card button
4. `supply-table-view.tsx` -- added tabIndex, onKeyDown (Enter/Space), role="columnheader" to sortable headers
5. `form-field.tsx` -- added visible red * asterisk for required fields alongside sr-only text
6. Cover image upload -- already had keyboard handler (no fix needed)

P2 fixes applied:
7. `genre-picker.tsx` -- added focus-visible:ring styles to toggle buttons
8. `charts/page.tsx` -- added sr-only table caption

Verification: build clean, 350/350 tests passing.
</completed_work>

<remaining_work>

Three audit recommendations remain, in priority order:

1. **`/impeccable:polish` -- Theming refactor (biggest lift)**
   - 51+ hard-coded Tailwind color scale instances bypass the CSS variable system
   - Primary targets: `src/lib/utils/status.ts` (14 instances), `src/lib/utils/size-category.ts` (10 instances), `src/components/features/charts/project-supplies-tab.tsx` (13 instances)
   - Also: fabric-catalog, fabric-detail, fabric-size-calculator, designer-detail, genre-detail, shopping-list, search-to-add, color-swatch, user-menu, charts/page.tsx
   - Strategy: extend globals.css with intent-based tokens (success-bg/text, warning-bg/text), refactor status.ts + size-category.ts to reference them, sweep components

2. **`/impeccable:optimize` -- Performance fixes**
   - Replace raw `<img>` tags with next/image in cover-image-upload.tsx and charts/page.tsx
   - Fix progress bar width animation to use transform: scaleX() (layout thrashing)
   - Fix sidebar width animation (transition-[width] causes reflow)
   - Stabilize useEffect dependencies in search-to-add.tsx

3. **`/impeccable:adapt` -- Responsive tweaks (smallest)**
   - Change `min-w-[200px]` to responsive constraint in 5 search input files
   - Shopping list grid: `grid-cols-3` -> `grid-cols-2 sm:grid-cols-3`
</remaining_work>

<decisions_made>

- Ran audit at phase boundary per quality-gates.md convention
- Prioritized harden (accessibility) first since P1 a11y issues are WCAG AA violations
- Confirmed cover-image-upload.tsx already had keyboard handlers -- audit agent false positive
- Chose to pause after harden pass to preserve context quality for the theming refactor
</decisions_made>

<blockers>
- None. All remaining work is straightforward implementation.
</blockers>

## Required Reading (in order)
1. This file -- full audit results and remaining work
2. `.impeccable.md` -- design context (users, brand, aesthetic direction)
3. `src/app/globals.css` -- existing CSS variable/token definitions (the system the theming refactor builds on)
4. `src/lib/utils/status.ts` -- primary theming violation target
5. `src/lib/utils/size-category.ts` -- secondary theming violation target

## Infrastructure State
- Dev server: not running
- Database: Neon PostgreSQL (cloud, always available)
- R2: not configured (uploads degrade gracefully)
- 7 modified files uncommitted (harden fixes)

<context>
This is post-MVP polish work before deploying to Vercel. The audit scored 13/20 with accessibility (now fixed) and theming as the weakest dimensions. Anti-patterns scored 4/4 -- the design is clean and intentional. The theming work is the biggest remaining lift and will need a fresh context window to do well since it touches 15+ files. After all three passes, re-run /audit to confirm score improvement, then deploy.
</context>

<next_action>
1. Commit the 7 harden fixes (they're staged and verified)
2. Run `/impeccable:polish` focused on theming -- start by reading globals.css to understand existing tokens, then refactor status.ts and size-category.ts, then sweep components
3. After polish, run `/impeccable:optimize` for performance fixes
4. After optimize, run `/impeccable:adapt` for responsive tweaks
5. Re-run `/audit` to confirm score improvement
6. Deploy to Vercel
</next_action>
