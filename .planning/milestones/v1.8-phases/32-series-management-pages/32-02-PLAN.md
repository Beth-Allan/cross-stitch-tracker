---
phase: 32-series-management-pages
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - src/app/(dashboard)/series/page.tsx
  - src/components/features/series/series-list.tsx
  - src/components/features/series/series-list.test.tsx
  - src/components/features/series/series-form-modal.tsx
  - src/components/features/series/series-form-modal.test.tsx
autonomous: true
requirements: [SERIES-02]

must_haves:
  truths:
    - "User can view all series on a /series page as a card grid with progress bars"
    - "User can sort series by Name, Completion, or Charts"
    - "User can create a new series via a modal with name, totalCount, designer, notes fields"
    - "User can delete a series from the list page via confirmation dialog"
    - "Empty state displays when no series exist with CTA to add first series"
  artifacts:
    - path: "src/app/(dashboard)/series/page.tsx"
      provides: "Server component page fetching series data"
      contains: "getSeriesWithStats"
    - path: "src/components/features/series/series-list.tsx"
      provides: "Client component with card grid, sort, create, delete"
      contains: "SeriesList"
    - path: "src/components/features/series/series-form-modal.tsx"
      provides: "Create/edit modal with form fields"
      contains: "SeriesFormModal"
  key_links:
    - from: "src/app/(dashboard)/series/page.tsx"
      to: "src/lib/actions/series-actions.ts"
      via: "getSeriesWithStats import"
      pattern: "import.*getSeriesWithStats.*series-actions"
    - from: "src/components/features/series/series-list.tsx"
      to: "src/lib/actions/series-actions.ts"
      via: "deleteSeries server action call"
      pattern: "deleteSeries"
    - from: "src/components/features/series/series-form-modal.tsx"
      to: "src/lib/actions/series-actions.ts"
      via: "createSeries server action call"
      pattern: "createSeries"
---

<objective>
Build the /series list page with a responsive card grid showing all series with progress bars, sort pills (Name/Completion/Charts), a create modal, and delete functionality.

Purpose: Delivers SERIES-02 — user can view, create, sort, and delete series from a dedicated management page.
Output: Server page, SeriesList client component, SeriesFormModal client component, full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-series-management-pages/32-PATTERNS.md
@.planning/phases/32-series-management-pages/32-UI-SPEC.md
@.planning/phases/32-series-management-pages/32-RESEARCH.md
@.planning/phases/32-series-management-pages/32-01-SUMMARY.md

<interfaces>
From src/types/series.ts (expanded in Plan 01):
```typescript
export type SeriesProgress = {
  ownedCount: number;
  finishedCount: number;
  totalCount: number | null;
};

export type SeriesWithStats = {
  id: string;
  name: string;
  totalCount: number | null;
  designerId: string | null;
  designerName: string | null;
  notes: string | null;
  progress: SeriesProgress;
};
```

From src/lib/actions/series-actions.ts:
```typescript
export async function createSeries(formData: unknown): Promise<{ success: true; series: Series } | { success: false; error: string }>;
export async function deleteSeries(id: string): Promise<{ success: true } | { success: false; error: string }>;
export async function getSeriesWithStats(): Promise<SeriesWithStats[]>;
```

From src/lib/validations/series.ts:
```typescript
export const seriesSchema = z.object({
  name: z.string().trim().min(1).max(200),
  totalCount: z.number().int().min(1).nullable().default(null),
  designerId: z.string().transform(v => v === "" ? null : v).nullable().default(null),
  notes: z.string().trim().max(5000).transform(v => v === "" ? null : v).nullable().default(null),
});
```

From src/__tests__/mocks/factories.ts (created in Plan 01):
```typescript
export function createMockSeriesWithStats(overrides?: Partial<SeriesWithStats>): SeriesWithStats;
```

From src/components/features/designers/delete-confirmation-dialog.tsx (extended in Plan 01):
```typescript
entityType: "designer" | "genre" | "brand" | "supply" | "series";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SeriesList and SeriesFormModal tests (RED)</name>
  <files>src/components/features/series/series-list.test.tsx, src/components/features/series/series-form-modal.test.tsx</files>
  <read_first>
    - src/components/features/designers/designer-list.test.tsx
    - src/components/features/designers/designer-form-modal.test.tsx
    - src/__tests__/mocks/factories.ts
    - src/__tests__/mocks/index.ts
    - .planning/phases/32-series-management-pages/32-UI-SPEC.md (CP-01, CP-02, CP-05)
  </read_first>
  <behavior>
    SeriesList tests:
    - Test 1: renders series cards with name, designer attribution ("by {name}"), progress bar
    - Test 2: renders stat text for totalCount series: "{finishedCount} of {ownedCount} finished"
    - Test 3: renders stat text for open-ended series: "{finishedCount} finished . {ownedCount} charts"
    - Test 4: shows completion percentage (e.g. "50%")
    - Test 5: sort pills (Name, Completion, Charts) toggle sort direction on click
    - Test 6: sort by Completion — 0-chart series sort to bottom
    - Test 7: empty state renders "No series created yet" with Add Series button
    - Test 8: clicking card navigates to /series/[id]
    - Test 9: delete button opens DeleteConfirmationDialog with entityType "series"

    SeriesFormModal tests:
    - Test 1: renders Name field (required), Total Count, Notes fields
    - Test 2: submit with empty name shows "Series name is required" error
    - Test 3: successful createSeries shows "Series created" toast and closes modal
    - Test 4: duplicate name error shows inline error from server response
    - Test 5: generic server error shows toast "Couldn't create series. Please try again."
  </behavior>
  <action>
    Create test files following designer-list.test.tsx and designer-form-modal.test.tsx patterns exactly.

    For series-list.test.tsx: mock series-actions (deleteSeries), mock next/navigation (useRouter), mock sonner (toast). Create test data using createMockSeriesWithStats with varied progress values — include one series with totalCount set, one open-ended, one with 0 charts. Test sort pills by clicking "Completion" and verifying reordered card names. Test empty state by passing empty array. Test card links by checking href attributes pointing to /series/{id}. Test delete by clicking trash icon, verifying DeleteConfirmationDialog renders.

    For series-form-modal.test.tsx: mock series-actions (createSeries). Render modal with open=true. Test submit validation (empty name). Test successful submission flow. Test error responses (duplicate name with "already exists" in message → inline error; generic error → toast).

    All tests should FAIL at this point (components do not exist yet).
  </action>
  <verify>
    <automated>npm test -- --run src/components/features/series/series-list.test.tsx src/components/features/series/series-form-modal.test.tsx 2>&1 | grep -c "FAIL\|Cannot find module"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/series/series-list.test.tsx exists with 9+ test cases
    - src/components/features/series/series-form-modal.test.tsx exists with 5+ test cases
    - Tests import from @/__tests__/test-utils (not @testing-library/react directly)
    - Tests use createMockSeriesWithStats factory
    - Tests mock series-actions, next/navigation, sonner
    - Tests reference specific UI text: "No series created yet", "Add Series", "Series created", sort labels "Name"/"Completion"/"Charts"
    - All tests fail because components do not exist (RED phase)
  </acceptance_criteria>
  <done>Test files define expected behavior for SeriesList and SeriesFormModal. All tests fail (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SeriesList, SeriesFormModal, and series page (GREEN)</name>
  <files>src/components/features/series/series-list.tsx, src/components/features/series/series-form-modal.tsx, src/app/(dashboard)/series/page.tsx</files>
  <read_first>
    - src/components/features/series/series-list.test.tsx (just created — defines expected behavior)
    - src/components/features/series/series-form-modal.test.tsx (just created)
    - src/components/features/designers/designer-list.tsx
    - src/components/features/designers/designer-form-modal.tsx
    - src/app/(dashboard)/designers/page.tsx
    - .planning/phases/32-series-management-pages/32-UI-SPEC.md (CP-01, CP-02, CP-05, CP-06)
    - .planning/phases/32-series-management-pages/32-PATTERNS.md (series-list section)
  </read_first>
  <action>
    Implement SeriesList (series-list.tsx) as "use client" component:
    - Props: { series: SeriesWithStats[] }
    - Sort state: useState with SortKey ("name" | "completion" | "charts"), SortDir ("asc" | "desc"), default { key: "name", dir: "asc" }
    - useMemo sortedSeries: sort by name (localeCompare), completion (finishedCount/ownedCount ratio, 0-chart series use -1 for sort-to-bottom in asc), charts (ownedCount)
    - Sort bar: "SORT BY" label + 3 pill buttons per CP-02 styling (active: bg-success-muted text-success-muted-foreground, inactive: text-muted-foreground hover:text-foreground, both rounded-full px-3 py-1 text-xs). ChevronUp/ChevronDown per direction.
    - Card grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4. Each card is a Link to /series/{id} with CP-01 styling (bg-card border-border rounded-xl border p-5 hover:shadow-md hover:border-border/80 transition-all).
    - Card content: series name (text-sm font-semibold font-heading), designer line ("by {name}" text-xs text-muted-foreground mt-0.5, only when designerName not null), progress bar (h-2 rounded-full bg-muted container, bg-primary fill, width from finished/owned percent), stat line per D-02, completion percent right-aligned (text-sm font-semibold text-primary).
    - Empty state: EmptyState component with Library icon, title "No series created yet", description "Add your first series to start organizing your collection.", Add Series button as child.
    - Delete: state for deletingSeries, trash icon on each card, DeleteConfirmationDialog with entityType="series" and chartCount from progress.ownedCount.
    - Create: state for createModalOpen, "Add Series" button in header, renders SeriesFormModal.
    - Delete handler: call deleteSeries action, toast.success "Series deleted" on success, router.refresh(). Error: toast.error with result.error fallback.

    Implement SeriesFormModal (series-form-modal.tsx) as "use client" component:
    - Props: { open, onOpenChange, series? (optional for edit mode) }
    - State: name, nameError, totalCount (string input → parse to number | null), notes, designerId
    - useEffect: reset fields when modal opens (populate from series prop if editing)
    - useTransition for pending state on submit
    - Validation: trim name, if empty set nameError "Series name is required"
    - Submit: call createSeries (or updateSeries if editing) with { name, totalCount: parsed number or null, designerId: value or null, notes: value or null }
    - Success: toast.success, router.refresh(), onOpenChange(false)
    - Error: if "already exists" in error → setNameError. Otherwise toast.error "Couldn't create series. Please try again."
    - Dialog structure: DialogHeader with title "Add Series" (or "Edit Series"), form with Name input (required), Total Count number input (optional, placeholder "Total charts in series"), Notes textarea (optional), DialogFooter with "Never mind" outline button + "Create Series" primary button (disabled when pending)
    - Note: Designer SearchableSelect in the create modal is optional for this phase — if complex, omit from modal and rely on detail page editing. But include if straightforward.

    Implement page.tsx (src/app/(dashboard)/series/page.tsx):
    - Server component, no "use client"
    - Import getSeriesWithStats from series-actions, SeriesList from series-list
    - Call getSeriesWithStats(), pass result to SeriesList
  </action>
  <verify>
    <automated>npm test -- --run src/components/features/series/series-list.test.tsx src/components/features/series/series-form-modal.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/series/series-list.tsx exists with "use client" directive
    - SeriesList renders card grid with responsive columns (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3)
    - Sort pills render with labels "Name", "Completion", "Charts"
    - Active sort pill uses bg-success-muted styling
    - Completion sort places 0-chart series at bottom
    - Cards show progress bar with h-2 rounded-full bg-muted and bg-primary fill
    - Empty state uses EmptyState component with Library icon and "No series created yet" title
    - series-form-modal.tsx validates name is required before submit
    - series-form-modal.tsx handles "already exists" error as inline name error
    - src/app/(dashboard)/series/page.tsx is a server component calling getSeriesWithStats
    - All series-list.test.tsx and series-form-modal.test.tsx tests pass (GREEN)
  </acceptance_criteria>
  <done>Series list page fully functional: cards with progress, sorting, create modal, delete confirmation. All tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server actions | createSeries and deleteSeries called from client components with user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering | createSeries input | mitigate | Zod seriesSchema.parse() validates all input in server action (Phase 31) |
| T-32-05 | Tampering | deleteSeries input | mitigate | requireAuth() + findUnique check before delete (Phase 31) |
| T-32-06 | Spoofing | series page access | mitigate | requireAuth() in getSeriesWithStats (Phase 31) |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
- `npm test -- --run src/components/features/series/series-list.test.tsx` all tests pass
- `npm test -- --run src/components/features/series/series-form-modal.test.tsx` all tests pass
- `grep "getSeriesWithStats" src/app/\(dashboard\)/series/page.tsx` confirms server-side data fetch
- `grep -c "use client" src/app/\(dashboard\)/series/page.tsx` returns 0 (server component)
</verification>

<success_criteria>
- /series page renders series as a card grid with progress bars and sort functionality
- Create modal validates input and calls createSeries action
- Delete flow uses DeleteConfirmationDialog with series-specific copy
- Empty state encourages adding first series
- All tests pass, no regressions in full test suite
</success_criteria>

<output>
Create `.planning/phases/32-series-management-pages/32-02-SUMMARY.md` when done
</output>
