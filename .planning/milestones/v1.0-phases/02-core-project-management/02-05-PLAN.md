---
phase: 02-core-project-management
plan: 05
type: execute
wave: 5
depends_on: [02-03, 02-04]
files_modified: []
autonomous: false
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-05]

must_haves:
  truths:
    - "User can create a chart with metadata and see it in the list"
    - "User can upload a cover photo and see it on the detail page"
    - "User can change status and see it update immediately"
    - "User can delete a chart with confirmation"
    - "Size category displays correctly based on stitch count"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 2 feature set with human testing of the full chart lifecycle.

Purpose: Confirm all PROJ-01 through PROJ-05 requirements work end-to-end before marking Phase 2 complete.
Output: Verified working chart management system.
</objective>

<execution_context>
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-project-management/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify full chart lifecycle</name>
  <what-built>
    Complete chart management system: create/edit/delete charts with ~50 metadata fields, cover photo upload via presigned R2 URLs, digital working copy upload, 7-stage status system with dedicated control, auto-calculated size categories, inline designer/genre creation, charts list with thumbnails and empty state.
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    **1. Empty state (PROJ-01):**
    - Navigate to http://localhost:3000/charts
    - Verify "No charts yet" empty state with "Add Your First Chart" button
    - Click the CTA button -- should navigate to /charts/new

    **2. Create a chart (PROJ-01):**
    - Fill in: Chart Name = "Enchanted Forest Sampler"
    - Create a new designer inline: click "Add designer" in the dropdown, enter "Mirabilia Designs", confirm
    - Verify the new designer is auto-selected
    - Set dimensions: Width = 300, Height = 400 (stitch count should auto-calculate to 120,000)
    - Toggle at least 2 genres (create one inline if needed)
    - Check "Paper chart" checkbox
    - Set status to "Kitting"
    - Enter a project bin name
    - Set start date to today
    - Click "Add Chart"
    - Verify redirect to detail page with toast "Chart added"

    **3. Verify detail page (PROJ-01, PROJ-04, PROJ-05):**
    - Cover image shows "No cover image" placeholder
    - Status badge shows "Kitting" in amber color
    - Size badge shows "BAP" (120,000 stitches > 50,000 threshold)
    - Stitch count shows "120,000 stitches" with commas
    - Dimensions show "300 x 400"
    - Designer shows "Mirabilia Designs"
    - Pattern Details card shows "Paper" format
    - Dates card shows today's start date
    - Tabs show Overview (active), Supplies (placeholder), Sessions (placeholder)

    **4. Change status (PROJ-04):**
    - Use the status control dropdown to change to "In Progress"
    - Verify toast "Status changed to Stitching"
    - Verify badge updates to sky blue "Stitching"

    **5. Upload cover photo (PROJ-02) -- requires R2 configured:**
    - If R2 is configured: drag or click to upload a JPG/PNG image
    - Verify upload progress indicator
    - Verify image appears in the upload zone and on the detail page
    - If R2 is NOT configured: verify graceful message "File storage is not configured"

    **6. Upload digital working copy (PROJ-03) -- requires R2 configured:**
    - Edit the chart, upload a PDF or image as working copy
    - Verify filename appears with download link on detail page
    - If R2 is NOT configured: verify graceful degradation message

    **7. Edit chart (PROJ-01):**
    - Click Edit button on detail page
    - Verify all fields are pre-populated
    - Change the chart name to "Enchanted Forest Sampler v2"
    - Click "Save Changes"
    - Verify redirect and toast "Changes saved"
    - Verify name updated on detail page

    **8. Delete chart (PROJ-01):**
    - Click Delete button on detail page
    - Verify confirmation dialog appears with "Keep Chart" and "Delete Chart" buttons
    - Verify "Keep Chart" has focus (not the destructive button)
    - Click "Delete Chart"
    - Verify redirect to /charts with toast "Chart deleted"
    - Verify chart is gone from the list

    **9. Charts list with thumbnails (PROJ-01, PROJ-02):**
    - Create 2-3 more charts with different statuses and stitch counts
    - If R2 configured: upload a cover photo on at least one
    - Verify list shows: thumbnail (or placeholder), name, designer, status badge, size badge, date
    - Verify cover thumbnail appears in list for charts with uploaded covers
    - Verify clicking a row navigates to the detail page
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification of all PROJ-01 through PROJ-05 requirements in the browser.
</verification>

<success_criteria>
- All 5 requirements (PROJ-01 through PROJ-05) verified as working
- Chart CRUD lifecycle complete (create, read, update, delete)
- File uploads work (or gracefully degrade if R2 not configured)
- Status system works with all 7 statuses
- Size category auto-calculates correctly
- Cover thumbnails visible in charts list page (PROJ-02)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-project-management/02-05-SUMMARY.md`
</output>
