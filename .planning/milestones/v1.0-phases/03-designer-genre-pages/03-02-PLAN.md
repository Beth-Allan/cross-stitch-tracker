---
phase: 03-designer-genre-pages
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/shell/nav-items.ts
  - src/app/(dashboard)/designers/page.tsx
  - src/components/features/designers/designer-list.tsx
  - src/components/features/designers/designer-list.test.tsx
  - src/components/features/designers/designer-form-modal.tsx
  - src/components/features/designers/designer-form-modal.test.tsx
autonomous: true
requirements:
  - PROJ-06

must_haves:
  truths:
    - "User can see a list of all designers with chart counts in a sortable table"
    - "User can search designers by name"
    - "User can create a new designer via modal form with name, website, and notes fields"
    - "User can edit an existing designer via modal form with pre-filled fields"
    - "Designers and Genres appear as sidebar navigation items below Charts"
    - "Designer table sorts by name (default A-Z), chart count, or finished count"
    - "Mobile view shows card layout instead of table"
  artifacts:
    - path: "src/app/(dashboard)/designers/page.tsx"
      provides: "Server Component list page"
      contains: "getDesignersWithStats"
    - path: "src/components/features/designers/designer-list.tsx"
      provides: "Client sortable table with search and modal triggers"
      contains: "use client"
    - path: "src/components/features/designers/designer-form-modal.tsx"
      provides: "Create/edit modal form"
      contains: "use client"
    - path: "src/components/shell/nav-items.ts"
      provides: "Designers and Genres nav items"
      contains: "Designers"
  key_links:
    - from: "src/app/(dashboard)/designers/page.tsx"
      to: "src/lib/actions/designer-actions.ts"
      via: "getDesignersWithStats() call"
      pattern: "getDesignersWithStats"
    - from: "src/components/features/designers/designer-list.tsx"
      to: "src/lib/actions/designer-actions.ts"
      via: "createDesigner and updateDesigner calls"
      pattern: "(createDesigner|updateDesigner)"
---

<objective>
Build the designer list page with sortable table, search, genre filter, modal forms for create/edit, and sidebar navigation. This delivers the primary designer management experience.

Purpose: Users need a dedicated page to browse, search, and manage their designer collection -- not just inline creation from the chart form.
Output: Working /designers page with full CRUD via modals, plus Designers and Genres in sidebar nav.
</objective>

<execution_context>
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-designer-genre-pages/03-CONTEXT.md
@.planning/phases/03-designer-genre-pages/03-RESEARCH.md
@.planning/phases/03-designer-genre-pages/03-UI-SPEC.md
@.planning/phases/03-designer-genre-pages/03-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan consumes -->

From src/types/designer.ts (created in Plan 01):
```typescript
export type DesignerWithStats = {
  id: string;
  name: string;
  website: string | null;
  notes: string | null;
  chartCount: number;
};
```

From src/lib/actions/designer-actions.ts (extended in Plan 01):
```typescript
export async function getDesignersWithStats(): Promise<DesignerWithStats[]>;
export async function createDesigner(formData: unknown): Promise<{ success: true; designer: Designer } | { success: false; error: string }>;
export async function updateDesigner(id: string, formData: unknown): Promise<{ success: true; designer: Designer } | { success: false; error: string }>;
```

From src/components/shell/nav-items.ts (current):
```typescript
export interface NavItem { label: string; href: string; icon: LucideIcon; }
export const navigationItems: NavItem[] = [
  { label: "Dashboard", href: "/", icon: LayoutDashboard },
  { label: "Charts", href: "/charts", icon: Scissors },
  { label: "Supplies", href: "/supplies", icon: Package },
  // ... rest
];
```

From src/components/features/charts/form-primitives/form-field.tsx:
```typescript
// FormField component for label + input + error layout
```

From src/components/ui/dialog.tsx:
```typescript
export { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle, DialogTrigger, DialogDescription };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Designer list component, form modal, and tests</name>
  <files>
    src/components/features/designers/designer-list.tsx,
    src/components/features/designers/designer-list.test.tsx,
    src/components/features/designers/designer-form-modal.tsx,
    src/components/features/designers/designer-form-modal.test.tsx
  </files>
  <read_first>
    src/components/features/charts/inline-designer-dialog.tsx,
    src/components/features/charts/chart-detail.tsx,
    src/app/(dashboard)/charts/page.tsx,
    src/components/ui/dialog.tsx,
    src/components/ui/input.tsx,
    src/components/ui/textarea.tsx,
    src/components/features/charts/form-primitives/form-field.tsx,
    src/components/ui/button.tsx,
    src/components/ui/link-button.tsx,
    src/types/designer.ts,
    product-plan/sections/fabric-series-and-reference-data/components/DesignerPage.tsx,
    product-plan/sections/fabric-series-and-reference-data/components/DesignerFormModal.tsx,
    .planning/phases/03-designer-genre-pages/03-UI-SPEC.md,
    .planning/phases/03-designer-genre-pages/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test: DesignerList renders table headers (DESIGNER, WEB, CHARTS, STARTED, FINISHED, TOP GENRE)
    - Test: DesignerList renders designer rows with name, website, and chart count
    - Test: DesignerList shows empty state with "No designers added yet" when list is empty
    - Test: DesignerList shows filtered empty state "No designers match your filters" when search has no results
    - Test: DesignerList search input filters designers by name (case-insensitive)
    - Test: DesignerList clicking "Add Designer" button opens form modal
    - Test: DesignerFormModal renders name, website, notes fields
    - Test: DesignerFormModal in create mode shows "Add Designer" title and button
    - Test: DesignerFormModal in edit mode shows "Edit Designer" title and "Save Changes" button with pre-filled fields
    - Test: DesignerFormModal shows validation error when name is empty
  </behavior>
  <action>
    **Write tests first (red phase), then implement.**

    **DesignerFormModal (`src/components/features/designers/designer-form-modal.tsx`):**

    "use client" component. Props: `{ open: boolean; onOpenChange: (open: boolean) => void; designer?: DesignerWithStats | null; onSuccess?: () => void; }`. When `designer` is provided, it's edit mode.

    Layout per UI-SPEC modal form wireframe:
    - Dialog with DialogContent, DialogHeader, DialogTitle ("Add Designer" or "Edit Designer")
    - Form with 3 FormField components:
      - Name (Input, required, placeholder "e.g. Heaven and Earth Designs")
      - Website (Input type="url", optional, placeholder "https://example.com")
      - Notes (Textarea, optional, placeholder "Optional personal reference (e.g., \"Only sells through distributors\")", max 5000)
    - Helper text below notes: "Chart count, genre, and project stats are calculated automatically from your projects."
    - DialogFooter: Cancel button (variant="outline"), Submit button (variant="default")
    - Submit text: create mode "Add Designer" / "Adding...", edit mode "Save Changes" / "Saving..."
    - Use `useTransition` for pending state (not manual isPending useState)
    - On submit: call `createDesigner({ name, website: website || null, notes: notes || null })` or `updateDesigner(designer.id, { name, website: website || null, notes: notes || null })`
    - Handle success: `toast.success("Designer created")` or `toast.success("Designer updated")`, call `onSuccess?.()`, close dialog
    - Handle error: if result.success is false, show error below name field (for duplicate names) or toast.error
    - Reset form state when dialog opens (useEffect keyed on open + designer)

    **DesignerList (`src/components/features/designers/designer-list.tsx`):**

    "use client" component. Props: `{ designers: DesignerWithStats[] }`.

    State:
    - `sort: { key: 'name' | 'chartCount' | 'projectsFinished'; dir: 'asc' | 'desc' }` -- default `{ key: 'name', dir: 'asc' }` (per discretion: A-Z default)
    - `search: string` -- empty string default
    - `createModalOpen: boolean`
    - `editingDesigner: DesignerWithStats | null`

    Layout per UI-SPEC Designer List Page wireframe:
    - Header row: h1 "Designers" (`font-heading text-2xl font-semibold`) + "Add Designer" button (primary, Plus icon)
    - Search bar: Input with Search icon (16px, absolute left), placeholder "Search designers..."
    - "Clear all" text button appears when search is non-empty
    - Desktop table (`md:` and above): rounded-xl border-border bg-card container
      - Sortable column headers: DESIGNER (sortable by name), WEB (not sortable), CHARTS (sortable by chartCount), STARTED (not sortable), FINISHED (sortable by projectsFinished), TOP GENRE (not sortable)
      - SortableHeader sub-component: clickable, shows ChevronUp/ChevronDown (w-3 h-3) when active, active header text uses `text-primary`, inactive uses `text-muted-foreground`
      - Designer rows: name as Link to `/designers/[id]`, website as ExternalLink icon (or em dash if null), chart count, started count (em dash -- not available from DesignerWithStats; show dash), finished count (em dash), top genre badge (em dash)
      - Row hover: `hover:bg-muted/50 transition-colors`
      - Row action buttons on hover: Edit (Pencil w-3.5 h-3.5, `aria-label="Edit {name}"`) and Delete (Trash2 w-3.5 h-3.5, `aria-label="Delete {name}"`) -- edit opens form modal, delete is handled by detail page (per D-08/D-15, but list also has row actions)
    - Mobile cards (`< md`): Card per designer with stacked layout, name + stats, always-visible edit/delete icons
    - Empty state (no designers): Users icon (48px, text-muted-foreground/40) + "No designers added yet" heading + "Add your first designer to start organizing your collection." body + "Add Designer" button
    - Empty state (search active, no results): "No designers match your filters" + "Clear all" button

    Note: DesignerWithStats from Plan 01 has id, name, website, notes, chartCount. The "Started", "Finished", and "Top Genre" columns need data from the detail query. For the list page, show chartCount but display em dash (--) for started/finished/topGenre since these require per-designer chart aggregation that would be expensive for the list. This is acceptable -- the detail page (Plan 04) shows full stats.

    Actually, re-reading D-07 and the DesignOS DesignerPage.tsx more carefully: the DesignOS reference shows projectsStarted, projectsFinished, topGenre in the LIST page table. The DesignerWithStats type from RESEARCH only has chartCount. To fully honor the DesignOS reference, extend the list query to include these. BUT this requires a more expensive query (joining through charts->projects for each designer). For a single-user app with <100 designers, this is fine.

    **Update approach:** Modify the DesignerList component to accept the richer data. The Plan 01 `getDesignersWithStats` action returns basic stats. We need to either:
    a) Extend `getDesignersWithStats` to include projectsStarted/projectsFinished/topGenre (expensive but accurate), OR
    b) Show only chartCount in the list, full stats on detail

    Going with (b) for now -- the list shows chartCount, and the extra columns show em dashes. The detail page (Plan 04) provides full stats. This avoids N+1 query complexity on the list page. The columns still exist per UI-SPEC but with dash values indicating "see detail for full stats."

    Wire the DesignerFormModal into DesignerList: "Add Designer" button opens create modal, row edit icon opens edit modal with pre-filled data.

    Use `useRouter().refresh()` after successful create/edit to refresh server component data (revalidatePath already called in the action).
  </action>
  <verify>
    <automated>npx vitest run src/components/features/designers/designer-list.test.tsx src/components/features/designers/designer-form-modal.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/designers/designer-list.tsx contains "use client"
    - src/components/features/designers/designer-list.tsx contains "No designers added yet"
    - src/components/features/designers/designer-list.tsx contains "Search designers..."
    - src/components/features/designers/designer-list.tsx contains 'aria-label={`Edit ${' (accessible icon buttons)
    - src/components/features/designers/designer-list.tsx contains 'aria-label={`Delete ${' (accessible icon buttons)
    - src/components/features/designers/designer-list.tsx contains SortableHeader or sortable header implementation with ChevronUp/ChevronDown
    - src/components/features/designers/designer-form-modal.tsx contains "use client"
    - src/components/features/designers/designer-form-modal.tsx contains "Add Designer" and "Edit Designer"
    - src/components/features/designers/designer-form-modal.tsx contains "Save Changes"
    - src/components/features/designers/designer-form-modal.tsx contains Textarea import for notes field
    - src/components/features/designers/designer-form-modal.tsx contains toast.success
    - All tests in designer-list.test.tsx pass
    - All tests in designer-form-modal.test.tsx pass
  </acceptance_criteria>
  <done>Designer list component renders sortable table with search, empty states, and row actions. Designer form modal handles create/edit with validation and toast feedback. All component tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Designer list page route and sidebar navigation</name>
  <files>
    src/app/(dashboard)/designers/page.tsx,
    src/components/shell/nav-items.ts
  </files>
  <read_first>
    src/app/(dashboard)/charts/page.tsx,
    src/components/shell/nav-items.ts,
    src/components/features/designers/designer-list.tsx,
    src/types/designer.ts,
    src/lib/actions/designer-actions.ts
  </read_first>
  <action>
    **1. Create the designer list page route (`src/app/(dashboard)/designers/page.tsx`):**

    Server Component following the exact pattern from `charts/page.tsx`:
    ```typescript
    import { getDesignersWithStats } from "@/lib/actions/designer-actions";
    import { DesignerList } from "@/components/features/designers/designer-list";

    export default async function DesignersPage() {
      const designers = await getDesignersWithStats();
      return <DesignerList designers={designers} />;
    }
    ```

    No "use client" -- this is a Server Component that fetches data and passes to client component.

    **2. Add Designers and Genres to sidebar navigation (per D-11):**

    In `src/components/shell/nav-items.ts`, add two imports and two items:
    - Import `Paintbrush` and `Tags` from `lucide-react` (verify these icon names exist; fall back to `Users` and `Tag` if not)
    - Insert after the Charts item (index 1):
      ```typescript
      { label: "Designers", href: "/designers", icon: Paintbrush },
      { label: "Genres", href: "/genres", icon: Tags },
      ```
    - Final order: Dashboard, Charts, Designers, Genres, Supplies, Sessions, Statistics, Shopping, Settings

    Verify the icons exist by checking the lucide-react package. If `Paintbrush` or `Tags` are not available, use `Users` for Designers and `Tag` for Genres.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(dashboard)/designers/page.tsx exists and does NOT contain "use client"
    - src/app/(dashboard)/designers/page.tsx contains "getDesignersWithStats"
    - src/app/(dashboard)/designers/page.tsx contains "DesignerList"
    - src/components/shell/nav-items.ts contains `{ label: "Designers"`
    - src/components/shell/nav-items.ts contains `{ label: "Genres"`
    - src/components/shell/nav-items.ts has Designers positioned after Charts
    - `npm run build` exits 0 (no type errors, route compiles)
  </acceptance_criteria>
  <done>Designer list page route created as Server Component. Designers and Genres added to sidebar nav below Charts. Build passes with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form data -> Server action | Modal form data passes to createDesigner/updateDesigner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | DesignerFormModal form submission | mitigate | Server-side Zod validation in action (Plan 01); client-side is convenience only |
| T-03-08 | Spoofing | Page route data fetching | mitigate | `getDesignersWithStats` calls `requireAuth()` -- unauthenticated users see nothing |
</threat_model>

<verification>
1. `npm run build` -- exits 0, no type errors
2. `npx vitest run src/components/features/designers/` -- all component tests pass
3. `npm test -- --run` -- full suite green
4. Sidebar shows "Designers" and "Genres" items below "Charts"
</verification>

<success_criteria>
- /designers route renders sortable table of designers with search
- Modal form creates new designers with name, website, notes
- Modal form edits existing designers with pre-filled fields
- Sidebar shows Designers and Genres navigation items below Charts
- Empty states render correctly for no-data and no-search-results
- All component and integration tests pass
- Build succeeds with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-designer-genre-pages/03-02-SUMMARY.md`
</output>
