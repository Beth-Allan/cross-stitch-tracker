---
phase: 03-designer-genre-pages
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/(dashboard)/genres/page.tsx
  - src/components/features/genres/genre-list.tsx
  - src/components/features/genres/genre-list.test.tsx
  - src/components/features/genres/genre-form-modal.tsx
  - src/components/features/genres/genre-form-modal.test.tsx
autonomous: true
requirements:
  - PROJ-07

must_haves:
  truths:
    - "User can see a list of all genres with chart counts in a sortable table"
    - "User can search genres by name"
    - "User can create a new genre via modal form with name field only"
    - "User can edit an existing genre via modal form with pre-filled name"
    - "Genre table sorts by name (default A-Z) and chart count"
    - "Genre page mirrors designer page layout per D-01"
    - "Mobile view shows card layout instead of table"
  artifacts:
    - path: "src/app/(dashboard)/genres/page.tsx"
      provides: "Server Component list page"
      contains: "getGenresWithStats"
    - path: "src/components/features/genres/genre-list.tsx"
      provides: "Client sortable table with search and modal triggers"
      contains: "use client"
    - path: "src/components/features/genres/genre-form-modal.tsx"
      provides: "Create/edit modal form"
      contains: "use client"
  key_links:
    - from: "src/app/(dashboard)/genres/page.tsx"
      to: "src/lib/actions/genre-actions.ts"
      via: "getGenresWithStats() call"
      pattern: "getGenresWithStats"
    - from: "src/components/features/genres/genre-list.tsx"
      to: "src/lib/actions/genre-actions.ts"
      via: "createGenre and updateGenre calls"
      pattern: "(createGenre|updateGenre)"
---

<objective>
Build the genre list page with sortable table, search, and modal forms -- mirroring the designer page pattern per D-01. Genre pages are simpler (2 sortable columns, name-only form) but follow the same layout conventions.

Purpose: Users need dedicated genre management, not just inline creation from the chart form. Consistency with the designer page reduces cognitive load.
Output: Working /genres page with full CRUD via modals.
</objective>

<execution_context>
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-designer-genre-pages/03-CONTEXT.md
@.planning/phases/03-designer-genre-pages/03-RESEARCH.md
@.planning/phases/03-designer-genre-pages/03-UI-SPEC.md
@.planning/phases/03-designer-genre-pages/03-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan consumes -->

From src/types/genre.ts (created in Plan 01):
```typescript
export type GenreWithStats = {
  id: string;
  name: string;
  chartCount: number;
};
```

From src/lib/actions/genre-actions.ts (extended in Plan 01):
```typescript
export async function getGenresWithStats(): Promise<GenreWithStats[]>;
export async function createGenre(formData: unknown): Promise<{ success: true; genre: Genre } | { success: false; error: string }>;
export async function updateGenre(id: string, formData: unknown): Promise<{ success: true; genre: Genre } | { success: false; error: string }>;
```

From src/components/ui/ (reused):
```typescript
// Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle from dialog.tsx
// Button from button.tsx
// Input from input.tsx
// FormField from features/charts/form-primitives/form-field.tsx
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Genre list component, form modal, and tests</name>
  <files>
    src/components/features/genres/genre-list.tsx,
    src/components/features/genres/genre-list.test.tsx,
    src/components/features/genres/genre-form-modal.tsx,
    src/components/features/genres/genre-form-modal.test.tsx
  </files>
  <read_first>
    src/components/features/designers/designer-list.tsx,
    src/components/features/designers/designer-form-modal.tsx,
    src/components/features/charts/inline-designer-dialog.tsx,
    src/app/(dashboard)/charts/page.tsx,
    src/components/ui/dialog.tsx,
    src/components/ui/input.tsx,
    src/components/features/charts/form-primitives/form-field.tsx,
    src/types/genre.ts,
    .planning/phases/03-designer-genre-pages/03-UI-SPEC.md,
    .planning/phases/03-designer-genre-pages/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test: GenreList renders table headers (GENRE, CHARTS) -- only 2 sortable columns per D-01
    - Test: GenreList renders genre rows with name and chart count
    - Test: GenreList shows empty state with "No genres added yet" when list is empty
    - Test: GenreList shows filtered empty state "No genres match your search" when search has no results
    - Test: GenreList search input filters genres by name (case-insensitive)
    - Test: GenreList clicking "Add Genre" button opens form modal
    - Test: GenreFormModal renders name field only (no website, no notes per D-04, D-17)
    - Test: GenreFormModal in create mode shows "Add Genre" title and button
    - Test: GenreFormModal in edit mode shows "Edit Genre" title and "Save Changes" button with pre-filled name
    - Test: GenreFormModal shows validation error when name is empty
  </behavior>
  <action>
    **Write tests first (red phase), then implement.**

    **GenreFormModal (`src/components/features/genres/genre-form-modal.tsx`):**

    "use client" component. Props: `{ open: boolean; onOpenChange: (open: boolean) => void; genre?: GenreWithStats | null; onSuccess?: () => void; }`. When `genre` is provided, it's edit mode.

    Layout per UI-SPEC Genre Form Modal wireframe:
    - Dialog with DialogContent, DialogHeader, DialogTitle ("Add Genre" or "Edit Genre")
    - Single FormField: Name (Input, required, placeholder "e.g. Landscape")
    - No website field, no notes field (per D-04, D-17 -- genres are simple tags)
    - DialogFooter: Cancel button (variant="outline"), Submit button (variant="default")
    - Submit text: create mode "Add Genre" / "Adding...", edit mode "Save Changes" / "Saving..."
    - Use `useTransition` for pending state
    - On submit: call `createGenre({ name })` or `updateGenre(genre.id, { name })`
    - Handle success: `toast.success("Genre created")` or `toast.success("Genre updated")`, call `onSuccess?.()`, close dialog
    - Handle error: if result.success is false, show error below name field
    - Reset form state when dialog opens

    **GenreList (`src/components/features/genres/genre-list.tsx`):**

    "use client" component. Props: `{ genres: GenreWithStats[] }`.

    This MIRRORS the designer list layout per D-01, D-02, D-03 but with simpler columns:

    State:
    - `sort: { key: 'name' | 'chartCount'; dir: 'asc' | 'desc' }` -- default `{ key: 'name', dir: 'asc' }`
    - `search: string`
    - `createModalOpen: boolean`
    - `editingGenre: GenreWithStats | null`

    Layout per UI-SPEC Genre List Page wireframe:
    - Header row: h1 "Genres" (`font-heading text-2xl font-semibold`) + "Add Genre" button (primary, Plus icon)
    - Search bar: Input with Search icon, placeholder "Search genres..." (per D-03)
    - "Clear all" text button when search is non-empty
    - Desktop table (`md:` and above): 2 sortable columns
      - GENRE (sortable by name) -- genre name as Link to `/genres/[id]`
      - CHARTS (sortable by chartCount) -- number
      - Same SortableHeader pattern: active column text `text-primary`, ChevronUp/Down (w-3 h-3)
      - Row hover: `hover:bg-muted/50 transition-colors`
      - Row action buttons on hover: Edit (Pencil, `aria-label="Edit {name}"`) and Delete (Trash2, `aria-label="Delete {name}"`)
    - Mobile cards (`< md`): Card per genre, name + chart count, always-visible edit/delete
    - Empty state (no genres): Tags icon (or Palette icon) + "No genres added yet" heading + "Add your first genre" body + "Add Genre" button
    - Empty state (search, no results): "No genres match your search" + "Clear all" button

    Wire GenreFormModal: "Add Genre" button opens create modal, row edit icon opens edit modal.

    Use `useRouter().refresh()` after successful create/edit.

    **Consistent patterns with designer list:**
    - Same SortableHeader sub-component structure (or extract shared if needed, but for 2 components, inline is fine)
    - Same search/filter bar layout
    - Same empty state pattern
    - Same row action icon sizing and aria-labels
    - Same bg-card rounded-xl border-border table container
    - Same mobile breakpoint (`md:` for table, `< md` for cards)
  </action>
  <verify>
    <automated>npx vitest run src/components/features/genres/genre-list.test.tsx src/components/features/genres/genre-form-modal.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/genres/genre-list.tsx contains "use client"
    - src/components/features/genres/genre-list.tsx contains "No genres added yet"
    - src/components/features/genres/genre-list.tsx contains "Search genres..."
    - src/components/features/genres/genre-list.tsx contains 'aria-label={`Edit ${' (accessible icon buttons)
    - src/components/features/genres/genre-list.tsx contains 'aria-label={`Delete ${' (accessible icon buttons)
    - src/components/features/genres/genre-list.tsx has only 2 sortable column headers (GENRE, CHARTS), not 6
    - src/components/features/genres/genre-form-modal.tsx contains "use client"
    - src/components/features/genres/genre-form-modal.tsx contains "Add Genre" and "Edit Genre"
    - src/components/features/genres/genre-form-modal.tsx contains "Save Changes"
    - src/components/features/genres/genre-form-modal.tsx does NOT contain Textarea (no notes field per D-04)
    - src/components/features/genres/genre-form-modal.tsx does NOT contain "website" (no website field per D-04)
    - All tests in genre-list.test.tsx pass
    - All tests in genre-form-modal.test.tsx pass
  </acceptance_criteria>
  <done>Genre list component renders 2-column sortable table with search, matching designer page layout. Genre form modal has name-only field. All component tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Genre list page route</name>
  <files>
    src/app/(dashboard)/genres/page.tsx
  </files>
  <read_first>
    src/app/(dashboard)/designers/page.tsx,
    src/app/(dashboard)/charts/page.tsx,
    src/components/features/genres/genre-list.tsx,
    src/types/genre.ts,
    src/lib/actions/genre-actions.ts
  </read_first>
  <action>
    **Create the genre list page route (`src/app/(dashboard)/genres/page.tsx`):**

    Server Component, same pattern as designers/page.tsx:
    ```typescript
    import { getGenresWithStats } from "@/lib/actions/genre-actions";
    import { GenreList } from "@/components/features/genres/genre-list";

    export default async function GenresPage() {
      const genres = await getGenresWithStats();
      return <GenreList genres={genres} />;
    }
    ```

    No "use client" -- Server Component fetches data, passes to client.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(dashboard)/genres/page.tsx exists and does NOT contain "use client"
    - src/app/(dashboard)/genres/page.tsx contains "getGenresWithStats"
    - src/app/(dashboard)/genres/page.tsx contains "GenreList"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Genre list page route created as Server Component. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form data -> Server action | Modal form data passes to createGenre/updateGenre |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | GenreFormModal form submission | mitigate | Server-side Zod validation in action (Plan 01); client form is convenience only |
| T-03-10 | Spoofing | Genre page route data fetching | mitigate | `getGenresWithStats` calls `requireAuth()` |
</threat_model>

<verification>
1. `npm run build` -- exits 0, no type errors
2. `npx vitest run src/components/features/genres/` -- all component tests pass
3. `npm test -- --run` -- full suite green
</verification>

<success_criteria>
- /genres route renders 2-column sortable table of genres with search
- Genre page mirrors designer page layout (per D-01)
- Modal form creates/edits genres with name-only field (per D-04, D-17)
- No website or notes fields on genre form (per D-04)
- Empty states render correctly
- All component tests pass
- Build succeeds with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-designer-genre-pages/03-03-SUMMARY.md`
</output>
