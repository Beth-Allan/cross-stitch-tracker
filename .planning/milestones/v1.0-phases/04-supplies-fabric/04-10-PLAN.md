---
phase: 04-supplies-fabric
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/fabric/fabric-form-modal.tsx
  - src/lib/actions/fabric-actions.test.ts
autonomous: true
gap_closure: true
requirements:
  - REF-01
must_haves:
  truths:
    - "TypeScript build completes without errors (npm run build passes)"
    - "Full test suite passes (npx vitest run exits with 0 failures)"
  artifacts:
    - path: "src/components/features/fabric/fabric-form-modal.tsx"
      provides: "FabricBrandWithCounts construction with all required fields"
      contains: "createdAt"
    - path: "src/lib/actions/fabric-actions.test.ts"
      provides: "Correct test assertion matching getFabric implementation"
      contains: "id: true"
  key_links:
    - from: "src/components/features/fabric/fabric-form-modal.tsx"
      to: "src/lib/actions/fabric-actions.ts"
      via: "createFabricBrand return value spread"
      pattern: "result\\.brand"
---

<objective>
Fix two code-quality gaps identified in VERIFICATION.md that block production deployment: a TypeScript build error in fabric-form-modal.tsx (missing createdAt/updatedAt fields) and a stale test assertion in fabric-actions.test.ts (missing id: true in chart select).

Purpose: Unblock `npm run build` and make the full test suite pass so Phase 4 can proceed through deployment gates.
Output: Both files fixed, build passes, all tests green.
</objective>

<execution_context>
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supplies-fabric/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FabricBrandWithCounts missing fields in fabric-form-modal.tsx</name>
  <files>src/components/features/fabric/fabric-form-modal.tsx</files>
  <read_first>
    - src/components/features/fabric/fabric-form-modal.tsx
    - src/types/fabric.ts
    - src/lib/actions/fabric-actions.ts (lines 11-34, createFabricBrand return shape)
  </read_first>
  <action>
In `fabric-form-modal.tsx`, find the `handleAddBrand` function (around line 78). The constructed `FabricBrandWithCounts` object is missing `createdAt` and `updatedAt` fields that the Prisma `FabricBrand` type requires.

The `createFabricBrand` server action returns `result.brand` which is a full Prisma `FabricBrand` object containing `id`, `name`, `website`, `createdAt`, and `updatedAt`.

Replace the manually constructed object:
```tsx
const newBrand: FabricBrandWithCounts = {
  id: result.brand.id,
  name: result.brand.name,
  website: null,
  _count: { fabrics: 0 },
};
```

With a spread of `result.brand` plus the `_count` field:
```tsx
const newBrand: FabricBrandWithCounts = {
  ...result.brand,
  _count: { fabrics: 0 },
};
```

This includes `id`, `name`, `website`, `createdAt`, and `updatedAt` from the server response, plus the locally computed `_count` (0 fabrics since the brand was just created). No other changes to the file.
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npx tsc --noEmit 2>&1 | grep -c "fabric-form-modal" || echo "0 errors (clean)"</automated>
  </verify>
  <acceptance_criteria>
    - grep "\.\.\.result\.brand" src/components/features/fabric/fabric-form-modal.tsx returns a match
    - grep "_count.*fabrics.*0" src/components/features/fabric/fabric-form-modal.tsx returns a match
    - grep -c "createdAt\|updatedAt" in the manually constructed object lines is 0 (fields come from spread, not explicit)
    - npx tsc --noEmit completes with no errors referencing fabric-form-modal.tsx
  </acceptance_criteria>
  <done>FabricBrandWithCounts object includes all required fields via spread of result.brand. TypeScript compilation passes for this file.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale test assertion in fabric-actions.test.ts</name>
  <files>src/lib/actions/fabric-actions.test.ts</files>
  <read_first>
    - src/lib/actions/fabric-actions.test.ts (lines 530-550)
    - src/lib/actions/fabric-actions.ts (lines 175-194, getFabric implementation)
  </read_first>
  <action>
In `fabric-actions.test.ts`, find the test around line 539-549 that asserts the `getFabric` Prisma call shape. The test currently asserts:

```ts
chart: { select: { name: true, stitchesWide: true, stitchesHigh: true } },
```

But the `getFabric` implementation (fabric-actions.ts line 185) includes `id: true` in the chart select:

```ts
chart: { select: { id: true, name: true, stitchesWide: true, stitchesHigh: true } },
```

The `id: true` was added to support the `FabricWithProject` type (needed for linking to chart detail pages). Update line 545 of the test to include `id: true`:

```ts
chart: { select: { id: true, name: true, stitchesWide: true, stitchesHigh: true } },
```

No other changes to the file.
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npx vitest run src/lib/actions/fabric-actions.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "id: true, name: true, stitchesWide: true, stitchesHigh: true" src/lib/actions/fabric-actions.test.ts returns a match
    - npx vitest run src/lib/actions/fabric-actions.test.ts exits with 0 failures
  </acceptance_criteria>
  <done>Test assertion matches the getFabric implementation. All fabric-actions tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. Both fixes are internal code corrections (type fix + test fix) with no user input, no API changes, and no auth changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10-01 | N/A | N/A | accept | No security surface — changes are a type-level fix and a test assertion correction. No runtime behavior change. |
</threat_model>

<verification>
1. `npm run build` completes without TypeScript errors
2. `npx vitest run` completes with 0 test failures
3. No regressions: existing fabric form modal behavior unchanged (spread preserves all field values from server response)
</verification>

<success_criteria>
- TypeScript build passes: `npm run build` exits 0
- Full test suite passes: `npx vitest run` exits with 0 failures
- Both VERIFICATION.md gaps resolved
</success_criteria>

<output>
After completion, create `.planning/phases/04-supplies-fabric/04-10-SUMMARY.md`
</output>
