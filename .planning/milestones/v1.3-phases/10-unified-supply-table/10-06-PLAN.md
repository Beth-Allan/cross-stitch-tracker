---
phase: 10-unified-supply-table
plan: 06
type: execute
wave: 5
depends_on: ["10-05"]
files_modified: []
autonomous: false
requirements:
  - SUPTBL-01
  - SUPTBL-02
  - SUPTBL-03
  - SUPTBL-04
  - SUPENT-01
  - SUPENT-02
  - SUPENT-03
  - SUPENT-04

user_setup: []

must_haves:
  truths:
    - "All supply-table tests pass"
    - "Full test suite passes with no regressions"
    - "TypeScript compiles without errors"
    - "User visually confirms the component renders correctly"
  artifacts: []
  key_links: []
---

<objective>
Run the full test suite and type check to verify the complete Unified Supply Table component system, then present a visual checkpoint for the developer to verify the rendered output.

Purpose: This is the phase gate -- confirming everything works before Phase 10 is marked complete. The automated checks catch functional regressions; the human checkpoint catches visual/interaction issues that tests alone cannot verify.

Output: Verification confirmation
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-unified-supply-table/10-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full verification suite</name>
  <files></files>
  <read_first>
    src/components/features/supply-table/index.ts,
    src/components/features/supply-table/supply-table.tsx
  </read_first>
  <action>
    Run three verification commands in sequence:

    1. **Supply-table tests only:**
       ```bash
       npx vitest run src/components/features/supply-table/ --reporter=verbose
       ```
       Expect: all tests in the directory pass (0 failures)

    2. **Full test suite:**
       ```bash
       npm test -- --run
       ```
       Expect: all existing tests still pass (no regressions from globals.css change or new imports)

    3. **TypeScript compilation:**
       ```bash
       npx tsc --noEmit
       ```
       Expect: no type errors (0 exit code)

    If any command fails, diagnose and fix the issue before proceeding to the checkpoint. Common issues:
    - Missing import in index.ts -> add the export
    - Type mismatch between adapter interface and implementation -> fix types.ts or implementation
    - globals.css change breaking existing animation tests -> check reduced-motion block
    - Import path issues -> verify all relative imports are correct
  </action>
  <verify>
    <automated>npx vitest run src/components/features/supply-table/ --reporter=verbose && npm test -- --run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/features/supply-table/` exits 0 with all tests passing
    - `npm test -- --run` exits 0 with full suite passing
    - `npx tsc --noEmit` exits 0 with no type errors
  </acceptance_criteria>
  <done>
    All three verification commands pass with 0 failures.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Developer verification of Unified Supply Table</name>
  <action>
    Present the complete Unified Supply Table component system for developer review.

    What was built:
    - Root SupplyTable with grouped sections (Thread/Beads/Specialty)
    - Persistent add row with segmented type toggle and portal autocomplete
    - Keyboard-first entry flow (search -> select -> qty -> Enter to commit)
    - SVG donut status indicators
    - Inline cell editing (stitches/need/have)
    - Hover-reveal delete buttons
    - Auto-calculated thread need with sparkle indicator
    - Section dividers with count badges
    - Footer with running totals and keyboard hints
    - slideIn animation for new rows
    - Empty state and loading state
    - LocalStateAdapter for isolated testing

    How to verify:
    Since Phase 10 builds a standalone component (no page integration until Phase 11), verification is via test results:

    1. Review the test output from Task 1 -- confirm all supply-table tests pass
    2. Review test count -- expect 60+ tests across the supply-table directory
    3. Spot-check a few test files to verify they test real behavior (not just render-without-crash):
       - `supply-table.test.tsx` -- integration tests with LocalStateAdapter
       - `portal-autocomplete.test.tsx` -- keyboard navigation tests
       - `use-supply-table.test.tsx` -- state machine transition tests

    If you want a visual preview, a temporary dev route can be added (will be removed in Phase 14). Say "add dev route" to proceed, or "approved" if test coverage is sufficient.
  </action>
  <verify>Developer types "approved" or describes issues</verify>
  <done>Developer confirms Phase 10 is complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Verification plan -- no new code boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | No new components in verification plan |
</threat_model>

<verification>
```bash
npx vitest run src/components/features/supply-table/ --reporter=verbose
npm test -- --run
npx tsc --noEmit
```
All three pass.
</verification>

<success_criteria>
- All supply-table directory tests pass (0 failures)
- Full test suite passes (no regressions)
- TypeScript compiles without errors
- Developer confirms completion
</success_criteria>

<output>
After completion, create `.planning/phases/10-unified-supply-table/10-06-SUMMARY.md`
</output>
