---
phase: 05-foundation-quick-wins
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/charts/form-primitives/searchable-select.tsx
  - src/components/features/charts/form-primitives/searchable-select.test.tsx
  - src/components/features/charts/use-chart-form.ts
  - src/components/features/supplies/search-to-add.tsx
  - src/components/features/supplies/search-to-add.test.tsx
  - src/components/features/charts/project-supplies-tab.tsx
  - src/components/features/charts/project-supplies-tab.test.tsx
autonomous: true
gap_closure: true
requirements: [PROJ-01, SUPP-02]
must_haves:
  truths:
    - "Chart form 'Add New' for storage location and stitching app only appears when user has typed a non-empty search term"
    - "Clicking 'Add New' in chart form creates the entity with the typed name and selects it in the dropdown"
    - "Thread picker stays open after adding a thread so user can continue adding more"
    - "Thread picker results area shows enough items to be comfortable (not cramped to 3 rows)"
    - "Thread picker scrolls smoothly without over-scrolling after each addition"
  artifacts:
    - path: "src/components/features/charts/form-primitives/searchable-select.tsx"
      provides: "Conditional 'Add {search}' button that only renders when search.trim() is non-empty"
    - path: "src/components/features/charts/use-chart-form.ts"
      provides: "Guard against empty name in handleAddStorageLocation and handleAddStitchingApp"
    - path: "src/components/features/supplies/search-to-add.tsx"
      provides: "Taller results list (max-h-72) and block:nearest scrollIntoView"
    - path: "src/components/features/charts/project-supplies-tab.tsx"
      provides: "handleAdded that refreshes existingIds without unmounting picker"
  key_links:
    - from: "src/components/features/charts/form-primitives/searchable-select.tsx"
      to: "src/components/features/charts/use-chart-form.ts"
      via: "onAddNew callback"
      pattern: "onAddNew\\(search\\)"
    - from: "src/components/features/charts/project-supplies-tab.tsx"
      to: "src/components/features/supplies/search-to-add.tsx"
      via: "onAdded callback"
      pattern: "onAdded.*handleAdded"
---

<objective>
Fix two UAT-reported gaps: (1) "Add New" in chart form dropdowns fails with "Name is required" because it passes an empty string, and (2) thread picker multi-add UX is cramped and closes after each add.

Purpose: Close the remaining UAT failures so Phase 5 can ship.
Output: Fixed SearchableSelect, use-chart-form handlers, SearchToAdd picker, and ProjectSuppliesTab — with updated tests.
</objective>

<execution_context>
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation-quick-wins/05-UAT.md

<interfaces>
<!-- Key contracts the executor needs -->

From src/components/features/charts/form-primitives/searchable-select.tsx:
```typescript
interface SearchableSelectProps {
  options: SelectOption[];
  value: string | null;
  onChange: (value: string | null) => void;
  onAddNew?: (searchTerm: string) => void;
  placeholder?: string;
  disabled?: boolean;
  id?: string;
}
```

From src/components/features/supplies/search-to-add.tsx:
```typescript
interface SearchToAddProps {
  supplyType: "thread" | "bead" | "specialty";
  projectId: string;
  existingIds: string[];
  onAdded: () => void;
  onClose: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix SearchableSelect "Add New" empty name bug</name>
  <files>
    src/components/features/charts/form-primitives/searchable-select.tsx,
    src/components/features/charts/form-primitives/searchable-select.test.tsx,
    src/components/features/charts/use-chart-form.ts
  </files>
  <behavior>
    - Test: "Add New" option is NOT rendered when search input is empty or whitespace-only
    - Test: "Add New" option IS rendered when search has non-empty trimmed text, with label 'Add "{search}"'
    - Test: clicking "Add New" calls onAddNew with the current search text
    - Test: after clicking "Add New", popover closes and search resets
  </behavior>
  <action>
    **searchable-select.tsx** (3 changes):

    1. Line 84 — Change the conditional from `{onAddNew && (` to `{onAddNew && search.trim() && (` so the "Add New" group only renders when search is non-empty.

    2. Line 97 — Change the label from the static text `Add New` to a dynamic label: `Add &quot;{search.trim()}&quot;` so users see exactly what will be created.

    3. No other changes needed — the onSelect handler already passes `search` which will be non-empty due to the guard above.

    **use-chart-form.ts** (2 changes — defensive guards):

    4. Line 307 (handleAddStorageLocation) — Add an early return guard at the top of the callback body: `if (!name.trim()) return;`

    5. Line 328 (handleAddStitchingApp) — Add the same early return guard: `if (!name.trim()) return;`

    **searchable-select.test.tsx** (new file):

    6. Create test file with tests covering the 4 behaviors listed above. Import test utils from `@/__tests__/test-utils`. Mock onAddNew as vi.fn(). Render SearchableSelect with options and onAddNew prop. Verify "Add" text does not appear with empty search, does appear after typing, and calls onAddNew on click.
  </action>
  <verify>
    <automated>npx vitest run src/components/features/charts/form-primitives/searchable-select.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - "Add New" only appears when user has typed a non-empty search term
    - Label reads 'Add "{typed text}"' so user knows what will be created
    - Handlers guard against empty names as a safety net
    - All new tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix thread picker multi-add UX — keep open, taller viewport, smoother scroll</name>
  <files>
    src/components/features/charts/project-supplies-tab.tsx,
    src/components/features/charts/project-supplies-tab.test.tsx,
    src/components/features/supplies/search-to-add.tsx,
    src/components/features/supplies/search-to-add.test.tsx
  </files>
  <behavior>
    - Test: handleAdded does NOT call setAddingType(null) — picker stays mounted after add
    - Test: scrollIntoView is called with { behavior: "smooth", block: "nearest" } (not "end")
    - Test: results container has max-h-72 class (not max-h-48)
  </behavior>
  <action>
    **project-supplies-tab.tsx** (1 change):

    1. Lines 416-418 — Change handleAdded from `setAddingType(null)` to a no-op (empty callback body). The picker's internal `onAdded` callback will still fire after a successful add (triggering data refresh via revalidation), but the picker won't unmount. The user closes the picker explicitly via the click-outside or Escape handlers already wired in SearchToAdd.

    ```typescript
    const handleAdded = useCallback(() => {
      // Intentionally empty — picker stays open for multi-add.
      // Data refreshes via server action revalidation.
      // User closes picker with Escape or click-outside.
    }, []);
    ```

    **search-to-add.tsx** (2 changes):

    2. Line 222 — Change `max-h-48` to `max-h-72` on the results container div. This gives ~8 rows of results (288px) instead of ~4 rows (192px), making the picker much more usable.

    3. Line 161 — Change `scrollIntoView({ behavior: "smooth", block: "end" })` to `scrollIntoView({ behavior: "smooth", block: "nearest" })`. This prevents the page from over-scrolling — the picker only scrolls if it's not already fully visible.

    **Update existing tests:**

    4. In `search-to-add.test.tsx` — Update the test "calls scrollIntoView with smooth behavior and block end" (line 237) to expect `block: "nearest"` instead of `block: "end"`.

    5. In `project-supplies-tab.test.tsx` — Add a test: "does not unmount SearchToAdd picker after successful add". Render with threads, click "Add more" to open picker, simulate an add via the onAdded callback, assert the picker is still visible in the DOM (search input still present).
  </action>
  <verify>
    <automated>npx vitest run src/components/features/supplies/search-to-add.test.tsx src/components/features/charts/project-supplies-tab.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - Thread picker stays open after adding a thread (user can add multiple without re-opening)
    - Results area shows ~8 rows instead of ~4 (max-h-72)
    - scrollIntoView uses block: "nearest" — no more jarring page jumps
    - All existing and new tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server action | SearchableSelect's onAddNew passes user-typed text to server actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | searchable-select.tsx | mitigate | Client-side guard (search.trim() non-empty) + server-side Zod validation already exists in createStorageLocation/createStitchingApp actions — defense in depth |
| T-05-07-02 | Spoofing | use-chart-form.ts | accept | Handlers call requireAuth() via server actions; empty name guard is UX, not security |
</threat_model>

<verification>
1. `npx vitest run src/components/features/charts/form-primitives/searchable-select.test.tsx` — all pass
2. `npx vitest run src/components/features/supplies/search-to-add.test.tsx` — all pass
3. `npx vitest run src/components/features/charts/project-supplies-tab.test.tsx` — all pass
4. `npm run build` — no type errors
</verification>

<success_criteria>
- "Add New" button in chart form dropdowns only appears when search has text, labeled 'Add "{text}"'
- Clicking "Add New" with typed text creates entity and selects it — no "Name is required" error
- Thread picker stays open after adding a thread for multi-add workflow
- Thread picker results area comfortably shows ~8 rows
- No jarring page scrolling after adding threads
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation-quick-wins/05-07-SUMMARY.md`
</output>
