---
phase: 09-dashboards-shopping-cart
plan: 09
type: execute
wave: 4
depends_on: [07, 08]
files_modified:
  - src/app/(dashboard)/page.tsx
  - src/app/(dashboard)/shopping/page.tsx
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-05, SHOP-01, SHOP-02, SHOP-03, SHOP-04]

must_haves:
  truths:
    - "Build succeeds without errors"
    - "All tests pass"
    - "Dashboard page renders at / with two functional tabs"
    - "Shopping Cart page renders at /shopping with project selection and supply tabs"
    - "Quick Add Log Stitches opens the LogSessionModal"
  artifacts:
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Working dashboard home page"
    - path: "src/app/(dashboard)/shopping/page.tsx"
      provides: "Working shopping cart page"
  key_links:
    - from: "src/app/(dashboard)/page.tsx"
      to: "src/lib/actions/dashboard-actions.ts"
      via: "data fetching"
      pattern: "getMainDashboardData"
    - from: "src/app/(dashboard)/shopping/page.tsx"
      to: "src/lib/actions/shopping-cart-actions.ts"
      via: "data fetching"
      pattern: "getShoppingCartData"
---

<objective>
Integration verification and visual checkpoint. Run full build and test suite, then verify the dashboard and shopping cart work correctly in the browser. Fix any integration issues found.

Purpose: Catch integration bugs between components, data actions, and page wiring before declaring the phase complete.
Output: Clean build, green tests, user-verified working pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-dashboards-shopping-cart/09-CONTEXT.md
@.planning/phases/09-dashboards-shopping-cart/09-07-SUMMARY.md
@.planning/phases/09-dashboards-shopping-cart/09-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build and test suite verification</name>
  <files>src/app/(dashboard)/page.tsx, src/app/(dashboard)/shopping/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/page.tsx
    - src/app/(dashboard)/shopping/page.tsx
    - src/lib/actions/dashboard-actions.ts
    - src/lib/actions/project-dashboard-actions.ts
    - src/lib/actions/shopping-cart-actions.ts
  </read_first>
  <action>
1. Run `npm run build` to verify TypeScript compilation and Next.js build succeeds
2. Run `npm test` to verify all tests pass (both new Phase 9 tests and existing test suite)
3. If build errors: fix type mismatches, missing imports, or component prop issues
4. If test failures: fix broken tests, update mock data, resolve import issues
5. Run `npx prisma generate` if needed to ensure Prisma client is up to date (no schema changes in this phase, but verify)

Common integration issues to check:
- Import path mismatches between types and components
- Missing presigned URL keys causing blank images
- Missing or incorrect imageUrls record shape
- GalleryCard data shape mismatch for Start Next section
- QuickAddMenu custom event not matching TopBar listener event name
- ShoppingCart localStorage key collision with other features
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits with code 0
    - npm test exits with code 0
    - No TypeScript errors in build output
    - No test failures
  </acceptance_criteria>
  <done>Clean build and green test suite. All Phase 9 code compiles and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Full Dashboard at `/` with two tabs (Your Library and Progress) and Shopping Cart at `/shopping` with project selection and supply tabs. This is the complete Phase 9 delivery:

**Your Library tab (Main Dashboard):**
- Currently Stitching: horizontal scroll of 280px cards showing WIPs sorted by most recently stitched
- Start Next: 1-2 GalleryCards for flagged projects
- Collection Stats sidebar: 8 aggregate stats with mobile collapse
- Rediscover This One (Spotlight): featured random project with Shuffle button
- Buried Treasures: oldest unstarted charts with age badges
- Quick Add: dropdown with 8 actions including Log Stitches (opens existing modal)

**Progress tab (Project Dashboard):**
- Hero Stats: 6 stat cards in auto-fit grid with emerald tint
- Progress Breakdown: CSS stacked bar chart + 5 collapsible accordion buckets with sorting
- Finished: expandable cards with stats grids, search, and 4 sort options (violet tint)

**Shopping Cart (/shopping):**
- Project selection with checkbox toggles and localStorage persistence
- Shopping-for bar with selected project chips
- Tabbed supply view (Threads, Beads, Specialty, Fabric, Shopping List) with badge counts
- Quantity control +/- stepper with acquired/total display
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Navigate to `http://localhost:3000` (dashboard)
3. Verify "Your Library" tab is default with all sections visible
4. Check Currently Stitching cards scroll horizontally with arrow buttons
5. Click "Quick Add" button — verify 8 menu items appear
6. Click "Log Stitches" in Quick Add — verify LogSessionModal opens
7. Click "Progress" tab — verify URL changes to `?tab=progress`
8. Verify Hero Stats show 6 stat cards with JetBrains Mono numbers
9. Verify Progress Breakdown has stacked bar chart and collapsible bucket sections
10. Click a bucket to expand — verify projects appear inside
11. Switch to Finished sub-tab — verify finished projects with expandable cards
12. Navigate to `http://localhost:3000/shopping`
13. Verify project selection list with checkboxes
14. Select 1-2 projects — verify Shopping-for bar shows their names as chips
15. Switch to Threads tab — verify supply items for selected projects appear with quantity controls
16. Increment a quantity with + button — verify it updates
17. Refresh the page — verify project selection persists from localStorage
18. Check mobile responsive: resize to < 768px, verify sidebar collapses and layout stacks
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Integration verification, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | All threats addressed in Plans 01-08. This plan verifies mitigations work end-to-end. |
</threat_model>

<verification>
```
npm run build
npm test
```
Manual verification per checkpoint steps.
</verification>

<success_criteria>
- Clean build with zero TypeScript errors
- All tests pass (Phase 9 new + existing suite)
- Dashboard at `/` renders both tabs correctly
- Shopping Cart at `/shopping` renders with project selection and supply tabs
- Quick Add Log Stitches opens the modal
- localStorage persistence works for shopping selection
- Mobile responsive layout functions correctly
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboards-shopping-cart/09-09-SUMMARY.md`
</output>
