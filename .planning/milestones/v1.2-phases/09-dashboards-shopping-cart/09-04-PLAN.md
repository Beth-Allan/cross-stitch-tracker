---
phase: 09-dashboards-shopping-cart
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/features/dashboard/section-heading.tsx
  - src/components/features/dashboard/scrollable-row.tsx
  - src/components/features/dashboard/scrollable-row.test.tsx
  - src/components/features/dashboard/currently-stitching-card.tsx
  - src/components/features/dashboard/currently-stitching-card.test.tsx
  - src/components/features/dashboard/collection-stats-sidebar.tsx
  - src/components/features/dashboard/collection-stats-sidebar.test.tsx
  - src/components/features/dashboard/spotlight-card.tsx
  - src/components/features/dashboard/spotlight-card.test.tsx
  - src/components/features/dashboard/buried-treasures-section.tsx
autonomous: true
requirements: [DASH-01, DASH-03, DASH-04, DASH-05]

must_haves:
  truths:
    - "CurrentlyStitchingCard renders compact 280px cards with progress overlay"
    - "ScrollableRow provides horizontal scrolling with left/right arrow buttons"
    - "CollectionStatsSidebar shows all 8 stat values with mobile collapse"
    - "SpotlightCard renders featured project with shuffle button"
    - "BuriedTreasuresSection renders numbered rows with age badges"
    - "SectionHeading renders Fraunces heading with emerald underline bar"
  artifacts:
    - path: "src/components/features/dashboard/section-heading.tsx"
      provides: "Reusable section heading with accent bar"
    - path: "src/components/features/dashboard/scrollable-row.tsx"
      provides: "Horizontal scroll container with arrow navigation"
    - path: "src/components/features/dashboard/currently-stitching-card.tsx"
      provides: "Compact WIP card for Currently Stitching section"
    - path: "src/components/features/dashboard/collection-stats-sidebar.tsx"
      provides: "Stats sidebar with mobile collapse"
    - path: "src/components/features/dashboard/spotlight-card.tsx"
      provides: "Featured project card with shuffle action"
    - path: "src/components/features/dashboard/buried-treasures-section.tsx"
      provides: "Numbered list of oldest unstarted charts"
  key_links:
    - from: "src/components/features/dashboard/currently-stitching-card.tsx"
      to: "src/types/dashboard.ts"
      via: "CurrentlyStitchingProject type import"
      pattern: "import.*CurrentlyStitchingProject"
    - from: "src/components/features/dashboard/spotlight-card.tsx"
      to: "src/lib/actions/dashboard-actions.ts"
      via: "getSpotlightProject server action call"
      pattern: "getSpotlightProject"
---

<objective>
Build the Main Dashboard section components: SectionHeading (shared primitive), ScrollableRow (horizontal scroll with arrows), CurrentlyStitchingCard, CollectionStatsSidebar, SpotlightCard, and BuriedTreasuresSection.

Purpose: These are the visual building blocks consumed by the Main Dashboard page. Building them before page wiring ensures each component is independently tested and matches the UI-SPEC.
Output: 6 component files with tests for interactive components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-dashboards-shopping-cart/09-CONTEXT.md
@.planning/phases/09-dashboards-shopping-cart/09-RESEARCH.md
@.planning/phases/09-dashboards-shopping-cart/09-PATTERNS.md
@.planning/phases/09-dashboards-shopping-cart/09-UI-SPEC.md

<interfaces>
From src/types/dashboard.ts (created by Plan 01):
```typescript
export interface CurrentlyStitchingProject {
  projectId: string; chartId: string; projectName: string;
  designerName: string | null; coverThumbnailUrl: string | null;
  status: ProjectStatus; stitchesCompleted: number; totalStitches: number;
  progressPercent: number; lastSessionDate: Date | null;
  totalTimeMinutes: number; stitchingDays: number;
}
export interface CollectionStats {
  totalProjects: number; totalWIP: number; totalOnHold: number;
  totalUnstarted: number; totalFinished: number;
  totalStitchesCompleted: number;
  mostRecentFinish: { projectId: string; name: string; finishDate: Date } | null;
  largestProject: { projectId: string; name: string; stitchCount: number } | null;
}
export interface SpotlightProject {
  projectId: string; chartId: string; projectName: string;
  designerName: string | null; coverThumbnailUrl: string | null;
  coverImageUrl: string | null; status: ProjectStatus;
  genres: string[]; totalStitches: number; progressPercent: number;
}
export interface BuriedTreasure {
  chartId: string; projectId: string | null; chartName: string;
  designerName: string | null; coverThumbnailUrl: string | null;
  dateAdded: Date; daysInLibrary: number; genres: string[];
}
```

From src/lib/actions/dashboard-actions.ts (Plan 01):
```typescript
export async function getSpotlightProject(): Promise<SpotlightProject | null>;
```

Existing components to reuse:
```typescript
// src/components/features/charts/status-badge.tsx
export function StatusBadge({ status }: { status: ProjectStatus }): JSX.Element;

// src/components/features/gallery/cover-placeholder.tsx
export function CoverPlaceholder({ status, size }: { ... }): JSX.Element;

// src/components/ui/card.tsx
export function Card({ children, className }: { ... }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for ScrollableRow, CurrentlyStitchingCard, CollectionStatsSidebar, and SpotlightCard</name>
  <files>src/components/features/dashboard/scrollable-row.test.tsx, src/components/features/dashboard/currently-stitching-card.test.tsx, src/components/features/dashboard/collection-stats-sidebar.test.tsx, src/components/features/dashboard/spotlight-card.test.tsx</files>
  <read_first>
    - product-plan/sections/dashboards-and-views/components/MainDashboard.tsx
    - product-plan/sections/dashboards-and-views/main-dashboard.png
    - .planning/phases/09-dashboards-shopping-cart/09-UI-SPEC.md
    - src/types/dashboard.ts
    - src/__tests__/test-utils.tsx
  </read_first>
  <behavior>
    - ScrollableRow Test 1: Renders children
    - ScrollableRow Test 2: Arrow buttons have correct aria-labels ("Scroll left", "Scroll right")
    - CurrentlyStitchingCard Test 1: Renders project name, designer name, progress percent
    - CurrentlyStitchingCard Test 2: Renders progress bar with correct width style
    - CurrentlyStitchingCard Test 3: Card links to /charts/{chartId}
    - CurrentlyStitchingCard Test 4: Renders "0%" when progressPercent is 0
    - CollectionStatsSidebar Test 1: Renders all 8 stat labels
    - CollectionStatsSidebar Test 2: Renders stat values (totalProjects, totalWIP, etc.)
    - CollectionStatsSidebar Test 3: Renders "—" when mostRecentFinish is null
    - SpotlightCard Test 1: Renders project name, designer name, genre tags
    - SpotlightCard Test 2: Renders "Check It Out" link button
    - SpotlightCard Test 3: Renders "Shuffle Spotlight" button
    - SpotlightCard Test 4: Returns null when project is null
  </behavior>
  <action>
Create all 4 test files with failing tests (RED phase). Each test file imports the component that does not yet exist — create minimal stub exports (empty components that return null) so TypeScript compiles but tests fail on assertions.

**scrollable-row.test.tsx**: Import ScrollableRow, render with children, assert children appear. Assert aria-label="Scroll left" and aria-label="Scroll right" buttons exist.

**currently-stitching-card.test.tsx**: Import CurrentlyStitchingCard. Create a mock CurrentlyStitchingProject fixture. Assert renders project name, designer name, `{progressPercent}%` text. Assert progress bar div has width style matching progressPercent. Assert Link href contains `/charts/{chartId}`. Assert "0%" renders when progressPercent is 0.

**collection-stats-sidebar.test.tsx**: Import CollectionStatsSidebar. Create mock CollectionStats fixture. Assert all 8 labels render ("Total Projects", "In Progress", "On Hold", "Unstarted", "Finished", "Total Stitches", "Most Recent Finish", "Largest Project"). Assert values render. Assert "—" renders when mostRecentFinish is null.

**spotlight-card.test.tsx**: Import SpotlightCard. Create mock SpotlightProject fixture. Assert renders project name, designer name, genre tags. Assert "Check It Out" and "Shuffle Spotlight" buttons render. Assert null return when project is null (use `container.innerHTML` to verify empty).

Mock `getSpotlightProject` from `@/lib/actions/dashboard-actions` in spotlight tests using `vi.mock`.

All tests MUST FAIL at this point (RED). Commit: `test(09-04): add failing tests for dashboard section components`
  </action>
  <verify>
    <automated>npx vitest run src/components/features/dashboard/scrollable-row.test.tsx src/components/features/dashboard/currently-stitching-card.test.tsx src/components/features/dashboard/collection-stats-sidebar.test.tsx src/components/features/dashboard/spotlight-card.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test files exist with test cases as specified
    - Tests reference the correct component imports
    - Tests FAIL (RED phase) because components are stubs returning null
    - TypeScript compiles (stub exports exist)
  </acceptance_criteria>
  <done>All failing test files written for ScrollableRow, CurrentlyStitchingCard, CollectionStatsSidebar, and SpotlightCard. Tests define expected behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SectionHeading, ScrollableRow, and CurrentlyStitchingCard to pass tests</name>
  <files>src/components/features/dashboard/section-heading.tsx, src/components/features/dashboard/scrollable-row.tsx, src/components/features/dashboard/currently-stitching-card.tsx</files>
  <read_first>
    - product-plan/sections/dashboards-and-views/components/MainDashboard.tsx
    - product-plan/sections/dashboards-and-views/main-dashboard.png
    - .planning/phases/09-dashboards-shopping-cart/09-UI-SPEC.md
    - src/components/ui/card.tsx
    - src/components/features/gallery/cover-placeholder.tsx
    - src/components/features/charts/status-badge.tsx
  </read_first>
  <action>
Replace stubs with full implementations (GREEN phase).

**SectionHeading** (server component, no "use client"):
```typescript
interface SectionHeadingProps {
  title: string;
  action?: React.ReactNode; // e.g., "View all" link
}
```
- Render: `<div className="flex items-center justify-between">` with `<div>` containing `<h2 className="font-heading text-xl font-bold">{title}</h2>` and `<div className="mt-1.5 h-0.5 w-10 rounded-full bg-emerald-400" />` (emerald accent bar per UI-SPEC: 40px wide, h-0.5, rounded-full)
- Optional `action` slot on the right

**ScrollableRow** (client component, "use client"):
```typescript
interface ScrollableRowProps {
  children: React.ReactNode;
  className?: string;
}
```
- Container div with `overflow-x-auto scrollbar-hide` (uses `scrollbar-width: none` and webkit override — add to component's style)
- `scroll-snap-type: x mandatory` on the scroll container
- Children wrapper with `flex gap-4` and each child gets `scroll-snap-align: start`
- Left/right arrow buttons: 36x36px circular, `bg-white border border-stone-200 shadow-md`, positioned absolute left/right. Show conditionally based on scroll position.
- Arrow click scrolls 300px with `scrollTo({ left: current +/- 300, behavior: "smooth" })`
- Use `useRef` for scroll container, `useState` for `canScrollLeft`/`canScrollRight`
- `useEffect` with scroll listener to update arrow visibility
- Arrow buttons: `aria-label="Scroll left"` and `aria-label="Scroll right"` per UI-SPEC accessibility
- Hide scrollbar with inline style `scrollbarWidth: "none"` and className for webkit: add `[&::-webkit-scrollbar]:hidden` or use Tailwind `scrollbar-hide` if available

**CurrentlyStitchingCard** (client component, "use client"):
```typescript
interface CurrentlyStitchingCardProps {
  project: CurrentlyStitchingProject;
  imageUrl: string | null;
}
```
- Card with `w-[280px] flex-shrink-0` (fixed 280px width per D-04)
- Cover image area: `h-40` (160px) with either `<img>` using `imageUrl` or `<CoverPlaceholder>` fallback
- Progress bar overlay at bottom of image: `h-1.5 rounded-full bg-emerald-400` with width = `progressPercent%`
- Below image: project name (font-medium text-sm, truncate), designer name (text-muted-foreground text-xs, truncate)
- Stats row: `{progressPercent}%` in font-mono font-bold, and `{stitchesCompleted.toLocaleString()} / {totalStitches.toLocaleString()}` in text-muted-foreground text-xs
- Status badge using existing StatusBadge component
- Entire card is a `<Link href={/charts/${chartId}}>` for navigation
- Use `buttonVariants` import from `@/components/ui/button-variants` (NOT from button.tsx) if used in a server context — but since this is client, either import location works

Commit: `feat(09-04): implement SectionHeading, ScrollableRow, CurrentlyStitchingCard`
  </action>
  <verify>
    <automated>npx vitest run src/components/features/dashboard/scrollable-row.test.tsx src/components/features/dashboard/currently-stitching-card.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/dashboard/section-heading.tsx exists, does NOT have "use client"
    - SectionHeading renders h2 with className containing "font-heading text-xl font-bold"
    - SectionHeading renders emerald accent bar div with className containing "bg-emerald-400" and "w-10" and "h-0.5"
    - src/components/features/dashboard/scrollable-row.tsx has "use client" directive
    - ScrollableRow renders aria-label="Scroll left" and aria-label="Scroll right" buttons
    - src/components/features/dashboard/currently-stitching-card.tsx has "use client" directive
    - CurrentlyStitchingCard renders with className containing "w-[280px]"
    - CurrentlyStitchingCard renders Link with href containing "/charts/"
    - ScrollableRow and CurrentlyStitchingCard tests pass (GREEN)
  </acceptance_criteria>
  <done>SectionHeading, ScrollableRow, and CurrentlyStitchingCard implemented. ScrollableRow and CurrentlyStitchingCard tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement CollectionStatsSidebar, SpotlightCard, and BuriedTreasuresSection to pass tests</name>
  <files>src/components/features/dashboard/collection-stats-sidebar.tsx, src/components/features/dashboard/spotlight-card.tsx, src/components/features/dashboard/buried-treasures-section.tsx</files>
  <read_first>
    - product-plan/sections/dashboards-and-views/components/MainDashboard.tsx
    - product-plan/sections/dashboards-and-views/main-dashboard.png
    - .planning/phases/09-dashboards-shopping-cart/09-UI-SPEC.md
    - src/components/ui/card.tsx
    - src/components/features/gallery/cover-placeholder.tsx
    - src/lib/actions/dashboard-actions.ts
  </read_first>
  <action>
Replace stubs with full implementations (GREEN phase).

**CollectionStatsSidebar** (client component, "use client" — needs useState for mobile collapse):
```typescript
interface CollectionStatsSidebarProps {
  stats: CollectionStats;
}
```
- Desktop (md+): Fixed 260px width, always expanded. Show stat rows vertically.
- Mobile (max-md): Collapsible. Heading "Collection Stats" with ChevronDown toggle. Collapsed by default. When expanded, show horizontal chip layout.
- Stat rows (8 total):
  1. "Total Projects" -> `stats.totalProjects`
  2. "In Progress" -> `stats.totalWIP`
  3. "On Hold" -> `stats.totalOnHold`
  4. "Unstarted" -> `stats.totalUnstarted`
  5. "Finished" -> `stats.totalFinished`
  6. "Total Stitches" -> `stats.totalStitchesCompleted.toLocaleString()`
  7. "Most Recent Finish" -> `stats.mostRecentFinish?.name ?? "—"` (text-muted-foreground if null)
  8. "Largest Project" -> `stats.largestProject?.name ?? "—"` with stitch count subtitle
- Each stat row: label in `text-[11px] font-bold uppercase tracking-wider text-muted-foreground`, value in `text-sm font-medium`
- Use Card component as wrapper with `p-4` padding
- ChevronDown rotates on toggle: `transition-transform duration-200` + `rotate-180` when expanded

**SpotlightCard** (client component, "use client" — needs useTransition for shuffle):
```typescript
interface SpotlightCardProps {
  project: SpotlightProject | null;
  imageUrl: string | null;
}
```
- If null project: return null (section hidden)
- Section label: "Rediscover This One" in `text-[11px] font-bold uppercase tracking-wider text-amber-600` with Sparkles icon
- Card with 2-column layout on md+: cover image left (aspect-[4/3], object-cover), content right
- Content: project name (font-heading text-lg font-semibold), designer name, genre tags as inline badges, stitch count
- "Check It Out" button: emerald-600 bg, white text, `rounded-xl`, links to `/charts/{chartId}` using LinkButton
- "Shuffle Spotlight" button: outline, stone-200 border, RefreshCw icon. Calls `getSpotlightProject()` server action via `useTransition`. On success, updates local state with new project. On error, `toast.error("Could not load a new spotlight project. Try again.")` per UI-SPEC error copy.
- Import `getSpotlightProject` from `@/lib/actions/dashboard-actions`
- Import `toast` from `sonner`
- Use `useState` for local spotlight project (initialized from prop), `useTransition` for shuffle pending state

**BuriedTreasuresSection** (server component, no "use client"):
```typescript
interface BuriedTreasuresSectionProps {
  treasures: BuriedTreasure[];
  imageUrls: Record<string, string>;
}
```
- If empty array: return null (section hidden per UI-SPEC empty state)
- Uses SectionHeading with title "Buried Treasures"
- Numbered list: each row shows index number (font-mono text-lg text-muted-foreground), cover thumbnail (32x32 rounded), chart name (Link to `/charts/{chartId}`), designer name, age badge ("X days" or "X months" or "X years" in library)
- Age formatting: < 30 days -> "X days", < 365 days -> "X months", else "X years"
- Row styling: `flex items-center gap-3 py-2 border-b border-border last:border-0`

Commit: `feat(09-04): implement CollectionStatsSidebar, SpotlightCard, BuriedTreasuresSection`
  </action>
  <verify>
    <automated>npx vitest run src/components/features/dashboard/collection-stats-sidebar.test.tsx src/components/features/dashboard/spotlight-card.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - CollectionStatsSidebar renders text "Total Projects", "In Progress", "On Hold", "Unstarted", "Finished", "Total Stitches", "Most Recent Finish", "Largest Project"
    - CollectionStatsSidebar renders "—" when mostRecentFinish is null
    - SpotlightCard renders "Rediscover This One" label
    - SpotlightCard renders "Check It Out" and "Shuffle Spotlight" buttons
    - SpotlightCard returns null when project prop is null
    - SpotlightCard imports getSpotlightProject from "@/lib/actions/dashboard-actions"
    - BuriedTreasuresSection does NOT have "use client" directive
    - BuriedTreasuresSection returns null when treasures array is empty
    - CollectionStatsSidebar and SpotlightCard tests pass (GREEN)
  </acceptance_criteria>
  <done>CollectionStatsSidebar, SpotlightCard, and BuriedTreasuresSection implemented and tests pass. Sidebar has mobile collapse, Spotlight has server action shuffle, Buried Treasures auto-hides when empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server Action | SpotlightCard calls getSpotlightProject via server action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Information Disclosure | SpotlightCard | mitigate | getSpotlightProject calls requireAuth() — no unauthenticated access to project data |
| T-09-09 | Denial of Service | ScrollableRow | accept | Scroll is local DOM operation, no server impact. Low risk. |
</threat_model>

<verification>
```
npx vitest run src/components/features/dashboard/ --reporter=verbose
```
</verification>

<success_criteria>
- All 6 Main Dashboard section components exist
- SectionHeading has Fraunces heading + emerald accent bar
- ScrollableRow has horizontal scroll with arrow buttons + aria-labels
- CurrentlyStitchingCard is 280px fixed width with progress overlay
- CollectionStatsSidebar has mobile collapse + all 8 stats
- SpotlightCard has shuffle via server action
- BuriedTreasuresSection auto-hides when empty
- All component tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboards-shopping-cart/09-04-SUMMARY.md`
</output>
