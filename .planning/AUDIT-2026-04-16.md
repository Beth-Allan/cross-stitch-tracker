# Impeccable Audit — 2026-04-16

**Initial Score: 10/20** (Acceptable) → **Current: ~14/20** after harden + polish + optimize passes

## What Was Fixed (harden pass)

### P0 — All 3 resolved
- **ThemeProvider**: installed `next-themes`, wrapped layout, added toggle to sidebar + mobile nav
- **Hover-only action buttons**: 10 files — `opacity-0` → `md:opacity-0` so buttons visible on touch
- **Session edit button**: visibility + 44px touch target + aria-label

### P1 — 4 of 10 resolved
- **Touch targets**: `min-h-11 min-w-11` on app-list, storage-list, supply-table-view, dialog close
- **Sort header keyboard**: `onKeyDown`, `tabIndex`, `aria-sort` on session-table
- **Tab ARIA**: `role="tablist/tab"`, `aria-selected`, `aria-controls` on supply-catalog
- **Toggle aria-pressed**: supplies-tab sort + calculator over-count buttons

**Build clean, 1031/1031 tests passing.**

---

## What Was Fixed (polish pass — dark theme)

### All 6 items resolved
- **Status badge dark text**: added `dark:text-{color}-300` to all 6 statuses in `status.ts`
- **Hex gradients dedup**: deleted `statusGradients` in `storage-view-tab.tsx` + `whats-next-tab.tsx`, imported `STATUS_GRADIENT_CLASSES` (already has dark variants)
- **fabric-requirements-tab.tsx**: added dark: variants to 11 emerald/amber locations (info banner, filter toggle, status badges, assign buttons, link)
- **CTA buttons**: removed raw `bg-emerald-600` in 4 files — default Button variant uses `--primary` (already emerald + dark mode)
- **Emerald link colors**: added `dark:text-emerald-400`, `dark:hover:border-emerald-800` to storage-view-tab + whats-next-tab
- **size-category.ts**: already had dark variants — no change needed

## What Was Fixed (optimize pass)

### 3 of 5 items resolved, 1 deferred, 1 N/A
- **`next/image` migration**: converted 4 high-impact files (hero-cover-banner with `priority` for LCP, gallery-card with `fill`, chart-detail, cover-thumbnail). Used `unoptimized` since presigned R2 URLs change per-request. Remaining 7 files are tiny thumbnails (28-40px) where conversion provides negligible benefit.
- **`useMemo` additions**: wrapped `sortProjects` in whats-next-tab, derived objects in project-detail-page, chips array in filter-chips
- **`getActiveProjectsForPicker` dedup**: wrapped with React `cache()` — layout + page calls now share one DB query per request
- **Presigned URL waterfall**: deferred — the "waterfall" is a data dependency (session photo keys unknown until sessions resolve); cover URLs already run in parallel. Near-optimal as-is.
- **`tw-animate-css` trim**: N/A — provides `animate-in/out`, `fade-in-*`, `zoom-in-*`, `slide-in-from-*` utilities used by 5 shadcn UI components. Cannot remove.

**Build clean, 1031/1031 tests passing.**

---

## Remaining Work (by recommended command)

### `/polish` — Theming dark: gaps (P1) ✅ COMPLETE

### `/optimize` — Performance (P1) ✅ COMPLETE

### `/adapt` — Responsive gaps (P1) ✅ COMPLETE
- [x] Session table: add mobile card fallback (`hidden md:block` table + `md:hidden` cards) — `session-table.tsx`
- [x] Supply table view: same mobile card fallback — `supply-table-view.tsx`
- [x] `whats-next-tab.tsx`: show kitting progress as compact badge on mobile (`md:hidden`)
- [x] `pattern-dive-tabs.tsx`: add `aria-label` on mobile icon-only TabsTrigger
- [x] `size="sm"` buttons: add `min-h-11 md:min-h-0` to `project-sessions-tab.tsx`, `fabric-detail.tsx`, `shopping-list.tsx`

### `/harden` — Remaining ARIA (P2) ✅ COMPLETE
- [x] `searchable-select.tsx`: add combobox ARIA (role="combobox", aria-expanded, aria-haspopup="listbox", aria-controls)
- [x] `form-field.tsx` / call sites: add aria-describedby to inputs with error/hint (chart-name, stitches-wide, stitch-count)
- [x] `basic-info-section.tsx`: removed dangling htmlFor="designer" (no matching input id)
- [x] `stitch-count-fields.tsx`: add aria-label="Height (stitches)" to height input
- [x] `card.tsx`: CardTitle now renders as `<h3>` by default (configurable via `as` prop)
- [x] `top-bar.tsx`: add aria-label="Add Chart" to icon-only mobile link
- [x] `gallery-card.tsx`: add role="progressbar" + aria-valuenow/min/max to WIP and Finished progress bars
- [x] `supply-catalog.tsx`: add aria-label to brand filter and color family filter selects

### `/distill` — Anti-pattern cleanup (P2) ✅ COMPLETE
- [x] Extract shared `EmptyState` component (`src/components/ui/empty-state.tsx`) — replaced 5 instances across designer-detail, genre-detail, designer-list (page, table, mobile)
- [x] Extract shared `ErrorCard` component (`src/components/ui/error-card.tsx`) — replaced error.tsx and not-found.tsx (global-error.tsx left standalone intentionally — renders outside root layout)

### Dimension Scores (Final)
| Dimension | Initial | Final |
|-----------|---------|-------|
| Accessibility | 2/4 | ~3.5 (combobox, progressbar, describedby, labels, aria-labels) |
| Performance | 2/4 | ~3 (next/image LCP, useMemo, cache dedup) |
| Theming | 1/4 | ~3 (dark mode provider + all dark: gaps fixed) |
| Responsive | 2/4 | ~3.5 (mobile cards, kitting badge, touch targets) |
| Anti-Patterns | 3/4 | ~3.5 (EmptyState + ErrorCard extracted) |

**Final score: ~16.5/20** (up from 10/20 initial)

**All 6 audit passes complete.** Build clean, 1050/1050 tests passing.
