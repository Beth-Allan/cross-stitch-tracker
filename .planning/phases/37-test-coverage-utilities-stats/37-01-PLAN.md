---
phase: 37-test-coverage-utilities-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Skein calculator tests cover fabricCount=0 edge case"
    - "Skein calculator tests cover resolveDefaultBrandId via public API"
    - "Stats action tests verify requireAuth rejection for all 3 exported functions"
    - "Stats action tests verify Zod boundary violations for year (2019/2101) and month (0/13)"
    - "Calendar year-rollover tests cover Jan-to-Dec and Dec-to-Jan navigation"
    - "Record-detection tests cover two sessions on same day with identical stitch counts"
    - "Completion-estimate tests verify exclusion of projects with stitchesCompleted == totalStitches"
    - "Completion-estimate tests verify exclusion of projects with stitchesCompleted > totalStitches"
    - "All 5 backlog items (999.0.24, 999.24, 999.27, 999.38, 999.39) are marked closed in CLAUDE.md"
  artifacts:
    - path: "src/lib/utils/skein-calculator.test.ts"
      provides: "fabricCount=0 edge case test"
      contains: "returns 0 for fabricCount of 0"
    - path: "src/lib/actions/supply-actions.test.ts"
      provides: "resolveDefaultBrandId tests"
      contains: "resolveDefaultBrandId"
    - path: "src/lib/actions/stats-actions.test.ts"
      provides: "Auth rejection + Zod boundary tests for 3 actions"
      contains: "throws when requireAuth rejects"
    - path: "src/components/features/stats/stitching-calendar.test.tsx"
      provides: "Year-rollover navigation tests"
      contains: "navigates backward across year boundary"
    - path: "src/lib/queries/stats/record-detection.test.ts"
      provides: "Duplicate stitch count test"
      contains: "identical stitch counts"
    - path: "src/lib/queries/stats/completion-estimates.test.ts"
      provides: "Already-completed exclusion tests"
      contains: "excludes projects where stitchesCompleted equals totalStitches"
  key_links: []
---

<objective>
Verify that all 5 success criteria for Phase 37 are satisfied by tests already added proactively during earlier phases (22-34), then close the corresponding backlog items.

Purpose: This is a verify-and-close phase (per D-01). No new tests need to be written -- all target coverage already exists. The plan confirms coverage and closes tracking items.
Output: Updated CLAUDE.md with 5 backlog items marked as shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-test-coverage-utilities-stats/37-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all test coverage against success criteria</name>
  <files>
    src/lib/utils/skein-calculator.test.ts
    src/lib/actions/stats-actions.test.ts
    src/lib/actions/supply-actions.test.ts
    src/components/features/stats/stitching-calendar.test.tsx
    src/lib/queries/stats/record-detection.test.ts
    src/lib/queries/stats/completion-estimates.test.ts
  </files>
  <read_first>
    src/lib/utils/skein-calculator.test.ts
    src/lib/actions/stats-actions.test.ts
    src/lib/actions/supply-actions.test.ts (line 1359+ for resolveDefaultBrandId section)
    src/components/features/stats/stitching-calendar.test.tsx
    src/lib/queries/stats/record-detection.test.ts
    src/lib/queries/stats/completion-estimates.test.ts
    .planning/phases/37-test-coverage-utilities-stats/37-CONTEXT.md (D-03 through D-09 for evidence mapping)
  </read_first>
  <action>
    Run all 6 test suites and verify each success criterion maps to a specific passing test:

    SC-1 (skein calculator fabricCount=0): Confirm skein-calculator.test.ts has "returns 0 for fabricCount of 0" test that passes. Per D-03, this is at line 157.

    SC-1 (resolveDefaultBrandId): Confirm supply-actions.test.ts has "resolveDefaultBrandId (via public API)" describe block with 3 tests covering default upsert and specific brandId skip. Per D-04, this is at line 1359.

    SC-2 (stats auth rejection): Confirm stats-actions.test.ts has "throws when requireAuth rejects" tests for all 3 exported functions: fetchCalendarMonth, fetchDailyBreakdown, fetchMonthlyTotals. Per D-05.

    SC-2 (stats Zod boundaries): Confirm stats-actions.test.ts tests year min (2019) and max (2101) for all 3 functions, and month boundaries (0, 13) where applicable. Per D-06.

    SC-3 (calendar year-rollover): Confirm stitching-calendar.test.tsx has "navigates backward across year boundary (Jan -> Dec)" and "navigates forward across year boundary (Dec -> Jan)" tests. Per D-07.

    SC-4 (record-detection duplicates): Confirm record-detection.test.ts has "handles two sessions on same day with identical stitch counts without false positives" test. Per D-08.

    SC-5 (completion-estimate exclusion): Confirm completion-estimates.test.ts has tests for both "equals totalStitches" (100% complete) and "exceeds totalStitches" (over 100%) exclusion. Per D-09, at lines 171 and 196.

    Run command: npm test -- --run --reporter=verbose src/lib/utils/skein-calculator.test.ts src/lib/actions/stats-actions.test.ts src/lib/actions/supply-actions.test.ts src/components/features/stats/stitching-calendar.test.tsx src/lib/queries/stats/record-detection.test.ts src/lib/queries/stats/completion-estimates.test.ts

    All 6 files must pass with 0 failures. Record total test count and duration.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/utils/skein-calculator.test.ts src/lib/actions/stats-actions.test.ts src/lib/actions/supply-actions.test.ts src/components/features/stats/stitching-calendar.test.tsx src/lib/queries/stats/record-detection.test.ts src/lib/queries/stats/completion-estimates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test files pass with 0 failures
    - skein-calculator.test.ts contains test named "returns 0 for fabricCount of 0"
    - supply-actions.test.ts contains describe block "resolveDefaultBrandId" with 3+ tests
    - stats-actions.test.ts contains 3 "throws when requireAuth rejects" tests (one per exported function)
    - stats-actions.test.ts contains tests for year 2019 and 2101 boundaries
    - stitching-calendar.test.tsx contains "navigates backward across year boundary (Jan -> Dec)" and "navigates forward across year boundary (Dec -> Jan)"
    - record-detection.test.ts contains "handles two sessions on same day with identical stitch counts"
    - completion-estimates.test.ts contains "excludes projects where stitchesCompleted equals totalStitches" and "excludes projects where stitchesCompleted exceeds totalStitches"
  </acceptance_criteria>
  <done>All 5 success criteria verified against specific passing tests with zero failures</done>
</task>

<task type="auto">
  <name>Task 2: Close backlog items in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md (backlog section, lines 270-320 for the 5 target items)
  </read_first>
  <action>
    Mark 5 backlog items as closed using the project's established pattern (strikethrough + bold + "Shipped in Phase 37" annotation). The items and their line references:

    1. 999.0.24 (line ~278): "Add skein calculator edge case tests (fabricCount=0, resolveDefaultBrandId)" -- strike through, add "Shipped in Phase 37"
    2. 999.24 (line ~301): "Stats action auth/validation test coverage" -- strike through, add "Shipped in Phase 37"
    3. 999.27 (line ~304): "Calendar year-rollover navigation tests" -- strike through, add "Shipped in Phase 37"
    4. 999.38 (line ~315): "Record-detection duplicate-stitch-count edge case test" -- strike through, add "Shipped in Phase 37"
    5. 999.39 (line ~316): "Completion-estimates already-completed project filter test" -- strike through, add "Shipped in Phase 37"

    Use the same format as other closed items in the backlog, e.g.:
    - ~~**999.0.24: Add skein calculator edge case tests (fabricCount=0, resolveDefaultBrandId)**~~ -- **Shipped in Phase 37**

    Also update the "Done This Session" section to record Phase 37 verification completion.
    Update "Next Up" section to point to Phase 38.
  </action>
  <verify>
    <automated>grep -c "Shipped in Phase 37" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Shipped in Phase 37" CLAUDE.md returns 5
    - All 5 items (999.0.24, 999.24, 999.27, 999.38, 999.39) have strikethrough formatting
    - "Done This Session" section includes Phase 37 verification entry
    - "Next Up" section references Phase 38
  </acceptance_criteria>
  <done>5 backlog items marked as shipped, session status updated, next phase pointer set to Phase 38</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed -- this plan only reads existing test files and edits a markdown documentation file.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | N/A | Test verification + CLAUDE.md | accept | No code changes, no auth boundaries, no user input. Documentation-only edit. |
</threat_model>

<verification>
1. All 6 target test files pass: `npm test -- --run src/lib/utils/skein-calculator.test.ts src/lib/actions/stats-actions.test.ts src/lib/actions/supply-actions.test.ts src/components/features/stats/stitching-calendar.test.tsx src/lib/queries/stats/record-detection.test.ts src/lib/queries/stats/completion-estimates.test.ts`
2. Each success criterion from ROADMAP.md Phase 37 maps to at least one passing test
3. 5 backlog items closed in CLAUDE.md: `grep -c "Shipped in Phase 37" CLAUDE.md` returns 5
</verification>

<success_criteria>
- 150 tests across 6 files pass with 0 failures
- All 5 ROADMAP success criteria verified against specific test names
- 5 backlog items (999.0.24, 999.24, 999.27, 999.38, 999.39) marked closed
- CLAUDE.md session status updated
</success_criteria>

<output>
Create `.planning/phases/37-test-coverage-utilities-stats/37-01-SUMMARY.md` when done
</output>
