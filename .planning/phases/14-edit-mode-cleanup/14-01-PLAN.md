---
phase: 14-edit-mode-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/charts/chart-add-form.tsx
  - src/components/features/charts/chart-add-form.test.tsx
  - src/components/features/charts/chart-detail.tsx
  - src/components/features/charts/project-supplies-tab.tsx
  - src/components/features/charts/project-supplies-tab.test.tsx
  - src/components/features/charts/project-detail/supply-row.tsx
  - src/components/features/charts/project-detail/supply-row.test.tsx
  - src/components/features/charts/project-detail/supply-section.tsx
  - src/components/features/charts/project-detail/supply-footer-totals.tsx
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "All 9 deprecated files are deleted from the codebase"
    - "TypeScript build passes with zero errors after deletion"
    - "Full test suite passes with zero regressions"
  artifacts: []
  key_links: []
---

<objective>
Remove 9 deprecated files that have zero live importers -- the first wave of dead code cleanup.

Purpose: These files were replaced by the unified supply table (Phase 10-11) and the merged form (Phase 12). Removing them reduces confusion and prevents accidental usage. This is the safe deletion wave -- all 9 files have been verified to have zero active import chains.

Output: 9 files deleted, green build, green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-edit-mode-cleanup/14-CONTEXT.md
@.planning/phases/14-edit-mode-cleanup/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 9 deprecated files with zero live importers</name>
  <files>
    src/components/features/charts/chart-add-form.tsx
    src/components/features/charts/chart-add-form.test.tsx
    src/components/features/charts/chart-detail.tsx
    src/components/features/charts/project-supplies-tab.tsx
    src/components/features/charts/project-supplies-tab.test.tsx
    src/components/features/charts/project-detail/supply-row.tsx
    src/components/features/charts/project-detail/supply-row.test.tsx
    src/components/features/charts/project-detail/supply-section.tsx
    src/components/features/charts/project-detail/supply-footer-totals.tsx
  </files>
  <read_first>
    src/components/features/charts/chart-add-form.tsx
    src/components/features/charts/chart-detail.tsx
    src/components/features/charts/project-supplies-tab.tsx
    src/components/features/charts/project-detail/supply-section.tsx
    src/components/features/charts/project-detail/supply-footer-totals.tsx
    src/components/features/charts/project-detail/supply-row.tsx
  </read_first>
  <action>
Delete the following 9 files. These have been verified via `grep -rl` to have zero live importers (the only importers are other files in this same deletion set):

**Replaced by chart-merged-form.tsx (Phase 12):**
1. `src/components/features/charts/chart-add-form.tsx` -- old multi-section chart creation form
2. `src/components/features/charts/chart-add-form.test.tsx` -- tests for above

**Replaced by project-detail/ modular components (Phase 7/9):**
3. `src/components/features/charts/chart-detail.tsx` -- monolithic project detail (replaced by modular project-detail/ directory)

**Replaced by unified supply-table feature (Phase 10-11):**
4. `src/components/features/charts/project-supplies-tab.tsx` -- old supply tab (only imported by dead chart-detail.tsx)
5. `src/components/features/charts/project-supplies-tab.test.tsx` -- tests for above
6. `src/components/features/charts/project-detail/supply-row.tsx` -- old supply row component (only imported by dead supply-section.tsx)
7. `src/components/features/charts/project-detail/supply-row.test.tsx` -- tests for above
8. `src/components/features/charts/project-detail/supply-section.tsx` -- old supply section (zero live importers)
9. `src/components/features/charts/project-detail/supply-footer-totals.tsx` -- old supply footer (zero live importers)

Before deleting, run a final safety check: `grep -rl "chart-add-form\|chart-detail\|project-supplies-tab\|supply-section\|supply-footer-totals\|supply-row" src/ --include='*.ts' --include='*.tsx' | grep -v ".test." | grep -v -E "(chart-add-form|chart-detail|project-supplies-tab|supply-section|supply-footer-totals|supply-row|supply-table-data-row)\.(tsx|ts)"` -- this should return zero results (no live importers outside the deletion set itself). If any file appears, do NOT delete the file it imports and investigate.

After deletion, run:
1. `npm run build` -- must pass with zero errors (per D-13)
2. `npx vitest run --reporter=dot` -- must pass with zero failures

Note: Do NOT delete `sections/` directory, `form-primitives/pattern-type-fields.tsx`, or `chart-edit-modal.tsx` in this plan. Those files are still actively imported by `chart-edit-modal.tsx` and `chart-list.tsx` respectively, and must wait for Plan 2 to rewire those importers before deletion in Plan 3.
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npm run build && npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/components/features/charts/chart-add-form.tsx 2>/dev/null` returns nothing (file deleted)
    - `ls src/components/features/charts/chart-add-form.test.tsx 2>/dev/null` returns nothing
    - `ls src/components/features/charts/chart-detail.tsx 2>/dev/null` returns nothing
    - `ls src/components/features/charts/project-supplies-tab.tsx 2>/dev/null` returns nothing
    - `ls src/components/features/charts/project-supplies-tab.test.tsx 2>/dev/null` returns nothing
    - `ls src/components/features/charts/project-detail/supply-row.tsx 2>/dev/null` returns nothing
    - `ls src/components/features/charts/project-detail/supply-row.test.tsx 2>/dev/null` returns nothing
    - `ls src/components/features/charts/project-detail/supply-section.tsx 2>/dev/null` returns nothing
    - `ls src/components/features/charts/project-detail/supply-footer-totals.tsx 2>/dev/null` returns nothing
    - `npm run build` exits 0
    - `npx vitest run --reporter=dot` exits 0
    - `src/components/features/charts/chart-edit-modal.tsx` still exists (NOT deleted yet)
    - `src/components/features/charts/sections/` directory still exists (NOT deleted yet)
  </acceptance_criteria>
  <done>All 9 deprecated files deleted. Build and test suite both pass green. chart-edit-modal and sections/ preserved for Plan 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | File deletion only -- no runtime trust boundaries involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Denial of Service | Build system | accept | File deletion verified by build pass; no runtime impact. If a live file is accidentally deleted, build fails immediately and git can restore. |
</threat_model>

<verification>
- `npm run build` exits 0 (TypeScript compilation, no missing modules)
- `npx vitest run --reporter=dot` exits 0 (no test regressions)
- All 9 files confirmed absent from filesystem
- `chart-edit-modal.tsx` and `sections/` directory still present
</verification>

<success_criteria>
9 deprecated files removed from codebase. TypeScript build passes. Full test suite passes. No files deleted that still have live importers.
</success_criteria>

<output>
After completion, create `.planning/phases/14-edit-mode-cleanup/14-01-SUMMARY.md`
</output>
