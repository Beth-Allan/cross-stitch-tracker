---
phase: 38-test-coverage-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/chart-actions.test.ts
  - src/components/features/charts/form-primitives/chart-file-upload.test.tsx
  - src/components/features/charts/project-detail/supplies-tab.nyquist.test.tsx
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "seriesId flows through to prisma.chart.create and prisma.chart.update data payloads when provided"
    - "seriesId is omitted or null in prisma payloads when not provided"
    - "A .zip file passes client-side validateFile and triggers upload"
    - "SuppliesTab rolls back calcParams to serverCalcParams and shows toast.error when updateProjectSettings fails"
    - "Backlog items 999.79 and 999.82 are verified as already covered and closed"
  artifacts:
    - path: "src/lib/actions/chart-actions.test.ts"
      provides: "seriesId flow-through tests for create and update"
      contains: "seriesId"
    - path: "src/components/features/charts/form-primitives/chart-file-upload.test.tsx"
      provides: "Zip file validation acceptance test"
      contains: ".zip"
    - path: "src/components/features/charts/project-detail/supplies-tab.nyquist.test.tsx"
      provides: "calcParams error rollback tests"
      contains: "rollback"
  key_links:
    - from: "chart-actions.test.ts"
      to: "chart-actions.ts createChartAndProject + updateChart"
      via: "prisma mock data payload assertions"
      pattern: "seriesId.*series-1"
    - from: "supplies-tab.nyquist.test.tsx"
      to: "supplies-tab.tsx handleCalcParamsChange"
      via: "updateProjectSettings mock rejection"
      pattern: "updateProjectSettings.*mockRejectedValue"
---

<objective>
Fill chart form component test gaps for seriesId flow-through in chart actions, .zip file validation, and calcParams error rollback. Verify-and-close 999.79 and 999.82 as already covered.

Purpose: Close backlog items 999.78, 999.80, 999.81 with new tests, and verify+close 999.79 and 999.82. Completes TEST-04 requirement.
Output: 3 test files supplemented with new test cases, 2 backlog items verified as already covered, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-test-coverage-components/38-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: seriesId flow-through and zip validation tests</name>
  <files>src/lib/actions/chart-actions.test.ts, src/components/features/charts/form-primitives/chart-file-upload.test.tsx</files>
  <read_first>
    src/lib/actions/chart-actions.ts (createChartAndProject lines 19-50 where seriesId goes into chart.create data, updateChart lines 226-280 where seriesId goes into chart.update data)
    src/lib/actions/chart-actions.test.ts (existing test structure, createMockPrisma setup, validChartInput fixture, beforeEach auth pattern)
    src/components/features/charts/form-primitives/chart-file-upload.tsx (validateFile function lines 36-50, ALLOWED_CHART_FILE_EXTENSIONS and ALLOWED_CHART_FILE_TYPES imports)
    src/components/features/charts/form-primitives/chart-file-upload.test.tsx (existing test patterns, mockGetPresignedUploadUrl, mockFetch)
    src/lib/validations/upload.ts (ALLOWED_CHART_FILE_TYPES includes "application/zip" and "application/x-zip-compressed", ALLOWED_CHART_FILE_EXTENSIONS includes ".zip")
  </read_first>
  <behavior>
    chart-actions.test.ts — seriesId flow-through (999.81):
    - Test: createChartWithSupplies with seriesId in chart input passes seriesId to prisma.chart.create data payload
    - Test: createChartWithSupplies without seriesId (null/undefined) does not include seriesId or includes null
    - Test: updateChart with seriesId passes seriesId to prisma.chart.update data payload

    chart-file-upload.test.tsx — zip validation (999.80):
    - Test: .zip file with application/zip MIME type passes validation and triggers upload (onFilesChange called)
  </behavior>
  <action>
    In chart-actions.test.ts, add a new describe block "seriesId flow-through" inside or after the "createChartWithSupplies" describe per D-11. Use the existing validChartInput pattern but add seriesId to the chart object:

    1. "includes seriesId in create payload when provided": set validChartInput.chart.seriesId to "series-1". Mock $transaction as in existing tests. After calling createChartWithSupplies, assert mockPrisma.chart.create was called with data containing seriesId: "series-1".

    2. "includes null seriesId when not provided": use validChartInput without seriesId (or with seriesId: null). Assert mockPrisma.chart.create was called with data containing seriesId: null (the schema default).

    3. "includes seriesId in update payload": add a test for updateChart. Set up auth, mock prisma.chart.findUnique to return existing chart with project userId matching, mock $transaction. Call updateChart("chart-1", formDataWithSeriesId). Assert mockPrisma.chart.update was called with data containing seriesId: "series-1".

    For the updateChart test, construct the formData using the same shape as validChartInput but with seriesId: "series-1". The updateChart function parses with chartFormSchema.parse(formData), so the input shape must match. Mock prisma.chart.findUnique to return { coverImageUrl: null, coverThumbnailUrl: null, project: { id: "proj-1", userId: "user-1" } }. Mock $transaction to execute the callback with mockPrisma.

    In chart-file-upload.test.tsx, add per D-10:

    1. "accepts .zip file and triggers upload": create a File with name "patterns.zip" and type "application/zip", size 5000 bytes. Mock getPresignedUploadUrl to return success. Fire change event on the file input. Use waitFor to verify onFilesChange was called with an array containing an object with filename "patterns.zip" and mimeType "application/zip".
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npx vitest run src/lib/actions/chart-actions.test.ts src/components/features/charts/form-primitives/chart-file-upload.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - chart-actions.test.ts contains describe "seriesId flow-through" with at least 3 test cases
    - Test asserts mockPrisma.chart.create called with data object containing seriesId: "series-1"
    - Test asserts mockPrisma.chart.create called with data containing seriesId: null when not provided
    - Test asserts mockPrisma.chart.update called with data containing seriesId: "series-1"
    - chart-file-upload.test.tsx contains test "accepts .zip file" that verifies onFilesChange called with filename "patterns.zip"
    - All tests in both files pass
  </acceptance_criteria>
  <done>seriesId flows through to create and update Prisma calls (verified with 3 tests). Zip file passes client-side validation (verified with 1 test). All existing + new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: calcParams error rollback tests and verify-and-close</name>
  <files>src/components/features/charts/project-detail/supplies-tab.nyquist.test.tsx</files>
  <read_first>
    src/components/features/charts/project-detail/supplies-tab.tsx (handleCalcParamsChange lines 123-154 — the rollback logic: setCalcParams(serverParamsRef.current) + toast.error on failure)
    src/components/features/charts/project-detail/supplies-tab.nyquist.test.tsx (existing Nyquist test structure, mocks, makeThread factory, defaultProject fixture)
    src/lib/actions/chart-actions.ts (updateProjectSettings signature at line 511 — takes chartId and formData)
    src/lib/actions/chart-actions-settings.test.ts (verify-and-close for 999.79 — 9 tests already exist: auth, 4 Zod boundary, ownership, happy path, partial update, Prisma error)
    src/components/features/charts/use-chart-form.test.tsx (verify-and-close for 999.82 — handleAddSeries empty/whitespace guard tests at lines 79, 89, 135, 145)
  </read_first>
  <behavior>
    supplies-tab.nyquist.test.tsx — calcParams error rollback (999.78):
    - Test: when updateProjectSettings rejects (throws), calcParams roll back to serverCalcParams (the initial values from project prop) and toast.error fires
    - Test: when updateProjectSettings returns {success: false}, same rollback and toast.error behavior
    - Test: when chartId is missing (no calculator prop), changing calc params does not call updateProjectSettings

    Verify-and-close (no code changes needed):
    - 999.79: chart-actions-settings.test.ts has 9 tests covering auth rejection, Zod boundaries, ownership, happy path, Prisma error
    - 999.82: use-chart-form.test.tsx has handleAddSeries empty and whitespace guard tests
  </behavior>
  <action>
    In supplies-tab.nyquist.test.tsx, add a new describe block "calcParams error rollback" per D-08. The existing file already mocks updateProjectSettings from chart-actions and sonner toast.

    1. "rolls back calcParams and shows toast.error when updateProjectSettings throws": render SuppliesTab with calculator={{ chartId: "chart-1", fabricOptions: [] }} and project with strandCount=2. The CalculatorCard component exposes strand count controls. Find the strand count control and change it (the CalculatorCard renders a select or stepper for strandCount). After the change, mock updateProjectSettings to reject with an Error. Use waitFor to verify toast.error was called with "Couldn't save settings. Please try again." and that the display reverts to the original strandCount value of 2.

    Actually, the CalculatorCard interaction is complex and internal. A more direct approach: since SuppliesTab exposes handleCalcParamsChange via the CalculatorCard's onCalcParamsChange prop, and the Nyquist test already renders SuppliesTab, we need to trigger a calc param change through the UI.

    The CalculatorCard has controls for strandCount, overCount, and wastePercent. Find the strandCount selector in the rendered output. If it's a combobox or select, change it. If that's too complex to interact with, use a simpler approach:

    Actually, looking at the rollback logic more carefully — the key behavior is: (a) optimistic update happens, (b) server call fails, (c) state reverts to serverParamsRef.current. The serverParamsRef.current is set from the project prop (strandCount, overCount, wastePercent).

    For the test: render with calculator prop (chartId "chart-1", fabricOptions []), project has strandCount 2, overCount 2, wastePercent 20. Mock updateProjectSettings to return { success: false }. Find and interact with the CalculatorCard's strandCount control. After the failed server call, verify the strandCount display shows "2" (reverted) and toast.error was called.

    The CalculatorCard renders strandCount as a select/dropdown. Find the strand count control by its label or role, change it to 3, then verify rollback to 2.

    2. "rolls back on server rejection (thrown error)": same setup but mock updateProjectSettings to throw an Error. Verify same rollback behavior.

    Note: The CalculatorCard may render inside the SuppliesTab only when the calculator prop is provided. Without it, the calculator is not shown. The existing Nyquist test renders without calculator prop, so the new tests need to add it.

    For verify-and-close items (999.79, 999.82): no code changes. Document in the SUMMARY that existing coverage was confirmed:
    - 999.79: chart-actions-settings.test.ts contains 9 tests covering all specified paths
    - 999.82: use-chart-form.test.tsx contains handleAddSeries empty and whitespace guard tests at 4 locations
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npx vitest run src/components/features/charts/project-detail/supplies-tab.nyquist.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - supplies-tab.nyquist.test.tsx contains describe "calcParams error rollback" with at least 2 test cases
    - Test mocks updateProjectSettings to return {success: false} and verifies toast.error called with "Couldn't save settings. Please try again."
    - Test verifies strandCount display reverts to original value (2) after failed update
    - Test mocks updateProjectSettings to throw and verifies same rollback + toast behavior
    - All tests in supplies-tab.nyquist.test.tsx pass (both existing GAP-2 tests and new rollback tests)
    - Verify-and-close: chart-actions-settings.test.ts has test "requires auth" and chart-actions-settings.test.ts has test "rejects strandCount below 1" (confirming 999.79 coverage)
    - Verify-and-close: use-chart-form.test.tsx has test containing "does not call server action when name is empty" in handleAddSeries context (confirming 999.82 coverage)
  </acceptance_criteria>
  <done>calcParams error rollback tests cover both server rejection and thrown error paths with toast.error verification. 999.79 and 999.82 verified as already covered. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan adds test files only, no production code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02 | N/A | Test files only | accept | No production code modified, no new packages installed |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
All chart form test files pass: `npx vitest run src/lib/actions/chart-actions.test.ts src/components/features/charts/form-primitives/chart-file-upload.test.tsx src/components/features/charts/project-detail/supplies-tab.nyquist.test.tsx --reporter=verbose`
No regressions in full test suite: `npm test`
Verify-and-close confirmation: `grep -c "rejects strandCount" src/lib/actions/chart-actions-settings.test.ts` returns >= 2 and `grep -c "handleAddSeries" src/components/features/charts/use-chart-form.test.tsx` returns >= 4
</verification>

<success_criteria>
1. chart-actions.test.ts verifies seriesId flows through to prisma.chart.create and prisma.chart.update data payloads
2. chart-file-upload.test.tsx verifies .zip files pass client-side validation
3. supplies-tab.nyquist.test.tsx verifies calcParams rollback to serverCalcParams on updateProjectSettings failure
4. 999.79 (updateProjectSettings) confirmed covered by 9 existing tests in chart-actions-settings.test.ts
5. 999.82 (handleAddSeries guards) confirmed covered by existing empty/whitespace tests in use-chart-form.test.tsx
6. All tests pass with zero regressions
</success_criteria>

<output>
Create `.planning/phases/38-test-coverage-components/38-02-SUMMARY.md` when done
</output>
