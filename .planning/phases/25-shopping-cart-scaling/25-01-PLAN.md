---
phase: 25-shopping-cart-scaling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/shopping/project-search-input.tsx
  - src/components/features/shopping/project-search-input.test.tsx
  - src/components/features/shopping/supply-search-input.tsx
  - src/components/features/shopping/supply-search-input.test.tsx
  - src/components/features/shopping/status-group.tsx
  - src/components/features/shopping/status-group.test.tsx
  - src/components/features/shopping/selection-counter.tsx
  - src/components/features/shopping/selection-counter.test.tsx
autonomous: true
requirements:
  - CRIT-02

must_haves:
  truths:
    - "ProjectSearchInput renders a search input with Search icon, placeholder, and clear button"
    - "SupplySearchInput renders a search input with Search icon, placeholder, and clear button"
    - "StatusGroup renders a collapsible section with status dot, label, count badge, and 'Select all' action"
    - "SelectionCounter shows '{N} of {M} projects selected' in normal mode"
    - "SelectionCounter shows '{visibleSelected} of {visibleTotal} visible selected ({totalSelected} total selected)' when search is active"
  artifacts:
    - path: "src/components/features/shopping/project-search-input.tsx"
      provides: "Search input component for project name filtering"
      exports: ["ProjectSearchInput"]
    - path: "src/components/features/shopping/supply-search-input.tsx"
      provides: "Search input component for supply filtering in By Supply view"
      exports: ["SupplySearchInput"]
    - path: "src/components/features/shopping/status-group.tsx"
      provides: "Collapsible status group wrapper with header and per-group select"
      exports: ["StatusGroup", "STATUS_GROUP_ORDER"]
    - path: "src/components/features/shopping/selection-counter.tsx"
      provides: "Smart selection counter with normal and search-active modes"
      exports: ["SelectionCounter"]
  key_links:
    - from: "status-group.tsx"
      to: "src/lib/utils/status.ts"
      via: "STATUS_CONFIG import"
      pattern: "STATUS_CONFIG\\[status\\]"
    - from: "selection-counter.tsx"
      to: "shopping-cart.tsx (Plan 02)"
      via: "props interface"
      pattern: "isSearchActive"
---

<objective>
Create the four new standalone components needed for shopping cart scaling: ProjectSearchInput, SupplySearchInput, StatusGroup, and SelectionCounter. Each component is tested first (TDD), then implemented.

Purpose: These are the building blocks that Plan 02 will wire into the existing shopping cart. Building them standalone first means Plan 02 can focus purely on integration without also creating components.

Output: 4 new components with full test coverage, ready for integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-shopping-cart-scaling/25-CONTEXT.md
@.planning/phases/25-shopping-cart-scaling/25-RESEARCH.md
@.planning/phases/25-shopping-cart-scaling/25-PATTERNS.md
@.planning/phases/25-shopping-cart-scaling/25-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/utils/status.ts:
```typescript
import type { ProjectStatus } from "@/generated/prisma/client";

export const STATUS_CONFIG: Record<
  ProjectStatus,
  {
    label: string;
    cssVar: string;
    bgClass: string;
    textClass: string;
    dotClass: string;
    darkBgClass: string;
  }
> = { /* KITTING, IN_PROGRESS, ON_HOLD, etc. */ };
```

From src/components/ui/empty-state.tsx:
```typescript
interface EmptyStateProps {
  icon: React.ComponentType<{ className?: string }>;
  title: string;
  description?: string;
  heading?: boolean;
  children?: React.ReactNode;
  className?: string;
}
export function EmptyState({ icon, title, description, ... }: EmptyStateProps)
```

From src/types/dashboard.ts:
```typescript
export interface ShoppingCartProject extends OptionalFocalPoint {
  projectId: string;
  chartId: string;
  projectName: string;
  designerName: string | null;
  coverThumbnailUrl: string | null;
  status: ProjectStatus;
  threadCount: number;
  beadCount: number;
  specialtyCount: number;
  fabricNeeded: boolean;
}
```

Test infrastructure pattern (from existing shopping tests):
```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen } from "@/__tests__/test-utils";
import userEvent from "@testing-library/user-event";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProjectSearchInput, SupplySearchInput, and their tests</name>
  <files>
    src/components/features/shopping/project-search-input.tsx,
    src/components/features/shopping/project-search-input.test.tsx,
    src/components/features/shopping/supply-search-input.tsx,
    src/components/features/shopping/supply-search-input.test.tsx
  </files>
  <read_first>
    src/components/features/gallery/filter-bar.tsx,
    src/components/features/shopping/shopping-cart.test.tsx,
    .planning/phases/25-shopping-cart-scaling/25-PATTERNS.md,
    .planning/phases/25-shopping-cart-scaling/25-UI-SPEC.md
  </read_first>
  <behavior>
    ProjectSearchInput:
    - Test 1: renders input with placeholder "Search projects..."
    - Test 2: renders Search icon (lucide) in the input
    - Test 3: calls onChange callback when user types
    - Test 4: shows X clear button only when value is non-empty
    - Test 5: clicking X clear button calls onChange with empty string
    - Test 6: has role="searchbox" and aria-label="Search projects"
    - Test 7: clear button has aria-label="Clear search"

    SupplySearchInput:
    - Test 1: renders input with placeholder "Search supplies..."
    - Test 2: calls onChange callback when user types
    - Test 3: shows X clear button only when value is non-empty
    - Test 4: clicking X clear button calls onChange with empty string
    - Test 5: has role="searchbox" and aria-label="Search supplies"
  </behavior>
  <action>
    Write tests first for both components, then implement.

    Both search inputs share the same structure -- they are thin wrappers around a common search input pattern from `filter-bar.tsx`. Per D-01, the project search input sits above the view toggle and is full-width. The supply search input sits within the By Supply view (Claude's discretion: separate input per RESEARCH.md recommendation).

    **ProjectSearchInput** (`project-search-input.tsx`):
    - "use client" directive
    - Props: `{ value: string; onChange: (value: string) => void }`
    - Export named: `ProjectSearchInput`
    - Full-width input (no max-w constraint, unlike the gallery's 280px)
    - Exact CSS classes from PATTERNS.md shared pattern:
      - Container: `relative w-full`
      - Search icon: `text-muted-foreground/60 absolute top-1/2 left-2.5 h-4 w-4 -translate-y-1/2` with `strokeWidth={1.5}`
      - Input: `border-border bg-card placeholder:text-muted-foreground focus:border-ring focus:ring-ring w-full rounded-lg border py-2 pr-8 pl-9 text-sm focus:ring-1 focus:outline-none`
      - Input attributes: `type="text"`, `role="searchbox"`, `aria-label="Search projects"`, `placeholder="Search projects..."`
      - Clear button (conditional on `value.length > 0`): `text-muted-foreground hover:text-foreground focus-visible:ring-ring absolute top-1/2 right-2.5 flex h-6 w-6 -translate-y-1/2 items-center justify-center rounded-sm transition-colors outline-none focus-visible:ring-2`
      - Clear button: `type="button"`, `aria-label="Clear search"`, calls `onChange("")`
      - X icon: `h-3.5 w-3.5` with `strokeWidth={2}`

    **SupplySearchInput** (`supply-search-input.tsx`):
    - "use client" directive
    - Props: `{ value: string; onChange: (value: string) => void }`
    - Export named: `SupplySearchInput`
    - Identical structure to ProjectSearchInput except:
      - `placeholder="Search supplies..."`
      - `aria-label="Search supplies"`

    Import icons from `lucide-react`: `Search`, `X`.
  </action>
  <verify>
    <automated>npx vitest run src/components/features/shopping/project-search-input.test.tsx src/components/features/shopping/supply-search-input.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/shopping/project-search-input.tsx contains `export function ProjectSearchInput`
    - src/components/features/shopping/project-search-input.tsx contains `role="searchbox"`
    - src/components/features/shopping/project-search-input.tsx contains `aria-label="Search projects"`
    - src/components/features/shopping/project-search-input.tsx contains `placeholder="Search projects..."`
    - src/components/features/shopping/project-search-input.tsx contains `aria-label="Clear search"`
    - src/components/features/shopping/supply-search-input.tsx contains `export function SupplySearchInput`
    - src/components/features/shopping/supply-search-input.tsx contains `role="searchbox"`
    - src/components/features/shopping/supply-search-input.tsx contains `aria-label="Search supplies"`
    - src/components/features/shopping/supply-search-input.tsx contains `placeholder="Search supplies..."`
    - project-search-input.test.tsx exits 0
    - supply-search-input.test.tsx exits 0
  </acceptance_criteria>
  <done>Both search input components render with correct placeholder, icon, clear button, ARIA attributes, and pass all tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create StatusGroup and SelectionCounter with tests</name>
  <files>
    src/components/features/shopping/status-group.tsx,
    src/components/features/shopping/status-group.test.tsx,
    src/components/features/shopping/selection-counter.tsx,
    src/components/features/shopping/selection-counter.test.tsx
  </files>
  <read_first>
    src/lib/utils/status.ts,
    src/components/features/shopping/project-accordion.tsx,
    src/components/features/shopping/project-accordion.test.tsx,
    .planning/phases/25-shopping-cart-scaling/25-UI-SPEC.md,
    .planning/phases/25-shopping-cart-scaling/25-PATTERNS.md
  </read_first>
  <behavior>
    StatusGroup:
    - Test 1: renders status label from STATUS_CONFIG (e.g., "Kitting" for KITTING status)
    - Test 2: renders count badge with the provided count number
    - Test 3: renders status dot with correct dotClass from STATUS_CONFIG
    - Test 4: renders children when expanded (isExpanded=true)
    - Test 5: does not render children when collapsed (isExpanded=false)
    - Test 6: calls onToggle when header chevron/area is clicked
    - Test 7: renders "Select all" button that calls onSelectAll when clicked
    - Test 8: "Select all" button has aria-label "Select all {StatusLabel} projects"
    - Test 9: toggle button has aria-expanded matching isExpanded prop
    - Test 10: has role="group" with aria-labelledby pointing to header

    SelectionCounter:
    - Test 1: normal mode shows "{N} of {M} projects selected" (e.g., "3 of 10 projects selected")
    - Test 2: normal mode with 1 project shows "project" not "projects" (singular)
    - Test 3: search mode shows "{visibleSelected} of {visibleTotal} visible selected ({totalSelected} total selected)"
    - Test 4: search mode when totalSelected equals visibleSelected omits the parenthetical
    - Test 5: has aria-live="polite" on the container
    - Test 6: normal mode when isSearchActive=false does not show "visible" text

    STATUS_GROUP_ORDER:
    - Test: exported constant matches [KITTING, IN_PROGRESS, ON_HOLD, UNSTARTED, KITTED]
  </behavior>
  <action>
    Write tests first, then implement both components.

    **StatusGroup** (`status-group.tsx`):
    - "use client" directive
    - Imports: `ChevronDown`, `ChevronRight` from `lucide-react`, `STATUS_CONFIG` from `@/lib/utils/status`, `cn` from `@/lib/utils`, `ProjectStatus` type from `@/generated/prisma/client`
    - Export a constant `STATUS_GROUP_ORDER` of type `ProjectStatus[]` with value: `["KITTING", "IN_PROGRESS", "ON_HOLD", "UNSTARTED", "KITTED"]`
      - FINISHED and FFO excluded because the server query already filters them out (per RESEARCH.md A2)
      - This constant is per D-03 workflow progression order
    - Export named: `StatusGroup`
    - Props interface `StatusGroupProps`:
      ```typescript
      {
        status: ProjectStatus;
        count: number;
        isExpanded: boolean;
        onToggle: () => void;
        onSelectAll: () => void;
        children: React.ReactNode;
      }
      ```
    - Render structure:
      - Outer `<div role="group" aria-labelledby={`group-${status}`}>`
      - Header row: `<div className="flex items-center justify-between py-2">`
        - Left side: `<button type="button" onClick={onToggle} aria-expanded={isExpanded} className="flex items-center gap-2" id={`group-${status}`}>`
          - Chevron: `isExpanded ? ChevronDown : ChevronRight`, className `h-4 w-4 text-muted-foreground`
          - Status dot: `<span aria-hidden="true" className={cn("h-2 w-2 rounded-full", config.dotClass)} />`
          - Status label: `<span className="text-[11px] font-semibold tracking-wider uppercase">{config.label}</span>` (per UI-SPEC typography: text-[11px] font-semibold tracking-wider uppercase)
          - Count badge: `<span className="bg-muted text-muted-foreground rounded-full px-1.5 py-0.5 font-mono text-[11px] font-semibold">{count}</span>` (font-semibold per UI-SPEC, not font-bold)
        - Right side: `<button type="button" onClick={onSelectAll} className="text-progress-foreground hover:text-selected-foreground text-xs font-medium transition-colors" aria-label={`Select all ${config.label} projects`}>Select all</button>` (per D-10)
      - Body: `{isExpanded && children}` (simple show/hide, no animation per UI-SPEC)

    **SelectionCounter** (`selection-counter.tsx`):
    - No "use client" needed (pure presentational, receives all data via props, no hooks)
    - Export named: `SelectionCounter`
    - Props interface `SelectionCounterProps`:
      ```typescript
      {
        selectedCount: number;
        totalCount: number;
        visibleCount?: number;
        visibleSelectedCount?: number;
        isSearchActive: boolean;
      }
      ```
    - Render: `<p className="text-muted-foreground text-sm" aria-live="polite">`
    - Normal mode (isSearchActive=false): `{selectedCount} of {totalCount} project{totalCount !== 1 ? "s" : ""} selected`
    - Search mode (isSearchActive=true AND visibleCount/visibleSelectedCount defined):
      - Always show: `{visibleSelectedCount} of {visibleCount} visible selected`
      - Conditionally append: ` ({selectedCount} total selected)` ONLY when `selectedCount !== visibleSelectedCount`
      - Per D-09 copywriting contract

    **Test infrastructure:**
    - Import from `@/__tests__/test-utils`, not `@testing-library/react`
    - Mock `@/lib/utils/status` in status-group tests to control STATUS_CONFIG output
    - Use `userEvent` for click interactions
  </action>
  <verify>
    <automated>npx vitest run src/components/features/shopping/status-group.test.tsx src/components/features/shopping/selection-counter.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/shopping/status-group.tsx contains `export function StatusGroup`
    - src/components/features/shopping/status-group.tsx contains `export const STATUS_GROUP_ORDER`
    - src/components/features/shopping/status-group.tsx contains `role="group"`
    - src/components/features/shopping/status-group.tsx contains `aria-expanded`
    - src/components/features/shopping/status-group.tsx contains `Select all`
    - src/components/features/shopping/selection-counter.tsx contains `export function SelectionCounter`
    - src/components/features/shopping/selection-counter.tsx contains `aria-live="polite"`
    - src/components/features/shopping/selection-counter.tsx contains `visible selected`
    - src/components/features/shopping/selection-counter.tsx contains `total selected`
    - status-group.test.tsx exits 0
    - selection-counter.test.tsx exits 0
  </acceptance_criteria>
  <done>StatusGroup renders collapsible sections with status dot, label, count, and per-group select. SelectionCounter shows normal and search-active counter formats. STATUS_GROUP_ORDER follows D-03 workflow progression. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | All components in this plan are pure presentational client-side components with no server interaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | ProjectSearchInput | accept | Search value is only used in `.toLowerCase().includes()` comparison in Plan 02. React JSX auto-escapes all rendered values. No XSS risk. Client-side only, no server-bound input. |
</threat_model>

<verification>
All 4 new component files exist with correct exports. All 4 test files pass. Components match UI-SPEC typography, spacing, color, and accessibility contracts.
</verification>

<success_criteria>
- 4 new components created: ProjectSearchInput, SupplySearchInput, StatusGroup, SelectionCounter
- 4 new test files, all passing
- STATUS_GROUP_ORDER exported with D-03 order
- All ARIA attributes present per UI-SPEC accessibility contract
- All styling uses semantic tokens (no hardcoded color scales)
</success_criteria>

<output>
After completion, create `.planning/phases/25-shopping-cart-scaling/25-01-SUMMARY.md`
</output>
