---
phase: 34-browse-pattern-dive-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - src/components/features/gallery/gallery-utils.ts
  - src/components/features/gallery/gallery-utils.test.ts
  - src/components/features/gallery/use-gallery-filters.ts
  - src/components/features/gallery/use-gallery-filters.test.ts
  - src/components/features/gallery/filter-bar.tsx
  - src/components/features/gallery/filter-bar.test.tsx
  - src/components/features/gallery/filter-chips.tsx
  - src/components/features/gallery/filter-chips.test.tsx
  - src/components/features/gallery/project-gallery.tsx
autonomous: true
requirements:
  - SERIES-09

must_haves:
  truths:
    - "User can filter the Browse tab by one or more series"
    - "User can filter for charts with no series assignment via 'Unassigned' option"
    - "User can see active series filter chips and remove them individually"
    - "User can clear all filters including series filter"
    - "Series filter state persists in URL as ?series=id1,id2"
  artifacts:
    - path: "src/components/features/gallery/filter-bar.tsx"
      provides: "Series MultiSelectDropdown in filter bar"
      contains: "Series"
    - path: "src/components/features/gallery/filter-chips.tsx"
      provides: "Series filter chip generation"
      contains: "Series:"
    - path: "src/components/features/gallery/use-gallery-filters.ts"
      provides: "seriesFilter URL state and toggleSeries callback"
      contains: "seriesFilter"
  key_links:
    - from: "src/components/features/gallery/use-gallery-filters.ts"
      to: "src/components/features/gallery/gallery-utils.ts"
      via: "seriesFilter param in filterAndSort call"
      pattern: "seriesFilter"
    - from: "src/components/features/gallery/project-gallery.tsx"
      to: "src/components/features/gallery/filter-bar.tsx"
      via: "seriesFilter/toggleSeries/seriesOptions props"
      pattern: "seriesFilter.*toggleSeries.*seriesOptions"
---

<objective>
Add a series filter to the Browse tab's filter bar, enabling users to filter the gallery by one or more series (or by "Unassigned" for charts without a series).

Purpose: SERIES-09 -- with 500+ charts, many users will want to view just one series at a time. The filter follows the exact same MultiSelectDropdown + FilterChips + URL state pattern as the existing Status and Size filters.

Output: Extended filterAndSort with series predicate, extended useGalleryFilters with seriesFilter URL state, FilterBar with Series dropdown, FilterChips with series chip generation, ProjectGallery prop threading, tests for all changes.
</objective>

<execution_context>
@/Users/wanderskye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-browse-pattern-dive-integration/34-CONTEXT.md
@.planning/phases/34-browse-pattern-dive-integration/34-PATTERNS.md
@.planning/phases/34-browse-pattern-dive-integration/34-UI-SPEC.md
@.planning/phases/34-browse-pattern-dive-integration/34-01-SUMMARY.md

<interfaces>
From src/components/features/gallery/gallery-types.ts (after Plan 01):
```typescript
export interface GalleryCardData extends OptionalFocalPoint {
  // ... existing fields ...
  seriesId: string | null;
  seriesName: string | null;
  dateAdded: Date;
}
```

From src/components/features/gallery/gallery-utils.ts (current filterAndSort):
```typescript
export function filterAndSort(
  cards: GalleryCardData[],
  options: {
    search: string;
    statusFilter: string[];
    sizeFilter: string[];
    sort: SortField;
    dir: SortDir;
  },
): GalleryCardData[]
```

From src/components/features/gallery/use-gallery-filters.ts (current return):
```typescript
return {
  view, sort, dir, search, statusFilter, sizeFilter,
  setView, setSort, setDir, setSearch, toggleStatus, toggleSize, clearFilters,
  filteredAndSorted, totalCount, filteredCount, hasActiveFilters,
};
```

From src/components/features/gallery/filter-bar.tsx:
```typescript
interface FilterBarProps {
  search: string;
  onSearchChange: (value: string) => void;
  statusFilter: string[];
  onStatusToggle: (value: string) => void;
  sizeFilter: string[];
  onSizeToggle: (value: string) => void;
}
```

From src/components/features/gallery/filter-chips.tsx:
```typescript
interface FilterChipsProps {
  search: string;
  statusFilter: string[];
  sizeFilter: string[];
  onRemoveSearch: () => void;
  onRemoveStatus: (value: string) => void;
  onRemoveSize: (value: string) => void;
  onClearAll: () => void;
}
```

From src/components/features/gallery/multi-select-dropdown.tsx:
```typescript
interface MultiSelectDropdownProps {
  label: string;
  options: { value: string; label: string }[];
  selected: string[];
  onToggle: (value: string) => void;
}
```

From src/__tests__/mocks/factories.ts (after Plan 01):
```typescript
export function createMockGalleryCard(overrides?: Partial<GalleryCardData>): GalleryCardData
// Default seriesId: null, seriesName: null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend filterAndSort with series filter predicate</name>
  <files>
    src/components/features/gallery/gallery-utils.ts,
    src/components/features/gallery/gallery-utils.test.ts
  </files>
  <read_first>
    src/components/features/gallery/gallery-utils.ts,
    src/components/features/gallery/gallery-utils.test.ts,
    .planning/phases/34-browse-pattern-dive-integration/34-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: filterAndSort with empty seriesFilter returns all cards (no filtering)
    - Test 2: filterAndSort with seriesFilter=["s1"] returns only cards with seriesId "s1"
    - Test 3: filterAndSort with seriesFilter=["s1","s2"] returns cards matching either series
    - Test 4: filterAndSort with seriesFilter=["__unassigned__"] returns only cards with seriesId null
    - Test 5: filterAndSort with seriesFilter=["__unassigned__","s1"] returns cards with null seriesId OR seriesId "s1" (union)
    - Test 6: filterAndSort with seriesFilter=["nonexistent"] returns empty array when no cards match
  </behavior>
  <action>
    Add seriesFilter: string[] to the filterAndSort options parameter (after sizeFilter). Default behavior: when seriesFilter is empty, no filtering occurs (same as statusFilter/sizeFilter).

    Add series filter predicate block after the existing size filter block in filterAndSort. The predicate handles two cases per D-09:
    - If seriesFilter includes "__unassigned__", include cards where seriesId is null
    - If seriesFilter includes named series IDs, include cards where seriesId matches any of those IDs
    - If both "__unassigned__" and named IDs, union (cards matching either condition)

    Implementation approach: split seriesFilter into hasUnassigned (includes "__unassigned__") and namedIds (filter out "__unassigned__"). Filter cards where: (hasUnassigned && card.seriesId === null) || (namedIds.length > 0 && namedIds.includes(card.seriesId)).

    Add the 6 tests to gallery-utils.test.ts in a new describe("series filter") block. Use createMockGalleryCard with seriesId/seriesName overrides for test data. Ensure each test creates cards with varying seriesId values and verifies the filtered output.
  </action>
  <verify>
    <automated>npm test -- --run src/components/features/gallery/gallery-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - filterAndSort options type includes seriesFilter: string[]
    - Series filter predicate handles "__unassigned__" for null seriesId cards
    - Series filter predicate handles named series IDs for matching cards
    - Union behavior when both "__unassigned__" and named IDs are in filter
    - All 6 new tests pass plus existing tests (no regression)
  </acceptance_criteria>
  <done>filterAndSort filters cards by series with Unassigned support, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire series filter through hook, FilterBar, FilterChips, and ProjectGallery</name>
  <files>
    src/components/features/gallery/use-gallery-filters.ts,
    src/components/features/gallery/use-gallery-filters.test.ts,
    src/components/features/gallery/filter-bar.tsx,
    src/components/features/gallery/filter-bar.test.tsx,
    src/components/features/gallery/filter-chips.tsx,
    src/components/features/gallery/filter-chips.test.tsx,
    src/components/features/gallery/project-gallery.tsx
  </files>
  <read_first>
    src/components/features/gallery/use-gallery-filters.ts,
    src/components/features/gallery/use-gallery-filters.test.ts,
    src/components/features/gallery/filter-bar.tsx,
    src/components/features/gallery/filter-bar.test.tsx,
    src/components/features/gallery/filter-chips.tsx,
    src/components/features/gallery/filter-chips.test.tsx,
    src/components/features/gallery/project-gallery.tsx,
    src/components/features/gallery/multi-select-dropdown.tsx,
    .planning/phases/34-browse-pattern-dive-integration/34-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1 (hook): useGalleryFilters returns seriesFilter (default empty array) and toggleSeries callback
    - Test 2 (hook): clearFilters resets seriesFilter to empty array
    - Test 3 (hook): hasActiveFilters is true when seriesFilter is non-empty
    - Test 4 (hook): seriesFilter is passed to filterAndSort options
    - Test 5 (FilterBar): renders Series MultiSelectDropdown after Size dropdown
    - Test 6 (FilterBar): Series dropdown receives seriesOptions and seriesFilter props
    - Test 7 (FilterChips): renders "Series: {name}" chip for named series filter
    - Test 8 (FilterChips): renders "Series: Unassigned" chip for __unassigned__ filter
    - Test 9 (FilterChips): hasFilters includes seriesFilter check
    - Test 10 (FilterChips): chip aria-label follows "Remove Series: {name} filter" pattern
  </behavior>
  <action>
    Four files to modify in the filter pipeline:

    1. useGalleryFilters (use-gallery-filters.ts):
       - Add seriesFilter URL state: useQueryState("series", parseAsArrayOf(parseAsString, ",").withDefault([]))
       - Add toggleSeries callback following toggleStatus/toggleSize pattern
       - Add setSeriesFilter to clearFilters (void setSeriesFilter([]))
       - Add seriesFilter to filterAndSort call options
       - Add seriesFilter to hasActiveFilters check: || (seriesFilter ?? []).length > 0
       - Add seriesFilter (seriesFilter ?? []) and toggleSeries to return object
       - Compute and return seriesOptions: derive from cards array. Build unique series options from cards that have seriesId, sorted alphabetically by name. Prepend { value: "__unassigned__", label: "Unassigned" } as first option (per D-09). Return as seriesOptions: { value: string; label: string }[].

    2. FilterBar (filter-bar.tsx):
       - Add to FilterBarProps: seriesFilter: string[], onSeriesToggle: (value: string) => void, seriesOptions: { value: string; label: string }[]
       - Add 3rd MultiSelectDropdown after Size: label="Series", options={seriesOptions}, selected={seriesFilter}, onToggle={onSeriesToggle} (per CP-04, D-08, D-10)

    3. FilterChips (filter-chips.tsx):
       - Add to FilterChipsProps: seriesFilter: string[], seriesNames: Record<string, string>, onRemoveSeries: (value: string) => void
       - Update hasFilters: add || seriesFilter.length > 0
       - Add series chip generation after size chips loop: for each value in seriesFilter, label is "Series: {seriesNames[value]}" for named series or "Series: Unassigned" for "__unassigned__" (per CP-05). ariaLabel follows "Remove Series: {name} filter" pattern.
       - Add seriesFilter and seriesNames to chips useMemo dependency array

    4. ProjectGallery (project-gallery.tsx):
       - Destructure seriesFilter, toggleSeries, seriesOptions from useGalleryFilters
       - Compute seriesNames: Record<string, string> from seriesOptions (value -> label mapping) for FilterChips
       - Pass seriesFilter, onSeriesToggle={toggleSeries}, seriesOptions to FilterBar
       - Pass seriesFilter, seriesNames, onRemoveSeries={toggleSeries} to FilterChips

    Update existing tests:
    - filter-bar.test.tsx: add seriesFilter/onSeriesToggle/seriesOptions to defaultProps, add test for Series dropdown rendering
    - filter-chips.test.tsx: add seriesFilter/seriesNames/onRemoveSeries to defaultProps, add tests for series chip rendering
    - use-gallery-filters.test.ts: add tests for seriesFilter state, clearFilters, hasActiveFilters
  </action>
  <verify>
    <automated>npm test -- --run src/components/features/gallery/use-gallery-filters.test.ts src/components/features/gallery/filter-bar.test.tsx src/components/features/gallery/filter-chips.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - useGalleryFilters returns seriesFilter, toggleSeries, and seriesOptions
    - seriesFilter URL state key is "series" with comma-separated parsing
    - clearFilters resets seriesFilter to []
    - hasActiveFilters includes seriesFilter check
    - seriesOptions includes "Unassigned" as first option plus unique series from cards sorted alphabetically
    - FilterBar renders 3 MultiSelectDropdowns: Status, Size, Series (in that order)
    - FilterChips generates "Series: {name}" chips with correct aria-labels
    - FilterChips hasFilters includes seriesFilter
    - ProjectGallery passes all series props through to FilterBar and FilterChips
    - All tests pass, npm run build exits 0
  </acceptance_criteria>
  <done>Browse tab has a fully functional series filter with URL persistence, filter chips, and clear-all support</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params -> client state | seriesFilter parsed from URL via nuqs; used client-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Spoofing | seriesFilter URL manipulation | accept | Client-side filter only; invalid IDs simply match no cards. No server queries with user-provided series IDs. |
| T-34-06 | Information Disclosure | Series names in URL | accept | User's own data; URL not shared externally by default |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- npm test -- --run src/components/features/gallery/gallery-utils.test.ts passes
- npm test -- --run src/components/features/gallery/use-gallery-filters.test.ts passes
- npm test -- --run src/components/features/gallery/filter-bar.test.tsx passes
- npm test -- --run src/components/features/gallery/filter-chips.test.tsx passes
- npm run build exits 0
</verification>

<success_criteria>
- Series MultiSelectDropdown appears after Size in the filter bar
- "Unassigned" option filters for charts with no series (seriesId null)
- Named series options filter for matching charts
- Filter state persists in URL as ?series=id1,id2
- Filter chips show "Series: {name}" with remove buttons
- "Clear all" clears series filter along with other filters
- All tests pass, build succeeds
</success_criteria>

<output>
Create `.planning/phases/34-browse-pattern-dive-integration/34-03-SUMMARY.md` when done
</output>
