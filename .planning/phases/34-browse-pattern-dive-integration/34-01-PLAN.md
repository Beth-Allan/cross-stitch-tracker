---
phase: 34-browse-pattern-dive-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/series/series-card.tsx
  - src/components/features/series/series-card.test.tsx
  - src/components/features/series/series-list.tsx
  - src/components/features/gallery/gallery-types.ts
  - src/components/features/gallery/gallery-utils.ts
  - src/components/features/gallery/gallery-utils.test.ts
  - src/types/chart.ts
  - src/lib/actions/chart-actions.ts
  - src/__tests__/mocks/factories.ts
autonomous: true
requirements:
  - SERIES-08
  - SERIES-09

must_haves:
  truths:
    - "SeriesCard is a standalone component importable from both /series page and Pattern Dive tab (D-04)"
    - "SeriesCard onDelete prop is optional — Pattern Dive omits it, /series page passes it (D-06)"
    - "Gallery chart data includes series relationship from the database (D-11)"
    - "transformToGalleryCard maps series data to seriesId and seriesName fields (D-11)"
  artifacts:
    - path: "src/components/features/series/series-card.tsx"
      provides: "Extracted standalone SeriesCard component"
      exports: ["SeriesCard", "getCompletionPercent"]
    - path: "src/components/features/gallery/gallery-types.ts"
      provides: "GalleryCardData with seriesId and seriesName"
      contains: "seriesId: string | null"
    - path: "src/types/chart.ts"
      provides: "GalleryChartData with series relation"
      contains: "series:"
  key_links:
    - from: "src/components/features/series/series-list.tsx"
      to: "src/components/features/series/series-card.tsx"
      via: "import { SeriesCard }"
      pattern: "import.*SeriesCard.*series-card"
    - from: "src/components/features/gallery/gallery-utils.ts"
      to: "src/components/features/gallery/gallery-types.ts"
      via: "seriesId/seriesName in transformToGalleryCard return"
      pattern: "seriesId.*chart\\.series"
---

<objective>
Extract SeriesCard into a standalone shared component and extend the gallery data layer with series fields.

Purpose: Establish shared foundations that both the Series tab (Plan 02) and Browse tab filter (Plan 03) depend on. SeriesCard extraction enables reuse across the /series list page and Pattern Dive. Type/query extensions enable the Browse tab to filter by series.

Output: Extracted SeriesCard component, extended GalleryCardData with seriesId/seriesName, extended GalleryChartData with series relation, updated Prisma query, updated mock factory, tests for extraction and new filter predicate field.
</objective>

<execution_context>
@/Users/wanderskye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-browse-pattern-dive-integration/34-CONTEXT.md
@.planning/phases/34-browse-pattern-dive-integration/34-PATTERNS.md
@.planning/phases/34-browse-pattern-dive-integration/34-UI-SPEC.md

<interfaces>
From src/types/series.ts:
```typescript
export type SeriesWithStats = {
  id: string;
  name: string;
  totalCount: number | null;
  designerId: string | null;
  designerName: string | null;
  notes: string | null;
  progress: SeriesProgress;
};
```

From src/components/features/gallery/gallery-types.ts:
```typescript
export interface GalleryCardData extends OptionalFocalPoint {
  chartId: string;
  // ... existing 26 fields ...
  dateAdded: Date;
}
```

From src/types/chart.ts:
```typescript
export type GalleryChartData = Chart & {
  project: GalleryProjectData | null;
  designer: Designer | null;
  genres: Genre[];
  _count?: { files: number };
};
```

From src/components/features/gallery/gallery-utils.ts:
```typescript
export function transformToGalleryCard(
  chart: GalleryChartData,
  imageUrls: Record<string, string>,
): GalleryCardData { ... }
```

From src/__tests__/mocks/factories.ts:
```typescript
export function createMockGalleryCard(overrides?: Partial<GalleryCardData>): GalleryCardData
export function createMockSeriesWithStats(overrides?: Partial<SeriesWithStats>): SeriesWithStats
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract SeriesCard component and update series-list</name>
  <files>
    src/components/features/series/series-card.tsx,
    src/components/features/series/series-card.test.tsx,
    src/components/features/series/series-list.tsx
  </files>
  <read_first>
    src/components/features/series/series-list.tsx,
    src/types/series.ts,
    src/components/ui/empty-state.tsx,
    .planning/phases/34-browse-pattern-dive-integration/34-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: SeriesCard renders series name and designer name ("by {designer}")
    - Test 2: SeriesCard renders progress bar with correct width percentage (finishedCount/ownedCount * 100)
    - Test 3: SeriesCard renders stats row with "X of Y finished" when totalCount is set
    - Test 4: SeriesCard renders stats row with "X finished . Y charts" when totalCount is null (open-ended)
    - Test 5: SeriesCard renders as a Link to /series/{id}
    - Test 6: SeriesCard renders delete button when onDelete is provided
    - Test 7: SeriesCard does NOT render delete button when onDelete is omitted
    - Test 8: SeriesCard delete button calls onDelete and stops event propagation
    - Test 9: getCompletionPercent returns 0 when ownedCount is 0
    - Test 10: getCompletionPercent returns correct rounded percentage
  </behavior>
  <action>
    Extract the inline SeriesCard function (lines 165-217) and getCompletionPercent helper (lines 18-20) from series-list.tsx into a new series-card.tsx file. Make onDelete optional — when undefined, hide the delete button with conditional rendering. Export both SeriesCard and getCompletionPercent.

    Update series-list.tsx to import SeriesCard from the new file. Remove the inline SeriesCard function and getCompletionPercent. Keep all other series-list.tsx functionality (sort state, create modal, delete confirmation, sort pills) unchanged.

    SeriesCard props: series (SeriesWithStats, required), onDelete (() => void, optional). Mark as "use client" since it uses Link and event handlers (onClick stopPropagation).

    Per D-06: Pattern Dive tab will pass no onDelete, /series page will pass onDelete.
  </action>
  <verify>
    <automated>npm test -- --run src/components/features/series/series-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/series/series-card.tsx exports SeriesCard and getCompletionPercent
    - SeriesCard accepts { series: SeriesWithStats; onDelete?: () => void }
    - series-list.tsx imports SeriesCard from "./series-card" (no inline definition)
    - Delete button renders only when onDelete prop is provided
    - npm test -- --run src/components/features/series/series-list.test.tsx still passes (no regression)
    - npm test -- --run src/components/features/series/series-card.test.tsx passes with 10+ tests
  </acceptance_criteria>
  <done>SeriesCard is a standalone shared component with optional delete, series-list.tsx imports it, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend gallery data layer with series fields</name>
  <files>
    src/components/features/gallery/gallery-types.ts,
    src/components/features/gallery/gallery-utils.ts,
    src/components/features/gallery/gallery-utils.test.ts,
    src/types/chart.ts,
    src/lib/actions/chart-actions.ts,
    src/__tests__/mocks/factories.ts
  </files>
  <read_first>
    src/components/features/gallery/gallery-types.ts,
    src/components/features/gallery/gallery-utils.ts,
    src/components/features/gallery/gallery-utils.test.ts,
    src/types/chart.ts,
    src/lib/actions/chart-actions.ts,
    src/__tests__/mocks/factories.ts,
    .planning/phases/34-browse-pattern-dive-integration/34-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: transformToGalleryCard maps series.id to seriesId when chart has series
    - Test 2: transformToGalleryCard maps series.name to seriesName when chart has series
    - Test 3: transformToGalleryCard sets seriesId to null when chart has no series
    - Test 4: transformToGalleryCard sets seriesName to null when chart has no series
    - Test 5: createMockGalleryCard includes seriesId and seriesName with null defaults
    - Test 6: createMockGalleryCard accepts seriesId/seriesName overrides
  </behavior>
  <action>
    Four changes across the data pipeline, per D-11:

    1. GalleryCardData (gallery-types.ts): Add seriesId: string | null and seriesName: string | null to the interface.

    2. GalleryChartData (chart.ts): Add series: { id: string; name: string } | null to the intersection type.

    3. getChartsForGallery (chart-actions.ts): Add series: { select: { id: true, name: true } } to the Prisma include block alongside existing designer, genres, _count includes.

    4. transformToGalleryCard (gallery-utils.ts): Add two fields to the return object: seriesId: chart.series?.id ?? null and seriesName: chart.series?.name ?? null.

    5. createMockGalleryCard (factories.ts): Add seriesId: null and seriesName: null as defaults in the return object.

    Add tests to gallery-utils.test.ts in a new describe block for the series field mapping. Use createMockGalleryCard to verify the defaults. For transformToGalleryCard tests, construct a mock GalleryChartData with series: { id: "s1", name: "Test Series" } and verify mapping, then test with series: null for the null case.
  </action>
  <verify>
    <automated>npm test -- --run src/components/features/gallery/gallery-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - GalleryCardData interface includes seriesId: string | null and seriesName: string | null
    - GalleryChartData type includes series: { id: string; name: string } | null
    - getChartsForGallery Prisma include contains series: { select: { id: true, name: true } }
    - transformToGalleryCard return object includes seriesId and seriesName mapped from chart.series
    - createMockGalleryCard defaults include seriesId: null and seriesName: null
    - npm test -- --run src/components/features/gallery/gallery-utils.test.ts passes
    - npm run build exits 0 (type-checks pass with new fields)
  </acceptance_criteria>
  <done>Gallery data pipeline carries series data from Prisma query through to GalleryCardData, mock factory updated, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server action | getChartsForGallery is already auth-gated via requireAuth() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | getChartsForGallery series include | accept | Query already scoped to userId via where clause; series data is user-owned |
| T-34-02 | Tampering | SeriesCard Link href | accept | Client-side navigation only; series detail page has its own auth guard |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- npm test -- --run src/components/features/series/series-card.test.tsx passes
- npm test -- --run src/components/features/gallery/gallery-utils.test.ts passes
- npm test -- --run src/components/features/series/series-list.test.tsx passes (no regression)
- npm run build exits 0
</verification>

<success_criteria>
- SeriesCard extracted as standalone component with optional onDelete
- series-list.tsx imports SeriesCard (no inline definition)
- GalleryCardData has seriesId and seriesName fields
- GalleryChartData has series relation type
- getChartsForGallery query includes series
- transformToGalleryCard maps series data correctly
- Mock factory updated with null defaults
- All tests pass, build succeeds
</success_criteria>

<output>
Create `.planning/phases/34-browse-pattern-dive-integration/34-01-SUMMARY.md` when done
</output>
