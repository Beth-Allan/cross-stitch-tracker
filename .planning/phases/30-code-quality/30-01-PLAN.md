---
phase: 30-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/lib/utils/status.ts
  - src/components/features/charts/status-badge.tsx
  - src/components/features/gallery/gallery-card.tsx
  - src/components/features/charts/whats-next-tab.tsx
autonomous: true
requirements: [QUAL-04]

must_haves:
  truths:
    - "Status colors are defined once in CSS and automatically handle light/dark mode"
    - "STATUS_CONFIG references CSS variables instead of raw Tailwind color scales"
    - "Gallery card completion bar uses status colors from STATUS_CONFIG, not hardcoded rose/violet"
    - "Whats Next kitting elements use status colors from STATUS_CONFIG, not hardcoded amber"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Status color CSS custom properties for bg, dot, and text variants"
      contains: "--status-unstarted-bg"
    - path: "src/lib/utils/status.ts"
      provides: "STATUS_CONFIG using CSS variable references"
      contains: "var(--status-"
  key_links:
    - from: "src/lib/utils/status.ts"
      to: "src/app/globals.css"
      via: "CSS variable references"
      pattern: "var\\(--status-"
    - from: "src/components/features/charts/status-badge.tsx"
      to: "src/lib/utils/status.ts"
      via: "STATUS_CONFIG import"
      pattern: "STATUS_CONFIG"
---

<objective>
Centralize all 7 status colors as CSS custom properties with light/dark variants, replacing scattered Tailwind color scales across STATUS_CONFIG and its consumers.

Purpose: Single-source status colors so adding/changing a status color requires editing one CSS file instead of hunting across 5+ component files with `dark:` overrides.

Output: CSS custom properties in globals.css, updated STATUS_CONFIG, and hardcoded color replacements in gallery-card and whats-next-tab.
</objective>

<execution_context>
@/Users/wanderskye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-code-quality/30-CONTEXT.md
@.planning/phases/30-code-quality/30-PATTERNS.md

<interfaces>
<!-- STATUS_CONFIG type and current structure -->
From src/lib/utils/status.ts:
```typescript
export const STATUS_CONFIG: Record<
  ProjectStatus,
  {
    label: string;
    cssVar: string;
    bgClass: string;
    textClass: string;
    dotClass: string;
    darkBgClass: string;
  }
>
```

<!-- Current CSS variable pattern in globals.css (lines 148-155) -->
Existing `--status-{name}` variables define dot/accent colors only.
New variables needed: `--status-{name}-bg`, `--status-{name}-dot`, `--status-{name}-text`.

<!-- STATUS_CONFIG consumers using color fields -->
status-badge.tsx: uses bgClass, textClass, dotClass, darkBgClass
status-control.tsx: uses dotClass
hero-status-badge.tsx: uses dotClass, textClass
gallery-grid.tsx: uses dotClass
status-group.tsx (shopping): uses dotClass
gallery-card.tsx: hardcoded rose/violet (not via STATUS_CONFIG)
whats-next-tab.tsx: hardcoded amber (not via STATUS_CONFIG)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define CSS custom properties and update STATUS_CONFIG</name>
  <files>src/app/globals.css, src/lib/utils/status.ts, src/lib/utils/status.test.ts</files>
  <read_first>
    - src/app/globals.css (full file -- see existing :root status vars at lines 148-155, .dark section starts at line 161)
    - src/lib/utils/status.ts (full file -- current STATUS_CONFIG structure, 72 lines)
    - .planning/phases/30-code-quality/30-PATTERNS.md (globals.css pattern section for oklch values)
  </read_first>
  <behavior>
    - Test 1: STATUS_CONFIG[status].bgClass contains "var(--status-" for every ProjectStatus
    - Test 2: STATUS_CONFIG[status].dotClass contains "var(--status-" for every ProjectStatus
    - Test 3: STATUS_CONFIG[status].textClass contains "var(--status-" for every ProjectStatus
    - Test 4: No STATUS_CONFIG entry contains "dark:" prefix in any class field
    - Test 5: STATUS_CONFIG type no longer includes darkBgClass field
  </behavior>
  <action>
    Per D-01: Add CSS custom properties to globals.css `:root` selector for all 7 statuses with three variants each: `--status-{name}-bg`, `--status-{name}-dot`, `--status-{name}-text`. Place immediately after existing `--status-{name}` variables (line 155). Use oklch color space.

    Mapping (light mode, :root):
    - UNSTARTED: bg = slate-50, dot = slate-500 (existing --status-unstarted), text = slate-700
    - KITTING: bg = amber-50, dot = amber-500 (existing --status-kitting), text = amber-700
    - KITTED: bg = emerald-50, dot = emerald-500, text = emerald-700
    - IN_PROGRESS: bg = sky-50, dot = sky-500 (existing --status-in-progress), text = sky-700
    - ON_HOLD: bg = orange-50, dot = orange-400, text = orange-700
    - FINISHED: bg = violet-50, dot = violet-500, text = violet-700
    - FFO: bg = rose-50, dot = rose-500, text = rose-700

    Add matching dark mode variables in `.dark` selector (add new "Status Colors" section before the closing brace):
    - bg = {color}-950/40 (opacity variant), dot = {color}-400 (lighter for dark), text = {color}-300

    Dot colors can reference existing `--status-{name}` vars in light mode. For dark mode, define separate lighter dot values.

    Per D-02: Update STATUS_CONFIG in status.ts to use `bg-[var(--status-{name}-bg)]` syntax for bgClass, `bg-[var(--status-{name}-dot)]` for dotClass, `text-[var(--status-{name}-text)]` for textClass.

    Remove the `darkBgClass` field from the type definition and all entries. Since CSS handles dark mode via the `.dark` selector, no `dark:` classes needed. Update the Record type to remove `darkBgClass` from the value type.

    Keep `cssVar` field as-is (used for inline style references elsewhere).
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils/status.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - globals.css :root contains `--status-unstarted-bg`, `--status-kitting-bg`, `--status-kitted-bg`, `--status-in-progress-bg`, `--status-on-hold-bg`, `--status-finished-bg`, `--status-ffo-bg` (and -dot, -text variants)
    - globals.css .dark contains matching dark-mode status variables (21 total: 7 statuses x 3 variants)
    - STATUS_CONFIG type definition has exactly: label, cssVar, bgClass, textClass, dotClass (no darkBgClass)
    - Every STATUS_CONFIG entry bgClass matches pattern `bg-[var(--status-*-bg)]`
    - Every STATUS_CONFIG entry dotClass matches pattern `bg-[var(--status-*-dot)]`
    - Every STATUS_CONFIG entry textClass matches pattern `text-[var(--status-*-text)]`
    - Zero occurrences of `dark:` in status.ts
    - `npm run build` passes (no type errors from removed darkBgClass)
  </acceptance_criteria>
  <done>STATUS_CONFIG uses CSS variables for all color fields, globals.css defines light+dark variants for all 7 statuses, darkBgClass removed from type and all consumers</done>
</task>

<task type="auto">
  <name>Task 2: Update status color consumers with hardcoded colors</name>
  <files>src/components/features/charts/status-badge.tsx, src/components/features/gallery/gallery-card.tsx, src/components/features/charts/whats-next-tab.tsx</files>
  <read_first>
    - src/components/features/charts/status-badge.tsx (full file -- uses bgClass, textClass, dotClass, darkBgClass at lines 10-31)
    - src/components/features/gallery/gallery-card.tsx (lines 100-135 -- hardcoded rose/violet for completion bar at lines 126, 132)
    - src/components/features/charts/whats-next-tab.tsx (lines 125-165 -- hardcoded amber at lines 130, 162)
    - src/lib/utils/status.ts (updated from Task 1 -- new type without darkBgClass)
  </read_first>
  <action>
    Per D-03: Update all status color consumers that have hardcoded Tailwind color classes.

    status-badge.tsx: Remove `config.darkBgClass` from the className cn() call (field no longer exists). The bgClass/textClass/dotClass fields now handle dark mode automatically via CSS variables.

    gallery-card.tsx: Replace hardcoded completion bar colors (lines 126, 132):
    - The isFFO ternary for background (`bg-rose-500 dark:bg-rose-400` vs `bg-violet-500 dark:bg-violet-400`) should import STATUS_CONFIG and use `STATUS_CONFIG[card.status].dotClass` (the dot color matches the completion bar fill color).
    - The isFFO ternary for text (`text-rose-600 dark:text-rose-400` vs `text-violet-600 dark:text-violet-400`) should use `STATUS_CONFIG[card.status].textClass`.
    - Add `import { STATUS_CONFIG } from "@/lib/utils/status"` if not already imported.

    whats-next-tab.tsx: Replace hardcoded kitting colors (lines 130, 162):
    - Star icon color `text-amber-500` (line 130) should use `text-[var(--status-kitting)]` referencing the existing `--status-kitting` CSS variable (the dot/accent color for kitting status).
    - Kitting bar color `bg-amber-400` (line 162) should use `bg-[var(--status-kitting-dot)]` from the new CSS variable set.
    - These are not generic status lookups (always kitting), so direct CSS variable references are cleaner than importing STATUS_CONFIG.

    Also check and fix any other files that consume `darkBgClass`:
    - grep for `darkBgClass` across the codebase; update any remaining references.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - status-badge.tsx: zero occurrences of `darkBgClass`
    - gallery-card.tsx: zero occurrences of `bg-rose-`, `bg-violet-`, `text-rose-`, `text-violet-`; imports STATUS_CONFIG
    - whats-next-tab.tsx: zero occurrences of `text-amber-500`, `bg-amber-400`; uses `var(--status-kitting` references
    - `grep -r 'darkBgClass' src/` returns zero results
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All status color consumers use CSS variables (via STATUS_CONFIG or direct var() references), no hardcoded Tailwind color scales for status colors remain, darkBgClass fully removed from codebase</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure CSS + client-side refactoring, no user input crosses trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | CSS custom properties | accept | CSS variables are client-rendered only; no security implication from color value changes |
</threat_model>

<verification>
- `npm run build` exits 0 (no TypeScript errors from removed darkBgClass)
- `npx vitest run src/lib/utils/ --reporter=verbose` passes
- `grep -r 'darkBgClass' src/` returns 0 results
- `grep -rn 'bg-rose-\|bg-violet-\|text-rose-\|text-violet-' src/components/features/gallery/gallery-card.tsx` returns 0 results
- `grep -rn 'text-amber-500\|bg-amber-400' src/components/features/charts/whats-next-tab.tsx` returns 0 results
</verification>

<success_criteria>
All 7 status colors defined as CSS custom properties with light/dark variants. STATUS_CONFIG uses CSS variable references. No hardcoded status Tailwind color scales in gallery-card, whats-next-tab, or status-badge. darkBgClass field eliminated. Build passes.
</success_criteria>

<output>
Create `.planning/phases/30-code-quality/30-01-SUMMARY.md` when done
</output>
