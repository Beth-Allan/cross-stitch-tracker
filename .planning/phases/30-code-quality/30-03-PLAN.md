---
phase: 30-code-quality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants.ts
  - src/lib/constants.test.ts
  - src/lib/validations/supply.ts
  - src/lib/actions/supply-actions.ts
  - src/components/features/charts/chart-merged-form.tsx
  - src/components/features/supply-table/inline-create-dialog.tsx
  - src/components/features/supply-table/local-state-adapter.ts
  - src/components/hooks/use-rejection-flash.ts
  - src/components/features/charts/editable-number.tsx
  - src/components/features/supply-table/editable-number.tsx
  - src/components/features/supply-table/supply-table.test.tsx
  - src/components/features/supply-table/inline-create-dialog.test.tsx
  - src/components/features/supply-table/server-action-adapter.test.ts
  - src/components/features/charts/chart-merged-form.test.tsx
autonomous: true
requirements: [QUAL-05, QUAL-06]

must_haves:
  truths:
    - "DEFAULT_SUPPLY_HEX is defined in one place and imported everywhere it was previously hardcoded"
    - "useRejectionFlash hook exists as a shared hook and both EditableNumber components use it"
    - "No literal '#79796e' strings remain in source files (only in the constants.ts definition)"
  artifacts:
    - path: "src/lib/constants.ts"
      provides: "Single-sourced DEFAULT_SUPPLY_HEX constant"
      contains: "DEFAULT_SUPPLY_HEX"
    - path: "src/lib/constants.test.ts"
      provides: "Tests for shared constants"
      contains: "DEFAULT_SUPPLY_HEX"
    - path: "src/components/hooks/use-rejection-flash.ts"
      provides: "Shared rejection flash hook"
      contains: "useRejectionFlash"
  key_links:
    - from: "src/lib/validations/supply.ts"
      to: "src/lib/constants.ts"
      via: "DEFAULT_SUPPLY_HEX import"
      pattern: "import.*DEFAULT_SUPPLY_HEX.*constants"
    - from: "src/components/features/charts/editable-number.tsx"
      to: "src/components/hooks/use-rejection-flash.ts"
      via: "useRejectionFlash import"
      pattern: "import.*useRejectionFlash"
    - from: "src/components/features/supply-table/editable-number.tsx"
      to: "src/components/hooks/use-rejection-flash.ts"
      via: "useRejectionFlash import"
      pattern: "import.*useRejectionFlash"
---

<objective>
Extract DEFAULT_SUPPLY_HEX to a shared constant and useRejectionFlash to a shared hook, replacing all scattered duplicates.

Purpose: Prevent value drift from 16+ copies of the hex literal across 7 files, and eliminate ~16 duplicated lines of rejection flash logic across 2 EditableNumber components.

Output: New constants.ts file with tests, new use-rejection-flash.ts hook, all consumers updated to import from shared locations.
</objective>

<execution_context>
@/Users/wanderskye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-code-quality/30-CONTEXT.md
@.planning/phases/30-code-quality/30-PATTERNS.md

<interfaces>
<!-- Constant import pattern (from upload.ts) -->
```typescript
export const ALLOWED_IMAGE_TYPES = [...] as const;
export const OPTIMIZED_MAX_WIDTH = 1600;
```

<!-- Hook convention (from existing hooks) -->
```typescript
"use client";
import { useState, useEffect, useRef } from "react";
// Named export, options interface with defaults
export function useRejectionFlash({ duration = 600 } = {}) { ... }
```

<!-- Files containing "#79796e" -->
Source files: supply.ts:109, supply-actions.ts:778,830, chart-merged-form.tsx:71,90,108,
             inline-create-dialog.tsx:93, local-state-adapter.ts:151
Test files: supply-table.test.tsx:165, inline-create-dialog.test.tsx:87,
            server-action-adapter.test.ts:511,541, chart-merged-form.test.tsx:1013,1035,1054,1075

<!-- Rejection flash pattern (charts/editable-number.tsx lines 28-36, 60-63) -->
State: useState(false) + useRef<ReturnType<typeof setTimeout>>
Cleanup: useEffect return clearing timeout
Trigger: setShowRejection(true), clear old timer, setTimeout to reset after 600ms
Consumer: showRejection boolean drives CSS class (border-destructive, bg-destructive/10)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract DEFAULT_SUPPLY_HEX constant</name>
  <files>src/lib/constants.ts, src/lib/constants.test.ts, src/lib/validations/supply.ts, src/lib/actions/supply-actions.ts, src/components/features/charts/chart-merged-form.tsx, src/components/features/supply-table/inline-create-dialog.tsx, src/components/features/supply-table/local-state-adapter.ts, src/components/features/supply-table/supply-table.test.tsx, src/components/features/supply-table/inline-create-dialog.test.tsx, src/components/features/supply-table/server-action-adapter.test.ts, src/components/features/charts/chart-merged-form.test.tsx</files>
  <read_first>
    - src/lib/validations/supply.ts (line 105-115 -- .default("#79796e") usage)
    - src/lib/actions/supply-actions.ts (lines 775-835 -- hexColor fallback at lines 778, 830)
    - src/components/features/charts/chart-merged-form.tsx (lines 65-115 -- data.hexColor ?? "#79796e" at lines 71, 90, 108)
    - src/components/features/supply-table/inline-create-dialog.tsx (line 90-95 -- hexColor assignment)
    - src/components/features/supply-table/local-state-adapter.ts (line 148-155 -- hexColor fallback)
    - .planning/phases/30-code-quality/30-PATTERNS.md (Constant Import Pattern section, src/lib/constants.ts section)
  </read_first>
  <behavior>
    - Test 1: DEFAULT_SUPPLY_HEX equals "#79796e"
    - Test 2: DEFAULT_SUPPLY_HEX is a 7-character hex string matching /^#[0-9a-f]{6}$/i
    - Test 3: DEFAULT_SUPPLY_HEX is exported (import resolves without error)
  </behavior>
  <action>
    Write tests first in `src/lib/constants.test.ts`:
    - Import `DEFAULT_SUPPLY_HEX` from `@/lib/constants`
    - Test that the value equals `"#79796e"` (exact value preservation)
    - Test that it matches hex color format /^#[0-9a-f]{6}$/i
    - Test that it is a string type (not undefined/null)
    Run tests -- they should fail (RED) because constants.ts does not exist yet.

    Then create `src/lib/constants.ts` per D-13 as a new file with a JSDoc-annotated export:
    - Export `DEFAULT_SUPPLY_HEX` with value `"#79796e"`
    - Add JSDoc: "Default hex color for supplies without an explicit color assignment."
    - Follow the export pattern from `src/lib/validations/upload.ts` (named exports, no default export)
    Run tests again -- they should pass (GREEN).

    Update all source files to import and use the constant:
    - `supply.ts:109`: `.default("#79796e")` -> `.default(DEFAULT_SUPPLY_HEX)` with `import { DEFAULT_SUPPLY_HEX } from "@/lib/constants"`
    - `supply-actions.ts:778,830`: `hexColor: "#79796e"` -> `hexColor: DEFAULT_SUPPLY_HEX`
    - `chart-merged-form.tsx:71,90,108`: `data.hexColor ?? "#79796e"` -> `data.hexColor ?? DEFAULT_SUPPLY_HEX`
    - `inline-create-dialog.tsx:93`: `hexColor: "#79796e"` -> `hexColor: DEFAULT_SUPPLY_HEX`
    - `local-state-adapter.ts:151`: `data.hexColor ?? "#79796e"` -> `data.hexColor ?? DEFAULT_SUPPLY_HEX`

    Update test files to also import the constant where they assert the value:
    - `supply-table.test.tsx:165`, `inline-create-dialog.test.tsx:87`, `server-action-adapter.test.ts:511,541`, `chart-merged-form.test.tsx:1013,1035,1054,1075`
    - Replace `"#79796e"` with `DEFAULT_SUPPLY_HEX` in test expectations
  </action>
  <verify>
    <automated>npx vitest run src/lib/constants.test.ts src/components/features/supply-table/supply-table.test.tsx src/components/features/supply-table/inline-create-dialog.test.tsx src/components/features/charts/chart-merged-form.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/constants.ts exists with `export const DEFAULT_SUPPLY_HEX = "#79796e"`
    - src/lib/constants.test.ts exists with 3 passing tests for the constant
    - `grep -rn '"#79796e"' src/lib/ src/components/ --include='*.ts' --include='*.tsx' | grep -v constants.ts | grep -v '.test.'` returns 0 results (no hardcoded literals remain in source files)
    - All 7 source files import `DEFAULT_SUPPLY_HEX` from `@/lib/constants`
    - Test files either import the constant or the literal is acceptable in test assertions (prefer importing)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>DEFAULT_SUPPLY_HEX defined once in constants.ts with tests, all 16 occurrences across 7 source files and 4 test files updated to import from the shared location</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract useRejectionFlash hook</name>
  <files>src/components/hooks/use-rejection-flash.ts, src/components/hooks/use-rejection-flash.test.ts, src/components/features/charts/editable-number.tsx, src/components/features/supply-table/editable-number.tsx</files>
  <read_first>
    - src/components/features/charts/editable-number.tsx (full file -- rejection flash state at lines 28-36, trigger at lines 60-63, cleanup effect, showRejection usage in className)
    - src/components/features/supply-table/editable-number.tsx (full file -- duplicate pattern, identify exact lines for state, effect, trigger, and className usage)
    - .planning/phases/30-code-quality/30-PATTERNS.md (use-rejection-flash.ts section for hook shape and consumer integration)
  </read_first>
  <behavior>
    - Test 1: useRejectionFlash returns { showRejection: false, triggerRejection: fn } initially
    - Test 2: After triggerRejection(), showRejection becomes true
    - Test 3: After duration (default 600ms), showRejection resets to false
    - Test 4: Calling triggerRejection() while active resets the timer (extends duration)
    - Test 5: Cleanup on unmount clears pending timeout (no act() warnings)
  </behavior>
  <action>
    Per D-14: Create the shared hooks directory `src/components/hooks/` and the hook file `use-rejection-flash.ts`.

    Hook implementation per the shape in PATTERNS.md:
    - `"use client"` directive (uses React hooks)
    - Interface `UseRejectionFlashOptions` with optional `duration` (default 600)
    - Function `useRejectionFlash` accepting options, returning `{ showRejection: boolean, triggerRejection: () => void }`
    - Internal: useState for showRejection, useRef for timer, useEffect for cleanup
    - `triggerRejection`: sets true, clears existing timer, starts new timeout

    Update both consumer components:

    charts/editable-number.tsx:
    - Remove: useState for showRejection, useRef for rejectionTimerRef, useEffect cleanup for timer
    - Remove: inline triggerRejection logic from the onBlur handler (the setShowRejection(true), clearTimeout, setTimeout block)
    - Add: `import { useRejectionFlash } from "@/components/hooks/use-rejection-flash"`
    - Add: `const { showRejection, triggerRejection } = useRejectionFlash()` at the top of the component
    - Keep: the showRejection usage in className (border-destructive, bg-destructive/10) unchanged
    - Replace: the inline trigger logic in onBlur with just `triggerRejection()`

    supply-table/editable-number.tsx:
    - Same extraction pattern as above. Remove duplicated state/ref/effect/trigger logic, replace with hook import and destructured call.
    - Keep: showRejection className usage unchanged
    - Check: if this component uses a different duration than 600ms (from PATTERNS.md it uses 600ms too)
  </action>
  <verify>
    <automated>npx vitest run src/components/hooks/use-rejection-flash.test.ts --reporter=verbose 2>&1 | tail -15 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/hooks/use-rejection-flash.ts exists with `"use client"` directive and named export `useRejectionFlash`
    - src/components/hooks/use-rejection-flash.test.ts exists and passes
    - charts/editable-number.tsx: contains `import { useRejectionFlash }` and `useRejectionFlash()` call; does NOT contain `rejectionTimerRef` or `setShowRejection`
    - supply-table/editable-number.tsx: contains `import { useRejectionFlash }` and `useRejectionFlash()` call; does NOT contain `rejectionTimerRef` or `setShowRejection`
    - Both editable-number components still use `showRejection` in their className for visual feedback
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>useRejectionFlash hook shared from src/components/hooks/, both EditableNumber components consume it, ~16 lines of duplicated logic eliminated per component</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure refactoring -- no new user input paths, no auth changes, no data flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Tampering | DEFAULT_SUPPLY_HEX | accept | Cosmetic default only (fallback color for supplies without color); no security implication |
</threat_model>

<verification>
- `npm run build` exits 0
- `npx vitest run src/lib/constants.test.ts src/components/hooks/use-rejection-flash.test.ts` passes
- `npx vitest run src/components/features/supply-table/ src/components/features/charts/chart-merged-form.test.tsx` passes (existing tests still pass with constant import)
- `grep -rn '"#79796e"' src/lib/ src/components/ --include='*.ts' --include='*.tsx' | grep -v constants.ts | grep -v '.test.'` returns 0
- `grep -c 'rejectionTimerRef' src/components/features/charts/editable-number.tsx` returns 0
- `grep -c 'rejectionTimerRef' src/components/features/supply-table/editable-number.tsx` returns 0
</verification>

<success_criteria>
DEFAULT_SUPPLY_HEX single-sourced in constants.ts with tests. useRejectionFlash hook extracted and shared, both EditableNumber components simplified. All tests pass. Build clean.
</success_criteria>

<output>
Create `.planning/phases/30-code-quality/30-03-SUMMARY.md` when done
</output>
