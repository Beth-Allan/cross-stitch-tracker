---
phase: 30-code-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/upload-actions.ts
  - src/app/(dashboard)/charts/[id]/page.tsx
  - src/components/features/sessions/log-session-modal.tsx
  - src/lib/actions/session-actions.ts
  - src/lib/actions/chart-actions.ts
  - src/lib/utils/status-groups.test.ts
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "No silent .catch(() => {}) or bare catch {} patterns remain in the 3 QUAL-02 target files"
    - "Old session photos are deleted from R2 when replaced with a new photo"
    - "Old chart cover images are deleted from R2 when replaced with a new cover"
    - "TypeScript compiles status-groups.test.ts without errors"
  artifacts:
    - path: "src/lib/actions/upload-actions.ts"
      provides: "Logged R2 cleanup instead of silent catch"
      contains: "console.warn"
    - path: "src/lib/actions/session-actions.ts"
      provides: "Old photo R2 cleanup on replace"
      contains: "old photo cleanup"
    - path: "src/lib/actions/chart-actions.ts"
      provides: "Old cover image R2 cleanup on replace"
      contains: "old cover cleanup"
  key_links:
    - from: "src/lib/actions/session-actions.ts"
      to: "src/lib/actions/upload-actions.ts"
      via: "deleteFile import"
      pattern: "deleteFile"
    - from: "src/lib/actions/chart-actions.ts"
      to: "src/lib/actions/upload-actions.ts"
      via: "deleteFile import"
      pattern: "deleteFile"
---

<objective>
Fix silent error patterns in 3 target files, add R2 photo orphan cleanup on session/chart photo replacement, and resolve the last TypeScript test error.

Purpose: Eliminate developer-invisible failures (silent catches hide bugs in production), prevent R2 storage leaks from photo replacements, and achieve zero TS errors across the test suite.

Output: Proper error logging in catch blocks, fire-and-forget R2 cleanup for replaced photos/covers, and a clean TypeScript build.
</objective>

<execution_context>
@/Users/wanderskye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-code-quality/30-CONTEXT.md
@.planning/phases/30-code-quality/30-PATTERNS.md

<interfaces>
<!-- Fire-and-forget R2 cleanup pattern (session-actions.ts lines 97-99) -->
```typescript
await deleteFile(session.photoKey).catch((err) =>
  console.warn("[R2] raw file cleanup failed:", session.photoKey, err),
);
```

<!-- deleteFile import path -->
```typescript
import { deleteFile, processAndStoreImage } from "@/lib/actions/upload-actions";
```

<!-- Existing catch-with-logging pattern (session-actions.ts lines 129-134) -->
```typescript
} catch (error) {
  console.error("createSession error:", error);
  return { success: false as const, error: "Failed to create session" };
}
```

<!-- Graceful degradation pattern (chart page lines 55-58) -->
```typescript
chart.project
  ? getUnassignedFabrics(chart.project.id).catch((error) => {
      console.error("Failed to load unassigned fabrics:", error);
      return [];
    })
  : [],
```

<!-- StatusGroup type for test fix -->
```typescript
import type { StatusGroup } from "./status-groups";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix silent error patterns in QUAL-02 targets</name>
  <files>src/lib/actions/upload-actions.ts, src/app/(dashboard)/charts/[id]/page.tsx, src/components/features/sessions/log-session-modal.tsx</files>
  <read_first>
    - src/lib/actions/upload-actions.ts (line 150-160 -- silent .catch(() => {}) at line 155)
    - src/app/(dashboard)/charts/[id]/page.tsx (lines 48-60 -- .catch(() => null) at line 52, reference pattern at lines 55-58)
    - src/components/features/sessions/log-session-modal.tsx (lines 160-170, 228-240, 248-260 -- bare catch {} at lines 166, 234, 254)
    - .planning/phases/30-code-quality/30-PATTERNS.md (Console Error in Catch Blocks section, Fire-and-Forget R2 Cleanup section)
  </read_first>
  <action>
    Per D-06, D-07, D-08, D-09: Fix only the 3 QUAL-02 target files, not the ~20 optimistic UI catch blocks.

    upload-actions.ts line 155: Replace `.catch(() => {})` with `.catch((err) => console.warn("[R2] raw file cleanup failed:", input.key, err))`. This matches the established fire-and-forget pattern from session-actions.ts lines 97-99.

    charts/[id]/page.tsx line 52: Replace `.catch(() => null)` with `.catch((error) => { console.error("Completion estimate failed:", error); return null; })`. Per D-09, keep returning null for graceful degradation. This matches the getUnassignedFabrics pattern at lines 55-58 in the same file.

    log-session-modal.tsx: Add `console.error` to the 3 bare catch blocks per D-07 and D-08. All 3 already have `toast.error()` calls for user feedback. Add the error parameter to each catch and a console.error before the toast:
    - Line 166 catch block (photo upload): `catch (error) { console.error("Photo upload failed:", error); toast.error(...); }`
    - Line 234 catch block (save handler): `catch (error) { console.error("Session save failed:", error); toast.error(...); }`
    - Line 254 catch block (delete handler): `catch (error) { console.error("Session delete failed:", error); toast.error(...); }`
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - upload-actions.ts line 155 region: contains `console.warn("[R2] raw file cleanup failed:"` instead of `.catch(() => {})`
    - charts/[id]/page.tsx: contains `console.error("Completion estimate failed:"` and `return null`
    - log-session-modal.tsx: all 3 catch blocks contain `console.error(` followed by `toast.error(`
    - Zero occurrences of `.catch(() => {})` in upload-actions.ts
    - Zero occurrences of `.catch(() => null)` in charts/[id]/page.tsx
    - Zero bare `catch {` (without error parameter) in log-session-modal.tsx
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 3 QUAL-02 target files have console.error/console.warn in catch blocks, no silent error swallowing remains</done>
</task>

<task type="auto">
  <name>Task 2: Add R2 photo orphan cleanup and fix TS test error</name>
  <files>src/lib/actions/session-actions.ts, src/lib/actions/chart-actions.ts, src/lib/utils/status-groups.test.ts</files>
  <read_first>
    - src/lib/actions/session-actions.ts (lines 185-210 -- updateSession photo replacement flow, lines 97-99 for the R2 cleanup pattern to replicate)
    - src/lib/actions/chart-actions.ts (lines 225-340 -- updateChart flow, line 234 existing select query, lines 324-332 cover image change detection)
    - src/lib/utils/status-groups.test.ts (lines 34-42 -- the two lines with `as StatusGroup[]` that need `as unknown as`)
    - .planning/phases/30-code-quality/30-PATTERNS.md (R2 photo orphan sections for both session-actions and chart-actions)
  </read_first>
  <action>
    Per D-10: In session-actions.ts updateSession, after the raw file cleanup (around line 204), add old photo cleanup. After `deleteFile(session.photoKey)` succeeds (raw file cleanup), add:
    ```
    if (existing.photoKey) {
      await deleteFile(existing.photoKey).catch((err) =>
        console.warn("[R2] old photo cleanup failed:", existing.photoKey, err),
      );
    }
    ```
    This deletes the previous session photo from R2 after the new one is processed and the DB is updated. The guard `if (existing.photoKey)` handles the case where there was no prior photo. Per D-12, log and move on if delete fails.

    Per D-11: In chart-actions.ts updateChart, after successful thumbnail generation (inside the `if (chart.coverImageUrl && chart.coverImageUrl !== existing.coverImageUrl)` block, after `generateThumbnail` succeeds), add old cover image cleanup:
    1. First, update the existing select query (line 234) to also fetch `coverThumbnailUrl`: add `coverThumbnailUrl: true` to the select object.
    2. After the `generateThumbnail` call succeeds (line 327), add cleanup for both old cover and old thumbnail:
    ```
    if (existing.coverImageUrl) {
      await deleteFile(existing.coverImageUrl).catch((err) =>
        console.warn("[R2] old cover cleanup failed:", existing.coverImageUrl, err),
      );
    }
    if (existing.coverThumbnailUrl) {
      await deleteFile(existing.coverThumbnailUrl).catch((err) =>
        console.warn("[R2] old thumbnail cleanup failed:", existing.coverThumbnailUrl, err),
      );
    }
    ```
    Make sure `deleteFile` is imported from `@/lib/actions/upload-actions` in chart-actions.ts (check if already imported, add if missing).

    Per D-15: In status-groups.test.ts, fix the two TypeScript errors:
    - Line 36: Change `as StatusGroup[]` to `as unknown as StatusGroup[]`
    - Line 40: Change `as StatusGroup[]` to `as unknown as StatusGroup[]`
    These are intentionally-invalid test inputs that need the double cast to bypass TypeScript's type narrowing.
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils/status-groups.test.ts --reporter=verbose 2>&1 | tail -10 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - session-actions.ts: contains `"[R2] old photo cleanup failed:"` string in updateSession function, after the raw file cleanup block
    - chart-actions.ts: select query includes `coverThumbnailUrl: true`; contains `"[R2] old cover cleanup failed:"` and `"[R2] old thumbnail cleanup failed:"` strings
    - chart-actions.ts: `deleteFile` imported from `@/lib/actions/upload-actions`
    - status-groups.test.ts: lines 36 and 40 use `as unknown as StatusGroup[]`
    - `npx vitest run src/lib/utils/status-groups.test.ts` exits 0
    - `npm run build` exits 0 (zero TS errors in test files)
  </acceptance_criteria>
  <done>Old photos/covers are deleted from R2 on replacement, status-groups test compiles without TypeScript errors, build passes with zero errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action -> R2 | deleteFile calls cross to external storage; failures are non-critical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Information Disclosure | console.error in catch blocks | accept | Error details logged server-side only (console.error), not exposed to client; toast messages are generic |
| T-30-03 | Denial of Service | deleteFile fire-and-forget | accept | Non-blocking; failure to delete old file does not affect the new upload flow |
</threat_model>

<verification>
- `npm run build` exits 0 (zero TypeScript errors)
- `npx vitest run src/lib/utils/status-groups.test.ts` passes
- `grep -c 'catch () {' src/components/features/sessions/log-session-modal.tsx` returns 0
- `grep -c '.catch(() => {})' src/lib/actions/upload-actions.ts` returns 0
- `grep -c '.catch(() => null)' src/app/(dashboard)/charts/[id]/page.tsx` returns 0
- `grep -c 'old photo cleanup' src/lib/actions/session-actions.ts` returns 1+
- `grep -c 'old cover cleanup' src/lib/actions/chart-actions.ts` returns 1+
</verification>

<success_criteria>
Zero silent error patterns in QUAL-02 targets. Old R2 photos/covers cleaned up on replacement. Status-groups test compiles cleanly. Full build passes with zero TS errors.
</success_criteria>

<output>
Create `.planning/phases/30-code-quality/30-02-SUMMARY.md` when done
</output>
