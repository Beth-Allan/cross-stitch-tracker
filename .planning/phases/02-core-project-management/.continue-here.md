---
context: phase
phase: 02-core-project-management
task: 5
total_tasks: 5
status: in_progress
last_updated: 2026-04-07T23:10:15.608Z
---

<current_state>
**Roadmap restructured 2026-04-07** — from 9 sequential phases to 4 milestones / 10 phases.
MVP (Milestone 1) = Phases 1-4: foundation + CRUD + designer pages + supplies & fabric → deploy.

Phase 2 plans 01-04 complete and merged (PR #2, 73 tests, build clean).
Plan 02-05 is human verification — test full chart lifecycle in browser.

**Key change from previous handoff:** The gallery/detail DesignOS rebuild (previously planned as 02-06) is NO LONGER in Phase 2 scope. Gallery cards and filtering moved to Phase 5 (Milestone 2: Browse & Organize). Phase 2 can close after human verification of existing CRUD.

**Cleanup task identified:** `docs/conventions/` files (auth-patterns, base-ui-patterns, form-patterns, server-client-split) should migrate to `.claude/rules/` with glob patterns for auto-loading. Currently CLAUDE.md has a manual "read first" dispatch table that rules would replace.
</current_state>

<completed_work>

- Plan 02-01: Data Foundation — Done (Prisma schema, R2 client, Zod validations, types)
- Plan 02-02: Server Actions — Done (chart CRUD, file upload, designer/genre creation)
- Plan 02-03: Chart Form UI — Done (rebuilt from scratch, PR #2)
- Plan 02-04: Chart Detail & List Pages — Done (basic versions, functional)
- PR #2 merged: 73 tests, two review passes, all findings fixed
- Roadmap restructure — Done (8 files updated, 40 requirements remapped, build passes)
</completed_work>

<remaining_work>

1. **Commit roadmap restructure** — 8 project files changed but uncommitted (ROADMAP, REQUIREMENTS, PROJECT, STATE, CLAUDE.md, ui-design-reference, DESIGN-REFERENCE, tech-stack)
2. **Cleanup: migrate docs/conventions/ → .claude/rules/** — move 4 files, add glob frontmatter, update CLAUDE.md
3. **Phase 2 human verification (02-05-PLAN.md)** — test full chart CRUD lifecycle in browser
4. **Discuss + plan Phase 3** — Designer & Genre dedicated pages
5. **Discuss + plan Phase 4** — Supplies & Fabric (parallel with Phase 3)
6. **Deploy MVP** — after Phase 4 completes
</remaining_work>

<decisions_made>

Roadmap restructure decisions (2026-04-07):
- MVP = CRUD + supplies + fabric → deploy → iterate based on real usage
- Designer/genre pages in MVP (Phase 3) — user wants proper management, not just inline creation
- Fabric bundled with supplies (Phase 4) — both are "stuff linked to projects"
- Pre-seeded DMC catalog in MVP — search-and-select is the Notion-beater
- Sessions/stats come AFTER browsing/dashboards (collection stats don't need sessions)
- Kitting auto-calc (8 conditions) + SAL support deferred from MVP
- Gallery/detail DesignOS rebuild moved from Phase 2 to Phase 5 (Milestone 2)
- docs/conventions/ should become .claude/rules/ with glob patterns

Prior Phase 2 decisions (still valid):
- Ground-up rebuild for forms over incremental patches
- Quick-add dropdowns for Project Bin and iPad App
- Supplies/Sessions tab content deferred (placeholders OK)
</decisions_made>

<blockers>
- R2 not configured — uploads show graceful error, not blocking
- `.env.local` bcrypt hashes must escape `$` as `\$`
- GSD plugin files also have unstaged changes (separate from roadmap work — don't mix commits)
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md` — new 4-milestone / 10-phase structure
2. `CLAUDE.md` — updated current status and next steps
3. `.planning/REQUIREMENTS.md` — remapped traceability table
4. `.planning/phases/02-core-project-management/02-05-PLAN.md` — human verification checklist

## Infrastructure State
- On `main` branch, build clean, 73 tests passing
- No running services
- R2 not configured (uploads degrade gracefully)
- GSD plugin updated but changes unstaged (separate concern)

<context>
The user decided to pare down the project to a lean MVP. We had a thorough collaborative discussion about scope, priorities, and ordering. The restructure is done — all docs updated and consistent. The user also noticed docs/conventions/ files would work better as .claude/rules/ and wants to clean that up.

The mood is "get organized, verify things are right, then move forward fresh."
</context>

<next_action>
Start fresh conversation:

1. Commit the roadmap restructure (8 changed project files — NOT the GSD plugin files)
2. Migrate `docs/conventions/` → `.claude/rules/` (4 files, add glob frontmatter, update CLAUDE.md)
3. Run `/gsd:execute-phase 2` to complete human verification (02-05)
4. Then `/gsd:discuss-phase 3` and `/gsd:discuss-phase 4` (can be parallel)
</next_action>
