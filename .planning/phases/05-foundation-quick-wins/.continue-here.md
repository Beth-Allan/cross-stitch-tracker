---
context: phase
phase: 05-foundation-quick-wins
task: null
total_tasks: null
status: paused_at_uat
last_updated: 2026-04-13T02:58:34.581Z
---

<current_state>
Phase 5 human UAT is nearly complete. 4/6 tests passed, 1 blocked (cover image — no local file storage), 1 issue remaining (thread picker auto-close). The SearchableSelect "Add New" recurring bug was definitively fixed with InlineNameDialog. Thread picker bug needs investigation.

UAT file: `.planning/phases/05-foundation-quick-wins/05-HUMAN-UAT.md`
</current_state>

<completed_work>

This Session:
- Human UAT tests 1-4: all passed (Storage CRUD, Stitching App, Add New dialog, Fabric selector)
- Test 5 (cover image): blocked — local dev server has no file storage configured
- Test 6 (thread picker): ISSUE — opens for a split second then auto-closes
- Fixed SearchableSelect "Add New" recurring bug (3-4 prior failed attempts):
  - Root cause 1: duplicate "+" prefix (Plus icon + literal "+" in text)
  - Root cause 2: `handleAddStorageLocation("")` had silent `if (!name.trim()) return` guard
  - Fix: Created `InlineNameDialog` component matching designer dialog pattern
  - Wired into `project-setup-section.tsx` — onAddNew opens dialog, dialog calls handler
  - Added 9 handler tests in `use-chart-form.test.tsx` + 8 dialog tests in `inline-name-dialog.test.tsx`
  - Commit: 05c2d07
</completed_work>

<remaining_work>

1. `/gsd-debug` — Thread picker auto-close bug in `search-to-add.tsx`
2. Re-test UAT item 6 (thread picker multi-add UX)
3. `/gsd-code-review-fix 05` — 3 critical code review findings (authz bypass, hardcoded userId)
4. `/gsd:ship 5` — create PR for review
</remaining_work>

<decisions_made>

- Used `InlineNameDialog` (proper Dialog component) instead of `window.prompt()` for storage/app Add New — matches existing `InlineDesignerDialog` pattern for UX consistency
- Reverted handler empty-name guards to simple `if (!name.trim()) return` since dialog now validates before calling handler
- Kept `forceMount` on SearchableSelect CommandItem/CommandGroup to prevent cmdk filtering on names with spaces
</decisions_made>

<blockers>
- Thread picker (SearchToAdd) opens for a split second then auto-closes when clicking "+ Add more"
  - NOT caused by changes in this session (no files in search-to-add.tsx or project-supplies-tab.tsx were modified)
  - Likely pre-existing from 05-08 viewport flip work or earlier
  - Key investigation files: `src/components/features/supplies/search-to-add.tsx` (click-outside handler line 70-78), `src/components/features/charts/project-supplies-tab.tsx` (onClose inline arrow fn)
  - Three hypotheses: (1) mousedown event timing vs component mount, (2) onClose prop reference instability causing effect re-run, (3) flipUp state change re-render interaction with containment check
- Cover image upload can't be tested locally (file storage not configured for dev) — skip in UAT, test on deployed preview
</blockers>

## Required Reading (in order)
1. `.planning/phases/05-foundation-quick-wins/05-HUMAN-UAT.md` — UAT results and gap details
2. `src/components/features/supplies/search-to-add.tsx` — the broken component (click-outside handler)
3. `src/components/features/charts/project-supplies-tab.tsx` — parent component (onClose, addingType state)
4. `.planning/phases/05-foundation-quick-wins/05-REVIEW.md` — 3 critical code review findings still to fix

## Infrastructure State
- Dev server: `npm run dev` (user starts manually)
- Database: Neon PostgreSQL (prisma db push needed after schema changes)
- 525+ tests passing, build clean

<context>
Phase 5 is functionally complete — all 8 plans executed, gap closures done. We're in the human UAT verification phase before shipping. The thread picker bug is the last UAT blocker. After that: code review fixes (3 critical), then ship PR. The thread picker bug should be investigated with /gsd-debug for proper root cause analysis — don't just patch it.
</context>

<next_action>
Start with: `/gsd-debug Thread picker auto-close — SearchToAdd opens for a split second then closes when clicking + Add more. Key files: search-to-add.tsx (click-outside mousedown handler), project-supplies-tab.tsx (onClose prop). Three hypotheses to check: mousedown timing, onClose reference instability, flipUp re-render.`
</next_action>
