---
phase: 04-supplies-fabric
plan: 06
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/components/features/supplies/search-to-add.tsx
  - src/components/features/charts/project-supplies-tab.tsx
  - src/components/features/charts/project-supplies-tab.test.tsx
  - src/components/features/charts/chart-detail.tsx
  - src/app/(dashboard)/charts/[id]/page.tsx
autonomous: true
requirements:
  - SUPP-03

must_haves:
  truths:
    - "User can see a Supplies tab on the chart detail page"
    - "User can add threads to a project via inline search dropdown with color swatches"
    - "User can add beads and specialty items the same way"
    - "User can see required/acquired/needed quantities for each linked supply"
    - "User can edit quantities inline (click-to-edit)"
    - "User can remove a supply from a project"
    - "Supply kitting summary shows fulfillment percentage"
  artifacts:
    - path: "src/components/features/charts/project-supplies-tab.tsx"
      provides: "Supplies tab for chart detail page with quantity tracking"
      contains: "use client"
    - path: "src/components/features/supplies/search-to-add.tsx"
      provides: "Inline search dropdown for adding supplies to projects"
      contains: "use client"
  key_links:
    - from: "src/components/features/charts/project-supplies-tab.tsx"
      to: "src/lib/actions/supply-actions.ts"
      via: "Server action calls for junction CRUD"
      pattern: "addThreadToProject|updateProjectSupplyQuantity|removeProjectThread"
    - from: "src/components/features/charts/chart-detail.tsx"
      to: "src/components/features/charts/project-supplies-tab.tsx"
      via: "New tab integration"
      pattern: "ProjectSuppliesTab"
---

<objective>
Build the project-supply linking UI: inline search-to-add dropdown, supplies tab on chart detail page with quantity tracking, and editable quantities.

Purpose: Implements SUPP-03 (link supplies to projects with quantities). Integrates with existing chart detail page.
Output: Supplies tab on chart detail, inline search-and-add, quantity editing.
</objective>

<execution_context>
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-supplies-fabric/04-CONTEXT.md
@.planning/phases/04-supplies-fabric/04-UI-SPEC.md
@.planning/phases/04-supplies-fabric/04-02-SUMMARY.md

<interfaces>
From src/lib/actions/supply-actions.ts:
```typescript
export async function addThreadToProject(formData: unknown): ...
export async function addBeadToProject(formData: unknown): ...
export async function addSpecialtyToProject(formData: unknown): ...
export async function updateProjectSupplyQuantity(id: string, type: "thread"|"bead"|"specialty", formData: unknown): ...
export async function removeProjectThread(id: string): ...
export async function removeProjectBead(id: string): ...
export async function removeProjectSpecialty(id: string): ...
export async function getProjectSupplies(projectId: string): Promise<{threads, beads, specialty}>
export async function getThreads(brandId?, colorFamily?, search?): ...
export async function getBeads(brandId?, search?): ...
export async function getSpecialtyItems(brandId?, search?): ...
```

From src/types/supply.ts:
```typescript
export type ProjectThreadWithThread = ProjectThread & { thread: ThreadWithBrand };
export type ProjectBeadWithBead = ProjectBead & { bead: BeadWithBrand };
export type ProjectSpecialtyWithItem = ProjectSpecialty & { specialtyItem: SpecialtyItemWithBrand };
```

From src/components/features/charts/chart-detail.tsx (existing, to modify):
```typescript
"use client";
// Currently renders chart metadata, status control, info cards
// Needs new Supplies tab added alongside existing content
```

From product-plan/sections/supply-tracking-and-shopping/components/ProjectSuppliesTab.tsx (design reference)
From product-plan/sections/supply-tracking-and-shopping/project-supplies-tab.png (visual reference)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SearchToAdd component and ProjectSuppliesTab</name>
  <files>
    src/components/features/supplies/search-to-add.tsx,
    src/components/features/charts/project-supplies-tab.tsx,
    src/components/features/charts/project-supplies-tab.test.tsx
  </files>
  <read_first>
    product-plan/sections/supply-tracking-and-shopping/components/ProjectSuppliesTab.tsx,
    product-plan/sections/supply-tracking-and-shopping/project-supplies-tab.png,
    src/components/features/charts/inline-designer-dialog.tsx,
    src/components/features/supplies/color-swatch.tsx,
    .planning/phases/04-supplies-fabric/04-UI-SPEC.md,
    .planning/phases/04-supplies-fabric/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test: ProjectSuppliesTab renders three sections: Threads, Beads, Specialty Items
    - Test: ProjectSuppliesTab shows "No threads linked to this project" empty state
    - Test: ProjectSuppliesTab shows kitting summary with percentage
    - Test: ProjectSuppliesTab renders thread row with color swatch, code, name, quantities
    - Test: ProjectSuppliesTab calls removeProjectThread when remove button clicked
  </behavior>
  <action>
1. **Write tests** for ProjectSuppliesTab.

2. **Create SearchToAdd** at `src/components/features/supplies/search-to-add.tsx`:
   - "use client"
   - Props: `supplyType: "thread" | "bead" | "specialty"`, `projectId: string`, `existingIds: string[]`, `onAdded: () => void`
   - Per D-09: inline search field -> dropdown shows matching catalog items with color swatches
   - Uses shadcn Command component (cmdk-based) for search dropdown
   - On mount or when search term changes, call the appropriate getThreads/getBeads/getSpecialtyItems with search filter
   - Filter out items already in `existingIds` (per UI-SPEC: "Already-linked supplies filtered out of results")
   - Dropdown shows max 8 results per UI-SPEC
   - Each result: ColorSwatch (sm/20px) + brand code + name
   - Click result: calls addThreadToProject/addBeadToProject/addSpecialtyToProject with `{ projectId, threadId/beadId/specialtyItemId, quantityRequired: 1, quantityAcquired: 0 }`, then calls onAdded callback
   - Escape or click outside closes dropdown
   - "No matches" message when search has no results
   - Per UI-SPEC Accessibility: Arrow keys navigate, Enter selects, Escape closes

3. **Create ProjectSuppliesTab** at `src/components/features/charts/project-supplies-tab.tsx`:
   - "use client"
   - Props: `projectId: string`, `threads: ProjectThreadWithThread[]`, `beads: ProjectBeadWithBead[]`, `specialty: ProjectSpecialtyWithItem[]`
   - Per UI-SPEC Chart Detail Supplies Tab layout:

   **Kitting Summary Card** (top of tab):
   - Calculate overall percentage: `(fulfilledCount / totalCount) * 100` where fulfilled = acquired >= required
   - Progress bar: emerald fill on stone-100 track
   - Percentage text: JetBrains Mono tabular-nums
   - Status message: "All supplies acquired" when 100%, or "Still needs: X threads, Y beads" listing unfulfilled categories

   **Three collapsible sections:**

   a) **Threads section** (CircleDot icon):
   - Each row: ColorSwatch (md/28px) + brand code + color name + "Req: {n}" + "Got: {n}" + "Need: {computed}" + remove button (Trash2)
   - "Req:" and "Got:" are click-to-edit per UI-SPEC Editable Quantity: click opens inline `<input type="number" className="w-12">`, Enter/blur saves via updateProjectSupplyQuantity, Escape reverts
   - "Need:" = quantityRequired - quantityAcquired (computed, not editable)
   - Fulfilled row: Check icon (emerald-500) when acquired >= required; AlertTriangle (amber-500) when not
   - Remove button: opacity-0 default, opacity-100 on row hover AND focus:opacity-100 (keyboard accessibility per UI-SPEC)
   - Remove action: calls removeProjectThread, no confirmation dialog per UI-SPEC Destructive Actions ("Remove supply from project" = immediate action, toast: "Removed {Brand} {Code} from project")
   - Footer: [+ Add threads] emerald link opens SearchToAdd with supplyType="thread"
   - Empty state: "No threads linked to this project" with [+ Add threads] link

   b) **Beads section** (Gem icon): Same pattern, no stitchCount field

   c) **Specialty section** (Sparkles icon): Same pattern

   - Sections use `<div>` containers (NOT nested forms) per base-ui-patterns.md
   - useTransition for optimistic updates on quantity edits and remove actions
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npx vitest run src/components/features/charts/project-supplies-tab.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/supplies/search-to-add.tsx contains `"use client"`
    - src/components/features/supplies/search-to-add.tsx contains `addThreadToProject` or similar
    - src/components/features/supplies/search-to-add.tsx contains `existingIds`
    - src/components/features/charts/project-supplies-tab.tsx contains `"use client"`
    - src/components/features/charts/project-supplies-tab.tsx contains `CircleDot`
    - src/components/features/charts/project-supplies-tab.tsx contains `Gem`
    - src/components/features/charts/project-supplies-tab.tsx contains `Sparkles`
    - src/components/features/charts/project-supplies-tab.tsx contains `removeProjectThread`
    - src/components/features/charts/project-supplies-tab.tsx contains `updateProjectSupplyQuantity`
    - src/components/features/charts/project-supplies-tab.tsx contains `"No threads linked to this project"`
    - src/components/features/charts/project-supplies-tab.tsx contains `"+ Add threads"` or `"Add threads"`
    - project-supplies-tab.test.tsx exits 0
  </acceptance_criteria>
  <done>ProjectSuppliesTab with three sections, inline search-to-add, editable quantities, remove actions, and kitting summary.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate supplies tab into chart detail page</name>
  <files>
    src/components/features/charts/chart-detail.tsx,
    src/app/(dashboard)/charts/[id]/page.tsx
  </files>
  <read_first>
    src/components/features/charts/chart-detail.tsx,
    src/app/(dashboard)/charts/[id]/page.tsx,
    src/components/features/charts/project-supplies-tab.tsx,
    src/lib/actions/supply-actions.ts
  </read_first>
  <action>
1. **Modify chart detail page route** at `src/app/(dashboard)/charts/[id]/page.tsx`:
   - Add import and call to `getProjectSupplies(chart.project.id)` alongside existing data fetching
   - Pass supplies data as additional props to `<ChartDetail />`

2. **Modify ChartDetail component** at `src/components/features/charts/chart-detail.tsx`:
   - Add `projectSupplies` to props type: `{ threads: ProjectThreadWithThread[], beads: ProjectBeadWithBead[], specialty: ProjectSpecialtyWithItem[] }`
   - Add a "Supplies" tab/section after the existing chart metadata sections
   - Use shadcn Tabs to organize: existing content in one tab ("Details"), new supplies in another ("Supplies")
   - Alternatively (simpler): add the ProjectSuppliesTab as a new section below existing content with a section heading "Supplies"
   - The approach should match the design reference — check `product-plan/sections/supply-tracking-and-shopping/components/ProjectSuppliesTab.tsx` for how it integrates
   - Import and render `<ProjectSuppliesTab projectId={chart.project.id} threads={projectSupplies.threads} beads={projectSupplies.beads} specialty={projectSupplies.specialty} />`
   - Only show supplies tab when project exists (chart has a project)
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npm test -- --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/charts/chart-detail.tsx contains `ProjectSuppliesTab`
    - src/components/features/charts/chart-detail.tsx contains `import` referencing project-supplies-tab
    - src/app/(dashboard)/charts/[id]/page.tsx contains `getProjectSupplies`
    - `npm test` exits 0 (full suite including existing chart tests)
  </acceptance_criteria>
  <done>Chart detail page has a Supplies section/tab rendering ProjectSuppliesTab with linked supply data, maintaining all existing functionality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server Actions | Junction CRUD operations from inline UI |
| Search input -> Query | Free-text search passed to supply query functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | Quantity input values | mitigate | updateQuantitySchema validates int >= 0 for acquired, int >= 1 for required; negative/non-number values rejected by Zod |
| T-04-18 | Tampering | Search input | accept | Search string used in Prisma `contains` filter which is parameterized; no SQL injection risk |
| T-04-19 | Elevation of Privilege | Junction operations | mitigate | All junction CRUD functions call requireAuth(); single-user app |
</threat_model>

<verification>
- `npx vitest run src/components/features/charts/project-supplies-tab.test.tsx` passes
- `npm test` passes (full suite)
- Chart detail page shows Supplies section with linked supplies
</verification>

<success_criteria>
- Supplies tab visible on chart detail page
- Inline search-to-add shows matching items with color swatches
- Already-linked items filtered from search results
- Quantities editable via click-to-edit inline inputs
- Remove supply from project with immediate action and toast
- Kitting summary shows fulfillment percentage
- All components tested
</success_criteria>

<output>
After completion, create `.planning/phases/04-supplies-fabric/04-06-SUMMARY.md`
</output>
