---
context: phase
phase: 04-supplies-fabric
task: post-execution-uat
total_tasks: 6 bugs + post-execution gates
status: bugs_to_fix
last_updated: 2026-04-11T03:13:35.070Z
---

<current_state>
All 7 Phase 4 plans executed and committed. Build passes (335 tests). Runtime works after dev server restart (Prisma stale singleton fixed). First human UAT completed — found 8 issues, 2 deferred to backlog, 6 bugs to fix before post-execution gates.

Jobelan added to FABRIC_TYPES. 4 backlog items created (999.1-999.4).
</current_state>

<completed_work>

### Phase 4 Plans (all 7 done)
- 04-01: Schema, types, validations, DMC seed — 9 tests
- 04-02: Supply/brand CRUD, junction ops, shopping aggregation — 61 tests
- 04-03: Fabric/brand CRUD server actions — 40 tests
- 04-04: Supply catalog UI — tabs, grid/table, swatches, filtering, modal CRUD — 16 tests
- 04-05: Fabric catalog, detail pages, size calculator — 20 tests
- 04-06: Project-supply linking, search-to-add, kitting summary — 7 tests
- 04-07: Shopping list page, nav update — 9 tests

### This Session
- Fixed Prisma findMany crash (stale globalThis singleton — dev server restart, no code change)
- Compared implementation against DesignOS designs
- Completed first human UAT pass
- Added Jobelan to FABRIC_TYPES dropdown (src/types/fabric.ts)
- Created 4 backlog items (999.1 supply detail modal, 999.2 bulk editor, 999.3 fabric type hierarchy, 999.4 project supplies tab)
</completed_work>

<remaining_work>

### 6 Bugs to Fix

**Bug 2 — No link to brands from /supplies**
- File: `src/components/features/supplies/supply-catalog.tsx`
- Issue: /supplies/brands page exists but no navigation link from the supplies page
- Fix: Add a "Manage Brands" button/link on the supplies page

**Bug 3 — Brand dropdown shows all brands regardless of tab**
- File: `src/components/features/supplies/supply-form-modal.tsx` (lines ~245-259)
- Issue: When adding a thread, the brand dropdown shows bead/specialty brands too
- Fix: Filter brands prop by active supply type (SupplyBrand has supplyType field: THREAD/BEAD/SPECIALTY)
- Note: The catalog already filters correctly (supply-catalog.tsx lines 536-540), just the form modal doesn't

**Bug 4 — Grid tiles need edit affordance on hover**
- File: `src/components/features/supplies/supply-grid-view.tsx` (lines 30-40)
- Issue: Grid swatch cards only have subtle border change on hover, no visual indication they're editable
- Fix: Add hover overlay with pencil icon, or scale/shadow lift effect

**Bug 6 — Fabric size calculation uses stitches as inches (CRITICAL)**
- File: `src/lib/actions/shopping-actions.ts` (lines 107-113)
- Issue: Code uses `stitchesWide` and `stitchesHigh` directly as inches. A 300x400 stitch design shows as needing 300x400 inches of fabric instead of ~21x29 inches (at 14ct)
- Fix: Calculate inches = stitches ÷ count + margin. Use actual fabric count from project's linked fabric, fallback to 14ct. Consider showing a multi-count reference table.
- Also: Hardcodes 14ct — should use project's actual fabric count if linked

**Bug 7 — Shopping list project link 404**
- File: `src/components/features/shopping/shopping-list.tsx` (lines 99-104)
- Issue: Link uses `/charts/${project.projectId}` — likely projectId is the Project record ID but route expects Chart ID
- Fix: Verify data shape from shopping-actions.ts, use correct ID field

**Bug 8 — "Got" → "Have" label**
- File: `src/components/features/charts/project-supplies-tab.tsx` (line 171)
- Issue: Label says "Got:" next to acquired quantity, should say "Have:"
- Fix: One-line text change

### Post-Execution Gates (after bugs fixed)
1. Code review gate — `Skill(gsd:code-review, args="04")`
2. Regression gate — run prior phase test files
3. Phase verification — gsd:verify-work
4. Human UAT re-test — all 7 areas
5. Update ROADMAP/STATE on completion
</remaining_work>

<decisions_made>

- Prisma findMany crash was stale globalThis singleton, not schema drift — dev server restart fixed it
- Supply detail modal → backlog 999.1 (post-MVP)
- Bulk supply editor → backlog 999.2 (post-MVP)
- Fabric type hierarchy → backlog 999.3 (post-MVP, flat dropdown with Jobelan for now)
- Project supplies tab layout → backlog 999.4 (post-MVP, inline is fine for MVP)
</decisions_made>

<blockers>
- R2 not configured (pre-existing — uploads degrade gracefully)
</blockers>

## Required Reading (in order)
1. This file — bug details with file paths and line numbers
2. `.planning/HANDOFF.json` — structured state for machine parsing

## Infrastructure State
- Dev server: running on localhost:3000 (may need restart in new session)
- Database: Neon PostgreSQL — schema in sync
- Prisma client: regenerated, all 13 models present
- Build: passing | Tests: 335/335 passing

<context>
Phase 4 execution is functionally complete — all plans landed and the app works. User did thorough UAT testing and found 8 issues. Two were design enhancements deferred to post-MVP backlog. Six are bugs ranging from critical (fabric calculation) to trivial (label text). The bugs are well-scoped with specific file paths and line numbers from investigation. After fixing bugs, run the standard post-execution gates (code review, verification, UAT re-test) before shipping Phase 4.
</context>

<next_action>
Fix 6 bugs in priority order: Bug 6 (fabric calc, critical) → Bug 7 (404 link) → Bug 3 (brand filter) → Bug 2 (brands nav) → Bug 4 (grid hover) → Bug 8 (label). Then run post-execution gates.
</next_action>
