---
phase: 32-series-management-pages
plan: 03
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - src/app/(dashboard)/series/[id]/page.tsx
  - src/components/features/series/series-detail.tsx
  - src/components/features/series/series-detail.test.tsx
autonomous: true
requirements: [SERIES-05]

must_haves:
  truths:
    - "User can view a series detail page at /series/[id] with series name, progress, and chart list"
    - "User can inline-edit series name with Enter to save and Escape to cancel"
    - "User can see chart rows with thumbnail, name, stitch count, size badge, and status badge"
    - "User can sort charts by Name, Stitches, or Status"
    - "User can click a chart row to navigate to /charts/[id]"
    - "User can edit designer via SearchableSelect and edit totalCount/notes inline"
    - "User can delete series from detail page and be redirected to /series"
  artifacts:
    - path: "src/app/(dashboard)/series/[id]/page.tsx"
      provides: "Server component page fetching series detail and designers"
      contains: "getSeriesDetail"
    - path: "src/components/features/series/series-detail.tsx"
      provides: "Client component with header, inline edit, chart rows, sort, delete"
      contains: "SeriesDetail"
  key_links:
    - from: "src/app/(dashboard)/series/[id]/page.tsx"
      to: "src/lib/actions/series-actions.ts"
      via: "getSeriesDetail import"
      pattern: "import.*getSeriesDetail.*series-actions"
    - from: "src/app/(dashboard)/series/[id]/page.tsx"
      to: "src/lib/actions/designer-actions.ts"
      via: "getDesigners import for SearchableSelect options"
      pattern: "import.*getDesigners.*designer-actions"
    - from: "src/components/features/series/series-detail.tsx"
      to: "src/lib/actions/series-actions.ts"
      via: "updateSeries and deleteSeries calls"
      pattern: "updateSeries\\|deleteSeries"
---

<objective>
Build the /series/[id] detail page showing series header with dual progress, inline editing for name/totalCount/notes/designer, sortable chart list with thumbnails and status badges, and delete functionality.

Purpose: Delivers SERIES-05 — user can view series detail with assigned charts and dual progress, and manage series properties inline.
Output: Server detail page, SeriesDetail client component, full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-series-management-pages/32-PATTERNS.md
@.planning/phases/32-series-management-pages/32-UI-SPEC.md
@.planning/phases/32-series-management-pages/32-RESEARCH.md
@.planning/phases/32-series-management-pages/32-01-SUMMARY.md

<interfaces>
From src/types/series.ts (expanded in Plan 01):
```typescript
export type SeriesChart = OptionalFocalPoint & {
  id: string;
  name: string;
  coverThumbnailUrl: string | null;
  coverImageUrl: string | null;
  stitchCount: number;
  stitchesWide: number;
  stitchesHigh: number;
  status: ProjectStatus | null;
  stitchesCompleted: number;
};

export type SeriesDetail = SeriesWithStats & {
  charts: SeriesChart[];
};
```

From src/lib/actions/series-actions.ts:
```typescript
export async function getSeriesDetail(id: string): Promise<SeriesDetail | null>;
export async function updateSeries(id: string, formData: unknown): Promise<{ success: true; series: Series } | { success: false; error: string }>;
export async function deleteSeries(id: string): Promise<{ success: true } | { success: false; error: string }>;
```

From src/lib/actions/designer-actions.ts:
```typescript
export async function getDesigners(): Promise<Designer[]>;
// Designer shape: { id: string; name: string; ... }
```

From src/lib/utils/size-category.ts:
```typescript
export function getEffectiveStitchCount(stitchCount: number, stitchesWide: number, stitchesHigh: number): { count: number; source: string };
```

From src/lib/utils/focal-point.ts:
```typescript
export function getObjectPositionStyle(x: number | null, y: number | null): CSSProperties | undefined;
```

From src/__tests__/mocks/factories.ts (created in Plan 01):
```typescript
export function createMockSeriesWithStats(overrides?: Partial<SeriesWithStats>): SeriesWithStats;
export function createMockSeriesChart(overrides?: Partial<SeriesChart>): SeriesChart;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SeriesDetail tests (RED)</name>
  <files>src/components/features/series/series-detail.test.tsx</files>
  <read_first>
    - src/components/features/designers/designer-detail.test.tsx
    - src/__tests__/mocks/factories.ts
    - .planning/phases/32-series-management-pages/32-UI-SPEC.md (CP-03, CP-04)
    - .planning/phases/32-series-management-pages/32-PATTERNS.md (series-detail section)
  </read_first>
  <behavior>
    - Test 1: renders series name in heading
    - Test 2: renders "Back to Series" link pointing to /series
    - Test 3: renders progress bar and completion text ("{finishedCount} of {ownedCount} finished")
    - Test 4: renders designer name as link to /designers/{id} when designerId is set
    - Test 5: hides designer line when designerId is null
    - Test 6: renders chart rows with chart name and stitch count
    - Test 7: chart rows link to /charts/{chartId}
    - Test 8: chart rows show StatusBadge for charts with status
    - Test 9: inline name edit: clicking pencil shows input, Enter saves with updateSeries call
    - Test 10: inline name edit: Escape cancels without saving
    - Test 11: sort pills (Name, Stitches, Status) reorder chart list
    - Test 12: delete button triggers DeleteConfirmationDialog, confirm calls deleteSeries
    - Test 13: empty chart list shows "No charts in this series yet"
    - Test 14: renders "owned" line when totalCount is set ("{ownedCount} of {totalCount} owned")
  </behavior>
  <action>
    Create series-detail.test.tsx following designer-detail.test.tsx patterns exactly.

    Mock setup: vi.mock series-actions (updateSeries, deleteSeries), vi.mock designer-actions (getDesigners — not needed if passing designers as prop), vi.mock next/navigation (useRouter with refresh and push mocks), vi.mock sonner (toast).

    Test data: create a SeriesDetail-shaped object using createMockSeriesWithStats spread with charts array of createMockSeriesChart entries. Include: one chart with status IN_PROGRESS and stitchesCompleted > 0, one with status FINISHED, one with null status. Include a series with designerName set and one test with designerName null.

    Props shape: { series: SeriesDetail-like, designers: Array<{ id: string, name: string }> }

    For inline edit tests: find the pencil button by aria-label "Edit series name", click it, verify input appears, type new name + press Enter, verify updateSeries called with new name. For Escape: click pencil, press Escape, verify updateSeries NOT called.

    For sort: click "Stitches" pill, verify chart order changes (sort by stitch count). Click again for descending.

    For delete: find trash button by aria-label "Delete series", click, verify dialog text appears, click "Delete Series", verify deleteSeries called and router.push("/series") on success.

    All tests should FAIL (component does not exist yet).
  </action>
  <verify>
    <automated>npm test -- --run src/components/features/series/series-detail.test.tsx 2>&1 | grep -c "FAIL\|Cannot find module"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/series/series-detail.test.tsx exists with 14+ test cases
    - Tests import from @/__tests__/test-utils
    - Tests use createMockSeriesWithStats and createMockSeriesChart factories
    - Tests mock updateSeries and deleteSeries from series-actions
    - Tests verify specific UI text: "Back to Series", "finished", "Charts", aria-labels
    - Tests cover inline edit flow (Enter saves, Escape cancels)
    - Tests cover chart row rendering (name, stitch count, status badge, link)
    - Tests cover sort pill interaction
    - All tests fail because SeriesDetail component does not exist (RED phase)
  </acceptance_criteria>
  <done>Test file defines expected behavior for SeriesDetail component. All 14 tests fail (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SeriesDetail component and detail page (GREEN)</name>
  <files>src/components/features/series/series-detail.tsx, src/app/(dashboard)/series/[id]/page.tsx</files>
  <read_first>
    - src/components/features/series/series-detail.test.tsx (just created — defines expected behavior)
    - src/components/features/designers/designer-detail.tsx
    - src/app/(dashboard)/designers/[id]/page.tsx
    - .planning/phases/32-series-management-pages/32-UI-SPEC.md (CP-03, CP-04)
    - .planning/phases/32-series-management-pages/32-PATTERNS.md (series-detail section, chart row pattern, delete with redirect)
  </read_first>
  <action>
    Implement SeriesDetail (series-detail.tsx) as "use client" component:
    - Props: { series: SeriesDetail (type), designers: Array&lt;{ id: string; name: string }&gt; }
    - State: isEditingName, editName, isEditingDesigner, isEditingNotes, isEditingTotalCount, deletingSeries, chartSort { key: ChartSortKey, dir: SortDir }
    - ChartSortKey: "name" | "stitchCount" | "status"
    - Inline name edit per CP-03: Pencil icon button (aria-label="Edit series name") toggles edit mode. Input with border-b-2 border-primary bg-transparent styling. Check icon (save) + X icon (cancel). Enter triggers save, Escape triggers cancel. onBlur also saves. useRef for focus on edit mode entry.
    - Save name: call updateSeries(series.id, { name: trimmed, totalCount: series.totalCount, designerId: series.designerId, notes: series.notes }). Toast "Series updated" on success, router.refresh(). Error: toast.error.
    - Designer display: "by {designerName}" as Link to /designers/{designerId}, with Pencil icon (aria-label="Edit designer") to open SearchableSelect for reassignment. On select, call updateSeries with new designerId.
    - TotalCount display: when set, show "{ownedCount} of {totalCount} owned" with inline number input for editing.
    - Notes display: when set, show notes text with edit icon for textarea editing.
    - Progress bar: same h-2 rounded-full bg-muted/bg-primary pattern. Text: "{finishedCount} of {ownedCount} finished" per CP-03.
    - Back link: Link to /series with ArrowLeft icon, text "Back to Series".
    - Chart section: "Charts ({count})" header label (uppercase tracking-widest text-xs font-semibold text-muted-foreground). Sort pills below: "Name", "Stitches", "Status" with same pill styling as list page sort.
    - Chart sort useMemo: name (localeCompare), stitchCount (getEffectiveStitchCount comparison), status (STATUS_ORDER record: IN_PROGRESS=0, KITTING=1, KITTED=2, UNSTARTED=3, ON_HOLD=4, FINISHED=5, FFO=6, null=99).
    - ChartRow sub-component: Link to /charts/{chart.id} with thumbnail (40x40 rounded-lg, focal point style, Image icon placeholder), chart name (text-sm font-semibold truncate), meta line (formatted stitch count + SizeBadge), StatusBadge (or "Not started" text when null), in-progress mini progress bar (h-1.5 w-16 when status IN_PROGRESS and effective count > 0). Exact styling from CP-04.
    - Empty chart state: EmptyState-like centered text "No charts in this series yet" with sub-text "Charts can be assigned from the chart form."
    - Delete: Trash2 icon button (aria-label="Delete series") in header actions. Opens DeleteConfirmationDialog. On confirm: call deleteSeries, toast "Series deleted", router.push("/series").
    - Action buttons: top-right, Pencil + Trash2, matching designer detail styling (text-muted-foreground hover:text-foreground rounded-md p-1.5, delete hover:text-destructive hover:bg-destructive/10).

    Implement page.tsx (src/app/(dashboard)/series/[id]/page.tsx):
    - Server component, no "use client"
    - Import getSeriesDetail from series-actions, getDesigners from designer-actions, SeriesDetail component, notFound from next/navigation
    - Await params to get id (Next.js 16 async params: { params }: { params: Promise&lt;{ id: string }&gt; })
    - Call getSeriesDetail(id), if null call notFound()
    - Call getDesigners() for SearchableSelect options
    - Render SeriesDetail with series and designers props
  </action>
  <verify>
    <automated>npm test -- --run src/components/features/series/series-detail.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/series/series-detail.tsx exists with "use client" directive
    - SeriesDetail renders series name in text-2xl heading
    - "Back to Series" link renders with href="/series"
    - Progress bar renders with correct finished/owned calculation
    - Designer name renders as Link to /designers/{id} when present
    - Chart rows render as Links to /charts/{id}
    - Chart rows use getObjectPositionStyle for thumbnail focal point
    - Chart rows use getEffectiveStitchCount for stitch count display
    - Chart rows render StatusBadge for non-null status
    - Inline name edit: Enter calls updateSeries, Escape does not
    - Sort pills reorder chart list correctly
    - Delete flow calls deleteSeries then router.push("/series")
    - Empty chart state shows "No charts in this series yet"
    - Detail page.tsx is server component calling getSeriesDetail and getDesigners
    - All series-detail.test.tsx tests pass (GREEN)
  </acceptance_criteria>
  <done>Series detail page fully functional: header with progress, inline editing, sortable chart list, delete. All tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params -> getSeriesDetail | Untrusted ID from route parameter |
| client -> updateSeries | User-edited name/totalCount/notes/designerId values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Elevation of Privilege | getSeriesDetail(id) | mitigate | requireAuth() called in server action (Phase 31) |
| T-32-08 | Tampering | updateSeries input | mitigate | Zod seriesSchema.parse() validates all fields (Phase 31) |
| T-32-09 | Information Disclosure | series detail data | accept | Single-user app, no multi-tenant concern |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
- `npm test -- --run src/components/features/series/series-detail.test.tsx` all tests pass
- `grep "getSeriesDetail" src/app/\(dashboard\)/series/\[id\]/page.tsx` confirms data fetch
- `grep "getDesigners" src/app/\(dashboard\)/series/\[id\]/page.tsx` confirms designers fetched for SearchableSelect
- `grep -c "use client" src/app/\(dashboard\)/series/\[id\]/page.tsx` returns 0 (server component)
- `grep "notFound" src/app/\(dashboard\)/series/\[id\]/page.tsx` confirms 404 handling
</verification>

<success_criteria>
- /series/[id] detail page renders series with header, progress, chart list
- Inline editing for name, totalCount, notes, designer all functional
- Chart rows display thumbnail, name, stitch count, size badge, status badge
- Chart sort works for Name/Stitches/Status
- Delete redirects to /series after success
- Empty chart list shows appropriate message
- All tests pass, no regressions in full test suite
</success_criteria>

<output>
Create `.planning/phases/32-series-management-pages/32-03-SUMMARY.md` when done
</output>
