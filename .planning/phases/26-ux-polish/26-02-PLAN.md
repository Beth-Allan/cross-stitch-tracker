---
phase: 26-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/designers/designer-list.tsx
  - src/components/features/designers/designer-list.test.tsx
  - src/components/features/genres/genre-list.tsx
  - src/components/features/genres/genre-list.test.tsx
  - src/components/features/gallery/gallery-card.tsx
  - src/components/features/gallery/gallery-card.test.tsx
  - src/components/features/charts/whats-next-tab.tsx
  - src/components/features/charts/whats-next-tab.test.tsx
  - src/components/features/shopping/shopping-for-bar.tsx
  - src/components/features/shopping/shopping-cart.test.tsx
  - src/components/features/stats/thread-insight-list.tsx
  - src/components/features/stats/thread-insight-list.test.tsx
autonomous: true
requirements:
  - UX-02
  - UX-05
  - UX-06
  - UX-12
  - UX-14

must_haves:
  truths:
    - "DesignerRow/DesignerCard and GenreRow/GenreCard have role=group and aria-labelledby linking to the name element"
    - "GalleryCard image area is wrapped in a Link for larger click target"
    - "What's Next cards display as vertical card stacks matching gallery card visual pattern"
    - "Kitting label shows 'Not kitted' at 0%, 'Kitting' at 1-99%, 'Fully kitted' at 100%"
    - "Shopping-for bar pills use rounded-lg with border instead of rounded-full"
    - "ThreadInsightList items show rank numbers matching DesignerInsightList pattern"
  artifacts:
    - path: "src/components/features/designers/designer-list.tsx"
      provides: "ARIA group pattern on DesignerRow and DesignerCard"
      contains: "role=\"group\""
    - path: "src/components/features/genres/genre-list.tsx"
      provides: "ARIA group pattern on GenreRow and GenreCard"
      contains: "role=\"group\""
    - path: "src/components/features/gallery/gallery-card.tsx"
      provides: "Link wrapping image area"
      contains: "Link href={`/charts/${card.chartId}`}"
    - path: "src/components/features/charts/whats-next-tab.tsx"
      provides: "Three-state kitting label + gallery card layout"
      contains: "Not kitted"
    - path: "src/components/features/shopping/shopping-for-bar.tsx"
      provides: "Squared pill styling"
      contains: "rounded-lg"
    - path: "src/components/features/stats/thread-insight-list.tsx"
      provides: "Rank numbers on thread insight items"
      contains: "{index + 1}."
  key_links:
    - from: "src/components/features/charts/whats-next-tab.tsx"
      to: "/charts/{chartId}"
      via: "Link in card wrapper"
      pattern: "Link href.*charts"
    - from: "src/components/features/gallery/gallery-card.tsx"
      to: "/charts/{chartId}"
      via: "Link wrapping image area"
      pattern: "Link href.*charts"
---

<objective>
Fix ARIA compliance on card rows, restyle What's Next cards to match gallery card layout, update kitting labels to three-state copy, apply squared pill styling to shopping-for bar, and add rank numbers to ThreadInsightList.

Purpose: Five UX requirements (UX-02, UX-05, UX-06, UX-12, UX-14) target card/list presentation components. These share no files with Plan 01 (supply-table) or Plan 03 (focal-point/data), enabling parallel execution.

Output: Modified card/list components with ARIA attributes, restyled layouts, and tests for all five fixes.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-ux-polish/26-CONTEXT.md
@.planning/phases/26-ux-polish/26-UI-SPEC.md
@.planning/phases/26-ux-polish/26-PATTERNS.md
@.claude/rules/base-ui-patterns.md
@.claude/rules/testing-requirements.md
@.claude/rules/comment-conventions.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/features/designers/designer-list.tsx:
```typescript
// DesignerRow: <tr> with <Link> name + edit/delete buttons (already have aria-labels)
// DesignerCard: <div> card with <Link> name + edit/delete buttons (already have aria-labels)
// Both need: role="group" on container, aria-labelledby pointing to name element id
```

From src/components/features/genres/genre-list.tsx:
```typescript
// GenreRow: identical structure to DesignerRow
// GenreCard: identical structure to DesignerCard
// Same ARIA pattern needed
```

From src/components/features/gallery/gallery-card.tsx:
```typescript
export function GalleryCard({ card }: GalleryCardProps)
// Current: image div NOT wrapped in Link, only name text is a Link
// D-02: Extend Link to wrap image area too (no action buttons = no nested interactives)
// Uses: getObjectPositionStyle for focal point on cover image
// Card CSS: bg-card hover:shadow-foreground/8 flex flex-col overflow-hidden rounded-xl
// Image: relative aspect-[4/3] overflow-hidden
// Body: bg-card flex flex-1 flex-col gap-1.5 p-4
```

From src/components/features/charts/whats-next-tab.tsx:
```typescript
export function WhatsNextTab({ projects, imageUrls }: WhatsNextTabProps)
// Current: horizontal row layout (flex items-center gap-3)
// Current kitting label (line 184-185): two-state (Fully kitted / Kitting)
// UX-05: three-state (Not kitted / Kitting / Fully kitted)
// UX-14: restyle to vertical card stack matching GalleryCard layout
```

From src/components/features/stats/thread-insight-list.tsx:
```typescript
// Items use: flex items-center justify-between py-2
// Sister component pattern: designer-insight-list.tsx has rank: {index + 1}.
// in: <span className="text-muted-foreground w-5 font-mono text-xs tabular-nums">
```

From src/components/features/shopping/shopping-for-bar.tsx:
```typescript
// Pill class (line 36): "bg-selected text-selected-foreground flex items-center gap-1 rounded-full px-3 py-1 text-sm"
// D-07: Change rounded-full -> rounded-lg, add border border-selected-border
// D-08: Add why-comment for DesignOS deviation
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Card row ARIA + GalleryCard link target (UX-02)</name>
  <files>
    src/components/features/designers/designer-list.tsx,
    src/components/features/designers/designer-list.test.tsx,
    src/components/features/genres/genre-list.tsx,
    src/components/features/genres/genre-list.test.tsx,
    src/components/features/gallery/gallery-card.tsx,
    src/components/features/gallery/gallery-card.test.tsx
  </files>
  <read_first>
    src/components/features/designers/designer-list.tsx,
    src/components/features/designers/designer-list.test.tsx,
    src/components/features/genres/genre-list.tsx,
    src/components/features/genres/genre-list.test.tsx,
    src/components/features/gallery/gallery-card.tsx,
    src/components/features/gallery/gallery-card.test.tsx
  </read_first>
  <behavior>
    DesignerRow ARIA tests:
    - Test: DesignerRow tr element has role="group"
    - Test: DesignerRow tr has aria-labelledby matching the name Link's id
    - Test: DesignerRow name Link has an id attribute containing the designer id

    DesignerCard ARIA tests:
    - Test: DesignerCard outer div has role="group"
    - Test: DesignerCard outer div has aria-labelledby matching the name Link's id

    GenreRow ARIA tests:
    - Test: GenreRow tr element has role="group" and aria-labelledby
    - Test: GenreRow name Link has id attribute

    GenreCard ARIA tests:
    - Test: GenreCard outer div has role="group" and aria-labelledby

    GalleryCard link tests:
    - Test: GalleryCard image area is inside a link element pointing to /charts/{chartId}
    - Test: GalleryCard renders two links to the same destination (image + name)
  </behavior>
  <action>
    **Per D-01, D-02, D-03, D-04 from CONTEXT.md.**

    **DesignerRow** (in `designer-list.tsx`):
    1. Add `id={`designer-name-${designer.id}`}` to the name `<Link>` element
    2. Add `role="group"` and `aria-labelledby={`designer-name-${designer.id}`}` to the `<tr>` element
    3. Action buttons already have `aria-label={`Edit ${designer.name}`}` and `aria-label={`Delete ${designer.name}`}` -- no change needed

    **DesignerCard** (in `designer-list.tsx`):
    4. Add `id={`designer-name-${designer.id}`}` to the name `<Link>` element
    5. Add `role="group"` and `aria-labelledby={`designer-name-${designer.id}`}` to the outer `<div>` element
    6. Action buttons already have aria-labels -- no change needed

    **GenreRow** (in `genre-list.tsx`):
    7. Add `id={`genre-name-${genre.id}`}` to the name `<Link>` element
    8. Add `role="group"` and `aria-labelledby={`genre-name-${genre.id}`}` to the `<tr>` element

    **GenreCard** (in `genre-list.tsx`):
    9. Add `id={`genre-name-${genre.id}`}` to the name `<Link>` element
    10. Add `role="group"` and `aria-labelledby={`genre-name-${genre.id}`}` to the outer `<div>` element

    **GalleryCard** (in `gallery-card.tsx`, per D-02):
    11. Wrap the existing image `<div className="relative aspect-[4/3] overflow-hidden">` with `<Link href={`/charts/${card.chartId}`} className="block" tabIndex={-1} aria-hidden="true">`. Use `tabIndex={-1}` and `aria-hidden="true"` on the image link to avoid duplicate tab stops since the name link below already provides keyboard access. The name `<Link>` in the body stays as-is.

    **BucketProjectRow and ChartRow** (per D-03): No changes -- already correct.

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/components/features/designers/designer-list.test.tsx src/components/features/genres/genre-list.test.tsx src/components/features/gallery/gallery-card.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `designer-list.tsx` contains `role="group"` (at least 2 occurrences: DesignerRow + DesignerCard)
    - `designer-list.tsx` contains `aria-labelledby={`designer-name-${designer.id}`}`
    - `designer-list.tsx` contains `id={`designer-name-${designer.id}`}`
    - `genre-list.tsx` contains `role="group"` (at least 2 occurrences)
    - `genre-list.tsx` contains `aria-labelledby`
    - `gallery-card.tsx` contains a Link wrapping the image div with `tabIndex={-1}`
    - designer-list.test.tsx has test for `role="group"` on DesignerRow
    - genre-list.test.tsx has test for `role="group"` on GenreRow
    - gallery-card.test.tsx has test for image link target
    - All tests pass
  </acceptance_criteria>
  <done>
    DesignerRow/Card and GenreRow/Card have role="group" with aria-labelledby pointing to the name element. GalleryCard image is wrapped in a supplementary Link. All existing tests still pass, new ARIA tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shopping pills + thread insight rank numbers (UX-06, UX-12)</name>
  <files>
    src/components/features/shopping/shopping-for-bar.tsx,
    src/components/features/shopping/shopping-cart.test.tsx,
    src/components/features/stats/thread-insight-list.tsx,
    src/components/features/stats/thread-insight-list.test.tsx
  </files>
  <read_first>
    src/components/features/shopping/shopping-for-bar.tsx,
    src/components/features/shopping/shopping-cart.test.tsx,
    src/components/features/stats/thread-insight-list.tsx,
    src/components/features/stats/thread-insight-list.test.tsx,
    src/components/features/stats/designer-insight-list.tsx
  </read_first>
  <behavior>
    Shopping pill tests:
    - Test: shopping-for bar pill elements have class "rounded-lg" (not "rounded-full")
    - Test: shopping-for bar pill elements have class "border" and "border-selected-border"

    Thread insight tests:
    - Test: first thread insight item shows rank number "1."
    - Test: third thread insight item shows rank number "3."
    - Test: rank number has class "text-muted-foreground" and "font-mono"
  </behavior>
  <action>
    **UX-06: Shopping-for bar pill styling (per D-07, D-08 from CONTEXT.md)**

    1. In `shopping-for-bar.tsx`, find the pill `<span>` element (around line 36) with class `rounded-full`
    2. Change `rounded-full` to `rounded-lg`
    3. Add `border border-selected-border` to the className
    4. Add a why-comment on the line above per D-08:
       `// Intentionally deviates from DesignOS rounded-full pills -- squared chips better fit shopping cart UI convention.`
    5. If there is a close button inside the pill with `rounded-full`, change it to `rounded-sm`

    Final pill className: `"bg-selected text-selected-foreground flex items-center gap-1 rounded-lg border border-selected-border px-3 py-1 text-sm"`

    **UX-12: ThreadInsightList rank numbers (per D-13, D-14 from CONTEXT.md)**

    6. In `thread-insight-list.tsx`, change the `.map((item) =>` to `.map((item, index) =>`
    7. Inside the item's left-side `<div className="flex items-center gap-2">`, add a rank number span as the FIRST child (before the color swatch):
       ```tsx
       <span className="text-muted-foreground w-5 font-mono text-xs tabular-nums">
         {index + 1}.
       </span>
       ```
       This matches the exact pattern from `designer-insight-list.tsx`.
    8. Do NOT add any hover styling, cursor-pointer, or link elements (per D-13, D-14 -- stays non-interactive)

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/components/features/shopping/shopping-cart.test.tsx src/components/features/stats/thread-insight-list.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `shopping-for-bar.tsx` contains `rounded-lg` (not `rounded-full` on the pill span)
    - `shopping-for-bar.tsx` contains `border border-selected-border`
    - `shopping-for-bar.tsx` contains the why-comment: "Intentionally deviates from DesignOS"
    - `thread-insight-list.tsx` contains `{index + 1}.`
    - `thread-insight-list.tsx` contains `text-muted-foreground w-5 font-mono text-xs tabular-nums`
    - shopping-cart.test.tsx has test for `rounded-lg` on pill elements
    - thread-insight-list.test.tsx has test for rank number "1."
    - All tests pass
  </acceptance_criteria>
  <done>
    Shopping-for bar pills use squared-off rounded-lg with border (intentional DesignOS deviation documented). ThreadInsightList items show rank numbers matching DesignerInsightList visual pattern. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: What's Next card restyling + three-state kitting label (UX-05, UX-14)</name>
  <files>
    src/components/features/charts/whats-next-tab.tsx,
    src/components/features/charts/whats-next-tab.test.tsx
  </files>
  <read_first>
    src/components/features/charts/whats-next-tab.tsx,
    src/components/features/charts/whats-next-tab.test.tsx,
    src/components/features/gallery/gallery-card.tsx
  </read_first>
  <behavior>
    Kitting label tests (UX-05):
    - Test: project with kittingPercent=0 shows "Not kitted" label
    - Test: project with kittingPercent=50 shows "Kitting" label
    - Test: project with kittingPercent=100 shows "Fully kitted" label

    Gallery card layout tests (UX-14):
    - Test: card wrapper has classes "bg-card", "rounded-lg", "border"
    - Test: image area has class "aspect-[4/3]"
    - Test: cards render in a grid container with class "grid"
    - Test: project name is rendered as a Link inside the card body
    - Test: hardcoded emerald hover classes are NOT present (replaced with semantic tokens)
  </behavior>
  <action>
    **UX-05: Three-state kitting label (per D-06 from CONTEXT.md)**

    1. In `whats-next-tab.tsx`, find the kitting label around line 184-185:
       ```tsx
       {project.kittingPercent === 100 ? "Fully kitted" : "Kitting"}
       ```
    2. Replace with three-state:
       ```tsx
       {project.kittingPercent === 100
         ? "Fully kitted"
         : project.kittingPercent === 0
           ? "Not kitted"
           : "Kitting"}
       ```

    **UX-14: What's Next gallery card styling (per CONTEXT.md Claude's Discretion for UX-14 and UI-SPEC UX-14 contract)**

    3. Restructure the card list from a single-column layout to a responsive grid:
       - Change the list container from `<div className="space-y-2">` (or similar) to:
         `<div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">`

    4. Restructure each card from horizontal row to vertical card stack. Replace the current horizontal layout:
       ```tsx
       // BEFORE: horizontal row with small thumbnail
       <Link className="group flex items-center gap-3 ...">
         <img className="h-12 w-12 rounded-md object-cover" ... />
         <div>name, designer, kitting</div>
       </Link>
       ```
       With vertical card matching GalleryCard patterns:
       ```tsx
       <div className="bg-card border-border group overflow-hidden rounded-lg border transition-[box-shadow,transform] duration-200 hover:-translate-y-0.5 hover:shadow-md">
         <Link href={`/charts/${project.chartId}`} className="block">
           <div className="relative aspect-[4/3] overflow-hidden">
             {thumbnailUrl ? (
               <img
                 src={thumbnailUrl}
                 alt={project.name}
                 className="h-full w-full object-cover transition-transform duration-300 group-hover:scale-[1.03]"
                 style={getObjectPositionStyle(project.focalPointX, project.focalPointY)}
                 loading="lazy"
               />
             ) : (
               <CoverPlaceholder status={project.status} />
             )}
           </div>
         </Link>
         <div className="flex flex-col gap-1.5 p-4">
           <Link
             href={`/charts/${project.chartId}`}
             className="font-heading text-foreground group-hover:text-primary text-sm font-semibold leading-snug transition-colors"
           >
             {project.name}
           </Link>
           {project.designerName && (
             <p className="text-muted-foreground text-xs">{project.designerName}</p>
           )}
           {/* kitting progress bar */}
           <div className="mt-1 flex items-center gap-2">
             <div className="bg-muted h-1.5 flex-1 overflow-hidden rounded-full">
               <div className="bg-progress h-full rounded-full" style={{ width: `${project.kittingPercent}%` }} />
             </div>
             <span className="text-muted-foreground text-xs tabular-nums">{project.kittingPercent}%</span>
           </div>
           <p className="text-muted-foreground/70 text-xs">
             {project.kittingPercent === 100
               ? "Fully kitted"
               : project.kittingPercent === 0
                 ? "Not kitted"
                 : "Kitting"}
           </p>
         </div>
       </div>
       ```

    5. Replace any hardcoded `group-hover:text-emerald-700 dark:group-hover:text-emerald-400` with `group-hover:text-primary` (semantic token, per UI-SPEC UX-14 note about semantic token consistency).

    6. Import `getObjectPositionStyle` from `@/lib/utils/focal-point` if not already imported. Use it on the card image with `project.focalPointX` and `project.focalPointY` (these fields already exist on `WhatsNextProject` type).

    7. Keep the sort bar and its functionality unchanged. Only the card rendering changes.

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/components/features/charts/whats-next-tab.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - `whats-next-tab.tsx` contains `"Not kitted"` string
    - `whats-next-tab.tsx` contains `project.kittingPercent === 0` check
    - `whats-next-tab.tsx` contains `grid grid-cols-1` in the card container
    - `whats-next-tab.tsx` contains `aspect-[4/3]` in the image area
    - `whats-next-tab.tsx` contains `bg-card` and `rounded-lg` and `border` on card wrappers
    - `whats-next-tab.tsx` does NOT contain `text-emerald-700` (hardcoded color removed)
    - `whats-next-tab.tsx` contains `group-hover:text-primary` (semantic replacement)
    - whats-next-tab.test.tsx has test for "Not kitted" at 0%
    - whats-next-tab.test.tsx has test for card grid layout
    - All tests pass
  </acceptance_criteria>
  <done>
    What's Next tab displays projects as vertical gallery-style cards in a responsive grid. Kitting label shows three-state copy (Not kitted / Kitting / Fully kitted). Hardcoded emerald hover classes replaced with semantic tokens. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. All changes are client-side ARIA attributes and CSS modifications.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | T (Tampering) | GalleryCard additional Link | accept | Link points to same `/charts/{id}` as existing name Link. No new navigation target. `aria-hidden="true"` + `tabIndex={-1}` prevents it from being an alternate keyboard target. |

No high-severity threats identified. This plan modifies only client-side rendering and ARIA attributes with no data access changes.
</threat_model>

<verification>
- All 6 test files pass: `npx vitest run src/components/features/designers/designer-list.test.tsx src/components/features/genres/genre-list.test.tsx src/components/features/gallery/gallery-card.test.tsx src/components/features/charts/whats-next-tab.test.tsx src/components/features/shopping/shopping-cart.test.tsx src/components/features/stats/thread-insight-list.test.tsx -x`
- No TypeScript errors: `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Card rows have proper ARIA group semantics (UX-02)
- GalleryCard image is a clickable link target (UX-02)
- Kitting label shows three states including "Not kitted" at 0% (UX-05)
- Shopping-for pills use squared-off design (UX-06)
- ThreadInsightList has rank numbers (UX-12)
- What's Next cards match gallery card visual pattern (UX-14)
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-ux-polish/26-02-SUMMARY.md`
</output>
