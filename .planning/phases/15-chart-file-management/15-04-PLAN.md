---
phase: 15-chart-file-management
plan: 04
type: execute
wave: 3
depends_on: [15-02, 15-03]
files_modified:
  - prisma/schema.prisma
  - src/lib/actions/upload-actions.ts
  - src/lib/actions/upload-actions.test.ts
  - src/components/features/charts/form-primitives/file-upload.tsx
  - src/components/features/gallery/project-gallery.test.tsx
  - src/components/features/gallery/gallery-utils.test.ts
  - src/__tests__/mocks/factories.ts
autonomous: true
requirements: [FILE-01, FILE-02, FILE-03]

must_haves:
  truths:
    - "D-13: Auto-migrate existing digitalWorkingCopyUrl data to ChartFile rows with label 'Working Copy'"
    - "D-14: Drop the digitalWorkingCopyUrl column immediately after data migration — clean break"
    - "All tests pass with the new schema (no stale references)"
    - "TypeScript builds cleanly (no type errors from removed field)"
    - "Old single-file FileUpload component is removed"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Chart model without digitalWorkingCopyUrl"
      contains: "model Chart"
    - path: "src/__tests__/mocks/factories.ts"
      provides: "Updated chart factory without digitalWorkingCopyUrl"
      contains: "files:"
  key_links:
    - from: "prisma/schema.prisma"
      to: "database"
      via: "prisma db push after column removal"
      pattern: "model Chart"
---

<objective>
Data migration and codebase cleanup — remove digitalWorkingCopyUrl

Purpose: Complete the migration from single-URL to ChartFile table (per D-13, D-14). Migrate existing data, drop the column, remove all stale references, and delete the old single-file upload component.
Output: Clean codebase with no references to digitalWorkingCopyUrl, data migrated, old component removed, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-chart-file-management/15-CONTEXT.md
@.planning/phases/15-chart-file-management/15-RESEARCH.md
@.planning/phases/15-chart-file-management/15-01-SUMMARY.md
@.planning/phases/15-chart-file-management/15-02-SUMMARY.md
@.planning/phases/15-chart-file-management/15-03-SUMMARY.md

<interfaces>
Files known to reference digitalWorkingCopyUrl (from grep):
```
src/components/features/gallery/project-gallery.test.tsx
src/components/features/gallery/gallery-utils.test.ts
src/components/features/charts/use-chart-form.ts (already updated in plan 02)
src/components/features/charts/project-detail/overview-tab.test.tsx (already updated in plan 03)
src/components/features/charts/project-detail/types.ts (already updated in plan 03)
src/components/features/charts/project-detail/overview-tab.tsx (already updated in plan 03)
src/__tests__/mocks/factories.ts
src/lib/actions/upload-actions.ts
src/lib/actions/upload-actions.test.ts
src/lib/actions/chart-actions.ts (already updated in plan 02)
```

Files to REMOVE:
```
src/components/features/charts/form-primitives/file-upload.tsx (replaced by chart-file-upload.tsx)
```

Data migration SQL (per D-13):
```sql
INSERT INTO "ChartFile" ("id", "chartId", "url", "filename", "mimeType", "fileSize", "label", "createdAt")
SELECT
  gen_random_uuid()::text,
  "id",
  "digitalWorkingCopyUrl",
  'Working Copy',
  'application/octet-stream',
  0,
  'Working Copy',
  CURRENT_TIMESTAMP
FROM "Chart"
WHERE "digitalWorkingCopyUrl" IS NOT NULL;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data migration and schema column removal</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (lines 41-63 for current Chart model — verify files relation was added in plan 01)
    - src/lib/actions/upload-actions.ts (grep for digitalWorkingCopyUrl and VALID_CHART_FIELDS to see what needs updating)
  </read_first>
  <action>
    **1. Run data migration SQL** via Prisma's `prisma db execute` or direct psql:
    ```bash
    npx prisma db execute --stdin <<'SQL'
    INSERT INTO "ChartFile" ("id", "chartId", "url", "filename", "mimeType", "fileSize", "label", "createdAt")
    SELECT
      gen_random_uuid()::text,
      "id",
      "digitalWorkingCopyUrl",
      'Working Copy',
      'application/octet-stream',
      0,
      'Working Copy',
      CURRENT_TIMESTAMP
    FROM "Chart"
    WHERE "digitalWorkingCopyUrl" IS NOT NULL
      AND "digitalWorkingCopyUrl" != '';
    SQL
    ```
    If `prisma db execute` is not available, use the Neon dashboard SQL editor or `psql` connection string from env.

    **2. Remove `digitalWorkingCopyUrl String?` from the Chart model** in prisma/schema.prisma (line 57).

    **3. Run `npx prisma db push`** to drop the column from the live database. This IS destructive (dropping a column) so use `--accept-data-loss` flag since we already migrated the data.

    **4. Run `npx prisma generate`** to regenerate the client without the field.

    Note: After step 4, TypeScript will show errors in any file still referencing `digitalWorkingCopyUrl`. Task 2 fixes those.
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma db push --accept-data-loss 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma does NOT contain "digitalWorkingCopyUrl"
    - `npx prisma validate` exits 0
    - Database column is dropped (push succeeds)
  </acceptance_criteria>
  <done>Existing data migrated to ChartFile records, digitalWorkingCopyUrl column dropped from schema and database</done>
</task>

<task type="auto">
  <name>Task 2: Remove all stale references and old component</name>
  <files>src/lib/actions/upload-actions.ts, src/lib/actions/upload-actions.test.ts, src/components/features/charts/form-primitives/file-upload.tsx, src/components/features/gallery/project-gallery.test.tsx, src/components/features/gallery/gallery-utils.test.ts, src/__tests__/mocks/factories.ts</files>
  <read_first>
    - src/lib/actions/upload-actions.ts (find VALID_CHART_FIELDS array and confirmUpload function that uses digitalWorkingCopyUrl)
    - src/lib/actions/upload-actions.test.ts (find tests referencing digitalWorkingCopyUrl or confirmUpload for that field)
    - src/__tests__/mocks/factories.ts (find chart factory with digitalWorkingCopyUrl)
    - src/components/features/gallery/project-gallery.test.tsx (grep for digitalWorkingCopyUrl)
    - src/components/features/gallery/gallery-utils.test.ts (grep for digitalWorkingCopyUrl)
    - src/components/features/charts/form-primitives/file-upload.tsx (confirm this is the old single-file component to delete)
  </read_first>
  <action>
    **1. Update src/lib/actions/upload-actions.ts:**
    - Remove `"digitalWorkingCopyUrl"` from the `VALID_CHART_FIELDS` array (if present)
    - If `confirmUpload` had logic specific to `digitalWorkingCopyUrl`, remove that branch
    - Keep all other upload functionality intact (covers, sessions still use this)

    **2. Update src/lib/actions/upload-actions.test.ts:**
    - Remove or update any test that references `digitalWorkingCopyUrl` as a valid chart field
    - Keep tests for other fields (coverImageUrl, coverThumbnailUrl)

    **3. Update src/__tests__/mocks/factories.ts:**
    - Remove `digitalWorkingCopyUrl: null` from the chart factory
    - Add `files: []` to the chart factory default return
    - Ensure the factory shape matches what components now expect

    **4. Update src/components/features/gallery/project-gallery.test.tsx:**
    - Remove `digitalWorkingCopyUrl` from any chart objects in test data
    - Add `files: []` where the chart shape is constructed

    **5. Update src/components/features/gallery/gallery-utils.test.ts:**
    - Same treatment: remove `digitalWorkingCopyUrl`, add `files: []` to chart objects

    **6. Delete src/components/features/charts/form-primitives/file-upload.tsx:**
    - This is the old single-file upload component, replaced by `chart-file-upload.tsx`
    - Verify no remaining imports of this file exist before deletion:
      `grep -rn "from.*file-upload" src/ --include="*.ts" --include="*.tsx" | grep -v chart-file-upload | grep -v node_modules`

    **7. Run full type check** to catch any remaining references:
    ```bash
    npx tsc --noEmit
    ```
    Fix any remaining type errors from the removed field.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "digitalWorkingCopyUrl" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v generated` returns empty (no references outside generated files)
    - src/components/features/charts/form-primitives/file-upload.tsx does NOT exist (deleted)
    - src/__tests__/mocks/factories.ts contains "files:" (not digitalWorkingCopyUrl)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 (full test suite passes)
  </acceptance_criteria>
  <done>All references to digitalWorkingCopyUrl removed from codebase, old component deleted, full test suite green, TypeScript compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL -> Database | Direct SQL modifies production data; must be idempotent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Information Disclosure | Data migration | accept | Migration copies URL strings to ChartFile table; same access controls apply post-migration |
| T-15-13 | Tampering | Schema drop | mitigate | Run data migration BEFORE column drop; verify ChartFile records exist for all non-null URLs via SQL count check |
</threat_model>

<verification>
- `npx prisma validate` passes (schema valid without digitalWorkingCopyUrl)
- `npx tsc --noEmit` passes (no type errors)
- `npx vitest run` passes (all tests green)
- `grep -rn "digitalWorkingCopyUrl" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v generated` returns empty
- Old file-upload.tsx component is deleted
</verification>

<success_criteria>
Clean break from old schema — no stale references, data migrated, tests passing, TypeScript clean.
</success_criteria>

<output>
After completion, create `.planning/phases/15-chart-file-management/15-04-SUMMARY.md`
</output>
