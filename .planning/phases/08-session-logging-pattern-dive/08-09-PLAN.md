---
phase: 08-session-logging-pattern-dive
plan: 09
type: execute
wave: 5
depends_on: ["08-03", "08-04", "08-07", "08-08"]
files_modified: []
autonomous: false
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - SESS-05
  - SESS-06
  - PDIV-01
  - PDIV-02
  - PDIV-03
  - PDIV-04
  - PDIV-05
must_haves:
  truths:
    - "All phase features verified working end-to-end"
    - "Build passes with zero type errors"
    - "All tests pass"
    - "No regressions in existing functionality"
  artifacts: []
  key_links: []
---

<objective>
Run the full test suite, build check, and verify all phase features work end-to-end. This is the integration verification before quality gates.

Purpose: Catch type errors, test failures, and integration issues before running /impeccable:audit and /gsd:verify-work.
Output: Clean build, green tests, verified features.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-session-logging-pattern-dive/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build check</name>
  <files></files>
  <read_first>
    - .planning/phases/08-session-logging-pattern-dive/08-CONTEXT.md (success criteria)
  </read_first>
  <action>
**1. Run the full test suite:**
```bash
npm test
```
All tests must pass. If any fail, fix them before proceeding.

**2. Run the production build:**
```bash
npm run build
```
Must complete with zero type errors. If type errors exist, fix them.

**3. Run Prettier formatting:**
```bash
npm run format
```
Ensure all files are formatted.

**4. Fix any issues** found in steps 1-3 before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (all tests pass)
    - `npm run build` exits 0 (no type errors)
    - `npm run format` exits 0 (all files formatted)
  </acceptance_criteria>
  <done>Full test suite green. Production build clean. Code formatted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of all Phase 8 features</name>
  <what-built>
    Session logging and Pattern Dive features for Phase 8:
    1. LogSessionModal accessible from TopBar "Log Stitches" button
    2. Per-project Sessions tab with mini-stat cards and sortable table
    3. Global /sessions page with chronological log
    4. Pattern Dive tabs: Browse (existing gallery), What's Next, Fabric Requirements, Storage View
    5. Automatic progress recalculation on session CRUD
    6. Conditional read-only stitchesCompleted when sessions exist
  </what-built>
  <action>Human verifies all features per the verification steps below.</action>
  <how-to-verify>
    Start the dev server: `npm run dev`

    **Session Logging (SESS-01 through SESS-06):**
    1. Visit http://localhost:3000 and click the emerald "Log Stitches" button in the TopBar
    2. Verify the modal opens with project picker, date (today), stitch count, time, and photo fields
    3. Select a project, enter stitch count (e.g. 150), optionally add time
    4. Click "Log Stitches" -- verify toast success and modal closes
    5. Navigate to that project's detail page, click Sessions tab
    6. Verify: mini-stat cards show (TOTAL STITCHES, SESSIONS LOGGED, AVG PER SESSION, ACTIVE SINCE)
    7. Verify: session row appears in the table with correct data
    8. Hover over the session row -- verify pencil edit icon appears
    9. Click pencil -- verify modal opens in edit mode with pre-populated fields and "Delete session" link
    10. Edit the stitch count, click "Save Changes" -- verify table updates
    11. Check Overview tab -- stitchesCompleted should show as read-only (not editable), with "Auto-calculated from N session(s)" text
    12. Visit http://localhost:3000/sessions -- verify global sessions page shows the session with project name
    13. Try "Log Session" button on the Sessions page -- verify modal opens with project picker

    **Pattern Dive (PDIV-01 through PDIV-05):**
    14. Check sidebar nav -- verify "Pattern Dive" label (not "Projects")
    15. Visit http://localhost:3000/charts -- verify "Pattern Dive" page header with subtitle
    16. Verify 4 tabs visible: Browse, What's Next, Fabric Requirements, Storage View
    17. Browse tab should show the existing gallery (unchanged)
    18. Click "What's Next" tab -- verify cards show for UNSTARTED/KITTED projects with kitting bars
    19. Click "Fabric Requirements" tab -- verify project list with size calculations and 3" margin info banner
    20. Click "Storage View" tab -- verify collapsible groups by storage location
    21. Switch between tabs -- verify URL updates (?tab=...) and switching is instant (no spinners)
    22. Refresh the page on a non-Browse tab -- verify the correct tab stays selected

    **Cross-cutting:**
    23. Check mobile view (resize to narrow) -- verify TopBar "Log Stitches" shows icon only, tabs show icons only
  </how-to-verify>
  <verify>Human confirms all 23 verification items pass</verify>
  <done>All phase features verified working end-to-end.</done>
  <resume-signal>Type "approved" if all features work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification plan -- no new code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new trust boundaries |
</threat_model>

<verification>
- Full test suite passes
- Production build succeeds
- Human verifies all 23 checkpoint items
</verification>

<success_criteria>
All phase features verified working end-to-end by human tester. Build and tests pass. Ready for /impeccable:audit and /gsd:verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/08-session-logging-pattern-dive/08-09-SUMMARY.md`
</output>
