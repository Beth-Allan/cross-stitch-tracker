---
phase: 28-stats-corrections
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - src/app/(dashboard)/stats/page.tsx
  - src/components/features/stats/status-filter-pills.tsx
  - src/components/features/stats/status-filter-pills.test.tsx
  - src/components/features/stats/stats-overview.tsx
  - src/components/features/stats/stats-overview.test.tsx
  - src/components/features/stats/records-overview.tsx
  - src/components/features/stats/records-overview.test.tsx
  - src/components/features/stats/lifetime-counters.tsx
  - src/components/features/stats/lifetime-counters.test.tsx
autonomous: true
requirements:
  - STAT-01
  - STAT-03
  - STAT-04

must_haves:
  truths:
    - "Overview tab shows thread, designer, and genre insight lists"
    - "Overview tab has status filter pills (All, Not Started, In Progress, Complete)"
    - "Records tab shows only session-based content (personal bests, completions, estimates)"
    - "Records tab has a session stitch total hero stat"
    - "Records tab has no YearScopeToggle or insight lists"
    - "Lifetime counters card shows COLLECTION TOTAL (not TOTAL STITCHES)"
    - "RankedList is not rendered in StatsOverview"
  artifacts:
    - path: "src/components/features/stats/status-filter-pills.tsx"
      provides: "StatusFilterPills client component with nuqs multi-select"
      exports: ["StatusFilterPills"]
    - path: "src/components/features/stats/stats-overview.tsx"
      provides: "Overview tab with insights + status filter pills"
      contains: "StatusFilterPills"
    - path: "src/components/features/stats/records-overview.tsx"
      provides: "Records tab with session hero stat only"
      contains: "STITCHES LOGGED"
    - path: "src/components/features/stats/lifetime-counters.tsx"
      provides: "Lifetime counters with COLLECTION TOTAL label"
      contains: "COLLECTION TOTAL"
    - path: "src/app/(dashboard)/stats/page.tsx"
      provides: "Stats page wiring insights to Overview, session total to Records"
  key_links:
    - from: "src/app/(dashboard)/stats/page.tsx"
      to: "src/components/features/stats/stats-overview.tsx"
      via: "props: threadInsights, designerInsights, genreInsights"
      pattern: "threadInsights="
    - from: "src/app/(dashboard)/stats/page.tsx"
      to: "src/components/features/stats/records-overview.tsx"
      via: "props: totalSessionStitches"
      pattern: "totalSessionStitches="
    - from: "src/components/features/stats/stats-overview.tsx"
      to: "src/components/features/stats/status-filter-pills.tsx"
      via: "JSX render"
      pattern: "StatusFilterPills"
---

<objective>
Restructure the Overview and Records tabs: move insight lists to Overview with status filter pills, simplify Records to session-only content with a hero stat, rename LifetimeCounters label, remove RankedList and YearScopeToggle usage, and rewire page.tsx prop threading.

Purpose: STAT-01 requires insights on the Overview tab (not Records) with status filtering. STAT-03 requires RankedList removal. STAT-04 requires the collection total label change and session total on Records.

Output: StatusFilterPills component, restructured Overview/Records tabs, updated page.tsx wiring.
</objective>

<execution_context>
@/Users/wanderskye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-stats-corrections/28-CONTEXT.md
@.planning/phases/28-stats-corrections/28-RESEARCH.md
@.planning/phases/28-stats-corrections/28-PATTERNS.md
@.planning/phases/28-stats-corrections/28-UI-SPEC.md
@.planning/phases/28-stats-corrections/28-01-SUMMARY.md

<interfaces>
<!-- Types and contracts from Plan 01 output + existing code -->

From src/types/stats.ts (after Plan 01):
```typescript
export interface StatsHeroData {
  stitchesToday: number;
  stitchesThisWeek: number;
  stitchesThisMonth: number;
  stitchesThisYear: number;
  totalLifetimeStitches: number;
  collectionTotalStitches: number;  // NEW from Plan 01
  totalSessions: number;
  totalTimeMinutes: number;
  projectsCompleted: number;
}
export interface ThreadInsight { /* ... */ }
export interface DesignerInsight { /* ... */ }
export interface GenreInsight { /* ... */ }
```

From src/lib/utils/status-groups.ts (created in Plan 01):
```typescript
export const STATUS_GROUPS = ["not-started", "in-progress", "complete"] as const;
export type StatusGroup = (typeof STATUS_GROUPS)[number];
export function resolveStatusFilter(groups: string[]): ProjectStatus[];
```

From src/components/features/stats/stats-overview.tsx (current):
```typescript
interface StatsOverviewProps {
  heroStats: StatsHeroData | null;
  collectionBreakdown: CollectionBreakdownData | null;
  sizeBreakdown: SizeBreakdownItem[] | null;
  designerBreakdown: DesignerBreakdownItem[] | null;
  genreBreakdown: GenreBreakdownItem[] | null;
}
```

From src/components/features/stats/records-overview.tsx (current):
```typescript
interface RecordsOverviewProps {
  personalBests: PersonalBestRecord[] | null;
  fastestCompletions: FastestCompletion[] | null;
  threadInsights: ThreadInsight[] | null;
  designerInsights: DesignerInsight[] | null;
  genreInsights: GenreInsight[] | null;
  completionEstimates: CompletionEstimate[] | null;
  availableYears: number[] | null;
  hasNoSessions: boolean;
}
```

From src/components/features/stats/year-scope-toggle.tsx (pattern reference):
```typescript
export function YearScopeToggle({ availableYears }: YearScopeToggleProps) {
  // uses useQueryState("scope", parseAsString.withDefault("all"))
  // renders div role="group" with aria-pressed buttons
}
```

From src/components/features/stats/lifetime-counters.tsx:
```typescript
interface LifetimeCountersProps {
  totalLifetimeStitches: number;
  totalSessions: number;
  totalTimeMinutes: number;
  projectsCompleted: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StatusFilterPills, restructure StatsOverview with insights and no RankedList</name>
  <files>src/components/features/stats/status-filter-pills.tsx, src/components/features/stats/status-filter-pills.test.tsx, src/components/features/stats/stats-overview.tsx, src/components/features/stats/stats-overview.test.tsx, src/components/features/stats/lifetime-counters.tsx, src/components/features/stats/lifetime-counters.test.tsx</files>
  <read_first>
    - src/components/features/stats/stats-overview.tsx
    - src/components/features/stats/stats-overview.test.tsx
    - src/components/features/stats/year-scope-toggle.tsx
    - src/components/features/stats/year-scope-toggle.test.tsx
    - src/components/features/stats/lifetime-counters.tsx
    - src/components/features/stats/lifetime-counters.test.tsx
    - src/components/features/stats/thread-insight-list.tsx
    - src/components/features/stats/designer-insight-list.tsx
    - src/components/features/stats/genre-insight-list.tsx
    - src/components/features/stats/ranked-list.tsx
    - src/components/features/stats/data-unavailable.tsx
    - src/components/features/gallery/use-gallery-filters.ts
    - .planning/phases/28-stats-corrections/28-UI-SPEC.md
  </read_first>
  <behavior>
    StatusFilterPills:
    - Renders 4 buttons: All, Not Started, In Progress, Complete
    - All pills have type="button" and aria-pressed attribute
    - Container has role="group" aria-label="Filter by status"
    - When no pills toggled, All is active (aria-pressed=true)
    - Clicking "Not Started" toggles it on, deactivates "All"
    - Clicking active pill toggles it off; if none active, "All" reactivates
    - "All" click clears all group selections
    - Uses nuqs useQueryStates with parseAsArrayOf for URL state

    StatsOverview:
    - Renders ThreadInsightList, DesignerInsightList, GenreInsightList (moved from Records)
    - Renders StatusFilterPills above the insight grid
    - Does NOT render RankedList
    - Shows DataUnavailable when insight props are null

    LifetimeCounters:
    - Accepts collectionTotalStitches instead of totalLifetimeStitches
    - Shows label "COLLECTION TOTAL" instead of "TOTAL STITCHES"
  </behavior>
  <action>
    Create src/components/features/stats/status-filter-pills.tsx per D-05, D-06, D-07:
    - "use client" directive
    - Import useQueryState and parseAsArrayOf, parseAsStringLiteral from "nuqs"
    - Import STATUS_GROUPS from @/lib/utils/status-groups
    - useQueryState("status", parseAsArrayOf(parseAsStringLiteral([...STATUS_GROUPS]), ",").withDefault([]))
    - Compute isAllActive = statusFilter.length === 0
    - toggleGroup function: toggle item in/out of array (same pattern as use-gallery-filters.ts toggleStatus)
    - clearAll function: setStatusFilter(null)
    - Render container div with role="group" aria-label="Filter by status" and className matching YearScopeToggle: bg-muted inline-flex gap-1 rounded-xl p-1
    - Render "All" button + 3 group buttons, each with aria-pressed, type="button"
    - Active pill classes: bg-selected text-selected-foreground border-selected-border rounded-lg border px-3 py-1.5 text-sm font-medium shadow-sm
    - Inactive pill classes: text-muted-foreground hover:text-foreground rounded-lg px-3 py-1.5 text-sm font-medium
    - Pill labels: "All", "Not Started", "In Progress", "Complete"

    Create src/components/features/stats/status-filter-pills.test.tsx:
    - Mock nuqs useQueryState with vi.fn()
    - Test all 4 pills render with correct text
    - Test All has aria-pressed=true when statusFilter is []
    - Test clicking "Not Started" calls setStatusFilter
    - Test clicking All calls setStatusFilter(null)
    - Test when statusFilter is ["not-started"], Not Started has aria-pressed=true, All has aria-pressed=false

    Modify src/components/features/stats/stats-overview.tsx per D-01, D-12:
    - Add imports: ThreadInsightList, DesignerInsightList, GenreInsightList, StatusFilterPills, DataUnavailable (already imported)
    - Remove import: RankedList
    - Extend StatsOverviewProps with: threadInsights: ThreadInsight[] | null, designerInsights: DesignerInsight[] | null, genreInsights: GenreInsight[] | null
    - Remove both RankedList JSX blocks from designer and genre cards
    - After the chart grid, add a new section with:
      - flex justify-end wrapper containing StatusFilterPills
      - Grid of 3 insight lists (same layout as current records-overview: grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3)
      - Each insight list wrapped in null check with DataUnavailable fallback

    Update src/components/features/stats/stats-overview.test.tsx:
    - Remove RankedList mock and assertions
    - Add mocks for ThreadInsightList, DesignerInsightList, GenreInsightList, StatusFilterPills
    - Add mock data for threadInsights, designerInsights, genreInsights
    - Update all render calls to include new props
    - Add test: renders insight lists when props provided
    - Add test: shows DataUnavailable when insight props null
    - Add test: renders StatusFilterPills
    - Update "shows DataUnavailable for each null prop" count (was 5, now 8 with 3 insights)

    Modify src/components/features/stats/lifetime-counters.tsx per D-09:
    - Rename prop: totalLifetimeStitches -> collectionTotalStitches in interface and component params
    - Change COUNTER_CARDS first entry: key "collectionTotalStitches", label "COLLECTION TOTAL"

    Update src/components/features/stats/lifetime-counters.test.tsx:
    - Update mock data and assertions to use collectionTotalStitches instead of totalLifetimeStitches
    - Update label assertion to check for "COLLECTION TOTAL"
  </action>
  <verify>
    <automated>npx vitest run src/components/features/stats/status-filter-pills.test.tsx src/components/features/stats/stats-overview.test.tsx src/components/features/stats/lifetime-counters.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - status-filter-pills.tsx has "use client" directive
    - status-filter-pills.tsx renders 4 buttons with aria-pressed attributes
    - status-filter-pills.tsx container has role="group" aria-label="Filter by status"
    - stats-overview.tsx does NOT import RankedList
    - stats-overview.tsx does NOT contain any RankedList JSX
    - stats-overview.tsx renders ThreadInsightList, DesignerInsightList, GenreInsightList
    - stats-overview.tsx renders StatusFilterPills
    - lifetime-counters.tsx COUNTER_CARDS[0].label is "COLLECTION TOTAL"
    - lifetime-counters.tsx COUNTER_CARDS[0].key is "collectionTotalStitches"
    - All 3 test files exit 0
  </acceptance_criteria>
  <done>StatusFilterPills created with multi-select toggle, StatsOverview has insights + pills + no RankedList, LifetimeCounters shows COLLECTION TOTAL, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Simplify RecordsOverview, add session hero stat, rewire page.tsx</name>
  <files>src/components/features/stats/records-overview.tsx, src/components/features/stats/records-overview.test.tsx, src/app/(dashboard)/stats/page.tsx</files>
  <read_first>
    - src/components/features/stats/records-overview.tsx
    - src/app/(dashboard)/stats/page.tsx
    - src/components/features/stats/stats-overview.tsx
    - src/app/(dashboard)/stats/search-params.ts
    - .planning/phases/28-stats-corrections/28-UI-SPEC.md
  </read_first>
  <behavior>
    RecordsOverview:
    - No longer accepts threadInsights, designerInsights, genreInsights, availableYears props
    - Accepts totalSessionStitches: number | null
    - Does not render YearScopeToggle
    - Does not render insight list grid
    - Renders session stitch total hero stat card above personal bests (when totalSessionStitches not null)
    - Hero stat card shows value with toLocaleString(), label "STITCHES LOGGED"
    - Hero stat hidden behind hasNoSessions guard (same as rest of tab)
    - Still renders RecordsTable, CompletionEstimatesSection

    page.tsx:
    - Extracts status from parsedParams
    - Passes status (not scope) to getThreadInsights, getDesignerInsights, getGenreInsights
    - Always passes "all" as scope to getPersonalBests, getFastestCompletions, getCompletionEstimates (D-03)
    - Passes threadInsights, designerInsights, genreInsights to StatsOverview (not RecordsOverview)
    - Passes heroStats.totalLifetimeStitches as totalSessionStitches to RecordsOverview
    - Passes heroStats.collectionTotalStitches as collectionTotalStitches to LifetimeCounters via StatsOverview
    - No longer passes availableYears to RecordsOverview
  </behavior>
  <action>
    Modify src/components/features/stats/records-overview.tsx per D-02, D-03, D-04:
    - Remove imports: YearScopeToggle, ThreadInsightList, DesignerInsightList, GenreInsightList
    - Remove type imports: ThreadInsight, DesignerInsight, GenreInsight
    - Update RecordsOverviewProps: remove threadInsights, designerInsights, genreInsights, availableYears. Add totalSessionStitches: number | null
    - Remove YearScopeToggle JSX block (lines 52-58)
    - Remove insight list grid JSX block (lines 70-86)
    - Add session hero stat section between the hasNoSessions guard and the personal bests card:
      - Conditionally render when totalSessionStitches is not null
      - Card with bg-card ring-foreground/10 rounded-xl p-4 ring-1
      - Label: text-muted-foreground text-xs tracking-wider uppercase — "STITCHES LOGGED"
      - Value: text-foreground mt-1 font-mono text-lg font-semibold tabular-nums — totalSessionStitches.toLocaleString()

    Create src/components/features/stats/records-overview.test.tsx:
    - Mock RecordsTable, CompletionEstimatesSection, DataUnavailable as test stubs
    - Test: renders empty state when hasNoSessions is true (text "No records yet")
    - Test: renders session hero stat with formatted number and "STITCHES LOGGED" label
    - Test: renders RecordsTable when personalBests and fastestCompletions provided
    - Test: renders CompletionEstimatesSection
    - Test: shows DataUnavailable when personalBests null
    - Test: does NOT render YearScopeToggle (no element with scope-related text)
    - Test: does NOT render insight lists (no ThreadInsightList/DesignerInsightList/GenreInsightList testids)
    - Test: hero stat hidden when totalSessionStitches is null

    Modify src/app/(dashboard)/stats/page.tsx:
    - Add status to destructured parsedParams: const { page, sort, dir, project, scope, status } = parsedParams
    - Change insight query calls: getThreadInsights(user.id, status), getDesignerInsights(user.id, status), getGenreInsights(user.id, status) — pass status array instead of scope string
    - For getPersonalBests, getFastestCompletions, getCompletionEstimates: always pass "all" as scope (D-03 removes YearScopeToggle, all Records data is all-time)
    - Move insight props from RecordsOverview to StatsOverview:
      - StatsOverview: add threadInsights={threadInsights} designerInsights={designerInsights} genreInsights={genreInsights}
      - RecordsOverview: remove threadInsights, designerInsights, genreInsights, availableYears props
      - RecordsOverview: add totalSessionStitches={heroStats?.totalLifetimeStitches ?? null}
    - The getAvailableYears query can remain in Promise.allSettled (it may be used elsewhere in the future) but its result is no longer passed to RecordsOverview
  </action>
  <verify>
    <automated>npx vitest run src/components/features/stats/records-overview.test.tsx -x && npx vitest run src/components/features/stats/ -x</automated>
  </verify>
  <acceptance_criteria>
    - records-overview.tsx does NOT import YearScopeToggle, ThreadInsightList, DesignerInsightList, GenreInsightList
    - records-overview.tsx RecordsOverviewProps has totalSessionStitches: number | null
    - records-overview.tsx RecordsOverviewProps does NOT have threadInsights, designerInsights, genreInsights, availableYears
    - records-overview.tsx contains "STITCHES LOGGED" text
    - records-overview.tsx renders hero stat card with toLocaleString()
    - page.tsx passes status (not scope) to getThreadInsights, getDesignerInsights, getGenreInsights
    - page.tsx passes "all" to getPersonalBests, getFastestCompletions, getCompletionEstimates
    - page.tsx StatsOverview JSX has threadInsights= prop
    - page.tsx RecordsOverview JSX has totalSessionStitches= prop
    - page.tsx RecordsOverview JSX does NOT have threadInsights= prop
    - All stats component tests pass
  </acceptance_criteria>
  <done>Records tab simplified to session-only content with hero stat, page.tsx rewired with status filter to insight queries and insight props moved to Overview, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → page.tsx | Status filter array from URL parsed server-side |
| StatusFilterPills → URL | Client component writes status values to URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering | StatusFilterPills URL writes | mitigate | parseAsStringLiteral rejects invalid values client-side; server parseAsStringLiteral double-validates |
| T-28-05 | Information Disclosure | totalSessionStitches prop | accept | Single-user app, data is user's own; no cross-user risk |
</threat_model>

<verification>
npx vitest run src/components/features/stats/ -x
</verification>

<success_criteria>
- StatusFilterPills renders 4 toggle buttons with correct aria attributes
- StatsOverview renders insight lists and StatusFilterPills, no RankedList
- RecordsOverview renders session hero stat, no insights or YearScopeToggle
- LifetimeCounters shows "COLLECTION TOTAL" label
- page.tsx passes status array to insight queries, insights to Overview, session total to Records
- All stats component tests pass
</success_criteria>

<output>
Create `.planning/phases/28-stats-corrections/28-02-SUMMARY.md` when done
</output>
