---
phase: 28-stats-corrections
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/stats/designer-breakdown-chart.tsx
  - src/components/features/stats/designer-breakdown-chart.test.tsx
  - src/components/features/stats/genre-distribution-chart.tsx
  - src/components/features/stats/genre-distribution-chart.test.tsx
  - src/components/features/stats/size-category-chart.tsx
  - src/components/features/stats/size-category-chart.test.tsx
  - src/components/features/dashboard/buried-treasures-section.tsx
  - src/components/features/dashboard/buried-treasures-section.test.tsx
autonomous: true
requirements:
  - STAT-02
  - STAT-05

must_haves:
  truths:
    - "Designer breakdown chart Y-axis shows only integer tick values"
    - "Genre distribution chart Y-axis shows only integer tick values"
    - "Size category chart Y-axis shows only integer tick values"
    - "Days-in-library displays large number with unit label, no number duplication"
  artifacts:
    - path: "src/components/features/stats/designer-breakdown-chart.tsx"
      provides: "Designer chart with allowDecimals={false}"
      contains: "allowDecimals={false}"
    - path: "src/components/features/stats/genre-distribution-chart.tsx"
      provides: "Genre chart with allowDecimals={false}"
      contains: "allowDecimals={false}"
    - path: "src/components/features/stats/size-category-chart.tsx"
      provides: "Size chart with allowDecimals={false}"
      contains: "allowDecimals={false}"
    - path: "src/components/features/dashboard/buried-treasures-section.tsx"
      provides: "Fixed formatAge returning unit only, formatAgeNumber for display"
  key_links:
    - from: "src/components/features/dashboard/buried-treasures-section.tsx"
      to: "template rendering"
      via: "formatAge returns unit string, formatAgeNumber returns display number"
      pattern: "formatAge"
---

<objective>
Fix chart axis formatting to show integer-only ticks and fix the days-in-library number duplication bug on the dashboard.

Purpose: STAT-02 requires charts to show clean integer axis values (no 0.5, 1.5). STAT-05 requires days-in-library to display as a large number with a small descriptive label without repeating the number.

Output: 3 chart components with allowDecimals={false}, fixed formatAge function and template.
</objective>

<execution_context>
@/Users/wanderskye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-stats-corrections/28-CONTEXT.md
@.planning/phases/28-stats-corrections/28-RESEARCH.md
@.planning/phases/28-stats-corrections/28-PATTERNS.md
@.planning/phases/28-stats-corrections/28-UI-SPEC.md

<interfaces>
<!-- Chart components from existing code -->

From src/components/features/stats/designer-breakdown-chart.tsx:
```typescript
// XAxis type="number" at line 24 — needs allowDecimals={false}
<XAxis type="number" tickLine={false} axisLine={false} />
```

From src/components/features/stats/genre-distribution-chart.tsx:
```typescript
// XAxis type="number" at line 24 — needs allowDecimals={false}
<XAxis type="number" tickLine={false} axisLine={false} />
```

From src/components/features/stats/size-category-chart.tsx:
```typescript
// YAxis type="number" at line 27 — needs allowDecimals={false}
<YAxis type="number" tickLine={false} axisLine={false} />
```

From src/components/features/dashboard/buried-treasures-section.tsx:
```typescript
function formatAge(days: number): string {
  if (days < 30) return `${days} days`;
  if (days < 365) return `${Math.floor(days / 30)} months`;
  return `${Math.floor(days / 365)} years`;
}
// Template at line 79: {t.daysInLibrary.toLocaleString()}
// Template at line 82: {formatAge(t.daysInLibrary)} in library
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add allowDecimals={false} to chart axes and create tests</name>
  <files>src/components/features/stats/designer-breakdown-chart.tsx, src/components/features/stats/designer-breakdown-chart.test.tsx, src/components/features/stats/genre-distribution-chart.tsx, src/components/features/stats/genre-distribution-chart.test.tsx, src/components/features/stats/size-category-chart.tsx, src/components/features/stats/size-category-chart.test.tsx</files>
  <read_first>
    - src/components/features/stats/designer-breakdown-chart.tsx
    - src/components/features/stats/designer-breakdown-chart.test.tsx
    - src/components/features/stats/genre-distribution-chart.tsx
    - src/components/features/stats/genre-distribution-chart.test.tsx
    - src/components/features/stats/size-category-chart.tsx
    - src/components/features/stats/size-category-chart.test.tsx
  </read_first>
  <behavior>
    - DesignerBreakdownChart XAxis has allowDecimals={false}
    - GenreDistributionChart XAxis has allowDecimals={false}
    - SizeCategoryChart YAxis has allowDecimals={false}
    - Existing chart tests still pass
    - New test verifies allowDecimals prop is present
  </behavior>
  <action>
    Modify src/components/features/stats/designer-breakdown-chart.tsx per D-11:
    - Add allowDecimals={false} to the XAxis element (the numeric count axis on horizontal bar charts)

    Modify src/components/features/stats/genre-distribution-chart.tsx per D-11:
    - Add allowDecimals={false} to the XAxis element

    Modify src/components/features/stats/size-category-chart.tsx per D-11:
    - Add allowDecimals={false} to the YAxis element (the numeric count axis on vertical bar charts)

    Update each chart's test file:
    - Add a test that verifies the chart renders with the allowDecimals prop. Since Recharts renders SVG, the simplest approach is to verify the component renders without error with small integer data (e.g., data with counts of 1, 2, 3) and check that the rendered output contains expected text. If existing tests already mock Recharts at the component level, add a snapshot or structural assertion that the allowDecimals prop is passed. Alternatively, use a grep-gate acceptance criterion since this is a single-prop addition.
  </action>
  <verify>
    <automated>npx vitest run src/components/features/stats/designer-breakdown-chart.test.tsx src/components/features/stats/genre-distribution-chart.test.tsx src/components/features/stats/size-category-chart.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - designer-breakdown-chart.tsx contains allowDecimals={false} on XAxis
    - genre-distribution-chart.tsx contains allowDecimals={false} on XAxis
    - size-category-chart.tsx contains allowDecimals={false} on YAxis
    - grep -c "allowDecimals={false}" on each file returns at least 1
    - All 3 chart test files exit 0
  </acceptance_criteria>
  <done>All 3 collection breakdown charts have integer-only axis ticks via allowDecimals={false}, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix formatAge number duplication in Buried Treasures section</name>
  <files>src/components/features/dashboard/buried-treasures-section.tsx, src/components/features/dashboard/buried-treasures-section.test.tsx</files>
  <read_first>
    - src/components/features/dashboard/buried-treasures-section.tsx
    - src/types/dashboard.ts
  </read_first>
  <behavior>
    - formatAge(15) returns "days" (not "15 days")
    - formatAge(29) returns "days"
    - formatAge(30) returns "months"
    - formatAge(200) returns "months"
    - formatAge(365) returns "years"
    - formatAge(730) returns "years"
    - formatAgeNumber(15) returns 15
    - formatAgeNumber(29) returns 29
    - formatAgeNumber(30) returns 1
    - formatAgeNumber(200) returns 6
    - formatAgeNumber(365) returns 1
    - formatAgeNumber(730) returns 2
    - Template renders: large number line (formatAgeNumber result), then "{formatAge result} in library" label line
    - For 23 days: displays "23" then "days in library" (no duplication)
    - For 200 days: displays "6" then "months in library"
    - For 400 days: displays "1" then "years in library"
  </behavior>
  <action>
    Modify src/components/features/dashboard/buried-treasures-section.tsx per D-14:
    - Change formatAge to return only the unit string:
      - days < 30: return "days"
      - days < 365: return "months"
      - days >= 365: return "years"
    - Add formatAgeNumber function that returns the display number:
      - days < 30: return days
      - days < 365: return Math.floor(days / 30)
      - days >= 365: return Math.floor(days / 365)
    - Update the age badge template (around line 77-84):
      - Line 79: change from {t.daysInLibrary.toLocaleString()} to {formatAgeNumber(t.daysInLibrary).toLocaleString()}
      - Line 82: stays as {formatAge(t.daysInLibrary)} in library (already correct after formatAge change)

    Create src/components/features/dashboard/buried-treasures-section.test.tsx:
    - Import render, screen from @/__tests__/test-utils
    - Mock @/components/features/gallery/cover-placeholder and ./section-heading as test stubs
    - Mock @/lib/utils/focal-point as stub returning empty style
    - Create mock BuriedTreasure data with various daysInLibrary values
    - Test: item with 23 daysInLibrary shows "23" number and "days in library" text (not "23 days in library")
    - Test: item with 200 daysInLibrary shows "6" number and "months in library" text
    - Test: item with 400 daysInLibrary shows "1" number and "years in library" text
    - Test: renders null when treasures array is empty
    - Test: renders correct number of treasure items
  </action>
  <verify>
    <automated>npx vitest run src/components/features/dashboard/buried-treasures-section.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - formatAge(23) returns "days" (not "23 days")
    - formatAge(200) returns "months" (not "6 months")
    - formatAge(400) returns "years" (not "1 years")
    - formatAgeNumber(23) returns 23
    - formatAgeNumber(200) returns 6
    - formatAgeNumber(400) returns 1
    - Template line 79 uses formatAgeNumber(t.daysInLibrary).toLocaleString()
    - Template line 82 uses formatAge(t.daysInLibrary) followed by " in library"
    - buried-treasures-section.test.tsx exits 0
  </acceptance_criteria>
  <done>Days-in-library displays correctly: large number + "unit in library" label, no number duplication, tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No trust boundaries crossed — pure presentation changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06 | (none) | chart prop + formatAge | accept | Pure presentation changes with no security surface |
</threat_model>

<verification>
npx vitest run src/components/features/stats/designer-breakdown-chart.test.tsx src/components/features/stats/genre-distribution-chart.test.tsx src/components/features/stats/size-category-chart.test.tsx src/components/features/dashboard/buried-treasures-section.test.tsx -x
</verification>

<success_criteria>
- All 3 chart components have allowDecimals={false} on their numeric axis
- formatAge returns unit string only (no number prefix)
- formatAgeNumber returns the appropriate display number for the unit
- Template composes number + unit without duplication
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/28-stats-corrections/28-03-SUMMARY.md` when done
</output>
