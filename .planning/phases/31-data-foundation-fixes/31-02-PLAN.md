---
phase: 31-data-foundation-fixes
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/series-progress.ts
  - src/lib/utils/series-progress.test.ts
autonomous: true
requirements: [SERIES-10]
must_haves:
  truths:
    - "computeSeriesProgress returns correct owned count (number of charts in array)"
    - "computeSeriesProgress returns correct finished count (charts with FINISHED or FFO project status per D-01)"
    - "computeSeriesProgress returns totalCount passthrough (null or positive int per D-03/D-04)"
    - "computeSeriesProgress handles zero charts gracefully (all counts = 0)"
    - "computeSeriesProgress handles charts with null project (counted as owned but not finished)"
  artifacts:
    - path: "src/lib/utils/series-progress.ts"
      provides: "Pure computation utility for series dual progress"
      exports: ["computeSeriesProgress", "FINISHED_STATUSES"]
      min_lines: 15
    - path: "src/lib/utils/series-progress.test.ts"
      provides: "Tests for series progress computation"
      min_lines: 40
  key_links:
    - from: "src/lib/utils/series-progress.ts"
      to: "src/types/series.ts"
      via: "Returns SeriesProgress type"
      pattern: "SeriesProgress"
---

<objective>
Implement the dual progress computation utility as a pure function with comprehensive tests.

Purpose: Series progress (owned/total + finished/owned per D-01 through D-04) is the core
novel logic in this phase. TDD ensures all edge cases are handled before CRUD actions consume it.

Output: Tested utility function that downstream getSeriesWithStats/getSeriesDetail queries will call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-data-foundation-fixes/31-CONTEXT.md
@.planning/phases/31-data-foundation-fixes/31-PATTERNS.md

<interfaces>
From src/types/series.ts (created in Plan 01, but this plan only needs ProjectStatus):
```typescript
export type SeriesProgress = {
  ownedCount: number;
  finishedCount: number;
  totalCount: number | null;
};
```

From src/generated/prisma/client (already exists):
```typescript
enum ProjectStatus {
  UNSTARTED, KITTING, KITTED, IN_PROGRESS, ON_HOLD, FINISHED, FFO
}
```
</interfaces>
</context>

<feature>
  <name>computeSeriesProgress</name>
  <files>src/lib/utils/series-progress.ts, src/lib/utils/series-progress.test.ts</files>
  <behavior>
    Cases:
    - Empty charts array, totalCount null -> { ownedCount: 0, finishedCount: 0, totalCount: null }
    - Empty charts array, totalCount 10 -> { ownedCount: 0, finishedCount: 0, totalCount: 10 }
    - 3 charts (FINISHED, FFO, IN_PROGRESS), totalCount null -> { ownedCount: 3, finishedCount: 2, totalCount: null }
    - 3 charts (FINISHED, FFO, IN_PROGRESS), totalCount 5 -> { ownedCount: 3, finishedCount: 2, totalCount: 5 }
    - 2 charts where one has null project (no Project created yet), totalCount null -> { ownedCount: 2, finishedCount: 0, totalCount: null }
    - 1 chart with UNSTARTED project -> { ownedCount: 1, finishedCount: 0, totalCount: null }
    - 4 charts (FINISHED, FFO, FINISHED, KITTING), totalCount 12 -> { ownedCount: 4, finishedCount: 3, totalCount: 12 }
  </behavior>
  <implementation>
    Define FINISHED_STATUSES as Set containing "FINISHED" and "FFO" per D-01.
    Export computeSeriesProgress accepting charts array (each with { project: { status: string } | null }) and totalCount (number | null).
    Return SeriesProgress: ownedCount = charts.length, finishedCount = count of charts where project is not null AND project.status is in FINISHED_STATUSES.
    Import SeriesProgress type from @/types/series. No other dependencies.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Write failing tests for computeSeriesProgress</name>
  <files>src/lib/utils/series-progress.test.ts</files>
  <read_first>
    - src/lib/utils/status-groups.test.ts (analog — pure utility test pattern)
    - .planning/phases/31-data-foundation-fixes/31-PATTERNS.md (series-progress.test.ts section)
  </read_first>
  <action>
    Create src/lib/utils/series-progress.test.ts with describe("computeSeriesProgress") containing 7 test cases from the behavior block above. Import computeSeriesProgress and FINISHED_STATUSES from ./series-progress. Each test constructs a charts array with { project: { status: "STATUS" } | null } shape and asserts the returned SeriesProgress object. Also add a describe("FINISHED_STATUSES") block testing that the set contains exactly "FINISHED" and "FFO" (per D-01) and has size 2.
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils/series-progress.test.ts 2>&1 | grep -q "FAIL" && echo "RED: Tests fail as expected"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/utils/series-progress.test.ts exists with 7+ test cases
    - Tests import from ./series-progress (module does not exist yet)
    - Running the test file produces failures (RED state)
  </acceptance_criteria>
  <done>Failing tests define the expected behavior of computeSeriesProgress</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - Implement computeSeriesProgress to pass all tests</name>
  <files>src/lib/utils/series-progress.ts</files>
  <read_first>
    - src/lib/utils/series-progress.test.ts (just created — understand expected interface)
    - src/lib/utils/status-groups.ts (analog — pure utility with status constants)
    - src/types/series.ts (SeriesProgress type — may not exist yet if Plan 01 hasn't run; define inline if needed)
  </read_first>
  <action>
    Create src/lib/utils/series-progress.ts. Export const FINISHED_STATUSES = new Set(["FINISHED", "FFO"]). Export function computeSeriesProgress taking charts: Array<{ project: { status: string } | null }> and totalCount: number | null, returning SeriesProgress (import from @/types/series if available, otherwise define inline type and re-export). ownedCount = charts.length. finishedCount = charts.filter where project is not null and FINISHED_STATUSES.has(project.status). Return { ownedCount, finishedCount, totalCount }.
    
    Note: If src/types/series.ts does not yet exist (Plan 01 running in parallel), define the SeriesProgress type locally in this file and export it. Plan 01's types file will be the canonical location; a later reconciliation during CRUD action implementation (Plan 03) will normalize imports.
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils/series-progress.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/utils/series-progress.ts exports computeSeriesProgress and FINISHED_STATUSES
    - All 7+ tests in series-progress.test.ts pass (GREEN state)
    - npx tsc --noEmit exits 0 (or only errors are from Plan 01 files not yet created)
  </acceptance_criteria>
  <done>computeSeriesProgress implemented, all tests pass, function handles all edge cases per D-01 through D-04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure utility with no I/O — no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Information Disclosure | computeSeriesProgress | accept | Pure function operating on already-authenticated query results; no user input enters directly |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- npx vitest run src/lib/utils/series-progress.test.ts passes all cases
- FINISHED_STATUSES contains exactly "FINISHED" and "FFO"
- Zero charts returns all counts as 0
- Null project on a chart counts as owned but not finished
</verification>

<success_criteria>
- computeSeriesProgress correctly computes dual progress for all 7 test scenarios
- FINISHED_STATUSES exports the set {"FINISHED", "FFO"} per D-01
- Function is pure (no side effects, no async, no imports beyond types)
- All tests pass in under 2 seconds
</success_criteria>

<output>
Create `.planning/phases/31-data-foundation-fixes/31-02-SUMMARY.md` when done
</output>
