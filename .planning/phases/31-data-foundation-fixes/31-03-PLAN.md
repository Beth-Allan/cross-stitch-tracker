---
phase: 31-data-foundation-fixes
plan: 03
type: tdd
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - src/lib/actions/series-actions.ts
  - src/lib/actions/series-actions.test.ts
autonomous: true
requirements: [SERIES-01, SERIES-03, SERIES-04, FIX-01, FIX-02]
must_haves:
  truths:
    - "createSeries validates input, creates Series in database, revalidates /series path"
    - "createSeries returns friendly error on duplicate name (P2002)"
    - "updateSeries validates input, updates Series by ID, revalidates /series and /series/:id"
    - "deleteSeries unlinks charts (sets seriesId=null) then deletes series in a transaction"
    - "getSeriesWithStats returns all series with progress computed via computeSeriesProgress"
    - "All actions reject unauthenticated calls via requireAuth()"
    - "D-10: FIX-01 (999.19) verified resolved — tsc --noEmit reports 0 errors across entire codebase"
    - "D-11: FIX-02 (999.22) verified resolved — stats page uses Promise.allSettled for query resilience"
    - "D-12: Both fixes require verification only — no new implementation needed"
  artifacts:
    - path: "src/lib/actions/series-actions.ts"
      provides: "Series CRUD server actions"
      exports: ["createSeries", "updateSeries", "deleteSeries", "getSeriesWithStats"]
      min_lines: 80
    - path: "src/lib/actions/series-actions.test.ts"
      provides: "Tests for all CRUD actions"
      min_lines: 150
  key_links:
    - from: "src/lib/actions/series-actions.ts"
      to: "src/lib/utils/series-progress.ts"
      via: "computeSeriesProgress called in getSeriesWithStats"
      pattern: "computeSeriesProgress"
    - from: "src/lib/actions/series-actions.ts"
      to: "src/lib/validations/series.ts"
      via: "seriesSchema.parse(formData)"
      pattern: "seriesSchema"
    - from: "src/lib/actions/series-actions.ts"
      to: "prisma.series"
      via: "Prisma CRUD operations"
      pattern: "prisma\\.series\\.(create|update|delete|findMany)"
---

<objective>
Implement Series CRUD server actions following the established Designer pattern, with
comprehensive tests covering auth, validation, happy paths, and error cases. Also verify
and close FIX-01 and FIX-02.

Purpose: Complete the Series data layer so Phases 32-34 can build UI against working,
tested server actions. Close pre-existing bug fix requirements.

Output: Fully tested series-actions.ts with create, update, delete, and getWithStats functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-data-foundation-fixes/31-CONTEXT.md
@.planning/phases/31-data-foundation-fixes/31-PATTERNS.md
@.planning/phases/31-data-foundation-fixes/31-01-SUMMARY.md
@.planning/phases/31-data-foundation-fixes/31-02-SUMMARY.md

<interfaces>
From src/lib/validations/series.ts (created in Plan 01):
```typescript
export const seriesSchema = z.object({
  name: z.string().trim().min(1).max(200),
  totalCount: z.number().int().min(1).nullable().default(null),
  designerId: z.string().nullable().default(null),
  notes: z.string().max(5000).nullable().default(null),
});
export type SeriesInput = z.infer<typeof seriesSchema>;
```

From src/types/series.ts (created in Plan 01):
```typescript
export type SeriesProgress = { ownedCount: number; finishedCount: number; totalCount: number | null };
export type SeriesWithStats = { id, name, totalCount, designerId, designerName, notes, progress: SeriesProgress };
```

From src/lib/utils/series-progress.ts (created in Plan 02):
```typescript
export function computeSeriesProgress(
  charts: Array<{ project: { status: string } | null }>,
  totalCount: number | null,
): SeriesProgress;
```

From src/__tests__/mocks/factories.ts (updated in Plan 01):
```typescript
export function createMockSeries(overrides?: Partial<Series>): Series;
// createMockPrisma().series: { create, findMany, findUnique, update, delete }
```
</interfaces>
</context>

<feature>
  <name>Series CRUD Actions</name>
  <files>src/lib/actions/series-actions.ts, src/lib/actions/series-actions.test.ts</files>
  <behavior>
    Auth:
    - createSeries rejects unauthenticated (throws "Unauthorized")
    - updateSeries rejects unauthenticated
    - deleteSeries rejects unauthenticated
    - getSeriesWithStats rejects unauthenticated

    createSeries:
    - Valid input returns { success: true, series } and calls prisma.series.create
    - Empty name returns { success: false, error: "Series name is required" }
    - Duplicate name (P2002) returns { success: false, error: "A series with that name already exists" }
    - Unexpected error returns { success: false, error: "Failed to create series" }

    updateSeries:
    - Valid input returns { success: true, series } and calls prisma.series.update with where: { id }
    - Duplicate name (P2002) returns { success: false, error: "A series with that name already exists" }
    - Revalidates both /series and /series/{id}

    deleteSeries:
    - Existing series calls $transaction([updateMany seriesId->null, delete]) and returns { success: true }
    - Non-existent ID returns { success: false, error: "Series not found" }
    - Revalidates /series and /charts

    getSeriesWithStats:
    - Returns mapped array with designerName and progress from computeSeriesProgress
  </behavior>
  <implementation>
    Follow designer-actions.ts pattern exactly. Import seriesSchema from @/lib/validations/series.
    Import computeSeriesProgress from @/lib/utils/series-progress. Import SeriesWithStats from @/types/series.
    getSeriesWithStats query includes { charts: { select: { project: { select: { status: true } } } }, designer: { select: { name: true } }, _count: { select: { charts: true } } }.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Write failing tests for series CRUD actions</name>
  <files>src/lib/actions/series-actions.test.ts</files>
  <read_first>
    - src/lib/actions/designer-actions.test.ts (exact analog — full test structure)
    - src/__tests__/mocks/factories.ts (createMockSeries factory, createMockPrisma with series block)
    - src/__tests__/mocks/index.ts (verify exports)
    - .planning/phases/31-data-foundation-fixes/31-PATTERNS.md (test patterns section)
  </read_first>
  <action>
    Create src/lib/actions/series-actions.test.ts following the designer-actions.test.ts structure exactly.
    
    Setup: vi.mock @/lib/auth (mockAuth), vi.mock @/lib/db (mockPrisma from createMockPrisma), vi.mock next/cache (revalidatePath). vi.mock @/lib/utils/series-progress with mockComputeSeriesProgress vi.fn(). beforeEach: clearAllMocks, mockAuth returns { user: { id: "user-1", name: "Test", email: "test@test.com" } }.
    
    Test groups:
    1. "auth guard" — 4 tests (one per action) using mockAuth.mockResolvedValueOnce(null), expect rejects with "Unauthorized". Use dynamic import pattern: const { createSeries } = await import("./series-actions").
    2. "createSeries" — happy path (returns success + series), validation error (empty name), P2002 duplicate (Object.assign(new Error("Unique"), { code: "P2002" })), generic error.
    3. "updateSeries" — happy path (id + formData), P2002, revalidation of /series and /series/{id}.
    4. "deleteSeries" — happy path ($transaction called with updateMany + delete), not found (findUnique returns null), revalidation of /series and /charts.
    5. "getSeriesWithStats" — returns mapped results with computeSeriesProgress applied to each series' charts.
    
    Use assertSuccess/assertFailure from @/__tests__/mocks. Use createMockSeries for mock return values.
  </action>
  <verify>
    <automated>npx vitest run src/lib/actions/series-actions.test.ts 2>&1 | grep -q "FAIL" && echo "RED: Tests fail as expected"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/actions/series-actions.test.ts exists with 12+ test cases across 5 describe groups
    - Tests use dynamic import pattern for module isolation
    - Tests use createMockSeries and mockPrisma.series methods
    - Running tests produces failures (series-actions.ts does not exist yet)
  </acceptance_criteria>
  <done>Comprehensive failing tests define the complete CRUD behavior contract</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - Implement series CRUD actions to pass all tests</name>
  <files>src/lib/actions/series-actions.ts</files>
  <read_first>
    - src/lib/actions/series-actions.test.ts (just created — understand expected interface)
    - src/lib/actions/designer-actions.ts (exact analog — full CRUD implementation)
    - src/lib/validations/series.ts (seriesSchema import)
    - src/lib/utils/series-progress.ts (computeSeriesProgress import)
    - src/types/series.ts (SeriesWithStats type import)
  </read_first>
  <action>
    Create src/lib/actions/series-actions.ts with "use server" directive. Import revalidatePath from next/cache, z from zod, requireAuth from @/lib/auth-guard, prisma from @/lib/db, seriesSchema from @/lib/validations/series, computeSeriesProgress from @/lib/utils/series-progress, SeriesWithStats from @/types/series.
    
    Implement 4 exported functions:
    
    1. createSeries(formData: unknown): requireAuth, try/catch with seriesSchema.parse, prisma.series.create({ data: validated }), revalidatePath("/series"), return { success: true as const, series }. Catch: ZodError -> first message, P2002 -> "A series with that name already exists", generic -> console.error + "Failed to create series".
    
    2. updateSeries(id: string, formData: unknown): requireAuth, same validation/error pattern, prisma.series.update({ where: { id }, data: validated }), revalidatePath("/series") + revalidatePath("/series/" + id). P2002 message same as create.
    
    3. deleteSeries(id: string): requireAuth, findUnique({ where: { id } }) — if null return "Series not found". prisma.$transaction([updateMany({ where: { seriesId: id }, data: { seriesId: null } }), prisma.series.delete({ where: { id } })]). revalidatePath("/series") + revalidatePath("/charts"). Return success.
    
    4. getSeriesWithStats(): Promise<SeriesWithStats[]>: requireAuth. prisma.series.findMany({ include: { charts: { select: { project: { select: { status: true } } } }, designer: { select: { name: true } } }, orderBy: { name: "asc" } }). Map each to SeriesWithStats: spread id/name/totalCount/designerId/notes, designerName: s.designer?.name ?? null, progress: computeSeriesProgress(s.charts, s.totalCount).
  </action>
  <verify>
    <automated>npx vitest run src/lib/actions/series-actions.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/actions/series-actions.ts starts with "use server"
    - Exports createSeries, updateSeries, deleteSeries, getSeriesWithStats
    - All 12+ tests pass (GREEN state)
    - deleteSeries uses $transaction with updateMany (seriesId: null) + delete per D-04
    - getSeriesWithStats calls computeSeriesProgress for each series
    - All actions call requireAuth() as first operation
  </acceptance_criteria>
  <done>Series CRUD actions implemented, all tests pass, follows established Designer pattern</done>
</task>

<task type="auto">
  <name>Task 3: Verify and close FIX-01 and FIX-02</name>
  <files></files>
  <read_first>
    - src/app/(dashboard)/stats/page.tsx (verify Promise.allSettled usage for FIX-02)
    - src/lib/utils/settled.ts (verify settled utility exists for FIX-02)
  </read_first>
  <action>
    Per D-10, D-11, D-12: both fixes are already resolved. Run verification commands to confirm:
    
    FIX-01 verification: Run npx tsc --noEmit. Expect 0 errors. This confirms the 3 test files (dashboard-tabs.test.tsx, chart-actions.test.ts, shopping-cart-actions.test.ts) no longer have TypeScript errors.
    
    FIX-02 verification: Confirm src/app/(dashboard)/stats/page.tsx uses Promise.allSettled (not Promise.all) for its parallel query groups. Confirm settled.ts utility exists and is imported.
    
    Run full test suite to confirm overall health after Plan 01 and Plan 02 changes.
    
    No code changes needed — this task documents the verification evidence for requirement closure.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Promise.allSettled" src/app/\(dashboard\)/stats/page.tsx && npm test -- --reporter=verbose 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 (FIX-01 confirmed resolved)
    - grep finds "Promise.allSettled" in stats/page.tsx (FIX-02 confirmed resolved)
    - Full test suite passes (overall phase health)
  </acceptance_criteria>
  <done>FIX-01 and FIX-02 verified as resolved, requirements can be marked complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> createSeries | Untrusted form data enters server action |
| client -> updateSeries | Untrusted form data + entity ID enters server action |
| client -> deleteSeries | Entity ID from client (could be forged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Spoofing | All CRUD actions | mitigate | requireAuth() as first line of every action; throws "Unauthorized" if no session |
| T-31-05 | Tampering | createSeries/updateSeries | mitigate | Zod seriesSchema.parse() validates all input; .trim().min(1).max(200) on name |
| T-31-06 | Tampering | deleteSeries ID param | accept | Single-user app; no ownership model on reference entities (matches Designer pattern) |
| T-31-07 | Repudiation | CRUD mutations | accept | No audit log requirement; single-user app with low repudiation risk |
| T-31-08 | DoS | Large notes field | mitigate | Zod .max(5000) on notes field |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- npx vitest run src/lib/actions/series-actions.test.ts passes all 12+ tests
- npx tsc --noEmit exits 0 (FIX-01)
- grep confirms Promise.allSettled in stats page (FIX-02)
- npm test full suite passes
- Series actions follow identical error handling pattern to designer-actions.ts
</verification>

<success_criteria>
- createSeries, updateSeries, deleteSeries, getSeriesWithStats all implemented and tested
- Auth guard verified on all 4 actions
- P2002 unique constraint handled with user-friendly message
- deleteSeries uses atomic $transaction to unlink charts before delete
- getSeriesWithStats integrates computeSeriesProgress for dual progress
- FIX-01 confirmed: TypeScript compiles with 0 errors
- FIX-02 confirmed: Stats page uses Promise.allSettled
- Full test suite green (2283+ tests)
</success_criteria>

<output>
Create `.planning/phases/31-data-foundation-fixes/31-03-SUMMARY.md` when done
</output>
