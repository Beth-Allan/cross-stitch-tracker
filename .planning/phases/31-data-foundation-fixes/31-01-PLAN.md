---
phase: 31-data-foundation-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/validations/series.ts
  - src/types/series.ts
  - src/__tests__/mocks/factories.ts
autonomous: true
requirements: [SERIES-01, SERIES-03, SERIES-04, SERIES-10]
must_haves:
  truths:
    - "Series model exists in Prisma schema with name, totalCount, designerId, notes, charts relation"
    - "D-05: Series has nullable FK to Designer (designerId String?) as entity property"
    - "D-06: No enforcement that charts' designers match series designer"
    - "D-07: Designer assignment is manual only (no auto-populate from charts)"
    - "D-08: Series name has @unique constraint (globally unique)"
    - "D-09: Series includes optional notes field (String?)"
    - "Chart model has optional seriesId FK to Series"
    - "Designer model has series relation array"
    - "SeriesProgress type defines ownedCount, finishedCount, totalCount shape"
    - "Zod seriesSchema validates name (trimmed, min 1), totalCount (nullable int), designerId (nullable), notes (nullable)"
    - "Test factories include createMockSeries and series on mock Prisma client"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Series model definition"
      contains: "model Series"
    - path: "src/lib/validations/series.ts"
      provides: "Zod validation schema for series input"
      exports: ["seriesSchema", "SeriesInput"]
    - path: "src/types/series.ts"
      provides: "Series type definitions with dual progress shape"
      exports: ["SeriesProgress", "SeriesWithStats", "SeriesDetail", "SeriesChart"]
    - path: "src/__tests__/mocks/factories.ts"
      provides: "createMockSeries factory and mock Prisma series methods"
      contains: "createMockSeries"
  key_links:
    - from: "prisma/schema.prisma"
      to: "src/types/series.ts"
      via: "Prisma types imported in series types"
      pattern: "ProjectStatus"
    - from: "src/__tests__/mocks/factories.ts"
      to: "src/types/series.ts"
      via: "Factory returns Series type"
      pattern: "createMockSeries"
---

<objective>
Establish the Series data model, type contracts, validation schema, and test infrastructure
that all subsequent Phase 31 and 32-34 work depends on.

Purpose: Define the foundational contracts (schema, types, validation, mocks) so that CRUD
actions and progress utilities can be built against stable interfaces.

Output: Prisma schema with Series model, TypeScript types, Zod schema, and updated test factories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-data-foundation-fixes/31-CONTEXT.md
@.planning/phases/31-data-foundation-fixes/31-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Series Prisma schema, types, and validation</name>
  <files>prisma/schema.prisma, src/types/series.ts, src/lib/validations/series.ts</files>
  <read_first>
    - prisma/schema.prisma (current state — Designer model at lines 23-31, Chart model at lines 41-65)
    - src/types/designer.ts (analog for type definitions)
    - src/lib/validations/chart.ts (analog for designerSchema at lines 107-113)
    - .planning/phases/31-data-foundation-fixes/31-PATTERNS.md (pattern assignments)
  </read_first>
  <behavior>
    - Test 1: seriesSchema parses valid input { name: "Frosted Pumpkins", totalCount: 12, designerId: "d1", notes: "Monthly" }
    - Test 2: seriesSchema rejects empty name (after trim) with "Series name is required"
    - Test 3: seriesSchema rejects totalCount of 0 with "Total count must be at least 1"
    - Test 4: seriesSchema accepts null totalCount, null designerId, null notes
    - Test 5: seriesSchema rejects name longer than 200 chars
    - Test 6: seriesSchema rejects notes longer than 5000 chars
  </behavior>
  <action>
    1. Add Series model to prisma/schema.prisma after Designer model (line 31). Fields: id (cuid), name (@unique), totalCount (Int?), designer (Designer? relation via designerId), designerId (String?), notes (String?), charts (Chart[]), createdAt, updatedAt. Per D-05, D-08, D-09.
    2. Add to Chart model (after genres line ~54): series (Series? @relation fields: [seriesId] references: [id]), seriesId (String?).
    3. Add to Designer model (after charts line ~28): series (Series[]).
    4. Run npx prisma generate to update the generated client types.
    5. Create src/types/series.ts exporting: SeriesProgress { ownedCount: number; finishedCount: number; totalCount: number | null }, SeriesWithStats { id, name, totalCount, designerId, designerName (string|null), notes, progress: SeriesProgress }, SeriesDetail extends SeriesWithStats with charts: SeriesChart[], SeriesChart { id, name, coverThumbnailUrl, status: ProjectStatus|null, stitchesCompleted, stitchCount }. Import ProjectStatus from @/generated/prisma/client.
    6. Create src/lib/validations/series.test.ts with the behavior tests above (RED — tests fail because seriesSchema does not exist yet).
    7. Create src/lib/validations/series.ts with seriesSchema (z.object): name z.string().trim().min(1, "Series name is required").max(200, "Series name too long"), totalCount z.number().int().min(1, "Total count must be at least 1").nullable().default(null), designerId z.string().nullable().default(null), notes z.string().max(5000, "Notes too long").nullable().default(null). Export SeriesInput = z.infer<typeof seriesSchema>. (GREEN — tests now pass.)
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/series.test.ts -x && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains "model Series" with @unique on name field
    - prisma/schema.prisma Chart model contains "seriesId String?"
    - prisma/schema.prisma Designer model contains "series Series[]"
    - src/types/series.ts exports SeriesProgress, SeriesWithStats, SeriesDetail, SeriesChart types
    - src/lib/validations/series.ts exports seriesSchema and SeriesInput
    - src/lib/validations/series.test.ts passes all 6 behavior tests
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Series schema defined, types exported, Zod validation schema created with passing tests, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Test factory and mock Prisma updates</name>
  <files>src/__tests__/mocks/factories.ts</files>
  <read_first>
    - src/__tests__/mocks/factories.ts (current state — createMockDesigner at line 31, createMockChart at line 135, createMockPrisma at line 430)
    - src/__tests__/mocks/index.ts (re-exports — must add createMockSeries)
    - .planning/phases/31-data-foundation-fixes/31-PATTERNS.md (factory patterns section)
  </read_first>
  <action>
    1. Add Series import to factories.ts type imports from @/generated/prisma/client.
    2. Add createMockSeries factory after createMockDesigner (around line 41). Returns: id "series-1", name "Test Series", totalCount null, designerId null, notes null, createdAt new Date(), updatedAt new Date(). Takes optional Partial<Series> overrides.
    3. Add seriesId: null to the createMockChart return object (after notes field, before createdAt).
    4. Add series block to createMockPrisma (after designer block, around line 457): { create: vi.fn(), findMany: vi.fn(), findUnique: vi.fn(), update: vi.fn(), delete: vi.fn() }.
    5. Re-export createMockSeries from src/__tests__/mocks/index.ts if not already handled by wildcard re-export.
    6. Run full test suite to confirm no regressions from adding seriesId to createMockChart.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - factories.ts contains "export function createMockSeries"
    - factories.ts createMockChart return object includes "seriesId: null"
    - factories.ts createMockPrisma contains "series:" block with create, findMany, findUnique, update, delete
    - Full test suite passes with 0 failures (no regressions from Chart type change)
  </acceptance_criteria>
  <done>Test infrastructure updated: createMockSeries factory exists, Chart mock includes seriesId, mock Prisma has series methods, all existing tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Database schema push</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (verify Series model is present from Task 1)
  </read_first>
  <action>
    Run npx prisma db push to apply the Series model and Chart.seriesId column to the development database. This is non-destructive (adding a new model and a nullable column). If the command prompts about data loss (unlikely for additive changes), use --accept-data-loss flag since dev database has no production data at risk.
  </action>
  <verify>
    <automated>npx prisma db push 2>&1 | grep -q "applied" || npx prisma db push 2>&1 | grep -q "synced" || npx prisma db push 2>&1 | grep -q "in sync"</automated>
  </verify>
  <acceptance_criteria>
    - npx prisma db push completes without error
    - Database schema matches prisma/schema.prisma (Series table exists, Chart has seriesId column)
  </acceptance_criteria>
  <done>Development database schema is in sync with prisma/schema.prisma including Series model</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server action | Untrusted form input crosses into server-side validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | seriesSchema input | mitigate | Zod validation with .trim().min(1).max(200) at boundary; Prisma parameterized queries |
| T-31-02 | DoS | notes field | mitigate | Zod .max(5000) constraint on notes input |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- npx tsc --noEmit exits 0
- npx vitest run src/lib/validations/series.test.ts passes
- npm test full suite passes (no regressions)
- npx prisma db push succeeds
</verification>

<success_criteria>
- Series model exists in Prisma schema with all specified fields
- Chart has seriesId FK, Designer has series relation
- TypeScript types define SeriesProgress, SeriesWithStats, SeriesDetail, SeriesChart
- Zod schema validates all series input with 6 passing tests
- Test factories updated and all 2283+ existing tests still pass
- Database schema pushed successfully
</success_criteria>

<output>
Create `.planning/phases/31-data-foundation-fixes/31-01-SUMMARY.md` when done
</output>
