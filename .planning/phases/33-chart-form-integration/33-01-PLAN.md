---
phase: 33-chart-form-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validations/chart.ts
  - src/lib/actions/chart-actions.ts
  - src/components/features/charts/inline-name-dialog.tsx
  - src/components/features/charts/inline-name-dialog.test.tsx
  - src/components/features/charts/use-chart-form.ts
  - src/components/features/charts/use-chart-form.test.tsx
autonomous: true
requirements: [SERIES-06, SERIES-07]

must_haves:
  truths:
    - "seriesId is accepted and persisted on chart create and chart edit"
    - "handleAddSeries creates a series via server action, appends to local list, and selects the new ID"
    - "D-04: handleAddSeries auto-populates designerId from current form values"
    - "D-05: If no designer selected, series created with designerId: null"
    - "D-02: InlineNameDialog supports custom submit button label and custom required-error message (name-only dialog)"
  artifacts:
    - path: "src/lib/validations/chart.ts"
      provides: "seriesId field in chartFormSchema.chart"
      contains: "seriesId"
    - path: "src/lib/actions/chart-actions.ts"
      provides: "seriesId in create and update Prisma calls"
      contains: "seriesId"
    - path: "src/components/features/charts/use-chart-form.ts"
      provides: "seriesId form value, seriesList state, handleAddSeries callback"
      exports: ["useChartForm"]
    - path: "src/components/features/charts/inline-name-dialog.tsx"
      provides: "submitLabel and requiredError optional props"
      exports: ["InlineNameDialog"]
  key_links:
    - from: "src/components/features/charts/use-chart-form.ts"
      to: "src/lib/actions/series-actions.ts"
      via: "import createSeries"
      pattern: "createSeries"
    - from: "src/components/features/charts/use-chart-form.ts"
      to: "src/lib/validations/chart.ts"
      via: "chartFormSchema includes seriesId"
      pattern: "seriesId"
---

<objective>
Add seriesId to the chart form data pipeline and hook, so charts can be assigned to a series on create/edit.

Purpose: Wires the data plumbing for series assignment (validation, persistence, hook state) before the UI layer connects in Plan 02.
Output: seriesId flows through validation -> actions -> Prisma; handleAddSeries callback available from useChartForm; InlineNameDialog supports custom copy for reuse as series dialog.
</objective>

<execution_context>
@/Users/wanderskye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-chart-form-integration/33-CONTEXT.md
@.planning/phases/33-chart-form-integration/33-PATTERNS.md

<interfaces>
From src/types/series.ts:
```typescript
export type SeriesWithStats = {
  id: string;
  name: string;
  totalCount: number | null;
  designerId: string | null;
  designerName: string | null;
  notes: string | null;
  progress: SeriesProgress;
};
```

From src/lib/actions/series-actions.ts:
```typescript
export async function createSeries(formData: unknown): Promise<
  { success: true; series: Series } | { success: false; error: string }
>;
export async function getSeriesWithStats(): Promise<SeriesWithStats[]>;
```

From src/components/features/charts/use-chart-form.ts:
```typescript
export interface ChartFormValues {
  name: string;
  designerId: string | null;
  // ... 20+ other fields
  startingStitches: number;
}
interface UseChartFormOptions {
  mode: "create" | "edit";
  initialData?: ChartWithProject;
  designers: Designer[];
  genres: Genre[];
  storageLocations?: StorageLocationWithStats[];
  stitchingApps?: StitchingAppWithStats[];
  onSuccess: (chartId: string) => void;
  getSupplyRows?: () => SupplyRow[];
  onValidationError?: () => void;
}
```

From src/components/features/charts/inline-name-dialog.tsx:
```typescript
interface InlineNameDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  title: string;
  initialName?: string;
  placeholder?: string;
  onSubmit: (name: string) => Promise<void>;
}
```

From src/__tests__/mocks/factories.ts:
```typescript
export function createMockSeriesWithStats(overrides?: Partial<SeriesWithStats>): SeriesWithStats;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add seriesId to validation + chart actions + hook tests (RED)</name>
  <files>src/lib/validations/chart.ts, src/lib/actions/chart-actions.ts, src/components/features/charts/use-chart-form.test.tsx, src/components/features/charts/inline-name-dialog.test.tsx</files>
  <read_first>
    - src/lib/validations/chart.ts (current schema, see designerId at line 9 for pattern)
    - src/lib/actions/chart-actions.ts (createChartAndProject at line 36-39, updateChart at line 257-261 for designerId pattern)
    - src/components/features/charts/use-chart-form.test.tsx (existing tests for handleAddStorageLocation at lines 40-108)
    - src/components/features/charts/inline-name-dialog.test.tsx (existing 7 tests, add 2 new)
    - src/components/features/charts/use-chart-form.ts (handleAddDesigner at lines 325-343 for pattern reference)
    - src/types/series.ts (SeriesWithStats type)
    - src/__tests__/mocks/factories.ts (createMockSeriesWithStats factory)
  </read_first>
  <behavior>
    - Test: handleAddSeries calls createSeries with { name, designerId } where designerId comes from current form values
    - Test: handleAddSeries on success appends to seriesList and sets seriesId form value
    - Test: handleAddSeries on server error throws (propagates to dialog catch)
    - Test: handleAddSeries with no designer selected passes designerId: null
    - Test: InlineNameDialog renders custom submitLabel when provided
    - Test: InlineNameDialog shows custom requiredError when submitting empty with requiredError prop
  </behavior>
  <action>
    1. In src/lib/validations/chart.ts: Add seriesId field to the chart object inside chartFormSchema, immediately after designerId (line 9). Use the identical pattern: z.string().nullable().default(null).

    2. In src/lib/actions/chart-actions.ts: Add seriesId to the Prisma create data object inside createChartAndProject (after designerId at line 39). Add seriesId to the Prisma update data object inside updateChart (after designerId at line 261). Both use chart.seriesId from the validated input.

    3. In src/components/features/charts/use-chart-form.test.tsx: Add vi.mock for @/lib/actions/series-actions with createSeries: vi.fn(). Import createSeries and createMockSeriesWithStats. Add a describe("handleAddSeries") block mirroring handleAddStorageLocation tests with 4 test cases:
       - Success: mock createSeries returning { success: true, series: { id: "series-new", name: "Test", totalCount: null, designerId: null, notes: null, createdAt, updatedAt } }. Assert createSeries called with { name: "Test", designerId: null }. Assert result.current.values.seriesId === "series-new". Assert result.current.seriesList contains the new entry.
       - Designer passthrough: set form.values.designerId to "des-1" first via act(() => result.current.setField("designerId", "des-1")), then call handleAddSeries("Test"). Assert createSeries called with { name: "Test", designerId: "des-1" }.
       - Server error: mock createSeries returning { success: false, error: "Already exists" }. Assert throws "Already exists".
       - Null designer: call handleAddSeries without setting designerId. Assert createSeries called with { name: "...", designerId: null }.

    4. In src/components/features/charts/inline-name-dialog.test.tsx: Add 2 tests:
       - "renders custom submit button label when submitLabel provided": render with submitLabel="Add Series", assert button with name "Add Series" exists
       - "shows custom required error when requiredError provided": render with requiredError="Series name is required", submit empty, assert "Series name is required" visible

    5. Run tests and confirm ALL new tests FAIL (components/functions not yet updated). Commit as test(33-01): add failing tests for series hook and dialog props.
  </action>
  <verify>
    <automated>npm test -- --run src/components/features/charts/use-chart-form.test.tsx src/components/features/charts/inline-name-dialog.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/validations/chart.ts contains "seriesId: z.string().nullable().default(null)" inside chartFormSchema.chart
    - src/lib/actions/chart-actions.ts line near 39 contains "seriesId: chart.seriesId" (create) and line near 261 contains "seriesId: chart.seriesId" (update)
    - use-chart-form.test.tsx has describe("handleAddSeries") with 4 test cases
    - inline-name-dialog.test.tsx has 2 new tests for submitLabel and requiredError
    - All new tests fail (RED phase)
    - All pre-existing tests still pass
  </acceptance_criteria>
  <done>Validation schema, chart actions, and test files updated. 6 new tests fail as expected (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hook + dialog changes (GREEN)</name>
  <files>src/components/features/charts/use-chart-form.ts, src/components/features/charts/inline-name-dialog.tsx</files>
  <read_first>
    - src/components/features/charts/use-chart-form.ts (full file -- handleAddDesigner at 325-343, buildInitialValues at 79-145, UseChartFormOptions at 48-58, return object at 420-441)
    - src/components/features/charts/inline-name-dialog.tsx (full file -- 110 lines)
    - src/types/series.ts (SeriesWithStats shape)
    - src/lib/actions/series-actions.ts (createSeries signature)
  </read_first>
  <action>
    1. In src/components/features/charts/use-chart-form.ts:
       a. Add import: import { createSeries } from "@/lib/actions/series-actions" and import type { SeriesWithStats } from "@/types/series".
       b. Add seriesId: string | null to ChartFormValues interface (after designerId).
       c. Add seriesId: null to the default return in buildInitialValues (after designerId: null).
       d. Add seriesId: data.seriesId to the data return in buildInitialValues (after designerId: data.designerId). The Chart Prisma type already has seriesId.
       e. Add series?: SeriesWithStats[] to UseChartFormOptions (optional, mirroring storageLocations).
       f. Add series: initialSeries = [] to the destructured options.
       g. Add useState: const [seriesList, setSeriesList] = useState of SeriesWithStats[] initialized from initialSeries (after stitchingAppsList state).
       h. Add seriesId: values.seriesId to the formData.chart object in submitForm (after designerId).
       i. Add handleAddSeries callback mirroring handleAddDesigner. The callback accepts (name: string). It reads values.designerId for auto-populate (per D-04). Calls createSeries({ name, designerId: values.designerId }). On success, constructs a SeriesWithStats with: id from result.series.id, name from result.series.name, totalCount from result.series.totalCount, designerId from result.series.designerId, designerName: null (not available from raw create), notes from result.series.notes, progress: { ownedCount: 0, finishedCount: 0, totalCount: result.series.totalCount }. Appends to seriesList via setSeriesList. Selects via setField("seriesId", result.series.id). Uses suppressUnloadRef and try/finally pattern.
       j. Add seriesList and handleAddSeries to the return object.

    2. In src/components/features/charts/inline-name-dialog.tsx:
       a. Add submitLabel?: string to InlineNameDialogProps (default "Add").
       b. Add requiredError?: string to InlineNameDialogProps (default "Name is required").
       c. Destructure submitLabel = "Add" and requiredError = "Name is required" in the function params.
       d. Replace the hardcoded "Name is required" error string in handleSubmit with requiredError.
       e. Replace the hardcoded "Add" button text with submitLabel. The pending text becomes submitLabel with "ing..." suffix -- actually keep it simple: isPending ? "Adding..." : submitLabel.

    3. Run all tests. All 6 new tests plus all existing tests must pass. Commit as feat(33-01): wire seriesId through chart form pipeline.
  </action>
  <verify>
    <automated>npm test -- --run src/components/features/charts/use-chart-form.test.tsx src/components/features/charts/inline-name-dialog.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - use-chart-form.ts exports seriesList (SeriesWithStats[]) and handleAddSeries from useChartForm return
    - ChartFormValues includes seriesId: string | null
    - UseChartFormOptions includes series?: SeriesWithStats[]
    - handleAddSeries calls createSeries with { name, designerId: values.designerId }
    - buildInitialValues maps data.seriesId to form values
    - submitForm includes seriesId in formData.chart
    - InlineNameDialog accepts optional submitLabel and requiredError props
    - All tests pass (both new and existing)
  </acceptance_criteria>
  <done>seriesId flows through validation, actions, and hook. InlineNameDialog supports custom copy. All tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form -> server action | seriesId from form input crosses to chart-actions |
| inline dialog -> createSeries | name from user input crosses to series-actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | chart-actions seriesId | mitigate | Zod validates seriesId as z.string().nullable() at form boundary; Prisma FK constraint rejects invalid IDs |
| T-33-02 | Spoofing | createSeries from hook | accept | createSeries already calls requireAuth(); no new auth surface |
| T-33-03 | Information Disclosure | designerId passthrough | accept | designerId is already in form state, owned by the authenticated user; no new exposure |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- npm test passes for use-chart-form.test.tsx and inline-name-dialog.test.tsx
- grep confirms seriesId in chart.ts validation schema, chart-actions.ts create + update
- grep confirms handleAddSeries and seriesList in use-chart-form.ts exports
</verification>

<success_criteria>
- seriesId accepted by chartFormSchema validation
- seriesId persisted in both createChartAndProject and updateChart Prisma calls
- handleAddSeries creates series with auto-populated designerId and updates local state
- InlineNameDialog supports custom submitLabel and requiredError props
- All new and existing tests pass
</success_criteria>

<output>
Create `.planning/phases/33-chart-form-integration/33-01-SUMMARY.md` when done
</output>
