---
phase: 24-code-quality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/stats/stitching-calendar.tsx
  - src/components/features/sessions/log-session-modal.tsx
  - src/lib/queries/stats/record-detection.test.ts
  - src/app/(dashboard)/page.tsx
  - src/app/(dashboard)/charts/page.tsx
  - src/components/shell/top-bar.tsx
  - src/components/features/supply-table/creation-flow-adapter.ts
  - src/components/features/supply-table/supply-table.tsx
  - src/components/features/supply-table/use-supply-table.ts
  - src/components/features/supply-table/types.ts
  - src/components/features/supply-table/server-action-adapter.ts
  - src/components/features/supply-table/index.ts
  - src/components/features/supply-table/editable-number.tsx
  - src/components/features/charts/use-draft-persistence.ts
  - src/components/features/charts/chart-merged-form.tsx
  - src/components/features/charts/project-detail/hero-cover-banner.tsx
  - src/components/features/charts/project-detail/supplies-tab.tsx
  - src/components/features/charts/project-detail/types.ts
  - src/lib/actions/chart-actions.ts
  - src/lib/auth.ts
  - src/__tests__/mocks/factories.ts
autonomous: true
requirements: [QUAL-06, QUAL-07, QUAL-08, QUAL-14]

must_haves:
  truths:
    - "D-01: No JSX section markers remain in stitching-calendar.tsx render blocks and no sub-section markers inside function bodies"
    - "D-02: Type-bundle file section markers (// ─── Section Name ───) preserved in stats.ts, dashboard.ts"
    - "No hardcoded emerald-* classes remain in log-session-modal.tsx"
    - "No WHAT-comment blocks remain in record-detection.test.ts"
    - "No planning doc references (D-XX, T-XX-XX, Phase N) remain in production code"
    - "Semantic tokens (primary, accent) used instead of hardcoded emerald colors"
  artifacts:
    - path: "src/components/features/stats/stitching-calendar.tsx"
      provides: "Clean JSX without section marker comments"
    - path: "src/components/features/sessions/log-session-modal.tsx"
      provides: "Semantic design tokens for all formerly-emerald classes"
    - path: ".claude/rules/comment-conventions.md"
      provides: "Convention documentation (created in Plan 01)"
  key_links:
    - from: "src/components/features/sessions/log-session-modal.tsx"
      to: "Tailwind semantic tokens"
      via: "CSS class names"
      pattern: "text-primary|bg-primary|hover:bg-accent"
---

<objective>
Clean up code comments and replace hardcoded color classes with semantic design tokens across the codebase.

Purpose: Enforce project conventions — remove WHAT-comments, JSX section markers, planning doc references, and non-semantic color classes. This makes the codebase internally consistent and easier to maintain.

Output: Zero JSX markers in stitching-calendar, zero emerald classes in log-session-modal, zero WHAT-comments in record-detection tests, zero planning doc references in production code.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-code-quality/24-CONTEXT.md
@.planning/phases/24-code-quality/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove JSX section markers from stitching-calendar and replace emerald classes in log-session-modal with semantic tokens</name>
  <files>src/components/features/stats/stitching-calendar.tsx, src/components/features/sessions/log-session-modal.tsx</files>
  <read_first>
    - src/components/features/stats/stitching-calendar.tsx (full file — locate all 8 JSX markers)
    - src/components/features/sessions/log-session-modal.tsx (lines 295-320 and 465-485 — emerald class locations)
    - .claude/rules/base-ui-patterns.md (semantic token reference)
  </read_first>
  <action>
    **In `stitching-calendar.tsx` (QUAL-07):**
    Remove these 8 JSX comment lines entirely (delete the whole line):
    - Line 121: `{/* Month navigation header */}`
    - Line 144: `{/* Weekday headers */}`
    - Line 156: `{/* Calendar grid */}`
    - Line 180: `{/* Day number */}`
    - Line 194: `{/* Session pills - full on sm+, dots on mobile */}`
    - Line 213: `{/* Mobile dots */}`
    - Line 235: `{/* Empty state */}`
    - Line 240: `{/* Calendar legend */}`

    Do NOT remove any `// ─── Section Name ───` markers if they exist in non-JSX code.

    **In `log-session-modal.tsx` (QUAL-08):**
    Replace all 5 emerald-* locations with semantic design tokens per the mapping in 24-PATTERNS.md:

    1. Line 301 — search input focus ring:
       - Replace: `focus:ring-emerald-500/40`
       - With: `focus:ring-primary/40`

    2. Line 316 — dropdown item hover:
       - Replace: `hover:bg-emerald-50 dark:hover:bg-emerald-900/20`
       - With: `hover:bg-accent`

    3. Line 318 — selected item state:
       - Replace: `bg-emerald-50 text-emerald-700 dark:bg-emerald-900/20 dark:text-emerald-400`
       - With: `bg-primary/10 text-primary`

    4. Line 470 — link text:
       - Replace: `text-emerald-600 hover:underline dark:text-emerald-400`
       - With: `text-primary hover:underline`
       (keep the `text-xs` class)

    5. Line 480 — upload area hover:
       - Replace: `hover:border-emerald-400 hover:text-emerald-600 dark:hover:border-emerald-600 dark:hover:text-emerald-400`
       - With: `hover:border-primary hover:text-primary`
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && echo "=== JSX markers ===" && grep -c '{/\*' src/components/features/stats/stitching-calendar.tsx && echo "=== Emerald classes ===" && grep -c 'emerald' src/components/features/sessions/log-session-modal.tsx && echo "=== Semantic tokens ===" && grep -c 'text-primary\|bg-primary\|hover:bg-accent\|ring-primary' src/components/features/sessions/log-session-modal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '{/\*' src/components/features/stats/stitching-calendar.tsx` returns 0
    - `grep -c 'emerald' src/components/features/sessions/log-session-modal.tsx` returns 0
    - `grep -c 'text-primary' src/components/features/sessions/log-session-modal.tsx` returns at least 3
    - `grep -c 'bg-primary' src/components/features/sessions/log-session-modal.tsx` returns at least 1
    - `grep -c 'hover:bg-accent' src/components/features/sessions/log-session-modal.tsx` returns at least 1
    - `grep -c 'ring-primary' src/components/features/sessions/log-session-modal.tsx` returns at least 1
  </acceptance_criteria>
  <done>All 8 JSX section markers removed from stitching-calendar. All 5 emerald-* locations in log-session-modal replaced with semantic tokens (primary, accent).</done>
</task>

<task type="auto">
  <name>Task 2: Remove WHAT-comments from record-detection test and strip planning doc references from all production code</name>
  <files>src/lib/queries/stats/record-detection.test.ts, src/app/(dashboard)/page.tsx, src/app/(dashboard)/charts/page.tsx, src/components/shell/top-bar.tsx, src/components/features/supply-table/creation-flow-adapter.ts, src/components/features/supply-table/supply-table.tsx, src/components/features/supply-table/use-supply-table.ts, src/components/features/supply-table/types.ts, src/components/features/supply-table/server-action-adapter.ts, src/components/features/supply-table/index.ts, src/components/features/supply-table/editable-number.tsx, src/components/features/charts/use-draft-persistence.ts, src/components/features/charts/chart-merged-form.tsx, src/components/features/charts/project-detail/hero-cover-banner.tsx, src/components/features/charts/project-detail/supplies-tab.tsx, src/components/features/charts/project-detail/types.ts, src/lib/actions/chart-actions.ts, src/lib/auth.ts, src/__tests__/mocks/factories.ts</files>
  <read_first>
    - src/lib/queries/stats/record-detection.test.ts (lines 220-250 — WHAT-comment block)
    - src/app/(dashboard)/page.tsx (lines 11, 18 — D-02, D-05 references)
    - src/components/features/supply-table/use-supply-table.ts (lines 42, 51, 103, 182, 223, 277 — multiple D-XX and T-XX refs)
    - src/components/features/charts/use-draft-persistence.ts (lines 42, 54, 74, 96, 109, 131 — D-XX refs in JSDoc)
    - src/lib/auth.ts (lines 33, 58 — D-02, D-03 refs)
    - src/__tests__/mocks/factories.ts (line 586 — D-05 ref)
  </read_first>
  <action>
    **In `record-detection.test.ts` (QUAL-06):**
    Remove the WHAT-comment block at lines 224-229 (the 6-line arithmetic explanation):
    ```
    // Two prior-day sessions with identical stitchCount (500 each) on May 16,
    // plus the current session (500) on May 18.
    // The self-skip logic only skips the first today-match, but the prior-day
    // sessions are NOT on today so they are never skipped.
    // previousBestSession = 500 (from prior sessions), today's session = 500 => no bestSession record
    // previousBestDay = 1000 (500+500 on May 16), todayTotal = 500 => no bestDay record
    ```
    Also remove the inline comments at lines ~245-246:
    ```
    // No false positives — today's 500 does not beat prior best session of 500,
    // and today's total of 500 does not beat prior best day of 1000
    ```
    The assertions that follow these comments already prove the behavior — the comments restate the math.

    **Planning doc reference cleanup (QUAL-14) — strip `(D-XX)`, `(T-XX-XX)`, `Phase N` suffixes from comments:**

    The rule: strip the planning reference portion. If the remaining comment text is meaningful, keep it. If the entire comment was just the reference, remove the line.

    Transform each occurrence. Here are the exact files and transformations:

    **`src/app/(dashboard)/page.tsx`:**
    - Line 11: `// D-02: Independent data-fetching functions, fetched via Promise.all()` → `// Independent data-fetching functions, fetched via Promise.all()`
    - Line 18: `// Transform Start Next charts to GalleryCardData for GalleryCard reuse (D-05)` → `// Transform Start Next charts to GalleryCardData for GalleryCard reuse`

    **`src/app/(dashboard)/charts/page.tsx`:**
    - Line 15: `// D-10: All four tab datasets fetched eagerly via Promise.all()` → `// All four tab datasets fetched eagerly via Promise.all()`
    - Line 35: `{/* D-12: Page header */}` → remove entirely (WHAT-comment after stripping ref)

    **`src/components/shell/top-bar.tsx`:**
    - Line 27: `// Listen for Quick Add "Log Stitches" custom event (D-08)` → `// Listen for Quick Add "Log Stitches" custom event`

    **`src/components/features/supply-table/creation-flow-adapter.ts`:**
    - Line 16: strip `(D-03, D-04, D-06)` from JSDoc
    - Line 20: strip `(D-08)` from JSDoc
    - Line 183: strip `(D-05)` from JSDoc

    **`src/components/features/supply-table/supply-table.tsx`:**
    - Line 23: strip `Phase 11 project detail, Phase 13 supply` from JSDoc → rewrite without phase refs: `Consumers (project detail page, supply takeover flow)`
    - Line 116: `200ms animation + 50ms buffer (per D-09: 0.2s ease)` → `200ms animation + 50ms buffer`

    **`src/components/features/supply-table/use-supply-table.ts`:**
    - Line 42: strip `(D-02)` → `// useTransition for non-blocking search state updates`
    - Line 51: strip `(T-10-12)` → `// The 150ms debounce prevents excessive adapter.searchSupplies calls.`
    - Line 103: strip `(D-01: single input architecture)` → `// --- Keyboard navigation for highlight ---` wait, this is also a `// ---` marker. Per D-01, remove sub-section markers inside function bodies. So remove this line entirely.
    - Line 182: strip `T-10-11:` → `// Validate inputs before adapter calls`
    - Line 223: strip `(D-03: auto-add + refocus)` → `// --- Handle inline create ---` — same issue, remove the `// ---` marker entirely.
    - Line 277: strip `(D-01: keyboard from table row input)` → `// Highlight navigation` — but this is a WHAT-comment. Remove entirely.

    **`src/components/features/supply-table/types.ts`:**
    - Line 74-75: strip `Phase 11:` and `Phase 13:` → `ServerActionAdapter (wired to supply-actions.ts)` and `CreationFlowAdapter (buffers until two-phase save)`

    **`src/components/features/supply-table/server-action-adapter.ts`:**
    - Line 36: strip `(D-07, D-10)` from JSDoc

    **`src/components/features/supply-table/index.ts`:**
    - Line 4: strip `(Phase 11 project detail, Phase 13 supply takeover)` → `Consumers (project detail page, supply takeover flow)`

    **`src/components/features/supply-table/editable-number.tsx`:**
    - Line 16: strip `T-10-01:` → `Threat mitigation: validates parseInt result is non-negative` — actually "Threat mitigation" without the ID is still meaningful. Keep as: `* Validates parseInt result is non-negative`

    **`src/components/features/charts/use-draft-persistence.ts`:**
    - Line 42: strip `(D-06)` from JSDoc
    - Line 54: strip `(D-07, D-08)` from JSDoc
    - Line 74: strip `(per D-08)` → `// Stale ID detection: null out IDs that no longer exist`
    - Line 96: strip `(D-09)` from JSDoc
    - Line 109: strip `(D-05)` from JSDoc
    - Line 131: strip `(D-08)` from JSDoc

    **`src/components/features/charts/chart-merged-form.tsx`:**
    - Line 270: strip `(D-07)` → `// Draft hydration on mount -- uses V2 format`

    **`src/components/features/charts/project-detail/hero-cover-banner.tsx`:**
    - Line 17: strip `(never crop per D-01)` from JSDoc → `Renders the cover image with object-contain`
    - Line 19: strip `(per D-02: compact metadata-forward layout)` → `Returns null when no image.`
    - Line 30: strip `D-02:` → `// Skip banner entirely when no cover image`
    - Line 44: strip `(D-01: never crop)` → `{/* Foreground image with object-contain — priority for LCP */}`

    **`src/components/features/charts/project-detail/supplies-tab.tsx`:**
    - Line 87: strip `(D-03)` → `// Stabilize router.refresh reference to prevent adapter recreation`
    - Line 96: strip `(D-01: read-only, no settings bar)` → `// CalcParams derived from project fields`
    - Line 107: strip `(D-05, D-06: parent pre-sorts, table is sort-unaware)` → `// Transform + sort supply rows (parent pre-sorts, table is sort-unaware)`
    - Line 123: strip `(D-04: carried from old tab)` → `{/* Sort toggle */}` — but this is then a WHAT JSX marker. Remove entirely.
    - Line 149: strip `(no CalculatorSettingsBar per D-02/D-03)` → remove the JSX comment entirely (WHAT-marker)

    **`src/components/features/charts/project-detail/types.ts`:**
    - Line 103: `// ─── Overview Tab Section Ordering (per D-06) ──────` → `// ─── Overview Tab Section Ordering ──────` (keep the `─── ───` marker since this is a type file per D-02)

    **`src/lib/actions/chart-actions.ts`:**
    - Line 154: strip `D-03/D-06:` from JSDoc → `Per atomic save: nothing is persisted until this atomic operation succeeds.`
    - Line 166: strip `(D-06, D-07)` → `// Batch insert supply junction records`

    **`src/lib/auth.ts`:**
    - Line 33: strip `(D-02)` → `// Generic failure: don't reveal which field is wrong`
    - Line 58: strip `(D-03)` → `maxAge: 30 * 24 * 60 * 60, // 30 days`

    **`src/__tests__/mocks/factories.ts`:**
    - Line 586: strip `D-05:` → `// Default $transaction handles both callback and array forms`
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && echo "=== Planning refs in prod code ===" && grep -rn 'D-0[0-9]\|D-1[0-2]\|T-[0-9]\{2\}-\|Phase [0-9]' --include="*.ts" --include="*.tsx" src/ | grep -v 'node_modules\|.test.\|.nyquist.\|date-fns\|TZDate\|D-0[0-9][0-9]\|overview-tab.test' | wc -l && echo "=== WHAT-comments in record-detection ===" && grep -c "prior-day\|false positives\|previousBest" src/lib/queries/stats/record-detection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'D-0[0-9])' --include="*.ts" --include="*.tsx" src/ | grep -v 'node_modules\|.test.\|.nyquist.' | wc -l` returns 0 (no `(D-XX)` refs in production code)
    - `grep -c 'T-10-' src/components/features/supply-table/use-supply-table.ts` returns 0
    - `grep -c 'T-10-' src/components/features/supply-table/editable-number.tsx` returns 0
    - `grep -c "Phase 11\|Phase 13" src/components/features/supply-table/types.ts` returns 0
    - `grep -c "Phase 11\|Phase 13" src/components/features/supply-table/index.ts` returns 0
    - `grep -c "previousBestSession = 500" src/lib/queries/stats/record-detection.test.ts` returns 0
    - `grep -c "false positives" src/lib/queries/stats/record-detection.test.ts` returns 0
    - `npx vitest run src/lib/queries/stats/record-detection.test.ts` exits 0
    - `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` returns 0
  </acceptance_criteria>
  <done>All WHAT-comments removed from record-detection test. All planning doc references stripped from production code (~42 occurrences across ~19 files). Comments retain meaningful content with D-XX/T-XX/Phase N suffixes removed. TypeScript compiles. Tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Comment removal and CSS class changes have no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | T (Tampering) | Semantic token swap | accept | CSS class changes are cosmetic only. Semantic tokens (primary, accent) are defined in the theme and tested visually. No functional change. |
| T-24-06 | R (Repudiation) | Comment removal | accept | Comments being removed are documentation, not security controls. No audit trail is affected. |
</threat_model>

<verification>
- `grep -rc 'emerald' src/components/features/sessions/log-session-modal.tsx` returns 0
- `grep -rc '{/\*' src/components/features/stats/stitching-calendar.tsx` returns 0
- `grep -rn 'D-0[0-9])' --include="*.ts" --include="*.tsx" src/ | grep -v 'node_modules\|.test.\|.nyquist.' | wc -l` returns 0
- `npx vitest run src/lib/queries/stats/record-detection.test.ts` passes
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
Zero JSX section markers in stitching-calendar. Zero emerald classes in log-session-modal. Zero WHAT-comments in record-detection tests. Zero planning doc references in production code. All tests pass. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/24-code-quality/24-03-SUMMARY.md`
</output>
