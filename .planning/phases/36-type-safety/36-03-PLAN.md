---
phase: 36-type-safety
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/shopping.ts
  - src/components/features/shopping/supply-overview.tsx
  - src/components/features/shopping/project-accordion.tsx
  - src/components/features/charts/inline-designer-dialog.tsx
  - src/components/features/charts/chart-merged-form.tsx
  - src/components/features/supply-table/local-state-adapter.ts
autonomous: true
requirements:
  - QUAL-07
  - QUAL-08

must_haves:
  truths:
    - "AggregatedSupply.items is typed as a non-empty tuple -- accessing items[0] does not require a null check"
    - "onUpdateAcquired callback type is defined once in src/types/shopping.ts and imported by 3 consumer files"
    - "InlineDesignerDialog has no trigger prop, no uncontrolled state, and open/onOpenChange are required"
    - "LocalStateAdapter.updateQuantity has no as unknown as cast -- field parameter matches the interface signature"
  artifacts:
    - path: "src/types/shopping.ts"
      provides: "OnUpdateAcquired shared callback type"
      exports: ["OnUpdateAcquired"]
    - path: "src/components/features/charts/inline-designer-dialog.tsx"
      provides: "Controlled-only InlineDesignerDialog"
      contains: "open: boolean"
    - path: "src/components/features/supply-table/local-state-adapter.ts"
      provides: "Type-safe updateQuantity without cast"
      contains: "field: \"stitchCount\" | \"need\" | \"have\""
  key_links:
    - from: "src/types/shopping.ts"
      to: "src/components/features/shopping/supply-overview.tsx"
      via: "OnUpdateAcquired import"
      pattern: "import.*OnUpdateAcquired.*from.*types/shopping"
    - from: "src/types/shopping.ts"
      to: "src/components/features/shopping/project-accordion.tsx"
      via: "OnUpdateAcquired import"
      pattern: "import.*OnUpdateAcquired.*from.*types/shopping"
---

<objective>
Tighten collection types (non-empty tuple, shared callback alias) and simplify InlineDesignerDialog to controlled-only mode with type-safe LocalStateAdapter field indexing.

Purpose: Encode the at-least-one invariant for aggregated supplies at the type level, eliminate 4 duplicated callback type definitions, remove dead uncontrolled code paths, and delete an unsafe type assertion.

Output: OnUpdateAcquired shared type in src/types/shopping.ts, non-empty tuple on AggregatedSupply.items, controlled-only InlineDesignerDialog, type-safe LocalStateAdapter.updateQuantity.
</objective>

<execution_context>
@/Users/wanderskye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-type-safety/36-CONTEXT.md

<interfaces>
From src/types/dashboard.ts:
```typescript
export interface ShoppingSupplyNeed {
  junctionId: string;
  supplyId: string;
  brandName: string;
  code: string;
  colorName: string;
  hexColor: string | null;
  quantityRequired: number;
  quantityAcquired: number;
  unit: string;
  projectId: string;
  projectName: string;
}
```

From src/components/features/supply-table/types.ts:
```typescript
export interface SupplyTableAdapter {
  updateQuantity(
    type: SupplyType,
    junctionId: string,
    field: "stitchCount" | "need" | "have",
    value: number,
  ): Promise<Result>;
}

export interface SupplyRow {
  id: string;
  supplyId: string;
  type: SupplyType;
  code: string;
  name: string;
  brandName: string;
  hexColor: string;
  stitchCount: number;
  need: number;
  have: number;
  isNeedOverridden: boolean;
}
```

From src/components/features/charts/inline-designer-dialog.tsx:
```typescript
interface InlineDesignerDialogProps {
  trigger?: React.ReactElement;
  open?: boolean;
  onOpenChange?: (open: boolean) => void;
  initialName?: string;
  onSubmit: (name: string, website?: string) => Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OnUpdateAcquired shared type and tighten AggregatedSupply.items</name>
  <files>src/types/shopping.ts, src/components/features/shopping/supply-overview.tsx, src/components/features/shopping/project-accordion.tsx</files>
  <read_first>
    - src/components/features/shopping/supply-overview.tsx
    - src/components/features/shopping/project-accordion.tsx
    - src/types/dashboard.ts
  </read_first>
  <action>
    Create src/types/shopping.ts (per D-14):
    - Import SupplyType-like type -- the callback uses lowercase "thread" | "bead" | "specialty" (check the actual usage in supply-overview.tsx and project-accordion.tsx; both use lowercase)
    - Export type OnUpdateAcquired = (type: "thread" | "bead" | "specialty", junctionId: string, newValue: number) => void

    Update src/components/features/shopping/supply-overview.tsx:
    - Import OnUpdateAcquired from @/types/shopping
    - Replace inline onUpdateAcquired type in SupplyOverviewProps with OnUpdateAcquired
    - Replace the 2 inline onUpdateAcquired types in SupplySection and AggregatedSupplyRow inline prop types with OnUpdateAcquired
    - Change AggregatedSupply.items type from ShoppingSupplyNeed[] to [ShoppingSupplyNeed, ...ShoppingSupplyNeed[]] (per D-13). The aggregateSupplies function always pushes at least one item before inserting into the map.

    Update src/components/features/shopping/project-accordion.tsx:
    - Import OnUpdateAcquired from @/types/shopping
    - Replace the inline onUpdateAcquired type in ProjectAccordionProps with OnUpdateAcquired
    - Replace the inline onUpdateAcquired type in SupplyGroup inline props with OnUpdateAcquired
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/types/shopping.ts exists and exports OnUpdateAcquired type
    - grep -c "OnUpdateAcquired" src/components/features/shopping/supply-overview.tsx returns at least 3 (SupplyOverviewProps + SupplySection + AggregatedSupplyRow)
    - grep -c "OnUpdateAcquired" src/components/features/shopping/project-accordion.tsx returns at least 2 (ProjectAccordionProps + SupplyGroup)
    - AggregatedSupply.items type is [ShoppingSupplyNeed, ...ShoppingSupplyNeed[]] in supply-overview.tsx
    - No inline (type: "thread" | "bead" | "specialty", junctionId: string, quantity: number) => void definitions remain in either file
    - npx tsc --noEmit exits 0
    - npm test passes
  </acceptance_criteria>
  <done>OnUpdateAcquired defined once and shared across 3 consumer sites, AggregatedSupply.items uses non-empty tuple</done>
</task>

<task type="auto">
  <name>Task 2: Simplify InlineDesignerDialog to controlled-only and fix LocalStateAdapter cast</name>
  <files>src/components/features/charts/inline-designer-dialog.tsx, src/components/features/charts/chart-merged-form.tsx, src/components/features/supply-table/local-state-adapter.ts</files>
  <read_first>
    - src/components/features/charts/inline-designer-dialog.tsx
    - src/components/features/charts/chart-merged-form.tsx
    - src/components/features/supply-table/local-state-adapter.ts
    - src/components/features/supply-table/types.ts
  </read_first>
  <action>
    Simplify InlineDesignerDialog (per D-15):
    - Remove trigger prop from InlineDesignerDialogProps
    - Make open required: open: boolean (not optional)
    - Make onOpenChange required: onOpenChange: (open: boolean) => void (not optional)
    - Delete the uncontrolledOpen useState
    - Delete the isControlled const and the ternary setOpen assignment
    - Use open and onOpenChange directly (no indirection)
    - Remove the DialogTrigger import and the {trigger && <DialogTrigger render={trigger} />} JSX
    - Replace the useState ref hack for initialName sync (prevOpenRef) with a useEffect + useRef pattern: useRef to track previous open value, useEffect that fires when open changes, sets name to initialName when open transitions from false to true
    - Verify that chart-merged-form.tsx already passes open and onOpenChange as required props (it does -- lines 488-492). No changes needed to the caller.

    Fix LocalStateAdapter.updateQuantity (per D-16):
    - Change the field parameter from field: string to field: "stitchCount" | "need" | "have" (matching the SupplyTableAdapter interface)
    - Replace the if (field in row) { (row as unknown as Record<string, unknown>)[field] = value; } block with direct property assignment using a type-safe approach: since field is now a known key of SupplyRow (all three are numeric), use row[field] = value directly. The SupplyRow interface has stitchCount: number, need: number, have: number -- all assignable from value: number.

    chart-merged-form.tsx:
    - Verify no changes needed (the caller already uses controlled mode with open={designerDialogOpen} onOpenChange={setDesignerDialogOpen}). If trigger prop was being passed, remove it. Read the file to confirm.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - InlineDesignerDialogProps has open: boolean (required, not optional)
    - InlineDesignerDialogProps has onOpenChange: (open: boolean) => void (required, not optional)
    - InlineDesignerDialogProps has no trigger prop
    - InlineDesignerDialog component has no useState for uncontrolledOpen
    - InlineDesignerDialog component has no isControlled variable
    - InlineDesignerDialog component has no DialogTrigger element
    - InlineDesignerDialog uses useEffect+useRef for initialName sync (no useState ref hack)
    - LocalStateAdapter.updateQuantity parameter is field: "stitchCount" | "need" | "have" (not string)
    - Zero occurrences of "as unknown as Record" in local-state-adapter.ts
    - npx tsc --noEmit exits 0
    - npm test passes
  </acceptance_criteria>
  <done>InlineDesignerDialog is controlled-only (no uncontrolled paths), LocalStateAdapter.updateQuantity is type-safe (no cast)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. InlineDesignerDialog simplification removes code paths (reducing surface area). LocalStateAdapter is used for testing only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Tampering | InlineDesignerDialog | accept | Controlled-only removal reduces code surface; dialog still validates name input via trim+empty check |
</threat_model>

<verification>
- npx tsc --noEmit exits 0
- npm test passes (no regressions)
- InlineDesignerDialog has zero references to uncontrolled, trigger, or DialogTrigger
- LocalStateAdapter has zero as unknown as casts
</verification>

<success_criteria>
- OnUpdateAcquired callback type defined once, imported in 3 consumer files
- AggregatedSupply.items enforces at-least-one at the type level
- InlineDesignerDialog is pure controlled component (no dead code paths)
- LocalStateAdapter.updateQuantity indexes SupplyRow without type assertions
</success_criteria>

<output>
Create `.planning/phases/36-type-safety/36-03-SUMMARY.md` when done
</output>
