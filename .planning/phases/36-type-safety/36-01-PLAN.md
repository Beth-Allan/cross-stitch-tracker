---
phase: 36-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/supply.ts
  - src/lib/utils/skein-calculator.ts
  - src/components/features/charts/project-detail/types.ts
  - src/components/features/charts/project-detail/supplies-tab.tsx
  - src/components/features/charts/project-detail/project-detail-page.tsx
  - src/components/features/supply-table/types.ts
autonomous: true
requirements:
  - QUAL-04
  - QUAL-06

must_haves:
  truths:
    - "Passing strandCount=0 or strandCount=7 to calculateSkeins is a compile-time type error"
    - "SuppliesTab rejects fabricOptions without chartId and vice versa at the type level"
    - "persistFields only accepts strandCount, overCount, or wastePercent keys"
    - "The as CalcParams cast in supplies-tab.tsx is replaced with a runtime type guard"
  artifacts:
    - path: "src/types/supply.ts"
      provides: "StrandCount literal union type and isStrandCount type guard"
      contains: "StrandCount"
    - path: "src/lib/utils/skein-calculator.ts"
      provides: "calculateSkeins with StrandCount parameter"
      contains: "StrandCount"
    - path: "src/components/features/charts/project-detail/supplies-tab.tsx"
      provides: "Co-dependent calculator prop and narrowed persistFields"
      contains: "calculator?"
  key_links:
    - from: "src/types/supply.ts"
      to: "src/lib/utils/skein-calculator.ts"
      via: "StrandCount import"
      pattern: "import.*StrandCount.*from.*types/supply"
    - from: "src/types/supply.ts"
      to: "src/components/features/supply-table/types.ts"
      via: "StrandCount import"
      pattern: "import.*StrandCount.*from.*types/supply"
---

<objective>
Narrow strandCount from number to a literal union (1-6) across all type definitions and collapse SuppliesTab's co-dependent optional props into a single object.

Purpose: Enforce domain invariants at compile time -- invalid strand counts become type errors, and SuppliesTab callers cannot pass fabricOptions without chartId or vice versa.

Output: StrandCount type alias + isStrandCount guard in src/types/supply.ts, narrowed CalculateParams, co-dependent calculator prop, narrowed persistFields type.
</objective>

<execution_context>
@/Users/wanderskye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-type-safety/36-CONTEXT.md

<interfaces>
From src/components/features/supply-table/types.ts:
```typescript
export interface CalcParams {
  fabricCount: number;
  strandCount: 1 | 2 | 3 | 4 | 5 | 6;
  overCount: 1 | 2;
  wastePercent: number;
}
```

From src/components/features/charts/project-detail/types.ts:
```typescript
export interface CalculatorSettings {
  strandCount: number; // 1-6
  overCount: 1 | 2;
  fabricCount: number;
  wastePercent: number;
}
```

From src/lib/validations/supply.ts:
```typescript
export const updateProjectSettingsSchema = z.object({
  strandCount: z.number().int().min(1).max(6).optional(),
  overCount: z.union([z.literal(1), z.literal(2)]).optional(),
  wastePercent: z.number().int().min(0).max(50).optional(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StrandCount type and narrow downstream consumers</name>
  <files>src/types/supply.ts, src/lib/utils/skein-calculator.ts, src/components/features/supply-table/types.ts, src/components/features/charts/project-detail/types.ts</files>
  <read_first>
    - src/types/supply.ts
    - src/lib/utils/skein-calculator.ts
    - src/components/features/supply-table/types.ts
    - src/components/features/charts/project-detail/types.ts
    - src/lib/validations/supply.ts
  </read_first>
  <behavior>
    - Test 1: isStrandCount returns true for values 1 through 6
    - Test 2: isStrandCount returns false for 0, 7, -1, 1.5, NaN
    - Test 3: TypeScript compilation succeeds with StrandCount in CalcParams.strandCount and CalculateParams.strandCount
  </behavior>
  <action>
    Add to src/types/supply.ts (per D-01):
    - Export a StrandCount type alias: 1 | 2 | 3 | 4 | 5 | 6
    - Export an isStrandCount type guard function that checks Number.isInteger and range 1-6, returning value is StrandCount

    Update src/lib/utils/skein-calculator.ts (per D-03):
    - Import StrandCount from @/types/supply
    - Change the strandCount parameter type from number to StrandCount in the calculateSkeins params object

    Update src/components/features/supply-table/types.ts (per D-02):
    - Import StrandCount from @/types/supply
    - Replace the inline literal 1 | 2 | 3 | 4 | 5 | 6 in CalcParams.strandCount with StrandCount

    Update src/components/features/charts/project-detail/types.ts (per D-04):
    - Import StrandCount from @/types/supply
    - Narrow CalculatorSettings.strandCount from number to StrandCount
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm test -- --run src/types/supply.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/types/supply.ts exports StrandCount as a type alias equal to 1 | 2 | 3 | 4 | 5 | 6
    - src/types/supply.ts exports isStrandCount function with return type value is StrandCount
    - src/lib/utils/skein-calculator.ts parameter strandCount has type StrandCount (not number)
    - CalcParams.strandCount in supply-table/types.ts imports StrandCount (not inline literal)
    - CalculatorSettings.strandCount in project-detail/types.ts has type StrandCount (not number)
    - src/types/supply.test.ts passes with guard validation tests
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>StrandCount type alias and isStrandCount guard exported, all four consumer files narrowed, tsc passes</done>
</task>

<task type="auto">
  <name>Task 2: Collapse SuppliesTab co-dependent props and narrow persistFields</name>
  <files>src/components/features/charts/project-detail/supplies-tab.tsx, src/components/features/charts/project-detail/project-detail-page.tsx</files>
  <read_first>
    - src/components/features/charts/project-detail/supplies-tab.tsx
    - src/components/features/charts/project-detail/project-detail-page.tsx
    - src/components/features/supply-table/types.ts
    - src/types/supply.ts
  </read_first>
  <action>
    Update SuppliesTabProps in supplies-tab.tsx (per D-11):
    - Remove separate fabricOptions?: FabricOption[] and chartId?: string props
    - Add a single optional calculator?: { fabricOptions: FabricOption[]; chartId: string } prop
    - Update the component destructure to extract calculator
    - Replace if (fabricOptions && chartId) conditional with if (calculator)
    - Replace chartId references in handleCalcParamsChange with calculator.chartId
    - Replace fabricOptions reference in CalculatorCard with calculator.fabricOptions

    Narrow persistFields type (per D-12):
    - Change const persistFields: Record<string, number> to const persistFields: Partial<Pick<CalcParams, 'strandCount' | 'overCount' | 'wastePercent'>>
    - Import CalcParams if not already imported (it is)
    - The Record<string, number> cast disappears because all three fields are numeric CalcParams keys

    Replace the as CalcParams["strandCount"] cast (per D-05):
    - Import isStrandCount from @/types/supply
    - In serverCalcParams useMemo, replace project.strandCount as CalcParams["strandCount"] with a guarded expression: isStrandCount(project.strandCount) ? project.strandCount : 2 (default fallback)

    Update caller in project-detail-page.tsx:
    - Change the SuppliesTab JSX from fabricOptions={fabricOptions} chartId={chart.id} to calculator={{ fabricOptions, chartId: chart.id }}
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SuppliesTabProps has calculator?: { fabricOptions: FabricOption[]; chartId: string } (no separate fabricOptions/chartId)
    - project-detail-page.tsx passes calculator={{ fabricOptions, chartId: chart.id }} to SuppliesTab
    - persistFields declared as Partial<Pick<CalcParams, 'strandCount' | 'overCount' | 'wastePercent'>> (not Record<string, number>)
    - No as CalcParams["strandCount"] cast in supplies-tab.tsx -- replaced with isStrandCount guard
    - npx tsc --noEmit exits 0
    - npm test passes with no regressions
  </acceptance_criteria>
  <done>SuppliesTab uses single calculator prop, persistFields narrowed, strandCount cast replaced with type guard</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. All changes are compile-time type narrowing with no runtime behavior changes beyond the isStrandCount guard (which validates data already validated by Zod at the server boundary).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | isStrandCount guard | accept | Guard is defense-in-depth; Zod validation at server boundary is primary. Prisma Int cannot be narrowed at DB level. |
</threat_model>

<verification>
- npx tsc --noEmit exits 0
- npm test passes (no regressions)
- Attempting to pass strandCount: 0 or strandCount: 7 to calculateSkeins causes TypeScript error
</verification>

<success_criteria>
- StrandCount type enforced at compile time across calculator, supply table, and project detail types
- SuppliesTab co-dependent props collapsed to single calculator object
- persistFields uses narrowed type instead of Record<string, number>
- Zero as/any casts remain in supplies-tab.tsx related to strandCount or persistFields
</success_criteria>

<output>
Create `.planning/phases/36-type-safety/36-01-SUMMARY.md` when done
</output>
