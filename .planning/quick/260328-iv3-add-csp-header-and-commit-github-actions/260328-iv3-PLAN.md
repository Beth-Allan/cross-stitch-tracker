---
type: quick
autonomous: true
files_modified: [next.config.ts, .github/workflows/ci.yml]
---

<objective>
Add a baseline Content-Security-Policy header to next.config.ts and commit the existing GitHub Actions CI workflow. Both are phase 1 review findings cleanup.

Purpose: Close out security header gap and CI configuration from phase 1 review.
Output: CSP header active, CI workflow committed.
</objective>

<context>
@next.config.ts
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSP header to next.config.ts</name>
  <files>next.config.ts</files>
  <action>
    Add a Content-Security-Policy header to the existing headers array in next.config.ts (which already has X-Content-Type-Options, X-Frame-Options, Referrer-Policy).

    Header value:
    `default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; font-src 'self'; img-src 'self' data: blob:; connect-src 'self'`

    Add a comment above the CSP header noting:
    - `unsafe-eval` is needed for Next.js dev mode hot reload
    - Policy should be tightened as features are added (e.g., restrict unsafe-inline with nonces, remove unsafe-eval in production)

    Do NOT change any existing headers.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>CSP header present in next.config.ts headers array alongside existing security headers. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and commit GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    The file .github/workflows/ci.yml already exists with correct content (triggers on push to main + PRs, Node 22, npm ci, lint, build). Verify the file is present and well-formed. No changes needed to the file itself — just ensure it gets included in the commit.
  </action>
  <verify>
    <automated>cat .github/workflows/ci.yml | head -5</automated>
  </verify>
  <done>ci.yml exists with push/PR triggers, Node 22, npm ci, lint, and build steps.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with CSP header in config
- .github/workflows/ci.yml present and well-formed
</verification>

<success_criteria>
- next.config.ts has Content-Security-Policy header with baseline policy
- .github/workflows/ci.yml committed to repo
- Build succeeds
</success_criteria>
