---
phase: quick
plan: 260411-js1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/upload-actions.ts
  - src/app/(dashboard)/charts/page.tsx
  - src/app/(dashboard)/charts/[id]/page.tsx
  - src/components/features/charts/chart-list.tsx
  - src/components/features/charts/chart-detail.tsx
  - src/components/features/charts/form-primitives/cover-image-upload.tsx
autonomous: true
must_haves:
  truths:
    - "Chart list page displays cover thumbnail images from R2 storage"
    - "Chart detail page displays full cover image from R2 storage"
    - "Edit modal cover image upload preview shows existing image from R2"
    - "Broken/missing images gracefully fall back to placeholder icon"
  artifacts:
    - path: "src/lib/actions/upload-actions.ts"
      provides: "Batch presigned URL generation for multiple R2 keys"
      contains: "getPresignedImageUrls"
    - path: "src/app/(dashboard)/charts/page.tsx"
      provides: "Server-side presigned URL resolution for chart list"
    - path: "src/app/(dashboard)/charts/[id]/page.tsx"
      provides: "Server-side presigned URL resolution for chart detail"
  key_links:
    - from: "src/app/(dashboard)/charts/page.tsx"
      to: "src/lib/actions/upload-actions.ts"
      via: "getPresignedImageUrls call"
      pattern: "getPresignedImageUrls"
    - from: "src/components/features/charts/chart-list.tsx"
      to: "presigned URLs"
      via: "imageUrls prop passed from server page"
      pattern: "imageUrls"
---

<objective>
Fix chart images not displaying. The coverThumbnailUrl and coverImageUrl fields store R2 object keys (e.g. "covers/abc123/xyz.jpg"), not displayable URLs. The chart list page, detail page, and edit modal all render `<img src={key}>` which returns a 404.

Purpose: Charts with uploaded cover images should display those images instead of showing broken images or placeholders.

Output: Server pages resolve R2 keys to presigned GET URLs before passing to client components; all `<img>` elements have onError fallbacks to placeholder.
</objective>

<execution_context>
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/actions/upload-actions.ts (has getPresignedDownloadUrl already — single-key only)
@src/lib/r2.ts (lazy S3Client singleton)
@src/app/(dashboard)/charts/page.tsx (server component — fetches charts, passes to ChartList)
@src/app/(dashboard)/charts/[id]/page.tsx (server component — fetches single chart)
@src/components/features/charts/chart-list.tsx (client component — CoverThumbnail renders img)
@src/components/features/charts/chart-detail.tsx (client component — CoverImage renders img)
@src/components/features/charts/form-primitives/cover-image-upload.tsx (preview uses currentImageUrl)
@src/types/chart.ts (ChartWithProject type)

<interfaces>
<!-- Existing infrastructure the executor needs -->

From src/lib/actions/upload-actions.ts:
```typescript
export async function getPresignedDownloadUrl(key: string): Promise<
  { success: true; url: string } | { success: false; error: string }
>;
```

From src/lib/r2.ts:
```typescript
export function getR2Client(): S3Client;
export const R2_BUCKET_NAME: string;
```

From src/types/chart.ts:
```typescript
export type ChartWithProject = Chart & {
  project: Project | null;
  designer: Designer | null;
  genres: Genre[];
};
```

Chart model fields (from Prisma schema):
- coverImageUrl: String? (R2 key for full image)
- coverThumbnailUrl: String? (R2 key for thumbnail)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add batch presigned URL resolver and wire server pages</name>
  <files>
    src/lib/actions/upload-actions.ts,
    src/lib/actions/upload-actions.test.ts,
    src/app/(dashboard)/charts/page.tsx,
    src/app/(dashboard)/charts/[id]/page.tsx
  </files>
  <behavior>
    - getPresignedImageUrls([]) returns empty record
    - getPresignedImageUrls(["key1", "key2"]) returns Record<string, string> mapping keys to presigned URLs
    - getPresignedImageUrls filters out null/undefined keys before processing
    - getPresignedImageUrls gracefully handles R2 errors (returns partial results, logs failures)
    - getPresignedImageUrls requires auth
  </behavior>
  <action>
    1. In `upload-actions.ts`, add a new exported function `getPresignedImageUrls`:
       - Signature: `async function getPresignedImageUrls(keys: (string | null | undefined)[]): Promise<Record<string, string>>`
       - Call `requireAuth()` first
       - Filter out null/undefined/empty keys, deduplicate
       - For each valid key, call `getSignedUrl(getR2Client(), new GetObjectCommand({Bucket, Key}), {expiresIn: 3600})` — use `Promise.allSettled` to handle partial failures gracefully
       - Return a `Record<string, string>` mapping R2 key -> presigned URL (only fulfilled results)
       - Wrap in try/catch — if R2 not configured, return empty record (graceful degradation per project convention)
       - This is NOT a "use server" action (the file already has "use server" at top) — it's a plain async function callable from Server Components

    2. In `charts/page.tsx` (server component):
       - After fetching charts, collect all non-null `coverThumbnailUrl` values from the charts array
       - Call `getPresignedImageUrls(thumbnailKeys)` to get the URL map
       - Pass `imageUrls={urlMap}` as a new prop to `<ChartList>`

    3. In `charts/[id]/page.tsx` (server component):
       - After fetching the chart, collect the `coverImageUrl` and `coverThumbnailUrl` keys
       - Call `getPresignedImageUrls([chart.coverImageUrl, chart.coverThumbnailUrl])` to get URLs
       - Pass `imageUrls={urlMap}` as a new prop to `<ChartDetail>`

    4. Write tests for `getPresignedImageUrls` in `upload-actions.test.ts`:
       - Mock `requireAuth`, `getR2Client`, and `getSignedUrl`
       - Test empty array input
       - Test filtering nulls
       - Test successful URL generation
       - Test partial failure handling (one key succeeds, one fails)
       - Test R2-not-configured graceful degradation
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npx vitest run src/lib/actions/upload-actions.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - getPresignedImageUrls function exists and is exported from upload-actions.ts
    - Charts list page resolves thumbnail URLs server-side and passes as prop
    - Chart detail page resolves image URLs server-side and passes as prop
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire presigned URLs into client components with onError fallback</name>
  <files>
    src/components/features/charts/chart-list.tsx,
    src/components/features/charts/chart-detail.tsx,
    src/components/features/charts/form-primitives/cover-image-upload.tsx
  </files>
  <action>
    1. In `chart-list.tsx`:
       - Add `imageUrls: Record<string, string>` to `ChartListProps`
       - Pass `imageUrls` down to `ChartRow` and `ChartCard` components
       - In `CoverThumbnail`: change `url` prop to accept the R2 key, and add `resolvedUrl` prop — OR simpler: look up the presigned URL from the imageUrls map using `chart.coverThumbnailUrl` as key before passing to `CoverThumbnail`
       - Best approach: in `ChartRow`, pass `url={imageUrls[chart.coverThumbnailUrl ?? ""] ?? null}` to `CoverThumbnail` (keeps CoverThumbnail simple — it just renders a URL or placeholder)
       - Add `onError` handler to the `<img>` in `CoverThumbnail` that hides the image and shows the placeholder icon instead. Use a local `const [imgError, setImgError] = useState(false)` — if imgError is true, render the placeholder div instead of the img.

    2. In `chart-detail.tsx`:
       - Add `imageUrls: Record<string, string>` to `ChartDetailProps`
       - In the `CoverImage` sub-component: resolve `coverImageUrl` via `imageUrls[coverImageUrl]` — pass the presigned URL, not the R2 key
       - Add `onError` fallback: same pattern as CoverThumbnail — useState for error, render placeholder on failure
       - The `CoverImage` component receives `coverImageUrl` (the key) — look up the resolved URL from `imageUrls` prop at the parent level and pass it as the URL

    3. In `cover-image-upload.tsx`:
       - The `currentImageUrl` prop currently receives the R2 key when editing an existing chart
       - The fix must come from the parent: `BasicInfoSection` receives `coverImageUrl` (the R2 key) and passes it to `CoverImageUpload` as `currentImageUrl`
       - Two approaches:
         a) Pass resolved URL from the edit modal's imageUrls (requires threading through ChartEditModal -> BasicInfoSection)
         b) Simpler: In `CoverImageUpload`, when `currentImageUrl` is set but NOT a blob URL and NOT a data URL and NOT an http URL (i.e., it looks like an R2 key), call `getPresignedDownloadUrl` on mount to resolve it
       - Use approach (b): Add a `useEffect` that checks if `currentImageUrl` is an R2 key (doesn't start with "http", "blob:", or "data:"), calls `getPresignedDownloadUrl(currentImageUrl)` and sets the preview to the resolved URL. Show a loading spinner while resolving. This is acceptable client-side since it's a single image in the edit modal, not a batch.
       - Add `onError` handler to the preview `<img>` that clears preview and shows the upload zone.

    4. For ALL image elements modified, ensure the `onError` pattern is:
       ```tsx
       const [imgError, setImgError] = useState(false);
       // If imgError, render placeholder instead of img
       // In img tag: onError={() => setImgError(true)}
       ```
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Chart list page shows actual cover thumbnails for charts that have them
    - Chart detail page shows the full cover image
    - Edit modal shows existing cover image in the upload preview
    - Broken images fall back to placeholder icon (no broken image indicators)
    - Build passes with no type errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server -> R2 | Presigned URLs generated server-side, exposed to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | I (Information Disclosure) | Presigned GET URLs | accept | URLs are time-limited (1 hour), single-user app, images are not sensitive (chart cover photos). Presigned URLs are the standard R2/S3 pattern for private bucket access. |
| T-quick-02 | S (Spoofing) | getPresignedImageUrls | mitigate | requireAuth() called before generating any URLs — unauthenticated users cannot obtain presigned URLs |
| T-quick-03 | D (Denial of Service) | Batch URL generation | accept | Single-user app; batch is bounded by number of charts (practical limit ~500). Promise.allSettled prevents one failure from blocking all. |
</threat_model>

<verification>
1. `npm run build` passes (type check + build)
2. `npm test` passes (including new upload-actions tests)
3. Manual: Visit /charts — charts with uploaded covers show thumbnail images
4. Manual: Click into a chart detail — full cover image displays
5. Manual: Edit a chart with a cover — edit modal shows existing image preview
6. Manual: If R2 is misconfigured, pages load without errors (graceful degradation)
</verification>

<success_criteria>
- Cover images display on chart list, detail, and edit modal
- No broken image icons visible
- onError fallback shows placeholder for failed loads
- Build and tests pass
- Graceful degradation when R2 not configured
</success_criteria>

<output>
After completion, create `.planning/quick/260411-js1-chart-images-not-displaying-generate-pre/260411-js1-SUMMARY.md`
</output>
