---
phase: quick
plan: 260328-im6
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shell/nav-items.ts
  - src/components/shell/nav-item-link.tsx
  - src/components/shell/logo.tsx
  - src/components/shell/sidebar.tsx
  - src/components/shell/top-bar.tsx
  - src/components/shell/user-menu.tsx
  - src/components/shell/logout-action.ts
  - src/app/(dashboard)/error.tsx
autonomous: true
must_haves:
  truths:
    - "All nav items are clickable Links navigating to their href"
    - "Logout failure shows error toast instead of silently failing"
    - "Auth/JWT errors in dashboard show friendly error boundary, not a crash"
    - "Logo and nav item rendering exist as single shared components"
  artifacts:
    - path: "src/components/shell/nav-item-link.tsx"
      provides: "Shared nav item rendering for sidebar and top-bar"
    - path: "src/components/shell/logo.tsx"
      provides: "Shared logo with size prop"
    - path: "src/app/(dashboard)/error.tsx"
      provides: "Dashboard error boundary"
  key_links:
    - from: "src/components/shell/sidebar.tsx"
      to: "src/components/shell/nav-item-link.tsx"
      via: "import NavItemLink"
      pattern: "NavItemLink"
    - from: "src/components/shell/top-bar.tsx"
      to: "src/components/shell/nav-item-link.tsx"
      via: "import NavItemLink"
      pattern: "NavItemLink"
---

<objective>
Fix broken navigation, add error handling, and DRY up duplicated shell components from phase 1.

Purpose: All nav items are currently dead (active: false renders divs instead of Links). Logout has no error handling. No error boundary catches auth failures. Logo and nav item markup are duplicated 3x and 4x respectively.
Output: Working navigation, resilient logout, dashboard error boundary, and two extracted shared components.
</objective>

<execution_context>
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/shell/nav-items.ts
@src/components/shell/sidebar.tsx
@src/components/shell/top-bar.tsx
@src/components/shell/user-menu.tsx
@src/components/shell/logout-action.ts
@src/app/(auth)/login/page.tsx

<interfaces>
<!-- Current NavItem type to be simplified -->
From src/components/shell/nav-items.ts:
```typescript
export interface NavItem {
  label: string
  href: string
  icon: LucideIcon
  active: boolean   // REMOVE
  phase: number     // REMOVE
}
```

<!-- Sidebar uses TooltipProvider, Tooltip, TooltipTrigger, TooltipContent from @/components/ui/tooltip -->
<!-- TopBar uses Sheet, SheetTrigger, SheetContent, SheetTitle from @/components/ui/sheet -->
<!-- UserMenu uses DropdownMenu* from @/components/ui/dropdown-menu, Avatar from @/components/ui/avatar -->
<!-- Login page logo: 16x16 container, size-8 icon, rounded-2xl -->
<!-- Sidebar/TopBar logo: 8x8 container, w-4 h-4 icon, rounded-lg -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared components and simplify NavItem type</name>
  <files>
    src/components/shell/nav-items.ts
    src/components/shell/nav-item-link.tsx
    src/components/shell/logo.tsx
  </files>
  <action>
**1. Simplify NavItem type** in `src/components/shell/nav-items.ts`:
- Remove `active` and `phase` fields from the `NavItem` interface (only keep `label`, `href`, `icon`)
- Remove `active: false, phase: N` from every item in the `navigationItems` array
- Placeholder pages already show "coming soon" messaging, so no gating needed

**2. Create `src/components/shell/logo.tsx`** — a shared Logo component:
- Props: `size?: "sm" | "lg"` (default `"sm"`)
- `"sm"` = `w-8 h-8 rounded-lg` container + `w-4 h-4` icon (sidebar/topbar usage)
- `"lg"` = `h-16 w-16 rounded-2xl` container + `size-8` icon (login page usage)
- Renders the emerald background square with Scissors icon inside
- Does NOT include the "Cross Stitch Tracker" text (that varies by context)
- Export as named export `Logo`
- Mark as a Server Component (no "use client" needed — it's pure markup)

**3. Create `src/components/shell/nav-item-link.tsx`** — a shared NavItemLink component:
- Must be a Client Component ("use client") since it uses `usePathname`
- Props interface `NavItemLinkProps`:
  - `item: NavItem` (from nav-items.ts)
  - `collapsed?: boolean` (for sidebar collapsed state, default false)
  - `onClick?: () => void` (for mobile drawer close)
- Uses `usePathname()` to determine `isCurrentPage`:
  - If `item.href === "/"` then `pathname === "/"`
  - Otherwise `pathname.startsWith(item.href)`
- All items render as `<Link>` (no more active/inactive gating)
- Active page styling: `bg-emerald-50 dark:bg-emerald-950/50 text-emerald-700 dark:text-emerald-400 font-medium`
- Inactive styling: `text-stone-600 dark:text-stone-400 hover:bg-stone-100 dark:hover:bg-stone-800 hover:text-stone-900 dark:hover:text-stone-200`
- When `collapsed`, apply `justify-center px-2 py-2.5` and hide label text
- When not collapsed, apply `px-3 py-2.5` and show label
- Icon: `<item.icon className="w-5 h-5 shrink-0" strokeWidth={1.5} />`
- Export as named export `NavItemLink`
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>NavItem type has no active/phase fields. Logo component renders emerald scissors icon at two sizes. NavItemLink renders a Link with pathname-based active styling. All three files type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor shell components to use shared components + add error handling</name>
  <files>
    src/components/shell/sidebar.tsx
    src/components/shell/top-bar.tsx
    src/components/shell/user-menu.tsx
    src/components/shell/logout-action.ts
    src/app/(dashboard)/error.tsx
    src/app/(auth)/login/page.tsx
  </files>
  <action>
**1. Refactor `src/components/shell/sidebar.tsx`:**
- Import `Logo` from `./logo` and `NavItemLink` from `./nav-item-link`
- Replace the inline logo markup (lines 47-59) with `<Logo />` plus the conditional text span
- Replace the mainItems map body (lines 63-108) with: map over mainItems rendering `<NavItemLink item={item} collapsed={collapsed} />`. For collapsed mode, wrap each in `<Tooltip>` with `<TooltipTrigger render={<div />}>` and `<TooltipContent side="right">{item.label}</TooltipContent>` — remove the `(Phase N)` tooltip suffix since phase field is gone
- Replace the bottomItems map body (lines 113-156) identically
- Remove the `Scissors` import from lucide-react (now in Logo)
- Keep `PanelLeftClose`, `PanelLeftOpen` imports for collapse toggle

**2. Refactor `src/components/shell/top-bar.tsx`:**
- Import `Logo` from `./logo` and `NavItemLink` from `./nav-item-link`
- Replace the inline logo in mobile drawer (lines 44-55) with `<Logo />` plus the text span
- Replace main nav items rendering (lines 58-89) with: map over filtered items using `<NavItemLink item={item} onClick={() => setSheetOpen(false)} />`
- Replace settings items rendering (lines 93-119) identically
- Remove the `Scissors` import from lucide-react

**3. Refactor `src/app/(auth)/login/page.tsx`:**
- Import `Logo` from `@/components/shell/logo`
- Replace the inline logo div (the 16x16 emerald square) with `<Logo size="lg" />`
- Remove the `Scissors` import from lucide-react

**4. Add error handling to logout** in `src/components/shell/logout-action.ts`:
- Wrap `signOut()` in try-catch
- Return `{ success: true }` on success, `{ success: false, error: "Failed to log out. Please try again." }` on catch
- Update return type to `Promise<{ success: boolean; error?: string }>`

**5. Update `src/components/shell/user-menu.tsx`:**
- Remove `DropdownMenuGroup` and `DropdownMenuLabel` from imports (unused/unnecessary)
- Remove the `<DropdownMenuGroup>` wrapper around the logout `<DropdownMenuItem>`
- Update the onClick handler to: call `logoutAction()`, check result, if `!result.success` show `toast.error(result.error)` (import `toast` from `sonner`)

**6. Create `src/app/(dashboard)/error.tsx`:**
- Must be a Client Component ("use client") — Next.js requirement for error boundaries
- Props: `{ error: Error; reset: () => void }` (standard Next.js error boundary props)
- Render a centered card with:
  - Heading: "Something went wrong"
  - If `error.message` includes "session" or "auth" or "unauthorized" (case-insensitive): show "Your session may have expired. Please log in again." with a Link to `/login`
  - Otherwise: show generic "An unexpected error occurred." with a "Try again" button calling `reset()`
  - Style with stone/emerald design tokens, consistent with the app's aesthetic
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Sidebar and top-bar use NavItemLink and Logo (no duplicated rendering). All nav items are Links. Logout returns error state and user-menu shows toast on failure. Dashboard has error.tsx boundary. Build passes with no errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npx tsc --noEmit` passes
3. `grep -r "active:" src/components/shell/nav-items.ts` returns nothing (field removed)
4. `grep -r "phase:" src/components/shell/nav-items.ts` returns nothing (field removed)
5. `grep -c "NavItemLink" src/components/shell/sidebar.tsx` shows usage
6. `grep -c "NavItemLink" src/components/shell/top-bar.tsx` shows usage
7. `ls src/app/\(dashboard\)/error.tsx` exists
</verification>

<success_criteria>
- Every nav item in sidebar and mobile drawer is a clickable Link (no more disabled divs)
- Logo markup exists in exactly one file (logo.tsx), imported by sidebar, top-bar, and login page
- Nav item rendering logic exists in exactly one file (nav-item-link.tsx), imported by sidebar and top-bar
- logoutAction returns success/error state; user-menu shows toast on failure
- Dashboard error boundary catches auth/JWT errors with friendly messaging
- Full build passes
</success_criteria>

<output>
After completion, create `.planning/quick/260328-im6-fix-shell-navigation-issues-and-dry-clea/260328-im6-SUMMARY.md`
</output>
