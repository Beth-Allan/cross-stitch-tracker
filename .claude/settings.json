{
  "permissions": {
    "allow": ["Bash(GH_TOKEN=$(gh auth token --user adolwyn) gh pr merge:*)"]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/guard-git.sh\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -q 'git commit' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"REMINDER: You just committed. The work log (docs/process/work-log.md) is the only memory between sessions — update it, and its Up-next queue, before this session ends. If you already updated it this session, ignore this.\"}}' || true",
            "timeout": 5
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "superpowers-laravel@superpowers-laravel-marketplace": false,
    "hookify@claude-plugins-official": false
  }
}
