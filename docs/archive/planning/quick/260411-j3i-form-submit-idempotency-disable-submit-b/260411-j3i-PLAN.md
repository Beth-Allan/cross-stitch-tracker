---
phase: quick
plan: 260411-j3i
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/charts/use-chart-form.ts
  - src/components/features/charts/chart-add-form.tsx
  - src/components/features/charts/chart-edit-modal.tsx
  - src/components/features/charts/chart-add-form.test.tsx
  - src/components/features/charts/chart-edit-modal.test.tsx
autonomous: true
requirements: [999.0.3]
must_haves:
  truths:
    - "Submit button stays disabled after successful save until navigation completes"
    - "Submit button shows 'Added!' or 'Saved!' text after successful save"
    - "Button remains disabled during pending state (existing behavior preserved)"
    - "Button re-enables on error (existing behavior preserved)"
  artifacts:
    - path: "src/components/features/charts/use-chart-form.ts"
      provides: "isSuccess state + isSubmitDisabled convenience boolean"
      contains: "isSuccess"
    - path: "src/components/features/charts/chart-add-form.tsx"
      provides: "Submit button uses isSubmitDisabled and shows success text"
    - path: "src/components/features/charts/chart-edit-modal.tsx"
      provides: "Submit button uses isSubmitDisabled and shows success text"
  key_links:
    - from: "use-chart-form.ts"
      to: "chart-add-form.tsx"
      via: "form.isSubmitDisabled and form.isSuccess"
    - from: "use-chart-form.ts"
      to: "chart-edit-modal.tsx"
      via: "form.isSubmitDisabled and form.isSuccess"
---

<objective>
Prevent duplicate chart creation by keeping the submit button disabled after a successful server action response. Currently, `isPending` resets to `false` in the `finally` block before `router.push()` completes navigation, creating a window where the user can click "Add Chart" again.

Purpose: Backlog item 999.0.3 — form submit idempotency
Output: Submit button stays disabled and shows success feedback after save completes
</objective>

<execution_context>
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/features/charts/use-chart-form.ts
@src/components/features/charts/chart-add-form.tsx
@src/components/features/charts/chart-edit-modal.tsx
@src/components/features/charts/chart-add-form.test.tsx
@src/components/features/charts/chart-edit-modal.test.tsx

<interfaces>
From use-chart-form.ts — current return shape (line 301-315):
```typescript
return {
  values, setField,
  effectiveStitchCount, sizeCategory, isAutoCalculated,
  errors, isPending, isDirty,
  handleSubmit,
  designers, genres,
  handleAddDesigner, handleAddGenre,
};
```

Current submit button pattern in both consumers:
```tsx
<Button type="submit" disabled={form.isPending}>
  {form.isPending ? "Adding..." : "Add Chart"}
</Button>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add isSuccess state to useChartForm and write tests</name>
  <files>
    src/components/features/charts/use-chart-form.ts
    src/components/features/charts/chart-add-form.test.tsx
    src/components/features/charts/chart-edit-modal.test.tsx
  </files>
  <behavior>
    - Test: After successful createChart, submit button shows "Added!" text and remains disabled
    - Test: After successful updateChart, submit button shows "Saved!" text and remains disabled
    - Test: After failed createChart (error response), submit button re-enables to "Add Chart"
    - Test: After network error (rejected promise), submit button re-enables to "Add Chart"
    - Test: During pending state, button shows "Adding..."/"Saving..." and is disabled (existing — verify still passes)
  </behavior>
  <action>
    1. In `use-chart-form.ts`:
       - Add `const [isSuccess, setIsSuccess] = useState(false)` alongside existing `isPending` state (after line 140)
       - Add `const isSubmitDisabled = isPending || isSuccess` as a derived value
       - In `handleSubmit`, set `setIsSuccess(true)` just BEFORE calling `onSuccess(response.chartId)` on the create success path (line 229), and `setIsSuccess(true)` before `onSuccess(initialData!.id)` on the edit success path (line 237)
       - Do NOT reset `isSuccess` in the `finally` block — that's the whole point
       - Do NOT reset `isSuccess` on error paths — it was never set to true
       - Export `isSuccess` and `isSubmitDisabled` in the return object (add to the return block at line 301)

    2. In `chart-add-form.tsx` (line 164):
       - Change `disabled={form.isPending}` to `disabled={form.isSubmitDisabled}`
       - Change the button text ternary to: `form.isSuccess ? "Added!" : form.isPending ? "Adding..." : "Add Chart"`

    3. In `chart-edit-modal.tsx` (line 283):
       - Change `disabled={form.isPending}` to `disabled={form.isSubmitDisabled}`
       - Change the button text ternary to: `form.isSuccess ? "Saved!" : form.isPending ? "Saving..." : "Save Changes"`

    4. In `chart-add-form.test.tsx`:
       - Add test: "keeps submit button disabled after successful save" — mock createChart to resolve with `{ success: true, chartId: "new-id" }`, fill valid form, click submit, wait for the button text to show "Added!" and verify `button.disabled === true`
       - Add test: "re-enables submit button after server error" — mock createChart to resolve with `{ success: false, error: "Failed" }`, submit, verify button text returns to "Add Chart" and is enabled
       - Existing "disables submit button during pending state" test should still pass as-is since `isPending` still drives the disabled state during the request

    5. In `chart-edit-modal.test.tsx`:
       - Add test: "keeps submit button disabled after successful save" — mock updateChart to resolve with `{ success: true }`, click Save Changes, wait for button to show "Saved!" and verify disabled
       - Add test: "re-enables submit button after server error" — mock updateChart to resolve with `{ success: false, error: "Failed" }`, click Save Changes, verify button returns to "Save Changes" and is enabled
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npx vitest run src/components/features/charts/chart-add-form.test.tsx src/components/features/charts/chart-edit-modal.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>
    - useChartForm exposes isSuccess and isSubmitDisabled
    - Submit button disabled after successful save in both add form and edit modal
    - Submit button shows "Added!"/"Saved!" success text
    - Button re-enables on error (not stuck disabled)
    - All existing tests still pass
    - All new idempotency tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — this is a client-side UX improvement only. The server actions already have their own idempotency characteristics (Prisma create is inherently non-idempotent, but duplicate detection is a separate backlog item 999.0.4).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | Submit button disabled state | accept | Client-side disable is UX convenience, not a security boundary. A determined user could still call the server action directly. Server-side duplicate detection (999.0.4) is the proper mitigation for that. |
</threat_model>

<verification>
- `npx vitest run src/components/features/charts/ --reporter=verbose` — all chart form tests pass
- `npm run build` — no type errors
- Manual: Navigate to /charts/new, fill form, submit — button shows "Added!" and stays disabled
</verification>

<success_criteria>
- Submit button stays disabled and shows success text after successful create/update
- Button re-enables after error so user can retry
- No regressions in existing form tests
- Build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/quick/260411-j3i-form-submit-idempotency-disable-submit-b/260411-j3i-SUMMARY.md`
</output>
