---
phase: 05-foundation-quick-wins
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/charts/form-primitives/cover-image-upload.tsx
  - src/components/features/charts/form-primitives/cover-image-upload.test.tsx
  - src/components/features/charts/chart-detail.tsx
  - src/components/features/supplies/search-to-add.tsx
  - src/components/features/supplies/search-to-add.test.tsx
  - prisma/fixtures/dmc-threads.json
  - prisma/fixtures/dmc-threads.test.ts
autonomous: true
requirements:
  - PROJ-02
  - SUPP-02
  - SUPP-03

must_haves:
  truths:
    - "Cover images display without cropping distortion (object-contain with bg-muted letterboxing)"
    - "Cover image containers are h-48 (192px) instead of h-32 (128px)"
    - "Thread colour picker auto-scrolls to keep search/add controls visible after adding an item"
    - "DMC thread catalog includes Blanc and threads 1-35 (~36 new entries)"
    - "Cover thumbnails (10x10) still use object-cover (NOT changed)"
  artifacts:
    - path: "src/components/features/charts/form-primitives/cover-image-upload.tsx"
      provides: "Fixed cover image display with object-contain and h-48"
      contains: "object-contain"
    - path: "src/components/features/charts/chart-detail.tsx"
      provides: "Fixed cover image on detail page"
      contains: "object-contain"
    - path: "src/components/features/supplies/search-to-add.tsx"
      provides: "Auto-scroll after thread addition"
      contains: "scrollIntoView"
    - path: "prisma/fixtures/dmc-threads.json"
      provides: "Complete DMC thread catalog with ~495 entries"
      contains: "Blanc"
  key_links:
    - from: "prisma/fixtures/dmc-threads.json"
      to: "prisma/seed.ts"
      via: "JSON import for idempotent upsert"
      pattern: "dmc-threads"
---

<objective>
Fix two UX bugs (cover image cropping, thread picker scroll) and complete the DMC thread catalog by adding ~36 missing entries.

Purpose: Delivers PROJ-02 (cover images display correctly), SUPP-02 (thread picker scroll), and SUPP-03 (complete DMC catalog). These are surgical fixes with no schema dependency.
Output: Better cover image display, smoother thread picker UX, complete DMC color catalog.
</objective>

<execution_context>
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/Projects/cross-stitch-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-foundation-quick-wins/05-CONTEXT.md
@.planning/phases/05-foundation-quick-wins/05-RESEARCH.md
@.planning/phases/05-foundation-quick-wins/05-UI-SPEC.md

<interfaces>
<!-- Key code locations the executor needs to modify. -->

From src/components/features/charts/form-primitives/cover-image-upload.tsx (lines 192, 196, 201, 231):
```typescript
// Line 192 - resolving state container:
<div className="border-border flex h-32 items-center justify-center rounded-lg border-2 border-dashed">

// Line 196 - preview container:
<div className="border-border relative h-32 overflow-hidden rounded-lg border-2">

// Line 201 - image tag:
<img ... className="h-full w-full object-cover" ... />

// Line 231 - drop zone:
className={cn("flex h-32 cursor-pointer flex-col items-center ...")}
```

From src/components/features/charts/chart-detail.tsx (line 187):
```typescript
<img
  src={coverImageUrl}
  alt={`Cover for ${chartName}`}
  className="aspect-[4/3] max-h-80 w-full rounded-lg object-cover lg:w-80"
  onError={() => setImgError(true)}
/>
```

From src/components/features/supplies/search-to-add.tsx (lines 155-158):
```typescript
if (result.success) {
  toast.success(`Added ${item.brand.name} ${getItemCode(item)} to project`);
  onAdded();
  onClose();  // Currently closes picker after each add
}
```

From src/components/features/charts/cover-thumbnail.tsx (line 20 -- do NOT change):
```typescript
// Keep object-cover for 10x10 thumbnails
className="h-full w-full rounded object-cover"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix cover image display (object-contain + h-48 + bg-muted)</name>
  <files>
    src/components/features/charts/form-primitives/cover-image-upload.tsx,
    src/components/features/charts/form-primitives/cover-image-upload.test.tsx,
    src/components/features/charts/chart-detail.tsx
  </files>
  <read_first>
    src/components/features/charts/form-primitives/cover-image-upload.tsx,
    src/components/features/charts/chart-detail.tsx,
    src/components/features/charts/cover-thumbnail.tsx,
    .planning/phases/05-foundation-quick-wins/05-UI-SPEC.md,
    .planning/phases/05-foundation-quick-wins/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: cover-image-upload preview container has class h-48 (not h-32)
    - Test 2: cover-image-upload preview img has class object-contain (not object-cover)
    - Test 3: cover-image-upload preview container has class bg-muted
    - Test 4: cover-image-upload drop zone has class h-48 (not h-32)
    - Test 5: cover-image-upload resolving state container has class h-48 (not h-32)
  </behavior>
  <action>
    **Per D-04 and D-05, make these exact changes:**

    **cover-image-upload.tsx -- 4 changes:**

    1. **Line 192 (resolving state container):**
       Change: `h-32` -> `h-48`
       Result: `<div className="border-border flex h-48 items-center justify-center rounded-lg border-2 border-dashed">`

    2. **Line 196 (preview container):**
       Change: `h-32` -> `h-48`, add `bg-muted`
       Result: `<div className="border-border bg-muted relative h-48 overflow-hidden rounded-lg border-2">`

    3. **Line 201 (preview img):**
       Change: `object-cover` -> `object-contain`
       Result: `<img ... className="h-full w-full object-contain" ... />`

    4. **Line 231 (drop zone):**
       Change: `h-32` -> `h-48`
       Result: `className={cn("flex h-48 cursor-pointer flex-col items-center ...")}`

    **chart-detail.tsx -- 1 change:**

    The CoverImage function component (line 187):
       Change: `object-cover` -> `object-contain`, add `bg-muted`
       Before: `className="aspect-[4/3] max-h-80 w-full rounded-lg object-cover lg:w-80"`
       After: `className="bg-muted aspect-[4/3] max-h-80 w-full rounded-lg object-contain lg:w-80"`

    **Do NOT change cover-thumbnail.tsx** -- the 10x10 pixel thumbnails (`cover-thumbnail.tsx` line 20) should remain `object-cover`. Small thumbnails look fine with cover cropping; only the larger display needs contain.

    **Tests:**

    For cover-image-upload, test that rendered output includes the expected classes. Use the existing test file pattern -- if no test file exists, create one. Test the 3 states (resolving, complete, empty) and verify the container classes include `h-48` and the img class includes `object-contain`.

    For chart-detail.tsx, the CoverImage is an internal component -- testing via the cover-image-upload tests is sufficient. A manual visual check in the checkpoint will confirm.
  </action>
  <verify>
    <automated>npx vitest run src/components/features/charts/form-primitives/cover-image-upload.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - cover-image-upload.tsx does NOT contain `h-32` anywhere (all changed to `h-48`)
    - cover-image-upload.tsx line 201 (or equivalent preview img) contains `object-contain` NOT `object-cover`
    - cover-image-upload.tsx preview container contains `bg-muted`
    - chart-detail.tsx CoverImage img contains `object-contain` NOT `object-cover`
    - chart-detail.tsx CoverImage img contains `bg-muted`
    - cover-thumbnail.tsx still contains `object-cover` (UNCHANGED)
    - Tests pass
  </acceptance_criteria>
  <done>
    Cover images display without cropping: object-contain shows full image with bg-muted letterboxing. Container height increased from 128px to 192px. Thumbnails unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread picker scrollIntoView + DMC catalog completion</name>
  <files>
    src/components/features/supplies/search-to-add.tsx,
    src/components/features/supplies/search-to-add.test.tsx,
    prisma/fixtures/dmc-threads.json,
    prisma/fixtures/dmc-threads.test.ts
  </files>
  <read_first>
    src/components/features/supplies/search-to-add.tsx,
    prisma/fixtures/dmc-threads.json,
    prisma/seed.ts,
    .planning/phases/05-foundation-quick-wins/05-RESEARCH.md
  </read_first>
  <behavior>
    search-to-add:
    - Test 1: After successful add, scrollIntoView is called
    - Test 2: scrollIntoView is called with { behavior: "smooth", block: "end" }
    - Test 3: scrollIntoView is NOT called after failed add

    dmc-threads:
    - Test 1: JSON contains entry with code "Blanc"
    - Test 2: JSON contains entries with codes "1" through "35"
    - Test 3: Each entry has required fields: code, name, hexColor, colorFamily
    - Test 4: Total entry count is >= 495 (459 existing + 36 new)
    - Test 5: No duplicate codes exist
  </behavior>
  <action>
    **Thread picker scroll (src/components/features/supplies/search-to-add.tsx):**

    Per SUPP-02 and UI-SPEC Interaction Contract:

    1. Add a ref to the search/add controls container. Place a `useRef` at the component level:
       ```typescript
       const searchContainerRef = useRef<HTMLDivElement>(null);
       ```

    2. Attach the ref to the outer container div of the search-to-add component (the div that wraps the search input and results).

    3. In `handleSelect`, after the successful add + toast (around line 156), REMOVE the `onClose()` call and ADD the scrollIntoView:
       ```typescript
       if (result.success) {
         toast.success(`Added ${item.brand.name} ${getItemCode(item)} to project`);
         onAdded();
         // Don't close -- user may want to add more items
         // Scroll to keep search/add controls visible
         setTimeout(() => {
           searchContainerRef.current?.scrollIntoView({ behavior: "smooth", block: "end" });
         }, 100);
       }
       ```

    The `setTimeout(100ms)` allows React to re-render the updated supply list before scrolling. The `block: "end"` ensures the bottom of the search container (where the controls are) stays visible.

    Note from RESEARCH.md Assumption A1: The current `onClose()` call contradicts the scroll requirement. Removing it allows multi-add workflow where the picker stays open. The parent supply list grows as items are added, and scrollIntoView keeps the search box visible.

    **DMC catalog completion (prisma/fixtures/dmc-threads.json):**

    Per SUPP-03 and RESEARCH.md DMC data:

    Read the existing dmc-threads.json to understand the format. Each entry has:
    ```json
    {
      "code": "150",
      "name": "Dusty Rose Ultra Very Dark",
      "hexColor": "#AB6B7E",
      "colorFamily": "RED"
    }
    ```

    ADD these 36 entries at the BEGINNING of the JSON array (before the existing entries, since they have lower numeric codes):

    ```json
    {"code": "Blanc", "name": "Blanc", "hexColor": "#FCFBF8", "colorFamily": "WHITE"},
    {"code": "1", "name": "White Tin", "hexColor": "#E3E3E6", "colorFamily": "GRAY"},
    {"code": "2", "name": "Tin", "hexColor": "#D7D7D8", "colorFamily": "GRAY"},
    {"code": "3", "name": "Tin Medium", "hexColor": "#B8B8BB", "colorFamily": "GRAY"},
    {"code": "4", "name": "Tin Dark", "hexColor": "#AEAEB1", "colorFamily": "GRAY"},
    {"code": "5", "name": "Driftwood Light", "hexColor": "#E3CCBE", "colorFamily": "BROWN"},
    {"code": "6", "name": "Driftwood Medium Light", "hexColor": "#DCC6B8", "colorFamily": "BROWN"},
    {"code": "7", "name": "Driftwood", "hexColor": "#8F7B6E", "colorFamily": "BROWN"},
    {"code": "8", "name": "Driftwood Dark", "hexColor": "#6A5046", "colorFamily": "BROWN"},
    {"code": "9", "name": "Cocoa Very Dark", "hexColor": "#552014", "colorFamily": "BROWN"},
    {"code": "10", "name": "Tender Green Very Light", "hexColor": "#EDFED9", "colorFamily": "GREEN"},
    {"code": "11", "name": "Tender Green Light", "hexColor": "#E2EDB5", "colorFamily": "GREEN"},
    {"code": "12", "name": "Tender Green", "hexColor": "#CDD99A", "colorFamily": "GREEN"},
    {"code": "13", "name": "Nile Green Medium Light", "hexColor": "#BFF6E0", "colorFamily": "GREEN"},
    {"code": "14", "name": "Apple Green Pale", "hexColor": "#D0FBB2", "colorFamily": "GREEN"},
    {"code": "15", "name": "Apple Green", "hexColor": "#D1EDA4", "colorFamily": "GREEN"},
    {"code": "16", "name": "Chartreuse Light", "hexColor": "#C9C258", "colorFamily": "GREEN"},
    {"code": "17", "name": "Yellow Plum Light", "hexColor": "#E5E272", "colorFamily": "YELLOW"},
    {"code": "18", "name": "Yellow Plum", "hexColor": "#D9D56D", "colorFamily": "YELLOW"},
    {"code": "19", "name": "Autumn Gold Medium Light", "hexColor": "#F7C95F", "colorFamily": "YELLOW"},
    {"code": "20", "name": "Shrimp", "hexColor": "#F7AF93", "colorFamily": "ORANGE"},
    {"code": "21", "name": "Alizarin Light", "hexColor": "#D79982", "colorFamily": "RED"},
    {"code": "22", "name": "Alizarin", "hexColor": "#BC604E", "colorFamily": "RED"},
    {"code": "23", "name": "Apple Blossom", "hexColor": "#EDE2ED", "colorFamily": "PURPLE"},
    {"code": "24", "name": "White Lavender", "hexColor": "#E0D7EE", "colorFamily": "PURPLE"},
    {"code": "25", "name": "Lavender Ultra Light", "hexColor": "#DAD2E9", "colorFamily": "PURPLE"},
    {"code": "26", "name": "Lavender Pale", "hexColor": "#D7CAE6", "colorFamily": "PURPLE"},
    {"code": "27", "name": "White Violet", "hexColor": "#F0EEF9", "colorFamily": "PURPLE"},
    {"code": "28", "name": "Eggplant Medium Light", "hexColor": "#9086A9", "colorFamily": "PURPLE"},
    {"code": "29", "name": "Eggplant", "hexColor": "#674076", "colorFamily": "PURPLE"},
    {"code": "30", "name": "Blueberry Medium Light", "hexColor": "#7D77A5", "colorFamily": "BLUE"},
    {"code": "31", "name": "Blueberry", "hexColor": "#50518D", "colorFamily": "BLUE"},
    {"code": "32", "name": "Blueberry Dark", "hexColor": "#4D2E8A", "colorFamily": "BLUE"},
    {"code": "33", "name": "Fuschia", "hexColor": "#9C599C", "colorFamily": "PURPLE"},
    {"code": "34", "name": "Fuschia Dark", "hexColor": "#7D3064", "colorFamily": "PURPLE"},
    {"code": "35", "name": "Fuschia Very Dark", "hexColor": "#46052D", "colorFamily": "PURPLE"}
    ```

    The existing seed.ts uses an idempotent upsert pattern -- no changes needed there. Running `npx prisma db seed` will add the new entries without duplicating existing ones.

    **Tests (prisma/fixtures/dmc-threads.test.ts):**

    Create a test file that validates the DMC fixture data:
    ```typescript
    import dmcThreads from "./dmc-threads.json";

    describe("DMC thread catalog", () => {
      test("contains Blanc entry", () => {
        expect(dmcThreads.find(t => t.code === "Blanc")).toBeDefined();
      });

      test("contains threads 1-35", () => {
        for (let i = 1; i <= 35; i++) {
          expect(dmcThreads.find(t => t.code === String(i))).toBeDefined();
        }
      });

      test("each entry has required fields", () => {
        for (const thread of dmcThreads) {
          expect(thread).toHaveProperty("code");
          expect(thread).toHaveProperty("name");
          expect(thread).toHaveProperty("hexColor");
          expect(thread).toHaveProperty("colorFamily");
        }
      });

      test("has at least 495 entries", () => {
        expect(dmcThreads.length).toBeGreaterThanOrEqual(495);
      });

      test("no duplicate codes", () => {
        const codes = dmcThreads.map(t => t.code);
        expect(new Set(codes).size).toBe(codes.length);
      });
    });
    ```

    After adding entries, run seed: `npx prisma db seed` to populate the database.
  </action>
  <verify>
    <automated>npx vitest run src/components/features/supplies/search-to-add.test.tsx prisma/fixtures/dmc-threads.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - search-to-add.tsx contains `scrollIntoView` with `behavior: "smooth"` and `block: "end"`
    - search-to-add.tsx does NOT call `onClose()` after successful add (removed to support multi-add)
    - search-to-add.tsx contains `setTimeout` wrapping the scrollIntoView call
    - dmc-threads.json contains entry with `"code": "Blanc"`
    - dmc-threads.json contains entries with codes "1" through "35"
    - dmc-threads.json has at least 495 entries total
    - dmc-threads.json has no duplicate codes
    - Each entry has code, name, hexColor, and colorFamily fields
    - All tests pass
  </acceptance_criteria>
  <done>
    Thread picker auto-scrolls to keep search controls visible after adding items (no longer closes picker). DMC catalog has 36 new entries (Blanc + 1-35) bringing total to ~495 standard threads. Database seeded with new entries.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON fixture -> Database seed | DMC thread data from external source loaded into database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering | dmc-threads.json | accept | Static fixture file; validated by tests for structure and completeness; hex values from cited source (dmc.crazyartzone.com) |
| T-05-15 | Denial of Service | scrollIntoView | accept | Client-side only; no server impact; smooth scroll is a UX enhancement |
</threat_model>

<verification>
- `npx vitest run src/components/features/charts/form-primitives/cover-image-upload.test.tsx src/components/features/supplies/search-to-add.test.tsx prisma/fixtures/dmc-threads.test.ts -x` passes
- `npm run build` completes without errors
- Cover image preview shows full image without cropping in chart form
- Chart detail page shows full cover image without cropping
- DMC thread picker finds "Blanc", "1 White Tin", "35 Fuschia Very Dark"
</verification>

<success_criteria>
Cover images display without cropping distortion. Thread picker stays open after adding items and auto-scrolls to keep controls visible. DMC catalog includes all standard threads including Blanc and 1-35.
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation-quick-wins/05-05-SUMMARY.md`
</output>
