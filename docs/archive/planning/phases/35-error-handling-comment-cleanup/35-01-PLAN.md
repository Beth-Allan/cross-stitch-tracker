---
phase: 35-error-handling-comment-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/upload-actions.ts
  - src/lib/actions/session-actions.ts
  - src/components/features/apps/stitching-app-detail.tsx
  - src/components/features/apps/stitching-app-list.tsx
  - src/components/features/charts/chart-merged-form.tsx
  - src/components/features/charts/fabric-requirements-tab.tsx
  - src/components/features/charts/form-primitives/cover-image-upload.tsx
  - src/components/features/charts/form-primitives/genre-picker.tsx
  - src/components/features/charts/project-detail/chart-file-list.tsx
  - src/components/features/charts/project-detail/delete-file-dialog.tsx
  - src/components/features/charts/status-control.tsx
  - src/components/features/designers/delete-confirmation-dialog.tsx
  - src/components/features/designers/designer-detail.tsx
  - src/components/features/designers/designer-form-modal.tsx
  - src/components/features/designers/designer-list.tsx
  - src/components/features/fabric/fabric-brand-list.tsx
  - src/components/features/fabric/fabric-catalog.tsx
  - src/components/features/fabric/fabric-detail.tsx
  - src/components/features/fabric/fabric-form-modal.tsx
  - src/components/features/genres/genre-detail.tsx
  - src/components/features/genres/genre-form-modal.tsx
  - src/components/features/genres/genre-list.tsx
  - src/components/features/shopping/shopping-cart.tsx
  - src/components/features/shopping/shopping-list-tab.tsx
  - src/components/features/shopping/shopping-list.tsx
  - src/components/features/stats/monthly-stitch-chart.tsx
  - src/components/features/stats/stitching-calendar.tsx
  - src/components/features/storage/storage-location-detail.tsx
  - src/components/features/storage/storage-location-list.tsx
  - src/components/features/supplies/supply-brand-form-modal.tsx
  - src/components/features/supplies/supply-brand-list.tsx
  - src/components/features/supplies/supply-catalog.tsx
  - src/components/features/supplies/supply-form-modal.tsx
  - src/components/features/supply-table/supply-table.tsx
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "Every catch block around a server action call includes console.error for diagnostic trail"
    - "processAndStoreImage call sites log a console.warn when optimization is skipped"
    - "localStorage try/catch guards remain intentionally silent"
    - "Backlog items 999.50, 999.51, 999.53, 999.54, 999.55 are verified closed or addressed"
  artifacts:
    - path: "src/lib/actions/upload-actions.ts"
      provides: "processAndStoreImage call site with console.warn on failure"
      contains: "console.warn"
    - path: "src/lib/actions/session-actions.ts"
      provides: "Two processAndStoreImage call sites with console.warn on failure"
      contains: "console.warn"
  key_links:
    - from: "all component files with catch blocks"
      to: "console.error"
      via: "catch (error) { console.error(..., error)"
      pattern: "catch \\(error\\).*console\\.error"
---

<objective>
Fix all remaining silent error handling patterns across the codebase so every catch block provides diagnostic information.

Purpose: Silent catch blocks swallow errors with no logging, making production debugging impossible. Adding console.error to server action catch blocks and console.warn to processAndStoreImage call sites creates a diagnostic trail without changing user-facing behavior.

Output: All ~40 bare catch blocks in component files gain console.error logging. Three processAndStoreImage call sites gain console.warn for skipped optimization. Backlog items 999.50-999.55 verified closed.
</objective>

<execution_context>
@/Users/wanderskye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wanderskye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-error-handling-comment-cleanup/35-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix processAndStoreImage call sites and verify backlog closures</name>
  <files>src/lib/actions/upload-actions.ts, src/lib/actions/session-actions.ts</files>
  <read_first>
    - src/lib/actions/upload-actions.ts (current state of processAndStoreImage call at line ~145 and the function definition at line ~257)
    - src/lib/actions/session-actions.ts (current state of processAndStoreImage calls at lines ~90 and ~195)
    - src/components/features/sessions/log-session-modal.tsx (verify Phase 30 fixes are present -- 3 catch blocks should already have console.error)
  </read_first>
  <action>
    Per D-04: At each of the 3 processAndStoreImage call sites, check the `result.success === false` return path. Add `console.warn("Image optimization skipped for [entity] -- using raw image")` where `[entity]` is contextual:

    - upload-actions.ts line ~145 (covers): entity is "chart cover"
    - session-actions.ts line ~90 (createSession): entity is "session photo"
    - session-actions.ts line ~195 (updateSession): entity is "session photo"

    The processAndStoreImage function already logs the actual error internally at line ~310 via `console.error("processAndStoreImage error:", error)`. The call-site log is informational only (noting that the raw image path was used instead of the optimized one).

    Per D-01/D-05: Verify that the following Phase 30 fixes are present (read-only verification, no changes needed if fixed):
    - 999.50: upload-actions.ts line ~155 -- the `.catch(() => {})` on deleteFile should now have error logging
    - 999.51: charts/[id]/page.tsx line ~50 -- the `.catch(() => null)` on completion estimate should now have error logging
    - 999.53: log-session-modal.tsx lines ~166, ~229, ~249 -- bare catch blocks should now have console.error
    - 999.54: deleteFile `.catch()` -- should now have error handling
    - 999.55: processAndStoreImage `{ success: false }` -- addressed by the console.warn additions in this task

    If any of 999.50-999.54 are NOT already fixed, fix them in this task following the same pattern.
  </action>
  <verify>
    <automated>grep -c "console.warn" src/lib/actions/upload-actions.ts | xargs test 1 -le && grep -c "console.warn" src/lib/actions/session-actions.ts | xargs test 2 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - upload-actions.ts contains at least 1 `console.warn("Image optimization skipped for chart cover` line near the processAndStoreImage call
    - session-actions.ts contains at least 2 `console.warn("Image optimization skipped for session photo` lines near the two processAndStoreImage calls
    - log-session-modal.tsx catch blocks at lines ~166, ~229, ~249 all contain `console.error`
    - upload-actions.ts deleteFile call site has error logging (not `.catch(() => {})`)
    - charts/[id]/page.tsx completion estimate catch has error logging (not `.catch(() => null)`)
  </acceptance_criteria>
  <done>All 3 processAndStoreImage call sites log console.warn on failure. Backlog items 999.50-999.55 verified closed or newly fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Add console.error to bare catch blocks in component files</name>
  <files>
    src/components/features/apps/stitching-app-detail.tsx,
    src/components/features/apps/stitching-app-list.tsx,
    src/components/features/charts/chart-merged-form.tsx,
    src/components/features/charts/fabric-requirements-tab.tsx,
    src/components/features/charts/form-primitives/cover-image-upload.tsx,
    src/components/features/charts/form-primitives/genre-picker.tsx,
    src/components/features/charts/project-detail/chart-file-list.tsx,
    src/components/features/charts/project-detail/delete-file-dialog.tsx,
    src/components/features/charts/status-control.tsx,
    src/components/features/designers/delete-confirmation-dialog.tsx,
    src/components/features/designers/designer-detail.tsx,
    src/components/features/designers/designer-form-modal.tsx,
    src/components/features/designers/designer-list.tsx,
    src/components/features/fabric/fabric-brand-list.tsx,
    src/components/features/fabric/fabric-catalog.tsx,
    src/components/features/fabric/fabric-detail.tsx,
    src/components/features/fabric/fabric-form-modal.tsx,
    src/components/features/genres/genre-detail.tsx,
    src/components/features/genres/genre-form-modal.tsx,
    src/components/features/genres/genre-list.tsx,
    src/components/features/shopping/shopping-cart.tsx,
    src/components/features/shopping/shopping-list-tab.tsx,
    src/components/features/shopping/shopping-list.tsx,
    src/components/features/stats/monthly-stitch-chart.tsx,
    src/components/features/stats/stitching-calendar.tsx,
    src/components/features/storage/storage-location-detail.tsx,
    src/components/features/storage/storage-location-list.tsx,
    src/components/features/supplies/supply-brand-form-modal.tsx,
    src/components/features/supplies/supply-brand-list.tsx,
    src/components/features/supplies/supply-catalog.tsx,
    src/components/features/supplies/supply-form-modal.tsx,
    src/components/features/supply-table/supply-table.tsx
  </files>
  <read_first>
    - src/components/features/shopping/shopping-cart.tsx (representative example -- has 4 bare catches to understand the pattern in context)
    - src/components/features/designers/designer-detail.tsx (another representative -- has catch around server action + toast)
  </read_first>
  <action>
    Per D-02: For every bare `catch {` block in the 32 component files listed, apply this mechanical transformation:

    Change: `} catch {`
    To: `} catch (error) {`
    And add as the FIRST line inside the catch: `console.error("[ActionDescription] failed:", error);`

    Where `[ActionDescription]` is derived from the server action being called in the corresponding try block. Examples:
    - `await deleteDesigner(id)` -> `console.error("Delete designer failed:", error);`
    - `await updateSupplyAcquired(...)` -> `console.error("Update supply acquired failed:", error);`
    - `await createGenre(...)` -> `console.error("Create genre failed:", error);`
    - `await updateChartStatus(...)` -> `console.error("Update chart status failed:", error);`

    Keep any existing `toast.error(...)` calls that follow -- this adds diagnostic logging alongside user-facing toasts, not replacing them.

    Per D-03: SKIP these files entirely -- their catch blocks guard localStorage operations and are intentionally silent:
    - src/components/features/charts/use-draft-persistence.ts (5 catches)
    - src/components/features/gallery/use-gallery-filters.ts (2 catches)
    - src/components/features/charts/back-to-gallery-link.tsx (1 catch)

    Run `grep -rn 'catch\s*{' src/components/ --include="*.tsx" --include="*.ts" | grep -v '.test.' | grep -v 'use-draft-persistence' | grep -v 'use-gallery-filters' | grep -v 'back-to-gallery-link' | grep -v 'loading.tsx'` after changes to confirm zero bare catches remain in component files (excluding the 3 intentionally-silent files).
  </action>
  <verify>
    <automated>BARE=$(grep -rn 'catch\s*{' src/components/ --include="*.tsx" --include="*.ts" | grep -v '.test.' | grep -v 'use-draft-persistence' | grep -v 'use-gallery-filters' | grep -v 'back-to-gallery-link' | grep -v 'loading.tsx' | wc -l | tr -d ' '); test "$BARE" -eq 0 && npm test -- --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Zero bare `catch {` blocks remain in component files (excluding use-draft-persistence.ts, use-gallery-filters.ts, back-to-gallery-link.tsx)
    - Every catch block now has the form `catch (error) { console.error("... failed:", error); ... }`
    - Existing toast.error calls are preserved alongside the new console.error
    - use-draft-persistence.ts, use-gallery-filters.ts, and back-to-gallery-link.tsx are untouched
    - All existing tests pass with no regressions
  </acceptance_criteria>
  <done>All ~40 bare catch blocks in component files have console.error logging. Three localStorage-guarding files are intentionally unchanged. Tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan adds logging to existing catch blocks -- no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Information Disclosure | console.error in catch blocks | accept | Error objects may contain stack traces visible in browser console. Acceptable: single-user app, no PII in error objects, console is only visible to authenticated user. Server-side console.warn in actions only visible in server logs. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `grep -rn 'catch\s*{' src/ --include="*.tsx" --include="*.ts" | grep -v '.test.' | grep -v '__tests__' | grep -v 'use-draft-persistence' | grep -v 'use-gallery-filters' | grep -v 'back-to-gallery-link' | grep -v 'loading.tsx'` returns zero results
2. `grep -c 'console.warn' src/lib/actions/upload-actions.ts` returns at least 1
3. `grep -c 'console.warn' src/lib/actions/session-actions.ts` returns at least 2
4. `npm test` passes with no regressions
</verification>

<success_criteria>
Every catch block in component and action files either logs with console.error (server action catches) or console.warn (processAndStoreImage skips), except for 3 intentionally-silent localStorage guard files. Zero bare `catch {}` blocks remain in production code outside those 3 files.
</success_criteria>

<output>
Create `.planning/phases/35-error-handling-comment-cleanup/35-01-SUMMARY.md` when done
</output>
