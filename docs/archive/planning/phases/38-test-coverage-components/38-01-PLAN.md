---
phase: 38-test-coverage-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/shopping/supply-overview.test.tsx
  - src/components/features/shopping/project-accordion.test.tsx
  - src/components/features/shopping/shopping-cart.test.tsx
  - src/components/features/shopping/quantity-control.test.tsx
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "D-04: Aggregated quantity distribution allocates increments by capacity and decrements by acquired across multi-item supplies"
    - "D-05: Project accordion shows 'Select this project to see supply details' when expanded but not selected"
    - "D-05: Project accordion shows supply details (threads, beads, fabric) when expanded and selected"
    - "D-07: QuantityControl commits edited value on blur (mobile commit path)"
    - "D-06: ShoppingCart handleUpdateAcquired triggers updateSupplyAcquired, surfaces success/error toasts, and tracks pending/failed state"
    - "D-01, D-02, D-03: Scope approach — verify-and-close for already-covered items, supplement-gaps-only for partial, new tests for genuine gaps"
  artifacts:
    - path: "src/components/features/shopping/supply-overview.test.tsx"
      provides: "Aggregated quantity distribution tests"
      contains: "aggregated quantity distribution"
    - path: "src/components/features/shopping/project-accordion.test.tsx"
      provides: "Not-selected message and supply detail tests"
      contains: "Select this project to see supply details"
    - path: "src/components/features/shopping/quantity-control.test.tsx"
      provides: "Blur commit test"
      contains: "commits value on blur"
    - path: "src/components/features/shopping/shopping-cart.test.tsx"
      provides: "updateSupplyAcquired integration wiring test"
      contains: "updateSupplyAcquired"
  key_links:
    - from: "supply-overview.test.tsx"
      to: "supply-overview.tsx AggregatedSupplyRow"
      via: "onUpdateAcquired callback verification"
      pattern: "onUpdateAcquired.*junctionId"
    - from: "shopping-cart.test.tsx"
      to: "shopping-cart-actions.ts"
      via: "mocked updateSupplyAcquired"
      pattern: "updateSupplyAcquired"
---

<objective>
Fill shopping cart component test gaps for aggregated quantity distribution, project accordion expand/collapse states, QuantityControl blur commit, and updateSupplyAcquired integration wiring.

Purpose: Close backlog items 999.62, 999.63, 999.64, 999.65 by adding the specific missing test scenarios to existing test files.
Output: 4 test files supplemented with new test cases, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-test-coverage-components/38-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aggregated quantity distribution and blur commit tests</name>
  <files>src/components/features/shopping/supply-overview.test.tsx, src/components/features/shopping/quantity-control.test.tsx</files>
  <read_first>
    src/components/features/shopping/supply-overview.tsx (AggregatedSupplyRow onChange handler lines 262-286 — the distribution logic being tested)
    src/components/features/shopping/supply-overview.test.tsx (existing tests and createMockSupplyNeed factory)
    src/components/features/shopping/quantity-control.tsx (commitEdit function, onBlur={commitEdit} at line 97, cancelledRef logic)
    src/components/features/shopping/quantity-control.test.tsx (existing Enter/Escape tests as pattern reference)
  </read_first>
  <behavior>
    supply-overview.test.tsx — Aggregated quantity distribution (999.62):
    - Test: increment on 3-item aggregated supply (items with capacities 2, 1, 3 where acquired are 0, 0, 0) — clicking +1 calls onUpdateAcquired once for first item with junctionId and quantityAcquired+1
    - Test: increment by +2 on 2-item aggregated supply (item A: required=3, acquired=1, capacity=2; item B: required=2, acquired=0, capacity=2) — allocates 2 to A filling its capacity, calls onUpdateAcquired(type, A.junctionId, 3)... wait, the onChange receives newValue which is totalAcquired delta. Actually: render AggregatedSupplyRow with items, click +, verify correct junctionId and new quantity in onUpdateAcquired calls
    - Test: decrement on 2-item aggregated supply (item A: acquired=2; item B: acquired=1) — clicking minus subtracts from first item with acquired > 0
    - Test: single-item supply calls onUpdateAcquired directly with the junction's ID

    quantity-control.test.tsx — Blur commit (999.65):
    - Test: clicking quantity display, typing new value, then blurring the input commits the value (calls onChange with parsed number)
    - Test: blur after Escape does NOT commit (cancelledRef prevents it)
  </behavior>
  <action>
    In supply-overview.test.tsx, add a new describe block "Aggregated quantity distribution" per D-04. Create test data with 2 items sharing the same supplyId but different junctionIds, projectIds, and quantity values. The SupplyOverview component aggregates items by supplyId into AggregatedSupplyRow internally. To test the distribution logic:

    1. Render SupplyOverview with 2 thread items sharing supplyId "shared-1" — item A (junctionId "j-a", required 3, acquired 1) and item B (junctionId "j-b", required 2, acquired 0). Total: required=5, acquired=1. Click the "Increment quantity" button. Verify onUpdateAcquired is called once with ("thread", "j-a", 2) — item A gets the increment because it has capacity (3-1=2).

    2. For decrement: render with item A (acquired 2) and item B (acquired 1). Click "Decrement quantity". Verify onUpdateAcquired called with ("thread", "j-a", 1) — first item with acquired > 0 gets the decrement.

    3. For single-item: render with one thread item (junctionId "j-single", required 3, acquired 1). Click increment. Verify onUpdateAcquired called with ("thread", "j-single", 2).

    In quantity-control.test.tsx, add 2 tests per D-07:

    1. "commits edited value on blur (mobile path)": click display, clear input, type "1", trigger blur on the input. Verify onChange called with 1 and input disappears.

    2. "blur after Escape does not re-commit": click display, press Escape (cancels), verify onChange not called. The blur fires after Escape due to focus loss — cancelledRef must prevent double-action. This tests the cancelledRef guard in commitEdit.
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npx vitest run src/components/features/shopping/supply-overview.test.tsx src/components/features/shopping/quantity-control.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - supply-overview.test.tsx contains describe block "Aggregated quantity distribution" with at least 3 test cases
    - Test verifies onUpdateAcquired called with specific junctionId "j-a" and quantity 2 for increment scenario
    - Test verifies onUpdateAcquired called with specific junctionId "j-a" and quantity 1 for decrement scenario
    - Test verifies single-item path calls onUpdateAcquired directly
    - quantity-control.test.tsx contains test "commits edited value on blur" that uses fireEvent.blur or userEvent tab-away
    - quantity-control.test.tsx blur test verifies onChange called with correct value and input removed from DOM
    - All tests in both files pass
  </acceptance_criteria>
  <done>Aggregated quantity distribution tests cover increment (capacity-based), decrement (acquired-based), and single-item paths. QuantityControl blur commit test covers the mobile commit path. All existing + new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project accordion supplement and shopping cart wiring tests</name>
  <files>src/components/features/shopping/project-accordion.test.tsx, src/components/features/shopping/shopping-cart.test.tsx</files>
  <read_first>
    src/components/features/shopping/project-accordion.tsx (lines 319-323 for "not selected" message, lines 251-316 for expanded+selected supply rendering)
    src/components/features/shopping/project-accordion.test.tsx (existing 14 tests, makeProject factory, defaultProps)
    src/components/features/shopping/shopping-cart.tsx (handleUpdateAcquired lines 239-271 — pending/failed/toast cycle)
    src/components/features/shopping/shopping-cart.test.tsx (existing tests, mockData structure, updateSupplyAcquired mock)
    src/lib/actions/shopping-cart-actions.ts (updateSupplyAcquired signature — already tested in its own file, just wiring check here)
  </read_first>
  <behavior>
    project-accordion.test.tsx — expand/collapse supplement (999.63):
    - Test: expanding a project that is NOT selected shows "Select this project to see supply details" message
    - Test: expanding a project that IS selected shows supply details (thread group with QuantityControl)
    - Test: expanding a selected project with no supplies shows "No supply data for this project"

    shopping-cart.test.tsx — updateSupplyAcquired wiring (999.64):
    - Test: clicking increment on a supply in By Supply Type view calls updateSupplyAcquired with correct type, junctionId, and quantity
    - Test: successful updateSupplyAcquired shows toast.success("Supply quantity updated")
    - Test: failed updateSupplyAcquired (result.success=false) shows toast.error with error message
  </behavior>
  <action>
    In project-accordion.test.tsx, add a new describe block "Expand/collapse detail rendering" per D-05:

    1. "shows not-selected message when expanded but not selected": render with a project, selectedIds empty set, simulate clicking the expand button (aria-label contains "Expand ... supplies"). Verify text "Select this project to see supply details" appears.

    2. "shows supply details when expanded and selected": render with a project (projectId "p1") in selectedIds, pass threads array containing a ShoppingSupplyNeed with projectId "p1". Click expand button. Verify "Threads" heading appears and QuantityControl rendered (check for the acquired/required display).

    3. "shows empty supply message when selected but no supplies": render with selected project that has threadCount=0, beadCount=0, specialtyCount=0, fabricNeeded=false and no supply data arrays. Click expand. Verify "No supply data for this project" text appears.

    In shopping-cart.test.tsx, add a new describe block "updateSupplyAcquired integration" per D-06:

    1. "increment in By Supply Type view calls updateSupplyAcquired": pre-select project p1 via localStorage, switch to "By Supply Type" view, click increment button on DMC 310 (jt1). Import updateSupplyAcquired from the mock. Verify it was called with ("thread", "jt1", 2).

    2. "successful update shows toast.success": mock updateSupplyAcquired to resolve {success: true}. After clicking increment, use waitFor to verify toast.success was called with "Supply quantity updated".

    3. "failed update shows toast.error": mock updateSupplyAcquired to resolve {success: false, error: "DB error"}. After clicking increment, verify toast.error called with "DB error" (or the fallback message).

    Mock sonner's toast in shopping-cart.test.tsx: add vi.mock("sonner") at top if not already present. Import toast from sonner for assertions.
  </action>
  <verify>
    <automated>cd /Users/wanderskye/Projects/cross-stitch-tracker && npx vitest run src/components/features/shopping/project-accordion.test.tsx src/components/features/shopping/shopping-cart.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - project-accordion.test.tsx contains describe "Expand/collapse detail rendering" with 3 test cases
    - Test verifies "Select this project to see supply details" text when expanded + not selected
    - Test verifies "Threads" heading appears when expanded + selected with thread data
    - Test verifies "No supply data for this project" when expanded + selected with no supplies
    - shopping-cart.test.tsx contains describe "updateSupplyAcquired integration" with at least 2 test cases
    - Test verifies updateSupplyAcquired called with correct arguments after increment click
    - Test verifies toast.success or toast.error fires based on updateSupplyAcquired result
    - All tests in both files pass
  </acceptance_criteria>
  <done>Project accordion tests cover not-selected message, supply detail rendering, and empty supply state. Shopping cart tests verify updateSupplyAcquired wiring with success and error toast paths. All existing + new tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan adds test files only, no production code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | N/A | Test files only | accept | No production code modified, no new packages installed |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
All shopping cart test files pass: `npx vitest run src/components/features/shopping/ --reporter=verbose`
No regressions in full test suite: `npm test`
</verification>

<success_criteria>
1. supply-overview.test.tsx covers aggregated quantity distribution for increment, decrement, and single-item paths
2. quantity-control.test.tsx covers blur commit (mobile path)
3. project-accordion.test.tsx covers not-selected message, supply detail rendering, and empty supply state
4. shopping-cart.test.tsx covers updateSupplyAcquired wiring with toast success/error
5. All tests pass with zero regressions
</success_criteria>

<output>
Create `.planning/phases/38-test-coverage-components/38-01-SUMMARY.md` when done
</output>
